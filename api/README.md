# lkap_api — FastAPI control plane

Agents, credentials vault, tools, knowledge bases, sessions, connect/token endpoint, internal worker endpoints. See docs/CONTRACTS.md §5, §7.
Run: `uv run uvicorn lkap_api.main:app --reload --port 8080`.
