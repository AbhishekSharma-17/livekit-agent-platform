# web — Next.js session + console

Session surface at /s/[slug], console at /console. Panel registry in src/panels/registry.ts. See docs/ARCHITECTURE.md §12, docs/CONTRACTS.md §10-§11.
Setup: `pnpm install && pnpm dlx shadcn@latest add --yes @agents-ui/all && pnpm dev`.
