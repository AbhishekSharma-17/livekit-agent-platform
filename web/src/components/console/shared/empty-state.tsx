/**
 * Re-export of the shared primitive (docs/UI_UX_SPEC.md §7.1) so existing
 * `@/components/console/shared/empty-state` imports keep working.
 */
export { EmptyState, type EmptyStateProps } from "@/components/shared/empty-state";
