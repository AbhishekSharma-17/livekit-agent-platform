# lkap_contracts

Pure-Pydantic contracts shared by api, agent, packs and web (generated TS). See docs/CONTRACTS.md. Owner: W0-CONTRACTS.
Export: `uv run python -m lkap_contracts.export` → `generated/` (committed, diff-tested).
