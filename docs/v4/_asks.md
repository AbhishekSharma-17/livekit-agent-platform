# v4 cross-package asks

One line per change a package needs in a file it does not own (`PLAN-V4.md`
"Exclusive ownership"). Format: **owner package** — file — the exact edit — who
asked — status. The coordinator's decisions here are binding.

Created by V4-01.

## Open — left by V4-01 (Starter templates: contracts, catalogue, api, MCP)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 1 | coordinator (no owner this wave) | `mcp/src/lkap_mcp/docs/__init__.py` | `RECIPE_NAMES += "start-from-template"` (after `"connect-livekit"`). This loader keeps its own copy of `content.RECIPES`; without the name, `test_docs_lint.py`'s kebab-token check fails on every doc that backticks the new recipe, and `loader.recipe("start-from-template")` raises. | V4-01 | **Applied by V4-01** (one line, needed for the MCP gate). Coordinator: confirm, or revert together with the recipe. |
| 2 | V4-03 or whoever next owns the MCP prompts | `mcp/src/lkap_mcp/docs/prompts/build_agent.md` | Lines 11–12 still steer to `pack_id="insurance_claim"` / `pack_id="generic"`. Suggested: pick a starter from `lkap://templates` and call `agent_create(template_id=...)`; name the `start-from-template` recipe. Both old forms still work (R-V4-3), so this is wording, not a break. | V4-01 | Open |
| 3 | coordinator | `api/src/lkap_api/auth/roles.py` | Optional: `_Rule("/v1/templates", Requirement("viewer", "agents:read"), Requirement("builder", "agents:write"))` next to `/v1/packs`. V4-01 did **not** need it: `templates/router.py` declares `require("viewer", "agents:read")` itself (the pattern `fleet.py` and `connect.py` use), because without a rule the table fails closed to `admin` + `*` and builder/read-only keys would get 403 on `lkap://templates`. Add the rule only if the policy table should stay the single place to read route requirements. | V4-01 | Open (no action needed) |
| 4 | V4-02 (console) | `web/src/components/console/agents/create/**` | Not an edit request, a contract note: `StarterTemplate.voice` and `.knowledge` are serialised with **only the fields the starter sets** (a `field_serializer` in `lkap_contracts/templates.py`, so a dump re-validates without tripping the "no `voice.greeting` / `knowledge.kb_ids`" validator). In the `.d.ts` both are `VoiceConfig`/`KnowledgeConfig` with every field optional, so treat absent fields as "keeps the pack's value", never as the model default. | V4-01 | FYI |

## Open — left by V4-02 (Console: the New agent dialog and template gallery)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 5 | coordinator (Fable ruling) | `docs/v4/PLAN-V4.md` V4-02 acceptance | The card expects a blank agent to land on `?section=providers&from=blank`, but `TEMPLATES.md` §6.4 lands on `<first next_steps[].section ?? "providers">`, and `blank`'s first next step is "Write the instructions" (§5.1), which gives `?section=instructions`. V4-02 follows §6.4 (no special case for blank) and its test asserts `instructions`. Rule on it: keep §6.4 and amend the card's example, or reorder `blank`'s next steps in the catalogue (V4-01's file). | V4-02 | Open |
| 6 | coordinator | `web/src/components/console/overview/{setup-checklist.tsx,probes.ts}` | Outside the V4-02 card, edited on the coordinator's instruction: "A LiveKit connection is tested" is ticked only by a connection whose `status == "ok"` (it was ticked by any saved connection, including an "Unverified" one), and the checklist's "New agent" action opens the dialog (needed for the card's "nothing links to `/console/agents/new`" grep). | V4-02 | Applied, please confirm |
| 7 | V4-02 follow-up / coordinator | `docs/v4/_briefs/v4-02-walk.md` | The card's live walk (create `knowledge_assistant` and `phone_agent` on the dev stack) was **not** run: this session was told the dev api must stay read-only. Screenshots with the create POST stubbed stand in for it. Run the walk on a scratch api when one is available. | V4-02 | Open |

## Open — left by V4-03 (OpenRouter: registry, credential home, catalog/test adapter, api and worker wiring)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 8 | coordinator | `mcp/src/lkap_mcp/docs/guide.md` | The registry count on line 22 went 121 → 126 (the same number the card changes in `concepts/providers-and-keys.md`); leaving it would make the two MCP docs disagree. | V4-03 | **Applied by V4-03**, please confirm |
| 9 | coordinator | `README.md` | Line 12 still says "a 121-entry registry"; it is 126 now. One word. | V4-03 | Open |
| 10 | coordinator | `contracts/tests/test_v2.py`, `api/tests/test_health.py`, `api/tests/test_providers.py`, `agent/tests/unit/test_factory.py` | Outside the card. The slim (`mvp`) set grew (five OpenRouter entries + `simli-avatar`), and these pinned the v1 18-id set, the count 18, `simli-avatar` as full-only, and a `module.Class` patch that cannot handle the `LLM.with_openrouter` path or Simli's nested secret. Edits: `test_v2.py` keeps `V1_MVP_ID_ORDER` and adds `MVP_ID_ORDER` (v1 + simli + OpenRouter); `test_health.py` 18 → 24; `test_providers.py` uses `anam-avatar` as the full-only example; `test_factory.py` patches classmethod paths on the owning class and reads dotted secrets from the nested object. | V4-03 | **Applied by V4-03**, please confirm |
| 11 | coordinator (user decision) | `contracts/src/lkap_contracts/providers.py`, `agent/pyproject.toml`, `agent/uv.lock`, `agent/requirements/slim.txt` | The user picked Beyond Presence + Simli as the avatars, so `simli-avatar` moved from `_FULL` to `_AVAILABLE` (slim, `status == "mvp"`, R-V2-1), `livekit-plugins-simli==1.8.2` joined the agent's base deps (plus the `simli` extra, like `bey`/`tavus`), and `slim.txt` gained that line. So the card's "`slim.txt`/`full.txt` byte-identical" holds for OpenRouter (it adds no package) but not for the run as a whole; `full.txt` is unchanged. The docker workflow's slim manifest check computes `selected_ids("slim")` from the registry, so it follows automatically. Existing slim images must be rebuilt to carry Simli. | V4-03 | Applied |
| 12 | coordinator (Phase 2) | `contracts/src/lkap_contracts/providers.py` + a data migration | R-V4-7 follow-up: adopt `credential_provider` for the other multi-entry vendors (OpenAI ×6, Google, Deepgram, ElevenLabs, Cartesia). Needs a migration that merges existing per-entry rows onto the home and rewrites `credential_id` references; not done in V4-03. | V4-03 | Open |
| 13 | V4-04 | `web/src/components/console/**` | Contract notes, no edit asked: (a) `ProviderSpec`/`ProviderOut.credential_provider` is in the regenerated `.d.ts`; (b) `GET /v1/credentials?provider_id=openrouter-stt` already answers with the rows stored under `openrouter-llm`, and `POST` with an alias id stores under the home (`CredentialOut.provider_id` is the home); `PUT` with an alias `provider_id` is accepted; (c) `openrouter-tts.voice` is `type="catalog"`, `catalog_kind="voices"`; the voices catalog has one item per (model, voice), so the same voice id (e.g. `Kore`) appears once per Gemini TTS model with a model-qualified label and `meta.model`; dedupe or key by `id + meta.model`; (d) `openrouter-llm.provider`/`fallback_models` are `type="json"` and must be sent as JSON text (the worker decodes them; invalid JSON fails that slot's build). | V4-03 | FYI |
| 14 | coordinator | `scripts/snapshot_plugin_signatures.py` | `with_openrouter` is a `@staticmethod`, captured by name like the classmethods; its fixture entry says `"is_classmethod": false` (the flag records the decorator honestly, and nothing reads it). The fixture was regenerated from a `livekit-agents@1.8.2` clone (commit `bb8c722`): only two keys were added (`…openai.LLM.with_openrouter`, `…openai.llm.LLM.with_openrouter`), nothing else changed. | V4-03 | FYI |
| 15 | coordinator | `api/src/lkap_api/kb/embed.py` | D-V4-13 says the new form "also checks `Credential.provider_id == credential_home(spec)`". V4-03 applies that check to `<provider_id>:<credential_id>` only; the legacy `openai:<credential_id>` keeps its old behaviour (lookup by id, no provider check), so an existing deployment that points it at an `openai-llm` row keeps working. Rule if the legacy form should be tightened too. | V4-03 | Open |

## Open — left by V4-05 (LiveKit-hosted phone numbers)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 16 | coordinator (Fable ruling) | `docs/v4/PHONE-NUMBERS.md` §4.4, §9 step 1 | Live step 0 showed that a freshly bought, unattached number reads `status=OFFLINE`, `inbound_status=UNSPECIFIED`, not `IN_STATUS_DETACHED`. §4.4 as written therefore derives `attach_state="offline"` for every new number. V4-05 implements §4.4 unchanged but keeps the inbound-agent picker enabled for `offline` rows (only `released` disables it). Rule after live step 2 records the status once a rule is attached: keep this, or derive `not_routed` for an unattached `offline` number. | V4-05 | Open |
| 17 | coordinator (Fable ruling) | `contracts/src/lkap_contracts/api_models.py` | Deviation: `PhoneNumberOut` gained `warnings: list[str] = []` (not in §3). R-V4-13 puts the conflict pre-check result "in the response" of an assignment, and `PhoneNumberOut` had no field for it. The default is empty, so existing fixtures stay valid; it is filled only on `PUT /numbers/{id}`. | V4-05 | Applied, please confirm |
| 18 | coordinator | `api/alembic/versions/v4_001_livekit_numbers.py` | Clarification of §4.1: the downgrade also deletes `source='livekit'` number rows, and logs how many. Kept, they would come back as trunk-less "trunk" numbers after a re-upgrade and then collide with Refresh as conflicts. The numbers stay in the LiveKit project. | V4-05 | Applied, please confirm |
| 19 | coordinator | `api/tests/test_health.py`, `api/tests/test_agent_access_v3.py` | Outside the card; they pinned the head. `test_health.py` accepts a `v4_` head. `test_agent_access_v3.py`'s round trip re-upgrades to `v3_001_agent_keys` explicitly instead of `head`. Both are one-line edits, needed for the api gate. | V4-05 | Applied, please confirm |
| 20 | coordinator | `api/tests/telephony_fakes.py` | Outside the card: `FakeLiveKitApi.update_outbound_trunk` (a full trunk replace), for the Telnyx addition's tests. V4-05's own SIP fake additions (`list_dispatch_rule`, a shared timeline) are a subclass inside `test_phone_numbers.py`. | V4-05 | Applied, please confirm |
| 21 | coordinator | `api/src/lkap_api/telephony/service.py` `refresh_numbers` | Behaviour note: without `connection_id` every SIP-capable connection is read. If one of several refuses (409 or 502), its entry carries the error in `warnings` and the others still refresh; with a single target the error is raised as usual. The tests always pass `connection_id`, so the bootstrap connection (`wss://example.livekit.cloud`) is never contacted. | V4-05 | FYI |
| 22 | coordinator | `web/src/components/console/telephony/trunks-section.tsx` | Telnyx addition, done on the coordinator's instruction and outside the card. The outbound "SIP address" hint reads "Telnyx: sip.telnyx.com" when the carrier is Telnyx, and the Username field notes that it is also sent as `X-Telnyx-Username`. The api sets `SIPOutboundTrunkInfo.headers` (proto field 9, "Include these SIP X-* headers in INVITE request"), **not** `headers_to_attributes` (field 10 maps 200 OK headers *into* attributes; LiveKit's Telnyx guide shows the wrong field). The field update (`SIPOutboundTrunkUpdate`) has no `headers`, so a change that alters the header (username, or carrier to or from Telnyx) is sent as a full `UpdateSIPOutboundTrunk{replace}`, with the stored password from the vault. | V4-05 | Applied |
| 23 | coordinator | `mcp/tests/test_tools_telephony.py` (new) | Not in the card's list: a unit test for the overview's hosted-number warnings, plus one end-to-end `telephony_overview` call against a seeded hosted row. | V4-05 | Applied, please confirm |
| 24 | coordinator (after V4-03 is out of the file) | `contracts/src/lkap_contracts/__init__.py` | Add `NumbersRefreshIn` and `NumbersRefreshOut` to the imports from `api_models` and to `__all__`, next to `PhoneNumberUpdate`. They are registered in `export.py`, and every other `api_models` model is exported from the package; nothing fails today, because `test_promoted_models_are_exported…` does not list them. V4-05 did not edit it, because V4-03 was editing that file. | V4-05 | Open |

## Open — left by V4-04 (Console: the shared OpenRouter key in pickers and dialogs, provider notes in the slot editor)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 25 | coordinator | `web/src/components/console/lib/api-hooks.ts` | `useCreateCredential`'s `onSuccess` invalidates `keys.credentials(credential.provider_id)` (the home, e.g. `openrouter-llm`) and `keys.credentials(undefined)` ("all"), but not `keys.credentials(<alias>)` — the exact query key a picker opened from an *aliased* slot uses (e.g. `openrouter-tts`'s picker holds `["credentials","openrouter-tts"]`). Before this package that never mattered (every provider was its own home); with R-V4-7 it means a key added from an alias slot doesn't reappear in that slot's own picker until something else refetches it. Fixed locally in `credential-picker.tsx` (its `onSaved` now also calls the picker's own `refetch()`), which covers this card's acceptance, but `useUpdateCredential`'s broader `invalidateQueries({queryKey: ["credentials"]})` pattern would fix every alias at the mutation layer instead of per-caller. Worth doing once `credential_provider` is adopted for the other multi-entry vendors (ask #12). | V4-04 | Open |

## Open — left by V4-06 (Populate and validate: demo agents on the real dev api)

The bug table with exact calls and results is in `docs/v4/_briefs/v4-06-populate.md` §7. V4-06 edited no source (R-V4-20). The owners below are guessed from the file.

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 26 | agent (maybe api) | `agent/src/lkap_agent/providers/special_cases.py::unwrap_nested_fields` (and the api's resolved kwargs for dotted secrets) | **B-1, blocks the Simli avatar.** A live session on an agent with `simli-avatar` fails with `failed to create simli session token … 'dict' object has no attribute 'create_json'`, then `could not start the session` (session `c53d98aa…`, `status=failed`). `SimliConfig` reaches the plugin as a plain dict. Cause (code read): the api's `config_service._assign_nested` sends `{"simli_config": {face_id, api_key}}` as one non-dotted key, and `unwrap_nested_fields` wraps only dotted keys or non-dotted field names, and every Simli field is dotted. Fix: also wrap a plain key that equals a dotted outer name when its value is a dict (this affects Anam too). Also consider degrading to voice-only when the avatar fails to start, instead of failing the whole call. Add a factory test with the api's real resolved-kwargs shape for Simli and Anam | V4-06 | **Done** (uncommitted, V4-06 fix pass): `unwrap_nested_fields` now builds the nested class from the api's pre-grouped `{"simli_config": {…}}` (dotted keys layered on top, `None` inner values dropped); a failing avatar (`start()` or `wait_for_join()`) degrades to voice-only with an `error` event (`severity: warning`) instead of failing the call. Tests: real-shape Simli (the installed `SimliConfig`) and Anam factory tests, `test_an_avatar_that_fails_to_start_degrades_the_call_to_voice_only`. |
| 27 | api (templates) or agent (flow runtime) | `api/src/lkap_api/templates/catalog/{receptionist,lead_qualification}/` or `agent/src/lkap_agent/flow/runtime.py::kb_ids_for` | **B-2.** In flow mode, `search_knowledge` and auto-inject search only `global.kb_ids + node.kb_ids`. Both flow starters leave `global.kb_ids` empty, so their seeded KBs are unreachable ("No relevant knowledge found." in 1–2 ms). Fix either way: seed the global node's `kb_ids` with the seeded KB ids at `agent_create`, or fall back to `knowledge.kb_ids` when the flow declares none. The demo agents were patched in config | V4-06 | **Ruled** R-V4-29 → V4-11: the runtime falls back to `knowledge.kb_ids` when no node scopes any (whole-flow); templates stay unseeded; console shows each step's effective scope; the demo patch is reverted in V4-11's live check |
| 28 | coordinator (R-V4-18) | `docs/v4/PLAN-V4.md` R-V4-18 | **B-3.** `api.frankfurter.app` now answers 301 to `api.frankfurter.dev/v1/…`, and `restcountries.com/v3.1/name/…` answers 301 to `files-03.restcountries.com/…/legacy.json`. HTTP tools never follow redirects, so dry runs give 301. Amend the allowlist: `api.frankfurter.dev` in place of `.app`; drop REST Countries or replace it with `date.nager.at/api/v3/CountryInfo/{code}` (already allowlisted, verified 200) | V4-06 | **Done**: R-V4-18 (both copies) and the V4-06 table rows now name `api.frankfurter.dev/v1/…` and `date.nager.at/api/v3/CountryInfo/{code}` in place of REST Countries; redirects still not followed. No MCP doc named either API; `tools-http.md` gained a redirects note |
| 29 | coordinator (R-V4-18) + mcp docs | `docs/v4/PLAN-V4.md` R-V4-18; `mcp/src/lkap_mcp/docs/concepts/tools-http.md` | **B-4.** Wikimedia answers 403 ("Please respect our robot policy") to the platform's httpx client unless the User-Agent carries **contact info** (a URL or email). A descriptive UA alone passes for curl but not for httpx. State this in the ruling and the tools doc. The user must set a real contact on `demo_wikipedia_summary` and `demo_company_summary` and re-enable them | V4-06 | **Done**: `LKAP_HTTP_TOOL_USER_AGENT` (worker `Settings` and api `Settings`, default `LKAP/0.1 (+<repo URL>)`) is sent by declarative HTTP tools, `http_request` and the api dry run unless a tool sets its own; R-V4-18, RUNBOOK and `tools-http.md` state the contact rule. Tests in `test_declarative.py`, `test_builtin_tools.py`, `test_settings.py`, `api/tests/test_tools.py`. Re-enabling the two Wikimedia demo tools stays the user's call |
| 30 | agent (+ mcp `chat_send`) | text-channel handling of `request_form` | **B-5.** On the text channel, a `request_form` call blocks the turn until the form times out (65–85 s; `chat_send` returns `state=timeout`, with no reply), because text chat cannot submit a form. Resolve the form as "answered verbally" on `channel=text`, or let `chat_send` submit form values | V4-06 | **Done**: on `channel=text`, `request_form` shows nothing and returns at once telling the model to ask for the fields in the conversation (all modes), and its reply is no longer silenced on realtime/half-cascade text sessions (`SessionContext.channel`). Tests in `test_ui_blocks_tools.py`, `test_ui_blocks_agent.py` |
| 31 | api (template) / agent (flow) | `templates/catalog/lead_qualification` flow edges | **B-6.** With the default LLM (`google/gemma-4-31b-it`), the lead flow never left `start` in two sessions, even after "Sounds good.": path `["start"]`, `variables: {}`. The receptionist flow advanced with the same model. Cause not isolated; check the edge wording ("the caller agrees") and the start node's transition tools | V4-06 | **Ruled** R-V4-30 → V4-11: cause (code read) is the two-edge start that runs as a router with only the generic line; fixes are imperative edge descriptions, a transitions list in every routing node's prompt, and one edge from the lead starter's `start` (decline moves to `company→nurture`); scripted-LLM tests plus a live re-check on gemma |
| 32 | mcp | `mcp/src/lkap_mcp/tools/discovery.py` (`me`) | **B-7.** `me.health.connections = {n: 1, ok: 0}` counts *tested* connections. With a ready worker on an `unverified` connection, models read it as "no worker reachable". Add a worker signal (the fleet's ready instances) or rename the field; SKILL.md says `me` reports worker reachability | V4-06 | **Done**: `me.health.workers` = `{ready, connections_with_ready_worker, by_connection}` from each connection's fleet; `health.connections.ok` stays the tested count. SKILL.md and `guide.md` wording updated; `tools.snap.json` regenerated (description only). Test: `test_me_reports_a_ready_worker_separately_from_tested_connections` |
| 33 | agent | text-channel `end_call` shutdown | **B-8.** After `end_call` on the text channel, the session summary was posted 51 s later, preceded by `failed to send session event … room session transport is closed`. `session_get` shows `active` with 0 turns until then | V4-06 | **Done** (root cause not confirmed at runtime): text sessions run the shutdown callback 1.5 s after any worker-owned trigger ends the job (`_EagerShutdownContext`/`_OnceShutdown`; `end_call` goes through `SessionContext.request_shutdown`), so the summary no longer waits for the SDK's `session.aclose()`; the pack's `on_session_end`, background cancel and UI-channel close now also run before the SDK's teardown on text; `end_call`'s goodbye wait is capped at 5 s on text; a failed last event flush no longer skips the summary. The `failed to send session event` line is the SDK's `SessionHost` and stays. Tests in `test_text_mode.py`, `test_builtin_tools.py`, `test_main.py` |
| 34 | api (templates) / packs | `api/src/lkap_api/templates/catalog/insurance_claim/template.json:26`, `packs/src/packs/insurance_claim/policy_directory.py` | **B-9.** Sample prompt policy `HO-4471-2210` is not in `POLICY_RECORDS` (`H044721`, `AUTO90210`, `RNT3008`, `TRV7711`), so `lookup_policy` returns `found: false`. Use a listed number, or add this one | V4-06 | **Done**: the sample prompt is now `My policy number is H0-44721.` (template, `TEMPLATES.md`, the web fixture, PLAN-V4 row); `test_insurance_sample_prompt_policy_numbers_exist_in_the_pack_directory` |
| 35 | coordinator / web | `docs/v4/PLAN-V4.md` V4-06 step 10(e); `web/src/components/console/settings/snippets.ts` | **B-10.** The card says the Connect dialog uses "1 day", but `AGENT_KEY_EXPIRY_OPTIONS` is 7/30/90/365. Amend the card, or add a 1-day option | V4-06 | **Done**: `AGENT_KEY_EXPIRY_OPTIONS` gained `1 day` (default stays 30); `console-settings-snippets.test.ts` pins the list |
| 36 | agent / coordinator | `agent/src/lkap_agent/platform_agent.py:440` | **B-11.** `injected knowledge` is logged at `debug`, so V4-06 acceptance (1)'s KB signal is invisible on an INFO worker. Log it at info (no content, counts only), or amend the acceptance to accept the `kb_citations` block update / reply content | V4-06 | **Done**: `injected knowledge` is logged at info with `hits`/`top_k` only; `test_injected_knowledge_is_logged_at_info_with_counts_only` |
| 53 | contracts (registry) | `contracts/src/lkap_contracts/providers.py` `anam-avatar` | Found while fixing #26: `livekit-plugins-anam` 1.8.2's `PersonaConfig` requires `name` (and `avatarId`), but the registry's `persona_config.name` has `required=False`, so a nameless Anam config fails `PersonaConfig(...)` at build time (now degraded to voice-only by #26's fallback, not fixed). Mark it `required=True` or give it a default. Verified from the GitHub tag `livekit-agents@1.8.2` (the plugin is not in any venv) | V4-06 fix pass | Open |

## Open — left by V4-07 (Custom model ids and live catalogs)

(Renumbered #37–#52 at merge time to avoid colliding with V4-06's #26–#36.)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 37 | coordinator | `api/src/lkap_api/db/guard.py` | `TENANT_TABLES += "provider_models"`. Outside the card, but `test_db_guard.py::test_tenant_tables_covers_every_model_carrying_a_workspace_id` fails on any new table that has a `workspace_id`. Every `custom_models/records.py` query already carries the workspace predicate. | V4-07 | **Applied by V4-07** (one line, needed for the api gate), please confirm |
| 38 | coordinator | `api/tests/test_credential_tests.py` | The hanging fake adapter's `fetch` now accepts `**_` as well. The adapter protocol gained `page=` and `first_page_only=` (the card's "`credential_tests`: `first_page_only=True`"), and the fake's fixed signature rejected them. | V4-07 | **Applied by V4-07**, please confirm |
| 39 | coordinator | `contracts/tests/test_export.py` | `providers.json` keys are now `{"v", "providers", "model_id_rules"}`, because the card fills `model_id_rules` in `build_providers_document`. `providers.json` still parses as a strict `ProvidersResponse`. | V4-07 | **Applied by V4-07**, please confirm |
| 40 | coordinator (Fable ruling) | `docs/v4/PLAN-V4.md` V4-07 acceptance; R-V4-22 | The "27" `model` fields are 26 string fields plus bitHuman's `model`, which is a real two-value enum (`expression`/`essence`, its runtime mode, not a vendor model id). V4-07 retyped the 26 and kept bitHuman as `enum`, so `grep -c 'type="model"'` gives **26**. `grep -c 'name="model", type="string"'` was already 0 before this package, because `label=` sits between the two. `test_registry.py::test_every_string_model_field_is_typed_model` pins 26, no `string`, and `bithuman-avatar` as the only enum. Amend the card, or rule that bitHuman is retyped too (that drops its enum check). | V4-07 | Open |
| 41 | coordinator (Fable ruling) | `docs/v4/PLAN-V4.md` V4-07 contracts bullet | The card lists `azure-tts` among the entries to wire, but it also says **no `azure_voices`**, and D-V4-25 / §1.4 item 7 say the same. Not wired; `test_registry.py::test_azure_voices_have_no_adapter_the_region_is_a_slot_field` pins that. A `fields`-aware catalog route (to carry `speech_region`) is still a later decision. | V4-07 | Open |
| 42 | V4-10 (drift) / coordinator | `docs/v4/PLAN-V4.md` V4-07 acceptance; `contracts/src/lkap_contracts/providers.py` `deepgram-stt` | Deepgram's public `/v1/models` has **449 STT entries but only 42 unique `canonical_name`s** (every version is listed). As D-V4-25 says, the adapter de-duplicates, so the catalog shows 42; the 102 TTS entries are all unique. The recorded, trimmed fixture keeps all 449/102 raw entries, and the test asserts 449/102 raw and 42/102 parsed. Also, the canonical names are `nova-3-general`, `nova-2-general` and so on; the registry's `nova-3`/`nova-2` and `flux-general-en` do not appear as ids. The drift job will report them as `registry_not_upstream` unless it maps short names to the `-general` names, or the registry adds the canonical ids. | V4-07 | **Settled for the drift job by V4-10**: `drift.REGISTRY_ALIASES` reads `nova-3`/`nova-2` as listed when Deepgram lists `nova-3-general`/`nova-2-general`. The live `/v1/models` of 2026-09-25 has 450 STT entries, 42 canonical names and **no Flux entry** (`deepgram-stt/catalog?q=flux` → 0). `flux-general-en` stays in `registry_not_upstream`, marked expected (`drift.KNOWN_UNLISTED`). Open for the coordinator: keep the short names (the plugin accepts them) or also list the canonical ids |
| 43 | V4-10 (live check) | `api/src/lkap_api/catalogs/adapters.py` | Settle three adapter guesses in the live check: (a) `elevenlabs_voices` now reads `/v2/voices` (paged: `page_size=100`, `next_page_token`, `has_more`) and falls back to `/v1/voices` **only on a 404**; a 401 is still a failed test. (b) `hume_voices` sends `provider=HUME_AI` and pages by `page_number`/`total_pages`; the envelope key `voices_page` was added to `parse_items`. (c) `cartesia_voices` is unchanged: the header/version question stays open, and paging is **not** enabled, because the old `2024-06-10` version may not accept `limit`. | V4-07 | **Still unverified** (V4-10 live check, 2026-09-25): no ElevenLabs, Hume or Cartesia key is stored, so (a), (b) and (c) were not run |
| 44 | coordinator | `contracts/src/lkap_contracts/providers.py` `inworld-tts` | `inworld_voices` is wired as a catalog (`Authorization: Basic <key as issued>`, paged by `pageToken`) with **no `test`**, because whether it rejects a bad key is UNVERIFIED. A keyless GET answered 401 on 2026-09-25. If the live check sees a 401 for a bogus key, set `test="inworld_voices"`. | V4-07 | Open |
| 45 | coordinator | `contracts/src/lkap_contracts/providers.py` | `together_models` is registered but wired to nothing: no registry entry is Together-specific, and `openai-compatible-llm` has no fixed `base_url`, so it cannot name one list. | V4-07 | FYI |
| 46 | V4-09 (console) | `web/src/components/console/**` | Contract change: `GET /v1/providers/{id}/catalog` now answers **at most 200 items by default** (`limit` ≤ 1000, `offset`, `total`). Before V4-07 it returned the whole list (OpenRouter LLM is about 458). Every console consumer that expects the full list (the Catalog dialog, `CatalogPickerField`, `useCatalog`) must page or pass `limit=1000`. Search reads the cache (`q`); `search_vendor=true` works for OpenRouter entries only. Voices take `model=`. | V4-07 | **Done in V4-09**: `useCatalog` takes `{q, limit, offset, model, search_vendor}`; the model combobox and `CatalogPickerField` ask for `limit=1000` and filter locally; the Catalog dialog pages at 200 with `total`; voices pass `model=` only for catalogs that also list models (OpenRouter, Rime) and only for an id that passes the rule |
| 47 | V4-08 | `api/src/lkap_api/custom_models/**`, `config_service.py` | Hand-off notes. `records.upsert(db, workspace_id=, spec=, model_id=, last_test_at=, last_test_ok=, last_test_message=<scrubbed>, last_test_latency_ms=, last_test_cost_usd=, last_test_credential_id=, last_test_fingerprint=, detected=<ModelCapabilities JSON>)` writes a probe result. `ids.scrub(text, secrets.values())` is the scrubber. `capabilities.resolve_capabilities(spec, model, record, catalog_item)` feeds `ResolvedProvider.capabilities`. `ValidationContext.record_for`/`catalog_item`/`fingerprint_for` exist. A slot **without** a credential (LiveKit Inference) accepts a test record with any fingerprint; V4-08 stores the connection key id there, as designed. `ProviderSpec.probe` is declared and unset everywhere. | V4-07 | FYI |
| 48 | coordinator | `api/data/lkap.db` | Apply `v4_002_provider_models` **right after merging**. The merged api reads `provider_models` on every agent save or validate and on every `models` catalog fetch, and the table does not exist until the migration runs (so those calls fail and `/v1/health` reports `db: error`). Log: `docs/v4/_briefs/migration-rehearsal-v4.md`. | V4-07 | **Applied by the coordinator** (observed 2026-09-25 with a read-only query: `alembic_version = v4_002_provider_models`, table `provider_models` present; R-V4-33) |
| 49 | coordinator (Fable ruling) | `contracts/src/lkap_contracts/providers.py` `validate_model_id` | Risk note on R-V4-21. The bare-token rule (32+ characters of `[A-Za-z0-9+]` with no separator) also applies to `avatar_id`/`face_id`/`pal_id`/`persona_id`. A vendor whose **real** avatar ids are 32-hex strings would get a validation **error**. Every id in the registry passes today (`test_model_ids.py`), and so do Bey/Simli/Cartesia/Hume UUIDs, which have `-`. Keep as ruled, or limit the bare-token rule to `model`/`type="model"` values. | V4-07 | **Ruled** R-V4-31 → V4-08 (+ V4-09 mirror): prefixes stay an error everywhere; the bare-token rule stays an error for model ids and becomes a value-free warning on `ID_LIKE_FIELD_NAMES`/`type="catalog"` fields (`validate_id_value`); a registry opt-out flag is a later ask |
| 50 | V4-10 | `api/src/lkap_api/custom_models/records.py` `apply_catalog_sightings` | Sightings are written for the **fetching workspace's** rows only. A public list (Deepgram, Rime) has one shared cache row, so workspace B's rows are marked missing only when B's own fetch sees the difference; if workspace A's fetch already refreshed the row, B's comparison finds nothing new. That is harmless (a missed warning, never a wrong one). A cross-workspace mark would need a `lkap_cross_workspace` query; the drift job's registry-level comparison is the backstop. | V4-07 | **Done** (V4-10): the drift job's registry-level comparison (`registry_not_upstream`) is the backstop, as the row says |
| 51 | coordinator | `contracts/src/lkap_contracts/__init__.py` | Ask #24 (re-export `NumbersRefreshIn`/`NumbersRefreshOut`) is still open. V4-07 owned the file this wave but left it alone, because it is outside the card. It is a two-line edit. | V4-07 | **Done** by the coordinator in b712faa |
| 52 | coordinator (Fable ruling) | `api/src/lkap_api/routers/providers.py` `GET/PUT /v1/providers/{id}/models/{model_id:path}` | The model id travels in the URL path (as designed), so a pasted key that reaches the api verbatim is written by uvicorn's access log (and by `main.py`'s `unhandled_error` line, which logs `request.url.path`) **before** `_checked_model_id` answers 422 without echo. The console's client-side `model_id_rules` check (V4-09) is the intended first line. Options: keep as is; filter `uvicorn.access` lines for this route; or move the id to a query or body parameter. The logger was not changed here. | V4-07 | **Ruled** R-V4-32 → V4-08 (MCP pre-check, `unhandled_error` logs the route template, RUNBOOK lines) and V4-09 (no request before the mirror passes): the id stays in the path; no uvicorn filter; the access log is a stated limitation |

## Open — left by the V4 live recheck (2026-09-25; `_briefs/v4-live-recheck.md`, `_briefs/v4-11-live.md`)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 54 | agent | `agent/src/lkap_agent/main.py:715` (`ctx.add_shutdown_callback(on_shutdown)`), `_OnceShutdown` at `:1116`; `agent/tests/unit/test_main.py:106` | **B-12: blocks every live session.** Commit `32750e6` (the B-8 fix) registers a `_OnceShutdown` *instance* as the job's shutdown callback. livekit-agents 1.8.2 `JobContext.add_shutdown_callback` reads `callback.__code__.co_argcount`, so every job crashes before it connects: `AttributeError: '_OnceShutdown' object has no attribute '__code__'`. The worker accepted 3 jobs after its 03:52 restart, and all 3 crashed: `9d151645…` and `f01a0a72…` (the user's console tests, `channel=test`) and `0e87a488…` (web). The line runs on every channel, so text chat, web, the console test and SIP all fail, and the V4-11 live check and the Simli (B-1) recheck are blocked. **Fix:** register a nested function (`async def _on_shutdown(reason: str) -> None: await on_shutdown(reason)`) or the bound `on_shutdown.__call__`. **Test gap:** the fake `add_shutdown_callback(callback: Any)` should apply the SDK's `__code__`/`co_argcount` check so a callable object fails the gate. The user then restarts the worker | V4 live recheck | **Resolved** in `3d9b8c0` (the coordinator): `run_session` registers a plain `async def _run_shutdown(reason)`, and `test_main.py` gained the check. Verified live after the worker restart (04:10 IST): a text chat, the Simli web call and the five V4-11 chats all started and ended normally |
| 55 | agent / api | `agent/src/lkap_agent/main.py` job entrypoint; the api's session lifecycle | **B-13: degrades.** A job that crashes in the SDK entrypoint, before the observer attaches, leaves its session `status=active`, `ended_at=null`, with no events: **seven** B-12-window sessions still read `active` at 22:48Z: `0e87a488…` (web), plus `9d151645…`, `f01a0a72…`, `a6bb039c…`, `1e958d6a…`, `c0f8d39b…` and `9e4ca9c5…` (console `test`, 22:27–22:39Z). None was modified. Past sessions whose start failed did end as `failed` (`c53d98aa…`). Ask: end the session as `failed` on an unhandled entrypoint exception (a top-level try/except around `run_session`), and/or have the api reap `active` sessions whose room is gone. Nothing was run to check whether a reaper eventually closes them | V4 live recheck | **Done** (the reaper half): `api/src/lkap_api/sessions_sweep.py::sweep_orphaned_sessions`, run from `sweep_loop` alongside the other sweeps. An `active` session with **zero** `session_events` rows and `coalesce(started_at, created_at)` older than `LKAP_SESSION_ORPHAN_TIMEOUT_S` (default 600s, RUNBOOK) is closed `failed`/`error="orphaned"`, gets an `error` session event ("Session never started: no worker activity"), and fans `session.ended` out through the same outbox-job pattern as `telephony.calls.CALL_EVENT_JOB`/`recordings.finalize.RECORDING_FINALIZE`. A session with even one event (however old) is left to the existing `session_stale_active_s` rule instead, so a genuinely long, quiet live call is never touched. The entrypoint try/except half of the ask (belt-and-suspenders on the agent side) is still open. |
| 56 | user (LiveKit account) / coordinator | — (LiveKit project state); relates to #16 and V4-05 live steps 2–6 | **Not an LKAP bug.** The hosted number `PN_PPN_EU4JBkgbNdCN` still reads `PHONE_NUMBER_STATUS_OFFLINE` (`lk number list`). One `PUT /v1/telephony/numbers/8a670475… {inbound_agent_id: Demo — Phone agent, label: "Demo phone line"}` returned 422: "LiveKit rejected attaching the phone number: twirp error unknown: Failed to update phone number" (`livekit_code=invalid_argument`, 400). LKAP deleted its new rule again as designed; there are 0 rules in LKAP and in LiveKit, and the label was not saved. The user should check the number's state with LiveKit, and re-run V4-05 live step 2 once it reads `ACTIVE` | V4 live recheck | Open (waiting on the user) |

## Open — left by V4-08 ("Test model": probes, the route, capability delivery, MCP)

(Renumbered #57–#63 at merge time; #53–#56 were taken by the V4-06 bug fixes.)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 57 | coordinator (agent `main.py` owner) | `agent/src/lkap_agent/main.py` `_assemble`, the `SessionContext(...)` call (≈ line 1194) | Add one keyword: `llm_capabilities=plan.llm_capabilities,`. V4-08 added `SessionContext.llm_capabilities` (`platform_agent.py`) and `SessionPlan.llm_capabilities` (`session_builder.py`, filled from `resolved.resolved["llm"].capabilities`), and `PlatformAgent`/`describe_current_frame` read it, but `main.py` is outside the card. Until this line lands, the worker keeps the registry-only vision decision (a custom id declared or detected text-only still gets frames). The unit tests build `SessionContext` directly and pass. | V4-08 | **Done** at merge: `llm_capabilities=plan.llm_capabilities` added to `SessionContext(...)` |
| 58 | coordinator (Fable ruling, later) | `contracts/src/lkap_contracts/providers.py` `FieldSpec` | R-V4-31's stated cost: a vendor whose real voice/avatar ids are bare 32-hex tokens gets a permanent value-free warning on that field. A registry opt-out (a `FieldSpec` flag meaning "ids here are bare tokens", which would skip the bare-token warning for that field) is filed here as the ruling asks; no such vendor is in the registry today. | V4-08 | Open |
| 59 | V4-10 (live check) | `api/src/lkap_api/custom_models/probes/**` | Settle these probe guesses live (all UNVERIFIED against a real key): (a) `xai_realtime_ws` uses OpenAI's flow on `wss://api.x.ai/v1/realtime?model=` and accepts `session.created` or `conversation.created`; (b) `tavus_replica_get` reads `GET /v2/replicas/{id}` while the registry's field is `face_id` (the catalog lists `/v2/faces`); (c) `cartesia_stt`/`cartesia_tts` send the catalog adapter's `X-API-Key` + `Cartesia-Version: 2024-06-10` (ask #43 c); (d) `elevenlabs_stt` posts `model_id` + `file` to `/v1/speech-to-text`; (e) the Gemini `tools` probe declares `ping` with no `parameters`. | V4-08 | **Still unverified** (V4-10 live check, 2026-09-25): no xAI, Tavus, Cartesia, ElevenLabs or Google key is stored, so (a)–(e) were not run |
| 60 | V4-10 / Fable (FYI) | `api/src/lkap_api/custom_models/probes/base.py` | Two deliberate readings of D-V4-26 to confirm in the live check: (1) the LLM `basic` probe passes on a 2xx well-formed completion even when the text is empty (a reasoning model spends the 4-token budget thinking), and an OpenAI-shaped 400 naming `max_tokens`/`temperature` is retried **once** with `max_completion_tokens: 4` and no temperature; (2) a `tools` call cut off at the budget (`finish_reason=length`) reports `tools=None` (inconclusive), not `False`. The STT probe passes on a 2xx with a transcript field, even an empty one. | V4-08 | **Partly exercised** (V4-10 live check): (1) the `basic` probe passed on a 2xx answer (`gemma-4-31b-it` answered `ok` plus a raw turn marker); (2) a `tools` call cut off at the budget read `None` as designed (`gemma-4-31b-it`), but a **400 naming `max_tokens`** on the tools call read `False` (`openai/gpt-4o-mini` on LiveKit Inference): new ask #65. The STT probe passed with the transcript `OK.` |
| 61 | coordinator (FYI) | `api/src/lkap_api/custom_models/probes/fixtures/ok_1s_16k.wav` | The 1 s clip was synthesized with the macOS `say` voice (see the fixture's README), not recorded. If the redistribution terms of that voice matter for this public repo, replace it with a CC0 recording of "okay" (1.0 s, 16 kHz mono 16-bit, ≤ 40 KB; `test_model_probes.py` pins the format). | V4-08 | FYI |
| 62 | coordinator (FYI) | `api/src/lkap_api/custom_models/service.py` | The 10-minute re-run window reads the `provider_models` row (`last_test_at` + the key fingerprint), so it holds across api replicas, and it does not look at `probes` (a `vision` request inside the window gets the cached answer unless `force`). The 2-in-flight cap is process-local (`InFlight` on `app.state`), as the ruling states. The Inference row's fingerprint is the connection's display fingerprint (`…` plus the key's last four characters), read as "the connection's key id". `gemini_embed` exists but is unwired: the registry has no Google embedding entry. | V4-08 | FYI |
| 63 | coordinator | `docs/v4/_asks.md` #47, #49, #52 | Done in V4-08: #47's hand-off was used as written; #49 → `IdIssue`/`validate_id_value` and the `custom_models/validation.py` split (field issues now sit at `<slot path>.fields.<name>`); #52 → the MCP pre-check, the `unhandled_error` route-template line and the RUNBOOK "Access logs" bullet. The RUNBOOK rule bullet also gained one clause for the R-V4-31 warning. | V4-08 | Done |

## Open — left by V4-10 (catalog drift, the promotion rule, the V4-08 live check)

(Numbered from #64. If V4-09 used these numbers first, renumber at merge and check each row by its content.) Evidence for each row is in `_briefs/v4-08-live.md`.

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 64 | V4-08 owner (probes) | `api/src/lkap_api/custom_models/probes/openai_like.py` (`openai_speech`) | **`openrouter-tts`'s default model fails "Test model".** The probe sends `response_format=mp3`. For `google/gemini-3.8-flash-tts` with voice `Kore`, OpenRouter answers `400: Gemini TTS only supports response_format="pcm"`. `deepgram/aura-2` on the same entry passes, with 4608 bytes of `audio/mpeg`. Suggested fix: send `pcm` for Gemini TTS ids, or retry once with `pcm` on that 400 and accept `audio/pcm`/`audio/L16` as audio. The ≥ 1 KB rule still applies | V4-10 | **Done** (V4-10 fixes, 2026-09-25): OpenRouter's speech models carry no format field (checked: `supported_parameters` is empty on all 20, no `response_format` anywhere, the `/endpoints` detail has none), so there is no metadata to read; `openai_speech` sends mp3 and retries **once** with `pcm` on a 400/415/422 that names `response_format`/`pcm`, and a pcm answer counts as audio (`audio/*`, `application/octet-stream` or untyped). Live re-run: `google/gemini-3.8-flash-tts`/`Kore` → ✅ 59520 bytes of `audio/pcm`. Tests: `test_model_probes.py` (`…retries_once_with_pcm…`, `…untyped_or_octet_stream_pcm…`, `…not_retried`, `…second_format_refusal…`) |
| 65 | V4-08 owner (probes) | `api/src/lkap_api/custom_models/probes/base.py` (the LLM `tools` probe) | **A false "no tools" is stored.** `livekit-inference-llm` with `openai/gpt-4o-mini`: the `tools` call got `400: Could not finish the message because max_tokens or model output limit was reached` and was recorded as `tools=False`, so the workspace row now says `detected.tools=false`. The model does support tools. Suggested fix: under #60's reading, a 400 naming `max_tokens` on the tools call is inconclusive (`None`); or retry once with a larger cap (for example 16 tokens) before deciding. The stored row can be cleared by re-testing after the fix with `force=true` | V4-10 | **Done** (V4-10 fixes): the `tools` call is read by `tools_from_refusal`: `False` only for a 400/404/422 that names the tools parameter (`tools`, `tool_choice`, tool use, function calling) and not the budget; a refusal naming `max_tokens`/an output limit, or any other refusal, is `None` "(inconclusive)". `vision` keeps `capability_from_status`. `max_tokens` stays 4 (R-V4-25; the larger tools budget is ask #78). Live re-run with `force=true`: `openai/gpt-4o-mini` → `tools=null`, and the stored row's `detected` no longer says `tools=false`. Tests: `…refused_for_the_output_limit_is_inconclusive…`, `…only_a_refusal_naming_tools…` |
| 66 | V4-08 owner (cost) | `api/src/lkap_api/custom_models/service.py` (the cost estimate) | **TTS cost reads zero.** `openrouter-tts` with `deepgram/aura-2`: `cost_estimate_usd="0.00000"`, with the note "from the vendor catalog's per-token pricing". The catalog item's `pricing.prompt` is `0.00003` and the probe sent 6 characters (`Hello.`), so about $0.00018 was expected. The characters are not counted as billable units (or the value is rounded to 5 decimals) | V4-10 | **Done** (V4-10 fixes): no rounding bug — `estimate_cost` multiplied the catalog's `prompt` by the probe's 0 tokens. A speech probe now prices its characters at the catalog `prompt` rate (OpenRouter bills Aura-2 per character, $30/1M); a `completion` price (Gemini TTS output audio) is not counted and the note says so. A priced unit that the probe did not use now gives `None` with the usage note, never a zero. `pricing.lookup` has no OpenRouter entries by design. Live re-run: `deepgram/aura-2` → $0.00018. Tests: `test_custom_models.py::test_estimate_cost_*`, `test_test_model_route.py::test_openrouter_tts_cost_counts_the_characters…` |
| 67 | V4-08 owner (probes) / coordinator | `api/src/lkap_api/custom_models/probes/avatars.py` (`simli_face_member`); `catalogs/adapters.py` (`simli_faces`) | **The Simli membership probe fails a face that works.** With the stored key, `GET https://api.simli.ai/faces` answered 2xx but parsed **0 items**. The console catalog is empty too. Testing the face the `Demo — Survey intake form` agent uses (it played in the V4 live recheck) returned `ok=false`, "not in this key's face list (0 faces listed)". Either the list holds only the account's own faces (stock faces are not listed), or the envelope is not one `parse_items` knows. Someone with the key should look at the body's **shape** without printing it. Then either add the envelope key, or treat "not listed" as inconclusive (`ok=None`) for stock faces | V4-10 | **Done** (V4-10 fixes; checked against Simli's docs, not guessed): `GET /faces` lists the account's **own** faces (`owner_id` in `https://api.simli.ai/openapi.yaml`); the preset faces are a static docs page (`/api-reference/preset-faces`, 21 ids; the Survey face is "Tina"), and the OpenAPI has no preset or get-by-id endpoint. So a listed id passes, an unlisted one is `ok=None` "… can't be verified without starting a session", and a refused key still fails. Live re-run: the Survey face → `ok=null`. The console catalog (`simli_faces`) is unchanged: it lists the account's own faces, which is what `GET /faces` returns; whether to also show the 21 documented presets there is a coordinator call. Tests: `test_simli_*` in `test_model_probes.py` |
| 68 | coordinator | — (live question, `CUSTOM-MODELS.md` §1.2 / §4 step 2) | **Does the LiveKit Inference gateway list models with a JWT?** Still unverified. The test-model route proved the JWT chat path (`openai/gpt-4o-mini` and `google/gemma-4-31b-it` both answered). Listing needs a JWT minted from the connection's key and secret, and this package does not read that secret. A one-off by the coordinator: mint with `livekit-api` (`with_inference_grants(InferenceGrants(perform=True))`, TTL 600 s), `GET https://agent-gateway.livekit.cloud/v1/models`, and record the status code and item count only | V4-10 | Open |
| 69 | coordinator (Fable ruling) | `.github/workflows/catalog-drift.yml` | **Secrets passed: a deviation from the card's list.** The workflow passes only the secrets the module reads: `OPENAI`, `GOOGLE`, `ANTHROPIC`, `GROQ`, `CEREBRAS`, `MISTRAL` and `XAI` `_API_KEY`. `CEREBRAS_API_KEY` is added (`cerebras-llm` has a keyed model list). `OPENROUTER_API_KEY` is not passed: the OpenRouter list is read keyless, because the adapter's trimmed `meta` drops `created`/`expiration_date`, which the report needs. `DEEPGRAM`/`ELEVENLABS`/`CARTESIA`/`HUME`/`INWORLD` are not passed either: their lists are public, or are voice lists with no model ids to compare. `test_catalog_drift.py` pins "passed == read". Confirm, or rule that the card's full list is passed anyway | V4-10 | Open |

## Open — left by V4-09 (Console: model combobox, Test model, capability toggles, Catalog dialog)

(Numbered from #70 while V4-10 was appending #64–#69 in parallel; renumber at merge if they collide.)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 70 | coordinator / Fable (FYI) | `web/src/components/console/registry/provider-slot-editor.tsx` | **One "Model" control per slot.** About thirty entries (AssemblyAI, Gladia, …) carry the model in a `type="model"` option field, not `ProviderRef.model`, and the registry never has both (D-V4-23). The editor lifts that field out of "Options" and renders it as the slot's one Model combobox (bound to `fields.<name>`, with the chip, Test and toggles), so a slot never shows two model inputs. `RegistryForm` still renders `type="model"` as a `ModelCombobox` anywhere else it is used. | V4-09 | FYI |
| 71 | coordinator / Fable (FYI) | `web/src/components/console/registry/model-test-panel.tsx` | **The tested chip is a local `TestedChip` (`StatusChip`), not a `CapabilityBadge` kind.** It needs a danger tone and a relative time; the badge metadata has three tones. `CapabilityBadge` gained only `custom` (neutral). | V4-09 | FYI |
| 72 | coordinator / Fable (FYI) | `web/src/components/console/registry/model-capabilities.tsx` | **"This model can…" saves on each change** (`PUT …/models/{id}` per click, "Saved" status) rather than with the agent form's Save: hooking the editor's Save is outside the card's files. It is shown for any id outside the suggestions (a catalog id or a custom one), because that is where the catalog source ("from OpenRouter's catalog") can appear; the `Custom` badge stays for ids outside both lists. Admin choices are Auto / Yes / No (Auto = clear the declaration). | V4-09 | FYI |
| 73 | agent editor owner | `web/src/components/console/agents/agent-editor.tsx` `focusFieldFor` | Id-field issues now sit at `pipeline.<slot>.fields.<name>` (V4-08). V4-09 puts `data-issue-path="<that path>"` on the field's control, which "Show field" finds **when the slot card is expanded**. With the card collapsed, none of the four selectors matches (the card carries `data-issue-path="pipeline.<slot>"`, the exact slot path, and the selectors only look for the issue path or deeper). Suggested: a fifth selector that walks up the issue path (`pipeline.avatar.fields.x` → `pipeline.avatar`) and expands that card. Slot-level model warnings (`pipeline.llm`) are unaffected. | V4-09 | Open |
| 74 | contracts (V4-07/V4-08 owner) | `contracts/src/lkap_contracts/api_models.py` `ModelIdRules` | `ID_LIKE_FIELD_NAMES` is not in `model_id_rules`, so `web/src/lib/model-ids.ts` copies the eight names and `web/tests/model-ids.test.ts` pins them. Optional: add `id_like_field_names: list[str]` to `ModelIdRules` (additive) so the mirror reads it like the rest. | V4-09 | Open (optional) |
| 75 | V4-10 / coordinator | `docs/v4/_briefs/v4-08-live.md` | The card's live walk (Test on the dev stack, save, validate) and the two screenshots for the brief were **not** done: this session was told the real api must stay read-only (Test POST stubbed, nothing saved). Stubbed-POST screenshots are in the session scratchpad (`ui-audit/v4-09/`). Run the walk on a scratch api or with the user's go-ahead. | V4-09 | Open |

## Left by the V4-10 fixes (#64–#67, plus two validation issues the user found)

(Numbered from #76 after V4-09's #70–#75.)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 76 | V4-07 owner (validation) | `api/src/lkap_api/config_service.py` (`_validate_modes`), `custom_models/validation.py` | **A false "cannot see images" warning.** The "Demo — Vision assistant" agent (`openrouter-llm`, `google/gemini-3.5-flash`, camera on) was warned "'google/gemini-3.5-flash' cannot see images … (e.g. google/gemini-3.5-flash)"; the same for `openai/gpt-4o` via OpenRouter. The check read the registry alone, and `openrouter-llm`'s suggestion list carries no `supports_video` flag. It now reads the resolved capabilities (declared → Test model → cached live catalog → registry, D-V4-24; the same answer the worker gets in `ResolvedProvider.capabilities`, whose `model_vision_support` already prefers it), in one place (`_validate_llm_vision`; the duplicate in `custom_models/validation.py` is gone). `False` warns and names its source; unknown is a `Tip:` to run the vision probe; the suggestion lists the same provider's `supports_video` models other than the configured one, else the vision probe; a typed id is never quoted. Live: the agent's `POST /v1/agents/{id}/validate` has no vision warning | user (via coordinator) | **Done** (V4-10 fixes) |
| 77 | V4-07 owner (validation) | `api/src/lkap_api/config_service.py` (`knowledge_auto_inject_issues`) | **The auto-inject / preemptive-generation message is advice, not a problem.** `Issue.severity` is `Literal["error","warning"]` (no info level; the web renders by those two), so it stays a `warning` and now starts "Tip: … Nothing is broken; …". Tests in `test_config_service.py` | user (via coordinator) | **Done** (V4-10 fixes) |
| 78 | coordinator (Fable ruling) | `api/src/lkap_api/custom_models/probes/base.py`, R-V4-25 | **A larger budget for the `tools` call only?** At `max_tokens` 4 some models cannot finish a forced tool call (LiveKit Inference `openai/gpt-4o-mini` answers 400 "max_tokens or model output limit was reached"; `google/gemma-4-31b-it` stops at `finish_reason=length`), so their tools reading stays `None` (#65). A `TOOLS_MAX_TOKENS = 16` on that one call would give a definite answer for about $0.00001 per test on gpt-4o-mini. Not done: R-V4-25 and D-V4-26 say `max_tokens ≤ 4` for every call, and `test_openai_chat_sends_the_budgeted_body…` pins it. Rule whether the tools call may take 16 | V4-10 fixes | **Done** (R-V4-41, 2026-09-25): `TOOLS_MAX_TOKENS = 16` in `probes/base.py`, used by the `tools` body of `openai_like` (both `max_tokens` and the reasoning-shape `max_completion_tokens`), `anthropic` and `gemini`; `basic`/`vision` stay at `MAX_TOKENS = 4`; the inconclusive message names 16; D-V4-26's table and Cost line and the `base.py` docstring amended; `test_model_probes.py` pins the split per variant (OpenAI, reasoning retry, Anthropic, Gemini). Live `force=true` (brief, "Vision probes for #79"): LiveKit Inference `openai/gpt-4o-mini` and `google/gemma-4-31b-it` now read definite `tools=true`. Ruled R-V4-41 → V4-10 fixes (Opus): yes — `TOOLS_MAX_TOKENS = 16` on the `tools` variant in all three chat wire shapes (and its reasoning-shape retry); `basic`/`vision` keep 4; readings unchanged (a cut-off at 16 is still `None`, no retry); R-V4-25 amended; D-V4-26's table and Cost line amended with the code; the two Inference ids re-tested `force=true` into the brief |
| 79 | contracts owner (registry) | `contracts/src/lkap_contracts/providers.py` (`openrouter-llm` `models`) | `openrouter-llm` lists `openai/gpt-4.1-mini`, `openai/gpt-4.1`, `openai/gpt-4o-mini`, `google/gemini-3.5-flash` and `anthropic/claude-sonnet-4.6` without `supports_video`, so with no cached catalog (a new key, the catalog never opened) the registry still answers "no vision" for them, in validation and in the worker. OpenRouter's catalog says all five take images. R-V4-27 lets `supports_video` flip only on a passing `vision` probe or a recorded session: run the `vision` probe for each and flip the flags that pass | V4-10 fixes | **Done** (R-V4-42, 2026-09-25): all five `vision` probes passed (brief, "Vision probes for #79"; about $0.0054 in all; the key revoked 204), so `supports_video=True` is set on all five `openrouter-llm` `ModelSpec` lines and the generated trees are regenerated (`export_contracts.sh` leaves no further diff). Tests: `test_registry.py` asserts `vision_support("openrouter-llm", id) is True` for each; `test_config_service.py` gains the no-record, no-catalog-item case (no warning, no Tip, `source="registry"`), and the text-only-catalog test now expects the flagged-model suggestion instead of the vision probe. Ruled R-V4-42 → coordinator, then Opus (contracts): promote now on probes — the coordinator runs `test-model` with `probes: ["basic","tools","vision"]`, `force: true` for the five ids on the dev api (a temporary Builder key; preferably after R-V4-41 lands so the same calls also settle `tools`, but not a precondition; section "Vision probes for #79" in `v4-08-live.md`; under $0.01); Opus flips `supports_video=True` only for the ids whose vision probe passed, in `providers.py`, then regenerates. Fetch-on-empty-cache in the resolved-capability read is **rejected** (a vendor call on validate/publish/session start, R-V4-9/R-V4-28); a keyed-catalog warm on credential save is deferred, not built ad hoc |
| 80 | V4-09 owner (console) | `web/src/components/console/registry/model-test-panel.tsx` (the `title` ternary), `web/tests/console-model-test.test.tsx` | A result with `ok=null` (no probe for the entry, an `image_gen` membership check, a Simli face outside the account's list) is headed "Not tested", which says nothing ran and collides with the chip's `Untested` (no record). The heading becomes exactly **"Couldn't verify this model"**; tone `neutral`, `CircleHelpIcon` and the `message` line unchanged; the chip states and the credential test's empty state stay | Fable (R-V4-43) | **Done** (R-V4-43, 2026-09-25): the heading reads "Couldn't verify this model"; one new `ok: null` test in `console-model-test.test.tsx` finds it and the message; `grep 'Not tested"'` in the panel → 0. Ruled R-V4-43 → V4-09 fixes (Sonnet): the string above plus one `ok: null` panel test |

## Open — left by V4-12 (background and parallel tool calls)

(Numbered from #80; renumbered #86–#91 at merge. If another package appended in parallel, renumber at merge and check each row by its content.)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 86 | coordinator (agent `main.py` owner) | `agent/src/lkap_agent/main.py`: `_wire_optional_modules` (the `build_mcp_servers` import and `deps.mcp_servers_builder`), `_assemble` (`mcp_servers=mcp_servers or None` into `PlatformAgent`, ≈ line 1404) | The card grants V4-12 two `main.py` hunks (the `tool_execution_updated` handler and the thinking sound); both landed. Three more lines are outside the grant: (a) `from lkap_agent.tools.declarative import build_http_tools, build_mcp_toolsets` and `deps.mcp_servers_builder = lambda defs, **kwargs: list(build_mcp_toolsets(defs, **kwargs))`; (b) in `_assemble`, rename the local and pass `mcp_toolsets=mcp_toolsets` to `PlatformAgent` (its `mcp_servers` keyword is kept as a deprecated alias). Until then: `declarative.build_mcp_servers` is a deprecated alias that already returns `MCPToolset`s (so per-tool options work today) and logs `declarative_tool.build_mcp_servers_deprecated` **once per worker process**, and the acceptance grep `grep -rn "mcp_servers=" agent/src` has exactly this one hit. No change is needed for the agent default: `_make_http(defs)` builds HTTP tools without it and `PlatformAgent` rebinds them to `tools.execution_default` and the flow gate (`execution.bind_agent_policy`); passing `execution_default=`/`flow_node=` from `_assemble` later only saves that rebuild | V4-12 | **Done** (merge, 2026-09-25): `main.py` imports `build_mcp_toolsets` and passes `mcp_toolsets=`; `grep -rn "mcp_servers=" agent/src` → 0 |
| 87 | coordinator (Fable, FYI) | `docs/v4/PLAN-V4.md` V4-12 card, `mcp/src/lkap_mcp/docs/concepts/` | The card names `docs/concepts/tools.md`; no such concept exists (`tools-http` and `tools-mcp` do), and a new topic would need `CONCEPT_TOPICS` in `mcp/src/lkap_mcp/docs/__init__.py` (not in the card). The "Background tools" text went into `tools-http.md` (modes, the safety rule, fillers need a voice, results land at idle, flows wait for 1.8.3) and a short section into `tools-mcp.md` (`tool_options`, `report_progress`). Both files are outside the card's list; `test_docs_lint.py` is green | V4-12 | FYI |
| 88 | V4-13 (console) / coordinator | `web/src/components/console/agents/defaults.ts` | The regenerated `.d.ts` makes `DEFAULT_VOICE: Required<VoiceConfig>` and `DEFAULT_TOOLS: Required<ToolsConfig>` fail `pnpm typecheck` (new fields). V4-12 added the three defaults (`thinking_sound: "none"`, `execution_default: "blocking"`, `builtin_execution: {}`), mirroring the Pydantic defaults, so the web gate stays green; nothing else under `web/src` changed. Also for V4-13: `ToolExecution.fillers` (`max_length=5`) generates as a tuple union (`[] \| [string] \| … \| [string×5]`), so the editor's `string[]` state needs a cast when it is posted | V4-12 | FYI |
| 89 | V4-13 (console timeline) | — (SDK behaviour, `voice/tool_executor.py`) | Observed offline in 1.8.2 (`test_tool_execution.py::test_real_sdk_a_slow_auto_tool_announces_then_replies_at_idle`): the deferred reply's `tool_reply {status: "scheduled"}` can be recorded **before** the call's `tool_call_ended`, because the SDK queues the reply from inside the tool task and reports `ended` from its done-callback. Order rows by `ts`, not by an assumed `ended → reply` sequence. `tool_reply.call_ids` holds the tools' base call ids (the SDK's `_final` / `_update_N` entry suffixes stripped), so they join `tool_call_*.call_id` directly | V4-12 | FYI |
| 90 | coordinator (Fable, FYI) | `BACKGROUND-TOOLS.md` §1.4, §8 | The SDK's in-progress placeholder ("The tool call is still in progress.") is injected only for a running call **missing** from the inference's context. In the normal flow the first update commits the call and its announcement as the call's output, so the model sees the call through that pair; the placeholder appears after a trimmed or rewound context. The real-SDK test pins the placeholder by trimming the context while the tool runs. Also observed offline: an interruption during the inline (`auto`) wait still commits the call, skips the announcement reply, and the result lands at the next idle (`…interrupting_the_turn_keeps_the_tool_running…`), which is live step 9's premise | V4-12 | FYI |
| 91 | later package (asks from BACKGROUND-TOOLS.md §10) | `packs/src/packs/base.py`, `agent/src/lkap_agent/tools/background.py`; flow `StartNode`/edge tools; `VoiceConfig.thinking_sound` | Filed as the design asks: (a) move the pack `BackgroundRunner`'s routine (non-urgent) path onto `ToolMeta.execution`; (b) `StartNode` and edge tools with background siblings once V4-14 lands 1.8.3; (c) an uploaded thinking/ambient clip (the conversation-tuning screen). The `background.py` docstring correction was done in V4-12 | V4-12 | Open |


## Open — left by the costs design (`COSTS.md`, 2026-09-25)

(Numbered from #92. If another package appended in parallel, renumber at merge and check each row by its content.)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 92 | V4-15 (pricing rows) | `contracts/src/lkap_contracts/pricing.py`, `api/src/lkap_api/costs/assumptions.py` | **Two OpenAI figures to read off the page on the day.** (a) `gpt-4o-transcribe` / `gpt-4o-mini-transcribe`: the research captured "$2.50 in / $10.00 out per 1M, est. $0.006/min" but the page prices **audio-in** tokens separately from text-in; add the `audio_tokens_in` row from the page, and if only a per-minute estimate is shown, add an `audio_s_in` row from it instead. (b) `audio_tokens_in_per_s` / `audio_tokens_out_per_s` for `openai-realtime`: derive from the page's per-minute column ($/min ÷ $/1M audio tokens × 1M / 60) and record the arithmetic in the assumption's `source_url` note; Gemini's 25 tokens/s is documented and needs no derivation | Fable (design) | **Done** (V4-15, pages read 2026-09-25): (a) the `gpt-4o-transcribe` model page heads its input price "Audio tokens" ($2.50 in / $10.00 out; mini $1.25 / $5.00), so the rows are `audio_tokens_in` + `tokens_out`, plus an `audio_s_in` row from the page's own per-minute estimate ($0.006 / $0.003) for the estimator; there is no separate text-in price. (b) The pricing page has no per-minute column for `gpt-realtime`; the realtime cost guide (`developers.openai.com/api/docs/guides/realtime-costs`) documents 1 audio token per 100 ms of user audio and per 50 ms of assistant audio, so `costs/assumptions.py` uses 10 in / 20 out tokens/s with that URL. Nothing derived from memory |
| 93 | V4-15 (pricing rows) / coordinator | `contracts/src/lkap_contracts/pricing.py` (`livekit-inference-llm` `openai/gpt-oss-120b`) | LiveKit's payload lists two routes for GPT-OSS-120B (Baseten $0.10/$0.50, Groq $0.15/$0.60 per 1M) and does not say which one a request lands on. Rule: keep it unpriced (today's state) or price at the **higher** route with `tier_note="priced at the Groq route; LiveKit may route to Baseten ($0.10/$0.50)"`. The design leans to the second (an over-estimate is safer than "no price" on a default model), but it is a ruling | Fable (design) | Done — Ruled: R-V4-58 — priced at the Groq route with tier_note; test_v2 assertion flips and test renamed |
| 94 | contracts owner (registry) / coordinator | `contracts/src/lkap_contracts/providers.py` (`deepgram-stt`, `deepgram-tts` `fields`) | **Deepgram reconciliation needs the project id.** `GET /v1/projects/{project_id}/requests/{request_id}` returns `response.details.usd` with a regular key, but the credential form collects only the API key. Add an optional `project_id` `FieldSpec` (help text "Found in the Deepgram console under Projects; lets LKAP read the exact charge per request") so V4-17 can build the Deepgram client; without it, Deepgram stays "designed, not built" | Fable (design) | Open |
| 95 | later package | — | **Display currency.** Every priced vendor lists USD; Beyond Presence lists EUR. The design is USD-only (R-V4-44); a per-workspace display currency with a stored rate is a later ask, not v4 | Fable (design) | Open |
| 96 | agent owner / coordinator | `agent/src/lkap_agent/tools/builtin/search_knowledge.py`, `observability.py` | **Embedding queries are not metered per session.** The api's `kb/embed.py` embeds a query per `search_knowledge` call and per auto-inject turn, but the worker reports no count, so embedding cost is an estimate line only (`note="not metered per session"`). Decide whether the worker posts a `metrics {kind: "knowledge_queries", data: {count, tokens}}` event (cheap) or the api counts `search_knowledge` `tool_call_started` events (free but misses auto-inject) | Fable (design) | Open |

## Open — left by V4-14 (`livekit-agents 1.8.3` bump, **blocked**)

V4-14 did not land the bump. These four asks are why. Evidence comes from a dry run in a throwaway copy of `agent/ packs/ testing/ contracts/ scripts/` in the session scratchpad, with nothing written to the repo: the pins were edited there, relocked, synced, and the full agent gate was run on 1.8.3.

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 97 | coordinator (packs / testing owners), then api / mcp | `packs/pyproject.toml` (`livekit-agents==1.8.2`), `testing/pyproject.toml` (same), and their `uv.lock`; then `api/uv.lock`, `mcp/uv.lock` | Both path deps of `agent/` pin `livekit-agents==1.8.2` exactly, so `uv lock` in `agent/` at 1.8.3 is unsatisfiable ("lkap-packs==0.1.0 depends on livekit-agents==1.8.2"). The edit is one line in each file, `==1.8.2` → `==1.8.3`, then `uv lock` in `testing/`, `packs/`, `agent/` (and `api/`, `mcp/`, whose locks record `lkap-packs → livekit-agents ==1.8.2`). V4-14 tried this edit, and the session's permission classifier denied it as "Modify Shared Resources". V4-14 then filed this ask as the hard rules require (a file outside the card goes to an ask) and did not retry. The card's exclusive list misses these two manifests. Dry run with the edit: `agent/uv.lock` moves **only** `livekit-agents` and the 9 pinned `livekit-plugins-*` from 1.8.2 to 1.8.3 (124 packages; `livekit` stays 1.1.18, `livekit-api` 1.2.1, no other transitive bump). Rejected alternative: `[tool.uv] override-dependencies` in `agent/pyproject.toml`, which would leave `packs`/`testing` declaring a pin the worker no longer runs | V4-14 | **Done** (V4-14 merge, 2026-09-25): applied per R-V4-52…55 |
| 98 | coordinator (scripts owner) | `scripts/gen_plugin_requirements.py` | Two problems. (a) The generator hardcodes the pin (`f"{pkg}==1.8.2"` at line 119, and "livekit-agents==1.8.2 baseline" in the emitted header), so re-running it cannot produce 1.8.3. Hand-editing `agent/requirements/*.txt` would fail `contracts.yml`'s `--check`. (b) The lockstep assumption breaks: **`livekit-plugins-bithuman` has no 1.8.3 on PyPI**. It jumped 1.8.2 → 1.8.4, uploaded together with livekit-agents 1.8.3 and requiring `livekit-agents>=1.8.3`. So `full.txt` at `==1.8.3` fails the Dockerfile's per-line install. Proposed: one `LIVEKIT_AGENTS_VERSION = "1.8.3"` constant plus a per-package override map `{"livekit-plugins-bithuman": "1.8.4"}`. All other 66 lines in `full.txt` exist at 1.8.3 (checked against the PyPI JSON API). Dry-run diff with (a) applied: `slim.txt` 10 pin lines + 1 header line; `full.txt` 67 pin lines + 1 header line; nothing else | V4-14 | **Done** (V4-14 merge, 2026-09-25): applied per R-V4-52…55 |
| 99 | coordinator (Fable ruling: tests to rewrite) | `agent/tests/unit/{test_tool_execution,test_declarative,test_flow_runtime,test_platform_agent,test_registration,test_ui_blocks_tools}.py` | On 1.8.3 the agent gate has **7 failures, 1129 passed, 9 skipped**. ruff, format and mypy `--strict` are clean. Six failures are tests that assume the installed SDK is 1.8.2, and they fail exactly because the downgrade lifts as designed: `test_tool_execution.py::test_resolve_the_installed_sdk_is_below_the_flow_gate` (asserts `__version__ == "1.8.2"`); `test_declarative.py::TestExecutionPolicy::test_a_flow_node_tool_is_built_blocking_below_1_8_3` and `::TestBuildMcpToolsets::test_flow_node_toolsets_keep_their_tools_blocking`; `test_flow_runtime.py::test_flow_node_background_tools_run_blocking_until_livekit_agents_1_8_3`; `test_platform_agent.py::test_a_flow_agent_records_one_info_event_when_it_keeps_background_tools_blocking` (each gets `CANCELLABLE`/declared mode instead of `NONE`/blocking); `test_registration.py::test_register_payload_carries_connection_image_sdk_providers_and_packs` (`sdk_version() == "1.8.2"`). The card contradicts itself here. Its acceptance says the `flow_node=True` case "now resolves to the declared mode", and the tripwire's own docstring says "The pin is 1.8.2 until V4-14", so the flip was expected. But the card also says "no test rewritten". Fable should rule which clause wins. Proposed: pin the version in these tests (`sdk_version="1.8.2"`, or monkeypatch `livekit.agents.__version__`) so they keep testing the downgrade, and flip the tripwire to `>= FLOW_BACKGROUND_MIN_SDK` with the declared mode. The seventh is a real 1.8.3 change: `test_ui_blocks_tools.py::test_block_tool_schemas_have_no_property_less_objects_for_gemini` raises `KeyError: 'parameters'`, because upstream #7312 sends a `FunctionTool` as `parameters_json_schema` on Gemini's text API; Gemini Live (`use_parameters_json_schema=False`) still gets `parameters`. A probe of both paths finds **0** property-less objects in the block tools, so the invariant holds and only the key moved. Proposed: walk `declaration.get("parameters_json_schema") or declaration.get("parameters")`, and parametrize over both flags | V4-14 | **Done** (V4-14 merge, 2026-09-25): applied per R-V4-52…55 |
| 100 | coordinator (scripts owner) | `scripts/snapshot_plugin_signatures.py` (and optionally `agent/tests/unit/test_factory_signatures.py`) | Upstream #7318 moved the OpenAI Realtime implementation into `livekit.agents.llm._realtime.openai`. `livekit/plugins/openai/realtime/realtime_model.py` is now a `from … import *` shim, which the AST-only snapshot cannot follow. Regenerated from the `livekit-agents@1.8.3` tag, the fixture **loses** `livekit.plugins.openai.realtime.RealtimeModel` / `.with_azure` / `RealtimeSession` / `InferenceRealtimeModel` (13 keys removed, 10 added: `livekit.agents.inference.realtime.*`, `liveavatar.LiveAvatarPlugin`, `GPTLiveModel.with_azure`). With that fixture, `test_factory_signatures.py` still passes, but the three OpenAI Realtime checks (`RealtimeModel`, `RealtimeModel.with_azure`, and the nested `with_azure` pass) turn into **skips** instead of failing: 215 passed / 9 skipped becomes 214 / 10. Two classes that used to be skipped for `**kwargs` are now checked, so the totals hide part of the loss. 16 classes gain kwargs (elevenlabs STT `audio_chunk_duration_ms`, Gemini TTS `speaker`/`speakers`, mistralai `api_mode`, GPTLive `azure_deployment`/`entra_token`/`service_tier`, phonic `phonic_model`); none are removed. Proposed: have the script resolve `from <livekit.agents module> import *` into the `livekit-agents/` tree of the same clone, and have the test fail, not skip, when a registry `python_class` in the MVP set is missing. The dry-run fixture (`plugin_signatures.183.json`, 1071 keys) is in the V4-14 session scratchpad | V4-14 | **Done** (V4-14 merge, 2026-09-25): applied per R-V4-52…55 |

## Open — left by the V4-12 live check (text half, `_briefs/v4-12-live.md`, 2026-09-25)

(Renumbered #98–#100 → #101–#103 at merge; V4-14 holds #97–#100. The brief cites these as #101–#103.)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 101 | coordinator (the live protocol) | `docs/v4/_briefs/v4-12-live.md` §"Before you start" 4 and step 2; `BACKGROUND-TOOLS.md` §9 item 2 | **Step 2 ("the fast path") fails as written, from latency alone.** From this host, httpbin's `/delay/0` answers in 1.1–1.5 s: the tool took 1160 ms, the dry run 1122 ms, and five `curl` samples 1.15–1.50 s. That is above `auto_threshold_ms` 700, so `auto` correctly takes the slow path. Session `a91b6f29…`, turn 2: `tool_call_updated {call_id: call_793e67b8…, tool: demo_fast_echo, message_preview: "Fetching the echo now."}`, then `tool_call_ended {status: done, duration_ms: 1160}`, then two assistant messages. With `auto_threshold_ms: 2500` on `demo_fast_echo`, the step passes (session `7e72920b…`: no update, no `tool_reply`, one message). Rule one of: (a) the protocol gives the fast tool `auto_threshold_ms: 2500`; (b) it uses a faster allowlisted endpoint; (c) it keeps 700 and records the endpoint's latency first. No code change is asked | V4-12 live (text) | Open |
| 102 | V4-12 owner (agent text channel / execution) | `agent/src/lkap_agent/text_mode.py` (`rewind`, `inject_user_text`, `_truncate_and_sync`), `agent/src/lkap_agent/tools/execution.py` (`cancel_running`) | **After a rewind cancels a background call, the regenerated reply says the result is on its way, and it never comes.** Sessions `887aae7e…` and `c67ec25a…` ran "Fetch the echo." and then `chat_rewind(turn_index=1)` during the delay. The worker logged `text_mode_rewind turn_index=1` → `tool cancelled` → `tool_call_ended {status: cancelled}`, which is correct. The regenerated reply was "It's on its way!" (and, the second time, "It's on its way! I'll let you know as soon as I have it."), with **no new tool call**. Its inference read 823 input tokens (the `session_usage` delta); the turn's first inference over the same kept items (system prompt, greeting, "Fetch the echo.") read 794. So about 29 tokens of a function-call pair for the cancelled call still reach the model. It is either the SDK's "The tool call is still in progress." placeholder or the call's first-update pair; which one is not confirmed. The placeholder is the likelier of the two, for three reasons. `truncate_to_turn` already cuts the update pair, which sits after the user message. `cancel_running` only calls `task.cancel()`, and the SDK executor's `_on_done` pops `_running_tasks` later, possibly while `_truncate_and_sync` awaits, so the call can still count as running when the reply is generated. And "It's on its way! I'll let you know as soon as I have it." is what `PIPELINE_NOTES` tells the model to say when a tool output reads "still in progress". The edit path (`inject_user_text` with `turn_index`) shows the same +34 to +36 (session `f0f7cb61…`), but that reply did not depend on it. Suggested: `cancel_running` returns the cancelled tasks and `rewind`/`inject_user_text` await them (or the executor's done callbacks) before truncating and generating. Then pin it with a real-SDK test: rewind during a running `auto` call, and the regenerated inference's context holds no pair for that call. `truncate_to_turn` itself is correct (it cuts at the user message) | V4-12 live (text) | **Done** in `79ed213`: the regenerated inference read the SDK placeholder (the call was still in `_RunningTasks`; `_on_done` pops it later, `tool_executor.py:431-434`). `cancel_running` is now async and waits ≤ 1 s for each cancelled call's `tool_call_ended`; `rewind`/`inject_user_text` also strip the cancelled calls' items (call, `_update_N`, `_final`). Tests: `test_tool_execution.py::test_real_sdk_a_rewind_frees_the_model_to_call_the_cancelled_tool_again`, `…strips_an_earlier_turn_s_cancelled_call`, `…an_edit_during_a_running_call_leaves_no_trace_of_it`, `test_cancel_running_*`. Holds on 1.8.3 (same pop-then-emit order). Worker restart needed |
| 103 | mcp owner | `mcp/src/lkap_mcp/tools/generic.py` (`lkap_delete`), and possibly `client.delete` on a 3xx | **`lkap_delete` with an empty `id` reports a deletion that did not happen.** `lkap_delete(kind="tool", id="", confirm=true)` sent `DELETE /v1/tools/`, which the api answered **307 Temporary Redirect**. The tool returned `{"deleted": true, "kind": "tool", "id": ""}`. Suggested: `id: Annotated[str, Field(min_length=1)]` (the chat tools already use `min_length=1`), and treat a non-2xx or 3xx on a write as a failure in the client. Found during step 10 (a scripting slip in the driver); nothing was lost | V4-12 live (text) | **Done** in `79ed213`: `lkap_delete` refuses an empty/blank id or one with `/` (and a kb_document `parent_id`) with `invalid_input` before any request; `LkapClient` never follows redirects and reports any 3xx to POST/PUT/DELETE as `unexpected_redirect`. Schema unchanged (runtime check, no `min_length`), so `tools.snap.json` is untouched. Tests: `test_tools_generic.py::test_lkap_delete_refuses_an_empty_or_slashed_id_before_sending`, `test_client.py::test_request_a_3xx_to_a_write_is_a_failure_and_is_not_followed` |

## Open — left by V4-14 (the 1.8.3 bump, landed)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 104 | V5-02 owner / coordinator | `testing/src/lkap_testing/fake_ctx.py` (`FakeUiChannel`) | `testing`'s `uv run mypy --strict src` fails with one error: `fake_ctx.py:596: Incompatible types in assignment (expression has type "FakeUiChannel", variable has type "UiChannel")`. The SDK bump did not cause this. It came from `d128e20` (V5-02 follow-ups, already on main at `098b541`), which added `request_block` / `pending_requests` / `cancel_pending` to the `packs.base.UiChannel` protocol and to the worker's no-op channel, but not to the shared fake. So `python.yml`'s `testing` matrix job fails its mypy step on main as well. Edit: add the three members to `FakeUiChannel`, mirroring the worker's no-op channel (return `None`, an empty mapping, and `[]`). `testing`'s ruff, format and pytest (15 passed) are green on 1.8.3 | V4-14 | **Done** (V4-14 merge, 2026-09-25): `FakeUiChannel` gains `request_block`, `pending_requests`, `cancel_pending`; `testing` mypy clean |

## Open — left by V4-13 (Console: tool execution fields, MCP table, Conversation card, activity detail, timeline events)

(Filed as #92–#95; renumbered #120–#123 at merge because the costs design holds #92–#96.)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 120 | coordinator (Fable, FYI) | `web/tests/console-zod-resolver.test.ts` | Not in V4-13's exclusive file list, but `toolsConfigSchema`/`voiceConfigSchema` gaining the required `execution_default`/`builtin_execution`/`thinking_sound` fields (the card's own scope) broke this pre-existing fixture's `pnpm typecheck` — its literal `voice`/`tools` objects predate those fields. Fixed by factoring them into two new top-level constants (`VOICE`, `TOOLS`, mirroring the file's existing `LIMITS`/`RECORDING`/… pattern) and reusing them at all four call sites; no assertion changed | V4-13 | FYI |
| 121 | coordinator (Fable, FYI) | `web/tests/console-tools-tab.test.tsx` | Not in the card's named test-file list (only console-http-tool-editor/console-mcp-tool-editor/console-instructions-tab/console-session-detail/panels-activity are named), but the card's own acceptance bullet ("Tools tab: the chip opens the dialog and writes `tools.builtin_execution.search_knowledge`") needed a test and this file is the pre-existing, dedicated `ToolsTab` suite. Extended with two `describe` blocks (four tests): the chip appears only on `search_knowledge`/`describe_current_frame`/`http_request` and Save writes `config.tools.builtin_execution.search_knowledge`; the `max_tool_steps` field auto-opens and carries `data-issue-path` when the warning applies (added after an advisor review found the Instructions-tab-only wiring never gets focused, since `tools.*` routes to this section). Nothing pre-existing changed | V4-13 | FYI |
| 122 | — | `web/src/components/console/tools/mcp-tool-editor-dialog.tsx` | ~~Was going to be filed as a follow-up (the MCP table not warning when a `background`/`auto` row has `report_progress` off)~~ — added instead, in-card, after review: a row now shows "No progress messages — the agent won't announce this tool." under its "Announce progress" switch whenever `mode !== "blocking"` and the switch is off, mirroring `config_service.py`'s warning. Not a cross-package ask; row kept only so the number isn't reused | V4-13 | **Done** |
| 123 | coordinator (Fable) | `docs/v4/BACKGROUND-TOOLS.md` §7 | §7 describes the HTTP editor's "Runs" select (and the builtin dialog's) as three options with "Blocking" as the default: "*Blocking* (default), *In the background*, *Automatic*". Built as written, every GET tool ever saved through the editor would post an explicit `mode: "blocking"`, which (`resolve_execution`: `declared.mode or (agent_default if is_read else "blocking")`) permanently overrides `tools.execution_default` for that tool — defeating the very card this feature is part of. Shipped instead: a fourth option, **"Agent default"** (posts `mode: null`), pre-selected for an unset/new tool; "Blocking" is now an explicit, deliberate choice, not the silent default. Same change in the builtin-execution dialog. Ask: amend §7 to read "Agent default (`null`, the initial state) / Blocking / In the background / Automatic", so the doc matches what ships and future packages don't rebuild the mode picker off the stale three-option text | V4-13 (found on advisor review of the first V4-13 commit) | **Ruled: R-V4-57** — four options, Agent default initial; §7 edited at merge |

## Open — left by the #102/#103 fixes (filed as #110; renumbered #124 at merge)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 124 | V4-12 owner (agent execution) / later package | `agent/src/lkap_agent/tools/execution.py` (`cancel_running`), `tools/declarative.py` (`build_mcp_toolsets`) | Found while fixing #102. A non-blocking **MCP** tool does not go through `run_with_policy`, so it is not in LKAP's `_RUNNING` registry: a text rewind neither cancels it nor waits for it. It keeps running, the SDK keeps injecting its "still in progress" placeholder (correct, since it is still running), and its result lands in the rewound conversation. Fixing it needs either the SDK's `_RunningTasks` (private) or a hook around `MCPToolset` calls. Not changed here | #102 fix | Open |

## Open — left by V4-15 (costs, api side)

(Numbered from #130. If another package appended in parallel, renumber at merge and check each row by its content.)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 130 | coordinator (FYI) | `contracts/tests/test_v2.py` (`test_ambiguous_inference_models_stay_unpriced`) | Outside the card's files: the test pinned `livekit-inference-tts inworld/inworld-tts-2` as unpriced, which COSTS.md §1.3 now prices ($25/1M chars, read 2026-09-25). One assertion flipped to `is not None`; `openai/gpt-oss-120b` stays unpriced (ask #93). No other edit | V4-15 | Accepted (R-V4-62); superseded by R-V4-58's second flip |
| 131 | MCP docs owner / coordinator | `mcp/src/lkap_mcp/docs/__init__.py` (`RECIPE_NAMES`), `AGENTS.md` and `mcp/src/lkap_mcp/docs/guide.md` recipe lists | The new recipe `estimate-agent-cost` is served by the live server (`content.recipe_names()` includes every file present) and doc-linted as a file, but the loader's pinned `RECIPE_NAMES` (not in V4-15's files) does not list it, so `docs.recipe("estimate-agent-cost")` raises and the per-recipe JSON-args lint skips it. Add it to `RECIPE_NAMES` (after `test-a-custom-model`) and to the recipe lists | V4-15 | Done — RECIPE_NAMES updated (R-V4-62) |
| 132 | coordinator (Fable ruling) | `contracts/src/lkap_contracts/providers.py` (`azure-openai-realtime`) | **No `price_ref` on Azure OpenAI Realtime.** The card allows the alias "only where the vendor bills identically"; Azure bills through its own price sheet by deployment type (Global / Data Zone / Regional differ), so it is left unpriced (workspace price) rather than aliased to `openai-realtime`. Rule whether a Global-deployment alias with a `tier_note` is wanted | V4-15 | Ruled: R-V4-59 — stays unpriced, no alias |
| 133 | coordinator (Fable ruling) | `contracts/src/lkap_contracts/pricing.py` (`deepgram-stt` rows, `OPENROUTER_TWINS`) | **Deepgram streaming prices are a limited-time promotion** on the page read 2026-09-25 (Nova-3 $0.0048/min, Flux $0.0065/min; regular $0.0077/min). The rows carry the current (promo) figure with the regular one in `tier_note`; when the promotion ends a re-read must bump them. Separately, the `deepgram-stt nova-3 ↔ deepgram/nova-3` twin compares Deepgram's streaming rate with OpenRouter's pre-recorded $0.0043/min, so `price_drift` will report ≈ 10 % every week — keep it as a reminder or drop that twin | V4-15 | Done (R-V4-60) — promotional rows stale at 30 days in the drift job; `nova-3` OpenRouter twin dropped, `aura-2` kept |
| 134 | contracts owner (registry) | `contracts/src/lkap_contracts/providers.py` (`deepgram-tts` `models`) | The registry's `deepgram-tts` model ids are voice ids (`aura-2-andromeda-en`), so the model-specific `aura-2` / `aura-1` rows never match and the model-agnostic row (Aura-2's $0.030/1k) prices every voice; an Aura-1 voice is over-estimated 2×. Either normalise the model id to its family before pricing (a prefix rule in `pricing.quote`) or list Aura-1 voices under their own rows | V4-15 | Done (R-V4-61) — `pricing.lookup` prices by the longest family prefix followed by `-`; one row per Aura-1 voice id; agnostic row last |
| 135 | contracts owner (registry) | `contracts/src/lkap_contracts/providers.py` (`openai-realtime` `models`), `pricing.py` | OpenAI's pricing page now leads with `gpt-realtime-2.1` (same audio prices, text out $24.00/1M) and lists `gpt-live-1` at $0.05/min; the registry lists only `gpt-realtime`. Add the ids (and their rows, re-read that day) when promoted | V4-15 | Accepted as filed (R-V4-62) — registry owner |
| 136 | coordinator (Fable ruling) | `contracts/src/lkap_contracts/pricing.py` (`inference-turn-detector`, `inference-vad`) | LiveKit's pricing page shows the turn-detection / interruption models as a plan-column icon with no price and never the word "included", so no known-zero row was added (COSTS.md §1.3: "only if the page states inclusion"). `eot_usage`/`interruption_usage` render as unpriced `requests` lines. Rule whether the icon counts as "included" | V4-15 | Ruled: R-V4-59 — stays unpriced, no known-zero; `eot_usage`/`interruption_usage` requests lines shown unpriced |
| 137 | agent owner | `agent/src/lkap_agent/providers/image_gen.py`, `observability.py` | **Image generation is not counted.** The actual-cost side prices `asset {kind: "image"}` session events under `images` (COSTS.md D-V4-44), but the worker emits no such event, so an image-generating session shows no image line. Emit one event per generated image (ids only) | V4-15 | Accepted as filed (R-V4-62) — V4-17 |
| 138 | V4-16 (console) / coordinator | `web/src/components/console/**` | `CostEstimateRequest.workspace_averages` (additive, second contracts commit): the estimate route applies the workspace's averages only when it is `true`, which is what "Use my workspace's averages" should send; the session snapshot always applies them. `CostLine` also gained `price_source`, `vendor_usd`, `vendor_ref` for the Cost tab | V4-15 | Done (R-V4-62) — `share_pct` is the share of the range's total with a synthetic `other` row; `workspace_averages` stays an explicit request flag |

## Open — left by the V4-15 fixes (R-V4-58 … R-V4-62, integ-db)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 139 | coordinator (Fable ruling) | `contracts/src/lkap_contracts/pricing.py` (`lookup`) | **R-V4-61's family-prefix step is limited to TTS entries** (`kind == "tts"`, which includes `livekit-inference-tts`). Applied to every kind, it priced the custom id `gpt-4.1-nano-2026` at the `gpt-4.1` row (20× the nano rate, no note) and broke V4-08's `test_a_passing_llm_test_is_recorded_with_detected_tools` ("no price on file"); `gemini-2.5-flash-lite` → `gemini-2.5-flash` and `gpt-4o-audio-preview` → `gpt-4o` would mis-price the same way. Every acceptance case of the ruling is TTS, and no registry id outside `deepgram-tts` changes. Inside TTS the rule can still under-price a quality variant typed as a custom id: `tts-1-hd` → the `tts-1` row ($15/1M chars, OpenAI charges $30/1M). Rule both edges: whether dated LLM snapshots (`gpt-4o-2024-08-06`) should take a narrower rule (a date suffix only), and whether `openai-tts` needs a `tts-1-hd` row or an exclusion | V4-15 fixes | Done (`bf6b405`) — Ruled: R-V4-66 — TTS-only confirmed; date-suffix rule cross-kind; tts-1-hd row + contracts test |

## Open — left by the V4-14 live check (`_briefs/v4-14-live.md`, 2026-09-25; filed as #105–#107; renumbered #125–#127 at merge)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 125 | coordinator (the live protocol) | `docs/v4/_briefs/v4-14-live.md` "Before you start" 6 and step 1a | **Step 1's premise does not hold on `google/gemma-4-31b-it`: with the brief's description the model never calls `demo_flow_delay` on `identify`.** Five sessions, no call. With the exact turns (`38f1d123…`, `484e85b1…`) the model asked for the full phone number, because `identify` says to confirm the number digit by digit and "phone ending 0000" is not one. So turn 2 cannot confirm either, and 1a can never reach `collect_booking` as written. With a full fictional number (`dc973f0a…`) it said **"Checking that now."** and made no call (a claimed lookup, the same family as #102). An outright request (`5d6bc6cc…`) also got no call. The worker showed the tool was wired: `config_version=3` on every job, and no "flow node references a tool this session does not have". After `tool_update` set the description to "Look up the caller's account status. Call this whenever the caller asks about their account status, even before their number is confirmed. Takes a few seconds.", the model called it every time (`6610903e…`, `7c09fbd5…`, `464af269…`). Suggested: put that description in "Before you start" 6, and give 1a a full number in turn 1 (a fictional 555-01xx). | V4-14 live check | **Ruled: R-V4-63** — protocol fix in the brief (explicit description, full 555-01xx number); no code |
| 126 | coordinator (Fable) → flow runtime owner, or upstream | `agent/src/lkap_agent/flow/edges.py` / `flow/runtime.py` (edge tools); SDK `livekit/agents/voice/agent_activity.py` 1.8.3 (the tool-output loop, ~3963–4030) | **On a routing start, when an edge tool and a sibling that returns output share a batch, the caller first hears a reply from the draining router, not from the target node.** 1.8.3 keeps the handoff (#7321 fixed: `path ["start","claims"]` in both 2a and 2b). But after `update_agent(new_agent_task)` the SDK sets `draining = True` and still generates the tool response on the **old** activity, with the router's instructions and `tool_choice="none"`. 2a (`274e452e…`, blocking sibling): the first reply was "Checking that now. I can certainly help you file a claim. To get started, could you please provide your policy number?" (17.929), and the claims node's question came 7.2 s later (25.110). The claims node's on-enter had re-run the `global` tool, taking another 4.2 s. 2b (`31e37b12…`, background): the router answered "…What happened?" before the claims node spoke. On 2a the brief's criterion "the reply is the claims node's question" is therefore only met by the second message. Options: have an edge tool's batch skip or forward the sibling's tool reply (for example, mark the sibling's output so `has_tool_reply` is false once a handoff is pending); document this as upstream behaviour; or reword the 2a criterion. Not changed here. | V4-14 live check | **Ruled: R-V4-64** — LKAP fix in the flow runtime (ToolResult reply_required=False after a handoff); card V4-19 |
| 127 | api owner (knowledge seeding) | `api/src/lkap_api/kb/seed.py` (`import_kb_seeds`: "not committed here (the caller controls the transaction)"), the `POST /v1/agents` template path, `kb/embed.py` | **Creating an agent from a starter with `kb_seeds`, on an install with an empty fastembed cache, holds the SQLite write lock while the embedder loads. The api then wedges.** Fresh scratch DB, `agent_create(template_id="receptionist")`: the api logged `kb_seed_kb_created` → `fastembed_model_loading model=BAAI/bge-small-en-v1.5` (empty cache dir), and the request never returned (MCP `ReadTimeout` at 30 s). For ~90 s every other write failed with `sqlite3.OperationalError: database is locked`: `worker_instances_sweep_failed`, `sessions_sweep_failed`, and `unhandled_error error_type=OperationalError path=/internal/v1/workers/{instance_key}/heartbeat` → 500. With the model cache pre-populated, the same create took 0.5 s. Suggested: load or warm the embedder before the transaction takes its first write (or at startup), or embed the seeds after the commit or in a background task, so the KB row insert does not keep the lock through a model download. | V4-14 live check | **Ruled: R-V4-65** — seeds via storage + KB_INGEST after commit, embedder warm-up, SQLite WAL/busy_timeout; card V4-18 after V5-01 merges |

## Open — left by V4-19 (no draining-node reply after a handoff)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 150 | coordinator (Fable) | `docs/v4/PLAN-V4.md` (R-V4-64 and the V4-19 card's Scope/Acceptance) | **The card prescribes a mechanism that cannot close #126, so V4-19 used a different one with the same effect. Please amend the wording.** Three SDK facts (1.8.3, verified in the installed package). (1) `generation.py` `_execute_tools_task` gathers the **whole** batch before `_pipeline_reply_task_impl` builds `has_tool_reply` over all its outputs (`agent_activity.py` ~3943–3985). Completion order inside a batch never matters, so the card's "outputs that complete before the edge tool are untouched (the SDK already handles that order)" is false: the new real-SDK case `sibling-before-edge` got a router reply before the fix, just like `sibling-after-edge`. (2) In 2b the router's reply came from a background tool's **synthetic** first output, which `RunContext.update()` builds (`events.py` ~240). That is not the tool's return value, so a `ToolResult(..., reply_required=False)` wrapper around the tool body never reaches it. (3) The SDK merges nothing into the new agent on a handoff (`agent_session.py` `_update_activity`), and the target node copied `source.chat_ctx` inside the edge tool, before the batch was committed. So "the output pair still rides into the target's copied context" was also false: before the fix the claims node's context had no `lookup_policy` pair (asserted in the new test), which fits live 2a's claims node re-running the `global` tool. **What V4-19 does** (`agent/src/lkap_agent/flow/runtime.py` only): a synchronous `function_tools_executed` handler registered in `FlowRuntime.__init__` (`FlowRuntime.on_function_tools_executed`). When the batch hands off (`ev.has_agent_handoff`), it calls `ev.cancel_tool_reply()`, the SDK's public knob that marks every output `reply_required=False`; `PlatformAgent.on_function_tools_executed` already uses the same sync-handler pattern. It then carries the batch's pairs into the target through `Agent.update_chat_ctx` (filtered to the target's tools), tracked with the extractions so the target's `on_enter` `settle()` awaits it. No `ToolResult` wrapper, no `_handoff_turn` marker, no tripwire import. The edge tool records the call id of the agent it returned (`_handoff_targets`) to find the target. Card acceptance: all four bullets are met by tests. Two are worded differently: "sibling finishing first behaves as before" is now "behaves like the sibling-after case (no router reply, target first)", and the grep bullet also hits `platform_agent.py:683` at HEAD (see #151). **Upstream-worthy** (optional, LKAP does not depend on it): skip the draining activity's tool reply when a handoff is pending, and merge the batch's pairs into the new agent's context. **Limit (realtime, untested):** plugins with `auto_tool_reply_generation=True` generate the tool reply server-side. In 1.8.3 these are `google/realtime/realtime_api.py:357` (Gemini Live, LKAP's `realtime`/`half_cascade` model) and `openai/realtime/gpt_live_model.py:200`. Gemini honours `reply_required=False` only through `SILENT` scheduling, which needs `tool_behavior=NON_BLOCKING` and is not available on Vertex (`realtime_api.py` ~720–743). Otherwise it logs "Gemini will answer it anyway" and the draining router may still speak on a realtime flow. 2a and 2b were cascaded. A realtime rerun of 2a (or a ruling that `NON_BLOCKING` is required for flows on Gemini) would settle it. | V4-19 | **Ruled: R-V4-67** — R-V4-64 amended and the V4-19 card rewritten to the shipped cancel_tool_reply + _carry_batch mechanism; wrapper withdrawn |
| 151 | whoever next owns `platform_agent.py` | `agent/src/lkap_agent/platform_agent.py` (`on_function_tools_executed` docstring, ~l.683) | The docstring says `reply_required` "is only honoured by realtime models … cascaded LLMs always answer a tool output". In 1.8.3 the pipeline (cascaded) path honours it too (`agent_activity.py` ~3985: `if fnc_executed_ev.has_tool_reply`). V4-19 relies on that. Suggested: drop the cascaded clause, or re-check whether `silent_reply` should now apply in cascaded mode too (a behaviour change, so a ruling first). | V4-19 | **Ruled: R-V4-68** — silent_reply honoured in cascaded mode on ≥ 1.8.3, docstring fixed; card V4-20 |
| 152 | coordinator (Fable) → D-V4-37 owner | `docs/v4/BACKGROUND-TOOLS.md` D-V4-37; `agent/src/lkap_agent/flow/runtime.py` | A background sibling that a transition cancels leaves its **announce** pair (the "still in progress" first update) in the target node's context, with no final result and no marker that it was cancelled. The target may tell the caller the lookup "is on its way" and it never arrives. Live 2b's claims node just re-ran the tool, so this is model-dependent. There is a second route to the same symptom: a **non-cancellable** background sibling (for example a POST declared `background`) is awaited by the drain. Its `_final` pair is then enqueued against the old node (`_owning_activity.agent`), and `_deliver_reply` stops on `ActivityClosedError`. The result is in `session.history` but not in the target's context, so the target again sees only the announce. Options: append a short "cancelled by the step change" output for that call id in the target's context (the runtime can see `tool_call_ended{cancelled}`), or leave it as is and document. Not changed by V4-19. | V4-19 | **Ruled: R-V4-69** — D-V4-37 amended: "cancelled" output for cancelled siblings, late results carried to the current node; card V4-20 |

## Open — left by V4-20 (`silent_reply` in cascaded mode; cancelled and late sibling results carried to the target node)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 160 | coordinator (Fable) | `agent/tests/unit/test_tool_execution.py` (`test_real_sdk_a_policy_sibling_of_an_edge_gets_no_router_reply_and_is_cancelled`, last assertion) | **Touched outside the card's exclusive files; please ratify.** V4-19's composition test pinned the exact behaviour R-V4-69 changes: "the claims node's first generation has exactly one `lookup_item` output (the announce)". With V4-20 it has two: the announce and the "Cancelled: …" pair the step change leaves (under `{call_id}_final`). The gate would be red without the change. The only edit is the unpack `(announce,) = outputs` → `announce, cancelled = outputs` plus two asserts: the second output starts with "Cancelled:" and has a different call id. This is the declarative HTTP path through `run_with_policy`, so it also covers R-V4-69 (a) end to end on the production wrapper. | V4-20 | **Accepted** (coordinator, V4-20 merge): the one assertion follows R-V4-69 |
| 161 | doc owners (`docs/ARCHITECTURE.md`; `packs/`; `agent/src/lkap_agent/tools/background.py`) | `docs/ARCHITECTURE.md:184` and `:213`; `packs/src/packs/insurance_claim/instructions.py:78-80` (comment); `agent/src/lkap_agent/tools/background.py:22-24` (module docstring) | After R-V4-68 the claim that `reply_required` "is only read by realtime models" and that "cascaded LLMs always reply / answer a tool output" is false on livekit-agents ≥ 1.8.3. `PlatformAgent.on_function_tools_executed` now silences a `silent_reply` batch in cascaded mode too, and V4-19's flow handoff relies on the pipeline reading `has_tool_reply`. Suggested wording: "honoured by realtime models, and by the cascaded pipeline from livekit-agents 1.8.3". No behaviour change for the insurance pack: all four of its `ToolMeta` rows have `silent_reply=False`, and its cascaded addendum stays correct for them. `docs/REVIEW-FINAL.md:193` says the same thing but is a historical review, so leave it as is. | V4-20 | **Ruled: R-V4-70** — doc-only sweep ("honoured by realtime models, and by the cascaded pipeline from livekit-agents 1.8.3"); REVIEW-FINAL left; owner V4-21 |
| 162 | agent owner (`platform_agent.py` / `tools/declarative.py`) → coordinator for a ruling | `contracts/src/lkap_contracts/tools.py:163` (`HttpToolDefinition.silent_reply`); `agent/src/lkap_agent/platform_agent.py` (`_silent_reply_tools`, ~l.281) | **An HTTP tool's `silent_reply` is not honoured by the worker in any mode.** `PlatformAgent._silent_reply_tools` is built only from `pack.tool_meta()` (plus `request_form` on realtime models). No code in `agent/src` reads `HttpToolDefinition.silent_reply` (`git grep silent_reply agent/src`). Yet the api validates it (`config_service.py:776`) and the MCP concept doc promises it (`mcp/src/lkap_mcp/docs/concepts/tools-http.md:25`: "the tool's result updates the UI without the model narrating it back"). Suggested: add the names of the session's declarative HTTP tools that have `silent_reply=True` to `_silent_reply_tools`, so they follow R-V4-68 (realtime always, cascaded on ≥ 1.8.3). This changes behaviour for existing HTTP tools that set the flag, so it needs a ruling first. Not changed by V4-20: outside the card's scope (pack `silent_reply`). | V4-20 | **Ruled: R-V4-71** — wired via _assemble → PlatformAgent(silent_reply_tools=…); realtime always, cascaded on ≥ 1.8.3; card V4-21 |

## Open — left by V4-21 (HTTP `silent_reply` wired into the worker; the `reply_required` doc sweep)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 170 | coordinator (Fable) → flow owner (`agent/src/lkap_agent/flow/{runtime,node_agent}.py`) | `agent/src/lkap_agent/flow/node_agent.py` (`FlowNodeAgent.__init__`'s `super().__init__(...)`); `agent/src/lkap_agent/flow/runtime.py` (`FlowServices`); the flow branch of `main.py::_assemble` | **An HTTP tool's `silent_reply` is still not honoured on flow agents.** The card says "the session-level registration already covers flow-node batches". That is true of the handler, but not of the names it checks. `run_session` registers `agent.on_function_tools_executed` for the agent `_assemble` returns. On a flow that agent is the entry `FlowNodeAgent`. The chain is `build_flow_agent(FlowServices(...))` → `FlowRuntime` → `FlowNodeAgent.__init__` → `PlatformAgent.__init__(ctx=, pack=, tools=, …)`, and it never passes `silent_reply_tools`, so the entry agent's `_silent_reply_tools` holds the pack set only. V4-21 passes `silent_http` only on the non-flow `PlatformAgent(...)` branch, because `flow/` is outside the card. Suggested fix, a few lines: add `silent_reply_tools: frozenset[str] = frozenset()` to `FlowServices`, have `_assemble` pass `silent_reply_tools=silent_http` into it, and have `FlowNodeAgent.__init__` forward `services.silent_reply_tools` to `super().__init__`. The session has one handler, so only the entry node's set is read. Every node gets the same set, so the result is consistent. After this fix, the `info` line for cascaded below 1.8.3 would be logged once per node construction, not once per session. To keep it once per session, gate it on `entry`, or move it to `_assemble`. Test: `_assemble` on a flow config with a silent HTTP row → `agent._silent_reply_tools` contains it. `_briefs/v4-21-live.md` runs on non-flow agents until this is ruled. | V4-21 | **Done** (`ebb4bbe`, coordinator-approved extension of V4-21 to `flow/**` and the flow branch of `_assemble`): `FlowServices.silent_reply_tools` is forwarded to every `FlowNodeAgent`, and the info line is gated on `entry`. The live brief has a flow step |
| 171 | coordinator (Fable) | `docs/v4/PLAN-V4.md` (V4-21 Acceptance, the grep bullet) | **Please ratify how the acceptance grep was read.** `git grep -n "only read by realtime\|only honoured by realtime\|always answer a tool output\|always reply" -- docs agent packs mcp ':!docs/REVIEW-FINAL.md'` has 0 hits outside `docs/v4/`. The remaining hits quote the stale sentence: the `PLAN-V4.md` V4-20 and V4-21 cards and R-V4-68, and `_asks.md` #151 and #161. They are records, not current-fact claims, and the card's own grep bullet matches itself. So they were left as they are. Suggested wording: add `':!docs/v4'` to the pathspec. | V4-21 | **Accepted** (coordinator) |

## Open — left by V4-16 (console)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 140 | contracts owner (registry) / analytics route owner | `contracts/src/lkap_contracts/api_models.py` (`AnalyticsDriver`), `api/src/lkap_api/routers/analytics.py` | `AnalyticsDriver` (`GET /v1/analytics/summary`'s `top_drivers`) carries only `provider_id`/`model`/`unit` — no `slot` and no plain `label`, unlike `CostDriver` (which has `slot`, joined against `EstimateLine.label` server-side). The analytics "Top cost drivers" table (`web/src/components/console/analytics/analytics-view.tsx::driverLabel`) works around this with a client-side heuristic (a `provider_id → ProviderSpec.kind → SLOT_LABELS` lookup, plus a hardcoded map for the four `livekit-*` pseudo ids), which drifts if the registry's `kind` values or the pseudo ids ever change independently of the console. Add `slot: EstimateSlot` (and ideally `label: str`) to `AnalyticsDriver`, filled the same way `drivers()` already does for `CostDriver` in `costs/service.py`, so the plain name is server-authored everywhere D-V4-47 asks for one | V4-16 | Open |

## Open — left by V4-17 (reconciliation: the `provider_requests` event, the `cost_reconcile` job)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 190 | agent owner (`main.py::_assemble` / `providers/image_gen.py`) | `agent/src/lkap_agent/main.py` (the `SessionContext(...)` call, `image_gen=providers.image_gen`), or each `generate` in `agent/src/lkap_agent/providers/image_gen.py` | **#137 is still open: generated images are not counted.** R-V4-62 routed #137 to V4-17, but the card's files cannot reach it. The api prices `asset {kind: "image"}` events (`costs/service.py::image_count`) and the worker emits none. The only generation call is a pack tool (`packs/insurance_claim/tools.py::draw_incident_sketch`, `ctx.image_gen.generate(prompt)`); the asset it pushes is `kind="sketch"`, a display asset, not a billing signal. `observability.py` never sees a generation, and V4-17's one `main.py` hunk is the `SessionObserver(...)` call. Suggested fix, a few lines: a small wrapper whose `generate` awaits the provider's and, on success, calls `record_event("asset", {"kind": "image", "provider_id": <slot provider>, "model": <slot model>})` (no bytes, no prompt), applied where `main.py` builds `SessionContext(image_gen=...)` with the session's `record_event`. Test: a fake image provider behind the wrapper → one `asset` event per successful call, none on a failure; the api's `image_lines` already prices the count | V4-17 | Open |
| 191 | coordinator (Fable ruling) → V4-17 follow-up | `api/src/lkap_api/jobs/reconcile.py` (`_plan`) | **`openrouter-stt` / `openrouter-tts` slots are not reconciled.** The worker already posts their `stt`/`tts` request ids under the opt-in. But those entries run through `livekit-plugins-openai`'s STT/TTS classes, and nothing says their `request_id` is an OpenRouter `gen-…` id: the TTS takes the HTTP response's request id header (`tts.py:283`, `stream.request_id`), the STT a transcription item id (`stt.py:881`/`907`) or none. If they are not generation ids, `/generation` answers 404 and the job would retry for nothing. V4-17 reconciles the LLM slot only. Ask: rule whether a live run should first record one OpenRouter STT/TTS session's `provider_requests` ids and try one lookup by hand. If they resolve, extending `_plan` to those slots is a few lines (the same client and write path, `vendor_ref` on the slot's line) | V4-17 | Open |
| 192 | V4-16 owner (console) / coordinator | `web/src/components/console/settings/**` (beside the Your prices dialog, or a workspace settings card) | **The opt-in has no console control.** V4-17 stores it with `PUT /v1/workspaces/{id}` `{"settings": {"cost": {"reconcile": ["openrouter"]}}}` (the only route that writes `workspaces.settings`). `settings.cost` now merges one level down, so `cost.prices` survives, and `reconcile` is validated: only `openrouter`, 422 otherwise. An API key reaches that route only with the `*` scope, so an MCP client with the Admin preset cannot switch it on. Suggested: one admin-only switch "Check OpenRouter's own charge after each call" beside the Your prices table, reading `settings.cost.reconcile` from `GET /v1/workspaces` and writing the same `PUT`. Help text: "LKAP reads what OpenRouter charged for each reply, using your OpenRouter key. Only the request ids leave the call." | V4-17 | Open |
| 193 | coordinator (FYI, ratify) | `api/src/lkap_api/costs/service.py`, `api/src/lkap_api/routers/internal.py`, `api/src/lkap_api/routers/workspaces.py` | **Two card phrases, and how they were read.** (1) "`cost_session` enqueues the job" became "decides, and the route enqueues after the commit". `cost_session` runs inside `put_summary`'s open transaction, and `JobsService.enqueue` opens its own connection (the ask #40 SQLite lock). So `costs/service.py` gained `reconcile_due(db, session)`: the workspace opted into a built vendor and a `provider_requests` event with LLM ids exists. `put_summary` calls `enqueue_reconcile(jobs, session_id, background_tasks=...)` after `db.commit()`, beside `webhooks.emit`. (2) "`reconcile` in the same `PUT`" became the workspace `PUT` (`routers/workspaces.py`), not `PUT /v1/workspace/prices`, whose body is V4-15's `WorkspacePricesIn` (a contracts change outside V4-17). `jobs/handlers.py::load_all_handlers` registers the job, as the coordinator directed | V4-17 | Open |
