/**
 * Re-export of the shared primitive (docs/UI_UX_SPEC.md §7.1) so existing
 * `@/components/console/shared/page-header` imports keep working.
 */
export { PageHeader, type BreadcrumbEntry, type PageHeaderProps } from "@/components/shared/page-header";
