# web/e2e — Playwright smoke tests

Run: `pnpm e2e` against a running app (`pnpm dev` or `next start`). No specs
yet — W3-E2E-INSURANCE adds the console → session → transcript smoke test
(docs/IMPLEMENTATION_PLAN.md).
