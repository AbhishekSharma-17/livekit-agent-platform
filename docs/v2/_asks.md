# Phase 1 cross-package asks

One line per change a package needs in a file it does not own (`PLAN-V2.md` §
"Exclusive ownership"). Format: **owner package** — file — the exact edit — who asked.

Created by V2-00.

## Open — left by V2-00 (Contracts v2)

These are the only places where the v2 contracts break a v1 consumer. V2-00 owns
`contracts/**`, the two pack manifests and the regenerated
`web/src/contracts/lkap-contracts.d.ts` only; the coordinator granted it the
api/agent/packs files (asks 1–4, now closed) and holds the two `web/` ones back
while WP-2 is in flight.

| # | Owner | File | Edit | Why |
|---|---|---|---|---|
| 5 | WP-3 / V2-13 (web agent editor) | `web/src/components/console/agents/defaults.ts` | `DEFAULT_VOICE += first_speaker: "agent"`, `DEFAULT_CAPABILITIES += dtmf: false` | `VoiceConfig.first_speaker` and `CapabilitiesConfig.dtmf` (CONTRACTS-V2 §4.3); both are `Required<…>` in that file |
| 6 | WP-3 / V2-13 (web agent editor) | `web/src/components/console/lib/schemas.ts` | Add `"half_cascade"` to the zod `mode` enum of `agentEditorFormSchema` | `PipelineMode` gained `half_cascade`; `agent-editor.tsx:37` assigns `config.pipeline.mode` into the form type |

Asks 1–4 were applied by V2-00 after the coordinator granted the files
(2026-09-20); `agent`, `api` and `packs` are back at their baseline counts
(331 / 230 / 189 + 5 xfail). Asks 5–6 stay open: WP-2 is editing `web/` and the
coordinator applies them once it lands. Until then `pnpm typecheck` reports
exactly those three errors.

These six were the *complete* list of downstream breaks V2-00 caused. Other web
`tsc`/vitest failures seen the same day (`pre-call-card`, `agents-table`,
`end-of-call-card`, `create-agent-dialog`, a transient `tests/zzrepro.test.tsx`)
come from the parallel UI packages, not from contracts.

## Open — left by V2-01 (DB schema v2)

V2-01 needed nothing in a file it does not own, so these are **hand-offs**, not
edits it wants made: decisions it had to take in its own files that wave-1
packages must build on, plus one gap in a file V2-00 owns.

| # | Owner | File / subject | What V2-01 did, and what the owner must do |
|---|---|---|---|
| 11 | V2-00 / coordinator — **applied by V2-02** (coordinator grant): `HealthResponse.agents_unbound: int = 0` (contracts regenerated, `web/src/contracts` byte-identical to `contracts/generated/ts`); `db` is `ok` only when `alembic_version` equals the head read from `api/alembic` (else `error`, with a `health_schema_not_at_head` warning) | `contracts/**` `HealthResponse` + `api/src/lkap_api/routers/health.py` | CONTRACTS-V2 §1.3 says `/v1/health` reports `agents_unbound: n`, but `HealthResponse` has no such field and neither file is V2-01's. **Not implemented.** `health.py` also has no owner in any card — the coordinator should assign it (V2-02 is the natural home, it already touches every admin route). The V2-01 live acceptance only needed `db: ok`, which passes. **Whoever takes it should also make the check compare `alembic_version` to the script head**: today it only does `select(Agent.id).limit(1)`, so a half-migrated or un-migrated schema still reports `db: "ok"` while the v2 columns are missing (see #17). |
| 12 | V2-02 | `api/src/lkap_api/auth/passwords.py` (new) | `bootstrap.ensure_owner` imports `lkap_api.auth.passwords.hash_password` lazily via `importlib` and stores `password_hash=NULL` when it is absent, so V2-01 did not pick an argon2 library on V2-02's behalf. Once that module exists, bootstrap hashes `LKAP_BOOTSTRAP_OWNER_PASSWORD` (or a `secrets.token_urlsafe(24)` logged once at WARNING). **V2-02 must also set the password on the *existing* owner row**, which today has a NULL hash and cannot sign in. Note `api/src/lkap_api/auth.py` is still a v1 *module*; turning it into the `auth/` package is V2-02's to resolve. |
| 13 | V2-02 — **applied by V2-02**: `tenant_guard` is `autouse=True`; it checks every application query (anything under `lkap_api/`, whether reached through a route or called directly by a test) and exempts only reads issued by test code itself (`exempt=` hook added to `tenant_scope_guard`) | `api/tests/conftest.py` — `tenant_guard` fixture | The "no unscoped tenant query" guard (`lkap_api.db.guard`) ships **opt-in**, not autouse: every v1 router still queries unscoped, so autouse would fail ~200 tests today. V2-02 flips `tenant_guard` to `autouse=True` once `WorkspaceContext` is on the admin routers. `audit_log` and `workspace_members` are deliberately outside `TENANT_TABLES` (see the test for why). |
| 14 | V2-02 | `workspace_id` defaults | Every tenant model column has a **Python-side default** of `DEFAULT_WORKSPACE_ID` (and a matching `server_default` in `v2_003`). That is what keeps the v1 routers inserting successfully after the tenancy migration (zero downtime); V2-02 replaces the default with the value from `WorkspaceContext`. |
| 15 | V2-03 / V2-08 / V2-17 | `*_ct` column encoding | Every encrypted column (`livekit_connections.api_key_ct`/`api_secret_ct`, `storage_configs.access_key_ct`/`secret_key_ct`, `webhook_endpoints.secret_ct`, `sip_trunks.auth_password_ct`) holds **`Vault` ciphertext over a one-key secret bag**, exactly like v1's `credentials.ciphertext`: `vault.encrypt({"api_key": k})`. Read them back with `Vault.decrypt(...)["api_key"]`. `python -m lkap_api.keys rotate` relies on this uniformity. |
| 16 | V2-03 | `fleet_desired` | The table exists and is empty. Bootstrap does **not** write it — V2-03 owns the `fleet_desired` writer, so a connection created by bootstrap has no desired-state row until V2-03 lands. |
| 17 | **Coordinator** (decision), V2-11 / WP-3 | Migrating the developer's live `api/data/lkap.db` | **This is a decision, not an instruction — and it cannot be deferred indefinitely.** Facts: (a) the v2 `Agent` model selects `workspace_id`, `connection_id`, `mode`, `limits`, `allowed_origins`, so as soon as the dev api restarts on V2-01 code against the un-migrated file, every agent route 500s with `no such column` — while `/v1/health` still answers `db: "ok"`, because it only selects `agents.id` (hence the addition asked for in #11). The process on :8080 is still running pre-V2-01 code and is unaffected **until it restarts**. (b) `upgrade head` moves 7 of the 10 live agents from `ui_panel_id="generic"` to `"composite"` with the four default blocks; `downgrade 4135323c6ecc` restores `generic`. Both verified on a copy. **Decision needed:** either accept that those 7 dev agents' panels do not render until V2-11, or file a one-line web ask to treat `composite` as `generic` in the interim. Sequence when migrating (no need to stop anything first): `sqlite3 api/data/lkap.db ".backup <path outside api/data>"`, then `cd api && uv run alembic upgrade head`, then restart the api. Take the backup before **every** alembic run on that file: SQLite DDL is non-transactional, so a mid-chain failure leaves `alembic_version` out of step with the schema. |
| 18 | **Closed by V2-19B** (2026-09-23): the stub is gone; `python.yml` job `test-postgres` runs up/down/up/down-base plus the full api suite on `postgres:16` (see `_briefs/migration-rehearsal.md`) | `.github/workflows/api-postgres.yml` | V2-01 shipped it as the stub its card allows. V2-09 folds the Postgres leg into its own `python.yml` matrix and should then delete this file. It is also the **only** place the v2 chain is exercised against a real server (no Docker on the dev host — see the V2-01 report). |

## Open — left by V2-03 (Connections service)

| # | Owner | File | Edit / hand-off | Why |
|---|---|---|---|---|
| 19 | V2-02 — **applied by V2-02** | `api/src/lkap_api/routers/connect.py` | Mint through the agent's connection: `minted = await mint_session_token(db, factory, agent, session_id=…, room_name=…, identity=…, participant_name=…, channel=…, attributes=…, ttl=…)` (`lkap_api.connections.service`; `factory: ClientFactoryDep` from `lkap_api.connections.clients`), store `SessionRow.connection_id=minted.connection_id`, return `serverUrl=minted.server_url`, and drop the `settings.livekit_*` / `settings.agent_name` reads. | V2-03 acceptance "connect for an agent bound to B mints with B's secret and returns B's URL" is implemented and tested at `mint_session_token` (`tests/test_connections_minting.py`); the route is V2-02's file this wave. Until applied, connect keeps minting with the env credentials, which equal the default connection's, so nothing breaks. |
| 20 | Coordinator (agents router has no wave-1 owner; V2-12 in wave 2) — **applied by V2-02** (coordinator grant): (a) `validate_in_db`, (b) `connection_id` on create/update (must belong to the workspace, 422 otherwise; default = workspace default), (c) `connection_context_for` in seeding | `api/src/lkap_api/routers/agents.py` | (a) `validate_stored_config` → `config_service.validate_in_db(db, config, workspace_id=agent.workspace_id, connection_id=agent.connection_id)`; (b) `create_agent` sets `connection_id = payload.connection_id or <workspace default>` and `update_agent` honours `AgentUpdate.connection_id` (must belong to the workspace); (c) `_seed_config` passes `connection=await config_service.connection_context_for(db, workspace_id=…, connection_id=…)` to `seed_config_from_manifest`. | Without (a) the R-V2-2 gate (installed-on + connection flags + `VALIDATORS`) only runs when a caller passes a connection; save/publish still get the v1 rules. Without (b) new agents stay `connection_id=NULL`; `resolve_agent_connection` then uses the workspace default at connect/resolve time. |
| 21 | V2-03 at merge, or V2-19 — **applied by V2-02** (coordinator grant): `current_workspace_id` now reads `AdminCtxDep` (role/scope per `ROUTE_POLICY`: reads `viewer`+`connections:read`, writes `admin`+`connections:write`); `hooks.py` untouched (signature auth only); `internal.py` untouched (service token; workspace comes from the session/agent row) | `api/src/lkap_api/routers/connections.py`, `routers/hooks.py` | Swap `AdminDep` + the `current_workspace_id()` seam for V2-02's `WorkspaceContext` (`admin` role; `connections:read` / `connections:write` scopes) once `lkap_api.auth.deps` is merged. `/hooks/livekit/*` stays unauthenticated (the JWT signature is the auth). | V2-02's auth package was still changing while V2-03 ran in parallel. |
| 22 | V2-04 | `fleet_desired` / worker env (hand-off) | Build on `lkap_api.connections.service`: `list_fleet_desired(db, packs)` → body of `GET /internal/v1/fleet/desired`; `set_desired_replicas(db, row, n, packs)` → `POST …/fleet` start/stop; `write_fleet_desired` already runs on every connection write. The decrypted env is `GET /internal/v1/connections/{id}/worker-env` (lives in `routers/internal.py`). `LKAP_API_BASE_URL` in it is `LKAP_PUBLIC_BASE_URL` or `http://127.0.0.1:$PORT`; the docker backend needs the former. | #16 closed by this. |
| 23 | V2-12 | `lkap_api.recordings` (hand-off) | `POST /internal/v1/sessions/{id}/recording/start` (in `routers/internal.py`) lazily imports `lkap_api.recordings.start_recording(db, session, connection, factory) -> RecordingStartOut` and 501s until it exists. Egress finalisation: `@register_webhook_handler("egress_ended")` from `lkap_api.connections.webhooks`; the built-in handler only sets `recording_status`. | Keeps V2-12 out of V2-03's files. |
| 24 | V2-00 / V2-13 | contracts + web | Not implemented: `GET/PUT /v1/connections/{id}/storage` (no contract model; `PUT /v1/connections/{id}` with `storage_config_id` covers it). Added: `POST /v1/connections/test` (`ConnectionCreate` → `ConnectionTestResult`, nothing stored) for the create form's "test before first save" (UI_UX_SPEC-V2 §2.1). | Contract gap / addition to note. |

## Open — left by V2-02 (Auth, tenancy, API keys, limits)

| # | Owner | File | Edit / hand-off | Why |
|---|---|---|---|---|
| 25 | V2-02 — **applied by V2-02** (coordinator go-ahead) | `routers/sessions.py`, `routers/provider_keys.py`, `routers/tools.py`, `routers/knowledge.py`; cross-workspace marks in `routers/internal.py`, `sessions_sweep.py`, `kb/seed.py`, `kb/ingest.py`, `kb/embed.py`, `connections/service.py`, `jobs/rollup.py`, `keys.py`, `qa/scorer.py`, `recordings/job.py`, `webhooks/delivery.py`; scoped lookups in `qa/resolve.py`, `recordings/storage.py`, `recordings/service.py`, `costs/service.py` | Done. Admin handlers use `ctx: AdminCtxDep` and filter on `ctx.workspace_id`; inserts set it; misses are 404. Worker `/internal` routes load the session (or, for `sessions/start`, the agent) by id cross-workspace and scope every further read to that row's workspace — including credential decryption and tools, so a config naming another workspace's credential resolves as missing. Pack KB seeds are created in the agent's workspace. `seed_bootstrap_credentials` seeds the `default` workspace explicitly. Two-workspace tests for all four routers in `api/tests/test_tenant_isolation.py`. | — |
| 26 | V2-00 / coordinator | `contracts/src/lkap_contracts/api_models.py` + TS export | Move `LoginIn`, `PasswordChangeIn`, `AcceptInviteIn`, `InviteCreate`, `InviteOut`, `WorkspaceOut`, `WorkspaceUpdate`, `MemberOut`, `MemberCreate`, `MemberUpdate`, `ApiKeyCreate`, `ApiKeyOut`, `ApiKeyCreated`, `AuditOut` from `api/src/lkap_api/auth/models.py` (field names are final) into contracts and regenerate `web/src/contracts`, so V2-14 gets TS types. | CONTRACTS-V2 §3.4 names the endpoints but only `Me`/`UserOut`/`WorkspaceMembership` exist as models; V2-05 was regenerating contracts in this wave. |
| 27 | Coordinator / V2-12 (`agents.py`) — **applied by V2-02** with #20 | `api/src/lkap_api/routers/agents.py` | `update_agent` persists `AgentUpdate.limits` (`model_dump()`) and `allowed_origins`; `to_out` fills `workspace_id`, `connection_id`, `mode`, `archived_at`, `limits`, `allowed_origins`. | Today both fields are accepted and silently dropped, so the only way to set `allowed_origins` is SQL. Limits have their own route (`GET/PUT /v1/agents/{id}/limits`, in `connect.py`). |
| 28 | — | `api/src/lkap_api/routers/health.py` | **Closed**: V2-02 took #11 on the coordinator's grant. | — |
| 29 | **User / coordinator** (writes the live DB) | `api/data/lkap.db` owner row (#12) | The existing `owner@local` row still has `password_hash = NULL`. Give it a password with the api's env loaded: `cd api && uv run python -m lkap_api.auth set-password --email owner@local` — uses `LKAP_BOOTSTRAP_OWNER_PASSWORD` when set, otherwise logs a generated one once at WARNING. `--force` replaces an existing password. V2-02 did not run it: it writes the live DB. New databases need nothing — bootstrap now hashes the owner password because `lkap_api.auth.passwords.hash_password` exists. | #12. |
| 30 | Announcement (files outside V2-02's list) | `api/src/lkap_api/deps.py`, `api/src/lkap_api/settings.py`, `api/src/lkap_api/db/guard.py`, `api/pyproject.toml` + `uv.lock`, `api/tests/test_bootstrap.py`, `api/tests/test_connect.py`; on the coordinator's grants also `routers/agents.py` (#20/#27 + scoping), `routers/connections.py` (#21), `routers/health.py` + `tests/test_health.py` + `contracts/.../api_models.py` `HealthResponse` + regenerated `contracts/generated` / `web/src/contracts` (#11) | `deps.py`: `AdminDep`/`OptionalAdminDep` now delegate to `lkap_api.auth.deps` (+ `AdminCtxDep`). `settings.py`: `allow_admin_token`, `session_secret`, `session_ttl_hours`, `web_base_url`, `redis_url`, `rate_limit_enabled`, `api_key_rate_per_min`, `login_rate_per_min` and a prod validator refusing weak/`dev-*` secrets. `guard.py`: `CROSS_WORKSPACE_OPTION` opt-out. `pyproject`: `argon2-cffi`. `test_bootstrap.py`: the "no hash until V2-02" tripwire now asserts an argon2id hash. `test_connect.py`: anonymous calls send the platform `Origin`; the F-13 test is inverted. | Needed for the card to work at all; no other package owns these lines this wave. |
| 31 | Everyone writing api tests | `connect` calls | An anonymous `POST /v1/agents/{id}/connect` now needs an `Origin` (or `Referer`) from `LKAP_CORS_ORIGINS`/`LKAP_WEB_BASE_URL` or the agent's `allowed_origins` — tests send `{"Origin": "http://localhost:3000"}`. `admin_client` calls are privileged and skip the origin check. Every call counts against `max_concurrent_sessions` (5) and the per-agent bucket (60/min). | Heads-up, not an edit. |
| 37 | V2-14 (web auth, wave 2) | `web/src/app/login/**`, `web/src/middleware.ts`, `web/src/app/api/console/[...path]/route.ts`, `web/src/lib/auth.ts` | Hand-off from V2-02 (built the API only, per the coordinator). Already in place: the proxy forwards every non-hop-by-hop header (so the browser's `lkap_session` cookie reaches the api) and returns the api's `Set-Cookie`; it sends WP-1's `lkap_workspace` cookie as `X-Workspace` (helpers in `web/src/lib/auth.ts`, test `web/tests/console-proxy-auth.test.ts`); it still attaches `X-Admin-Token`. V2-14: build `/login` (`POST /api/console/auth/login {email,password}` → 204 + cookie; honour `?next=`), `/login?invite=<token>` (`POST /api/console/auth/accept-invite {token,password,name?}`), logout (`POST /api/console/auth/logout`), `middleware.ts` redirecting `/console/**` without `lkap_session` to `/login?next=…`, then **stop attaching `X-Admin-Token`** in the proxy (or run with `LKAP_ALLOW_ADMIN_TOKEN=false`) — that is what makes the api 401 and WP-1's redirect kick in. Settings tabs call `GET /v1/auth/me`, `GET/PUT /v1/workspaces[/{id}]`, `GET/POST /v1/workspaces/{id}/members`, `PUT/DELETE …/members/{user_id}`, `POST …/invites`, `GET/POST /v1/api-keys`, `DELETE /v1/api-keys/{id}`, `GET /v1/audit`, `GET/PUT /v1/agents/{id}/limits`, `POST /v1/auth/password {current,new}`. Models: `lkap_api/auth/models.py` until #26 moves them to contracts. | Card V2-02 lists these web files; the coordinator moved the login page, middleware and admin-token removal to V2-14. |

## Open — left by V2-05 (Provider registry at full breadth)

| # | Owner | File | Edit / hand-off | Why |
|---|---|---|---|---|
| 32 | V2-09 | `scripts/gen_plugin_requirements.py` output for `requirements/slim.txt` | CONTRACTS-V2 §7 says slim = "v1 set + silero + turn-detector + inference"; ruling R-V2-1 requires every entry V2-05 adds (including `silero-vad`, `turn-detector-plugin`, `inference-vad`, `inference-turn-detector`) to carry `worker_image="full"` so the v1 `mvp` id set stays byte-identical. Those four ids are therefore *not* discoverable by filtering the registry on `worker_image=="slim"`. **V2-09 must hardcode those four ids into the slim requirements generator** (or equivalent), not derive slim purely from `worker_image`. | Ruling R-V2-1 vs. CONTRACTS §7 conflict; the registry test `test_status_alias_is_deferred_for_every_full_image_entry` (contracts/tests/test_registry.py) intentionally locks the `worker_image="full"` side of this so V2-09 doesn't miss it. |
| 33 | V2-07 | `agent/src/lkap_agent/providers/factory.py` (`CONSTRUCTIBLE_KINDS`, `BuiltProviders`) | `vad`/`turn_detection`/`noise_cancellation` are valid `SLOT_KINDS` entries but not yet in `CONSTRUCTIBLE_KINDS`, and `BuiltProviders` has no field for them. The moment `PipelineConfig.vad`/`.turn_detection`/`.noise_cancellation` resolves to a non-`None` `ResolvedProvider`, `build_all` will raise `ProviderBuildError` for that slot unless V2-07 either adds the kind + dataclass field, or names those three slots in its own `optional=` set. Not a live bug today (nothing before V2-07 populates those slots). | Ask #1's ruling ("kind mapping only; construction stays V2-07's") explicitly left this for V2-07; documented here so it isn't rediscovered as a surprise. |
| 34 | V2-07 | `agent/src/lkap_agent/providers/factory.py` — `ProviderFactory.build`/`build_all` | Both now take/thread a `mode: PipelineMode = "cascaded"` kwarg (`special_cases.apply_pipeline_mode` needs it to pick audio-native vs. text-only realtime construction for half-cascade). `build_all` reads it from `resolved.config.pipeline.mode`. Any code that monkeypatches or wraps `ProviderFactory.build` needs to accept (or ignore) this kwarg — `agent/tests/unit/test_factory.py::test_build_all_downgrades_optional_slots_and_raises_on_required_ones` needed a one-line fix for its two local stubs, applied by this package since the break was caused by V2-05's own interface change; flagging here because `agent/tests/unit/test_factory.py` (no `_*` suffix) is V2-07's exclusive file, not V2-05's. | New signature is additive/backward-compatible for real callers (default `"cascaded"`), but not for a stub with a fixed positional-only signature. |
| 35 | Coordinator (ruling conflict) | `contracts/src/lkap_contracts/providers.py` — Hedra | PLAN-V2's card text says "Hedra `removed`" (i.e. register it with `availability="removed"`); the task's own instruction to this package said "Hedra's avatar plugin is vendor-disabled: don't register it" (no entry at all). This package followed the **instruction**, not the card: Hedra has no registry entry whatsoever, so `EXPECTED_REMOVED_IDS` in `contracts/tests/test_registry.py` is empty. If the coordinator wants a visible-but-disabled `"removed"` entry instead (e.g. so the web catalog can show it struck through with a reason), that is a one-line follow-up, not a redesign. | Direct conflict between two instructions this package received; flagging rather than silently picking one. |
| 36 | V2-06 / V2-13 | `aws-transcribe-stt`'s `credentials` field | Deliberately unresolved: the field accepts a `boto3`-style `Credentials` object but the wire format can only carry JSON, so this package passes a dict straight through — `aws.STT` will reject it if an admin actually fills it in (most will leave it empty and rely on the ambient AWS credential chain, which is why `requires_credential=False`). Converting an admin-entered access key/secret pair into whatever shape `aws.STT`'s `credentials` param expects is left to whichever package builds the credentials UI for it. | `agent/src/lkap_agent/providers/special_cases.py` docstring says the same; recorded here too so it surfaces in the asks log. |

## Open — left by V2-08 (Jobs, storage, webhooks, QA job)

| # | Owner | File | Edit / hand-off | Why |
|---|---|---|---|---|
| 38 | V2-01 / coordinator | `api/src/lkap_api/settings.py` | Fold `LKAP_JOBS_BACKEND`, `LKAP_JOBS_POLL_INTERVAL_S`, `LKAP_JOBS_MAX_ATTEMPTS`, `LKAP_STORAGE_KIND`, `LKAP_STORAGE_BUCKET`, `LKAP_STORAGE_ENDPOINT_URL`, `LKAP_STORAGE_REGION`, `LKAP_STORAGE_ACCESS_KEY`, `LKAP_STORAGE_SECRET_KEY`, `LKAP_STORAGE_PUBLIC_BASE_URL` into the shared `Settings` when convenient. Not urgent: `jobs/settings.py::JobsSettings` and `storage/settings.py::StorageSettings` read them independently today (same `env_prefix="LKAP_"` convention as `Settings`, so no behaviour changes if/when they move) — this file is not in V2-08's exclusive list and ask #30 (V2-02) already touched it this wave, so V2-08 avoided a second concurrent edit rather than risk clobbering. `Settings.redis_url` (added by #30) and `JobsSettings.redis_url` happen to read the same `LKAP_REDIS_URL` var already, so arq and rate-limiting agree without coordination. |
| 39 | V2-00 / coordinator | `contracts/src/lkap_contracts/api_models.py` — `WebhookEndpointOut`/`WebhookEndpointCreate` | Add a `secret: str` field to a proper `WebhookEndpointCreated` (or similar) contracts model. Today `routers/webhooks.py` defines a **local** `WebhookEndpointCreated(WebhookEndpointOut)` subclass with `secret: str` only for the `POST /v1/webhooks` response, because the contracts model has no field to carry it and this package doesn't own `contracts/**`. Web (V2-14) needs the real type in `web/src/contracts` to show the secret in the create dialog. |
| 40 | V2-03 — **QA half superseded by ruling R-V2-5/R-V2-6, webhook half applied by V2-03** | `api/src/lkap_api/routers/internal.py` — `put_summary` (`PUT /internal/v1/sessions/{id}/summary`) | Original ask: call `qa.enqueue_for_session` and `webhooks.emit` from `put_summary`, in that order, after `db.commit()`. **The QA call is now wrong and has been removed**: R-V2-5 moved first-pass QA scoring into the worker (`PUT /internal/v1/sessions/{id}/qa`, this follow-up's own new route — see #52); calling `qa.enqueue_for_session` here too would double-score every session and, for the common Inference-only judge, overwrite the worker's `done` verdict with the api's inevitable `qa_judge_unavailable` failure. The **webhook half stands and is already applied**: `put_summary` commits first, then calls `webhooks.emit(database, jobs, workspace_id=..., event_type=webhook_events.SESSION_ENDED, ..., background_tasks=background_tasks)` — the "commit before a call that opens its own connection" rule this ask documented is now used at four call sites (`redeliver`, `put_summary`, `recordings/finalize.py`, `routers/sessions.py::rescore_session`). |
| 41 | V2-12 | `lkap_api.storage` / `lkap_api.jobs` (hand-off, no action needed unless building recordings now) | `storage.resolve.storage_from_config(row, vault, settings=settings)` turns a `StorageConfig` row into a `StorageBackend` (local or S3/MinIO) — reuse this for Egress recordings, don't re-derive it. `LocalStorage.signed_url()` returns a well-formed, HMAC-verifiable URL (`storage.local.verify_local_signature`) at `{public_base_url}/internal/v1/storage/local/{key}`, but **no route serves that path** — V2-12's playback endpoint needs to add it (or its own signed-URL scheme) if it wants local-backend recordings to actually play in dev; S3/MinIO's `signed_url()` is a real presigned GET and needs nothing further. `jobs.kinds.USAGE_DAILY_ROLLUP` already runs nightly-on-poll; recording finalisation should be its own job kind registered via `@job("recording_finalize")` (name only reserved in `ARCHITECTURE-V2.md` §"Jobs", not yet in `jobs/kinds.py` — add the constant there, or file a one-line ask back). |
| 42 | V2-03 / V2-06 / V2-04 | `jobs.kinds.CONNECTION_PROBE` / `CATALOG_REFRESH` / `WORKER_SWEEP` (hand-off) | These three kind names exist in `jobs/kinds.py` as reserved strings; V2-08 built the generic handler registry (`jobs.registry.job` decorator) and the `InlineBackend`/`arq` dispatch but registered no handler for them (that business logic lives in each owner's own files). Register with `from lkap_api.jobs.registry import job` + `from lkap_api.jobs.kinds import CONNECTION_PROBE` (etc.) and `@job(CONNECTION_PROBE)` an async `(ctx: JobContext, payload: dict) -> None` — `ctx` gives `database`/`settings`/`vault`/`http`/`jobs`. An unregistered kind's job row is marked `failed` (not retried forever), so nothing breaks by leaving them unregistered a while longer. |
| 43 | **Closed by rulings R-V2-5 and R-V2-6** (architect) | `api/src/lkap_api/qa/llm_client.py`, `qa/resolve.py`, `agent/src/lkap_agent/qa.py`, `agent/src/lkap_agent/main.py`, `config_service.py`, `routers/internal.py` | Resolved as option (a) from R-V2-5's decision list: the judge runs **in the worker** at session end (the only place Inference is callable), through a new resolved slot `qa_llm` (R-V2-6, `ProviderSlot` += `qa_llm`) the api fills from `qa.model -> workflow_llm -> llm -> Inference default` in `config_service.resolve_providers`/`routers/internal.py::_build_resolved` (both `GET .../resolved` and `POST .../sessions/start`), secrets included — the worker's `qa.build_judge` only builds `resolved["qa_llm"]` via the factory, no chain-walking, no credential handling. `qa/llm_client.py`'s OpenAI-compatible client is now used only for the api's own **re-score** path (`routers/sessions.py::rescore_session`, already wired by V2-12 against `qa.resolve.resolve_judge`/`qa.job.enqueue_for_session`), which still 409s `qa_judge_unavailable` for a non-vendor-key judge — that restriction is deliberate for re-score, not a gap. |
| 52 | (informational — this follow-up's own new route) | `api/src/lkap_api/routers/internal.py` | `PUT /internal/v1/sessions/{id}/qa` (service-token auth) upserts `session_qa` from the worker's verdict, `scored_by="worker"`. Migration `v2_010_qa_status` added `session_qa.scored_by` and widened `status` to include `skipped`. **Not yet applied to the live dev DB** — the coordinator applies it (rehearsed up/down/up on a fresh DB and on a `.backup` copy of `api/data/lkap.db`, never the original). |

## Open — left by V2-04 (Supervisor)

| # | Owner | File | Edit / hand-off | Why |
|---|---|---|---|---|
| 44 | **Closed by ruling R-V2-4** (implemented by the V2-04 follow-up) | `contracts/.../fleet.py`, `api_models.py::FleetStatus`, `connections/service.py::compute_desired_hash`, `v2_009_fleet_restart`, `routers/fleet.py` | `restart` bumps `fleet_desired.restart_generation` (part of `desired_hash`) and stamps `restart_requested_at`; the supervisor's rolling replacement does the rest. The live dev DB still needs `alembic upgrade head` (coordinator). | Option (a) was chosen. |
| 45 | V2-09 | `deploy/docker-compose.dev.yml`, `deploy/docker-compose.prod.yml`, `supervisor/Dockerfile`, `agent/Dockerfile` | Add a `supervisor` service: `python -m lkap_supervisor`, `LKAP_SUPERVISOR_BACKEND=docker`, `/var/run/docker.sock` mounted, `LKAP_API_BASE_URL=http://api:8080`, `LKAP_SERVICE_TOKEN`, `LKAP_SUPERVISOR_WORKER_API_URL=http://api:8080` (workers cannot reach the api's loopback; the backend refuses a loopback `LKAP_API_BASE_URL`), `LKAP_SUPERVISOR_DOCKER_NETWORK=<the compose network name>`, `LKAP_AGENT_IMAGE_SLIM/FULL`, a volume for `LKAP_SUPERVISOR_STATE_DIR` (it remembers which containers were already SIGINT'd), `LKAP_REDIS_URL` in prod (lease), exactly **1** replica, metrics on `:9105`. `supervisor/Dockerfile`: `uv sync` of `supervisor/` + `contracts/`. `agent/Dockerfile`: `STOPSIGNAL SIGINT` (D-W2-13). | V2-04 owns no file under `deploy/**` (PLAN-V2 V2-04 card). |
| 46 | V2-07 | `agent/src/lkap_agent/registration.py`, `agent/src/lkap_agent/main.py` | When registering: use `LKAP_INSTANCE_KEY` if set, else `f"{socket.gethostname()}:{os.getpid()}"`; send `managed_by=os.environ.get("LKAP_MANAGED_BY", "external")` (the supervisor sets `supervisor`); heartbeat `ready` every 30 s; on SIGINT heartbeat `draining`. Accept `LKAP_AGENT_NAME`/`LIVEKIT_AGENT_NAME` values other than `lkap-agent` (the current `REQUIRED_AGENT_NAME` check refuses them, so no supervised pool with another agent name can start until this lands). | The supervisor registers each replica it starts under the same key (`hostname:pid` for subprocess, the container name for docker) as `managed_by=supervisor`, then status `starting`. Matching keys make the worker's own `register` update that row. The api also keeps `managed_by=supervisor` sticky for a claimed key. Until V2-07, supervisor rows are swept to `gone` after 90 s because nothing heartbeats them. |
| 47 | V2-03 — **applied by V2-02** with #25 | `api/src/lkap_api/connections/service.py::list_fleet_desired` | Add `.execution_options(lkap_cross_workspace=True)` to its `select(LiveKitConnection)` before the tenant guard flips to autouse (#25). `GET /internal/v1/fleet/desired` is platform-wide by design. | Without it the guard rejects the unscoped query and the supervisor's feed 500s under the test guard. |
| 48 | V2-13 | `web/**` fleet card (hand-off) | `GET /v1/connections/{id}/fleet` → `FleetStatus` (instances include `gone` rows from the last hour); `POST …/fleet {action: start\|stop, replicas?}` → `FleetStatus`; `restart` rolls the pool (R-V2-4); `FleetStatus.restart_generation` / `restart_requested_at` show "restart requested … ago". Warn when one connection shows both `external` and `supervisor` instances (PLAN-V2 §7). | Hand-off, no action needed from V2-04. |

## Open — left by V2-07 (Worker v2)

Asks #33, #34 and #46 are done by V2-07 (see Closed). #33: `vad`/`turn_detection`/`noise_cancellation` are in `CONSTRUCTIBLE_KINDS`, have `BuiltProviders` fields, take no `model` kwarg and are lenient by default. #34: `test_factory.py`'s one-line stub fix is kept. #46: `LKAP_INSTANCE_KEY` / `LKAP_MANAGED_BY` / `LKAP_AGENT_NAME` are honoured; a heartbeat goes every `LKAP_HEARTBEAT_INTERVAL_S` (30 s), and `draining` is sent within 1 s of the SDK starting to drain.

| # | Owner | File | Edit / hand-off | Why |
|---|---|---|---|---|
| 49 | V2-03 (or whoever owns `routers/internal.py` now) | `api/src/lkap_api/routers/internal.py` | Add `POST /internal/v1/sessions/{id}/metrics` (`SessionMetricsIn` → store `sessions.latency`; `usage_lines` arrive empty because the api prices `usage` itself) and `POST /internal/v1/sessions/{id}/recording` (`SessionRecordingIn` → `recording_status` / `recording_duration_s`, same as the `egress_ended` handler). | CONTRACTS-V2 §3.4 lists both, but neither route exists. The worker already posts to both (best effort; today they 404 and it logs one warning). |
| 50 | **Closed by V2-19B** (2026-09-23): (a) the route exists; (b) `agent/src/lkap_agent/qa.py` already imports `DEFAULT_RUBRIC_PROMPT`, `DEFAULT_TAGS`, `QaVerdict`, `SessionQaIn` from `lkap_contracts.qa` | `contracts/src/lkap_contracts/qa.py`, `api/src/lkap_api/routers/internal.py` | (a) Add the `PUT /internal/v1/sessions/{id}/qa` handler. The worker sends `{status: done\|failed\|skipped, score?, sentiment?, tags[], summary?, raw?, model?, error?}`; `status=skipped` also arrives when the transcript is empty (`error="no conversation to score"`). (b) When `lkap_contracts.qa` lands, V2-07's `agent/src/lkap_agent/qa.py` swaps its local `DEFAULT_TAGS`, `DEFAULT_RUBRIC_PROMPT`, `QaVerdict` and `SessionQaIn` (marked `TODO(V2-08 follow-up)`) for imports. That is a one-file change; whoever lands `qa.py` may do it. | Coordinator instruction: local shapes behind a TODO until the contracts module exists. Verified live: the worker PUTs after the summary and gets 404 today. |
| 51 | V2-03 / V2-00 (**decision**) | `api/config_service.resolve_providers`, `ProviderSlot` | Credentialed `qa.model` providers aren't resolved for the worker, because `qa` is not a `ProviderSlot`. The worker builds `qa.model` only when it needs no credential (e.g. `livekit-inference-llm`). With a credentialed `qa.model` it posts `status=failed` with an explicit error rather than guessing. The default chain (`workflow_llm → llm → Inference`) always has a judge. To support a vendor-key `qa.model`, resolve it into the resolved map (for example a `qa` slot) and the worker will use it. | R-V2-5 says "already-resolved `qa.model`"; the resolution itself does not exist. |
| 52 | ~~V2-09~~ **Closed by V2-09** (2026-09-23) | `agent/Dockerfile`, `scripts/check_installed_providers.py` | Done as asked: `agent/Dockerfile`'s builder stage runs `requirements/check_installed_providers.py --flavor $LKAP_IMAGE_FLAVOR --out /app/installed_providers.json` and the runtime stage `COPY --from=builder /app/installed_providers.json /app/installed_providers.json`; the manifest's id list is under the key `"provider_ids"` (one of the three the worker already accepts, confirmed by reading `agent/src/lkap_agent/registration.py::_INSTALLED_JSON_KEYS`). `ENV LKAP_IMAGE_FLAVOR=${LKAP_IMAGE_FLAVOR}` is set in the runtime stage (default `slim`, matching the `ARG`'s own default). | CONTRACTS-V2 §7 names the file but not its shape. Without the file, the worker derives the list from installed distributions (28 ids in the dev venv). Api validation then uses the list strictly, so under-reporting would reject valid providers. |
| 53 | V2-03 | `api/src/lkap_api/connections/bundle.py::_shared_env` | `LIVEKIT_AGENT_NAME` can be dropped from the worker env. The worker registers under `LKAP_AGENT_NAME`, and still refuses to start if a set `LIVEKIT_AGENT_NAME` differs from it, so keeping both equal is harmless. | Cleanup only. |
| 54 | **Closed by V2-19B** (2026-09-23): `describe_current_frame` takes the realtime path for `realtime` and `half_cascade` (tests parametrized over both, incl. `session.llm=None`); the `build_http_tools` docstring says intersection + private-range deny-list; `vision.py` docstring notes half-cascade skips per-turn injection (already enforced in `platform_agent.py`) | `agent/src/lkap_agent/tools/declarative.py` (docstring of `build_http_tools`: "unioned"), `tools/builtin/describe_current_frame.py` | (a) The docstring still says the platform allowlist is *unioned*; since F-14 it is an intersection with a private-range deny-list, enforced in `_http_safety.check_url_allowed`, so callers need no code change. (b) `describe_current_frame` in `half_cascade` mode takes the cascaded branch and raises "No cascaded LLM is configured". It should behave as in `realtime` (the model already sees frames, because V2-07 enables `video_input` for half-cascade): `if ctx.pipeline_mode in ("realtime", "half_cascade")`. | Files outside V2-07's list. |
| 55 | V2-17 (telephony) | `agent/src/lkap_agent/main.py::_resolve_or_start` hand-off | A session-less dispatch is created with `caller=None`, and when the metadata carries no `participant_identity` RoomIO links to the first caller. SIP attributes (`sip.phoneNumber`, `sip.trunkID`, `sip.callID`) need a connected room, so filling `caller` belongs with V2-17's worker hook. `DispatchMetadata.config_version` / `participant_identity` are still required (only `session_id` was widened, per the V2-00 note); SIP dispatch rules should send `config_version: 0, participant_identity: ""` unless the architect widens them as CONTRACTS-V2 §4.3 shows. | Keep the shape the worker already accepts. |

## Open — left by V2-10 (Panels v2 runtime)

V2-10 made one edit outside its listed files: `agent/src/lkap_agent/tools/builtin/__init__.py` (no owner, see #54) now registers the four block tools. Asks #65–#71 below; the V2-11 wire contract follows the table.

| # | Owner | File | Edit / hand-off | Why |
|---|---|---|---|---|
| 65 | **Architect (decision)**, then V2-03/V2-00 + V2-11 | `ConnectResponse` / `AgentPublicOut` (`contracts/api_models.py`), `api/routers/connect.py` | The browser has no way to learn the panel's `BlockSpec` list (`type`/`title`/`order`/`config`, `layout`). `ConnectResponse` carries only `uiPanelId`, and `UiState.blocks` holds block **state** keyed by id, not the spec. Proposal: add `panel: PanelLayout` to `ConnectResponse` (and `AgentPublicOut`), with the api resolving it exactly like the worker's `lkap_agent.ui.blocks.resolve_block_specs` (`config.panel.blocks`; if empty, the pack `default_panel.blocks` when it targets the same panel id; for a custom panel with none, the pack's `blocks`). The alternative is a new `UiState.layout` field that the worker publishes. | CONTRACTS-V2 §4.4 doesn't say how the composite panel gets its layout. It blocks V2-11 on public `/s/[slug]` sessions; the console can read `AgentConfig.panel`. |
| 66 | V2-07 owner of `main.py` (or V2-19) | `agent/src/lkap_agent/main.py::NoopUiChannel` | Add no-op `async set_block(block_id, state)`, `async patch_block(block_id, ops)`, `async request_form(block_id, schema, prefill=None, timeout_s=120) -> None` (returns `None`) and `async cite(block_id, hits)`. | `packs.base.UiChannel` gained these (CONTRACTS-V2 §4.4). The no-op channel is only used when `ui.channel` fails to import and in unit tests, and every V2-10 caller guards with `getattr`, so this is conformance only. |
| 67 | **Closed by V2-19B** (2026-09-23): `FakeUiChannel` in the shared `testing/src/lkap_testing/fake_ctx.py` has `set_block`/`patch_block`/`request_form`/`cite` and a `/blocks/...` tree branch mirroring `_apply_tree_op`; mypy proves it satisfies `packs.base.UiChannel` | `agent/tests/fakes/fake_ctx.py::FakeUiChannel` + `apply_patch_op`; `packs/tests/insurance_claim/fake_ctx.py` | Mirror the four methods, and add a `/blocks/...` branch that reuses `lkap_agent.ui.channel._apply_tree_op` semantics. | V2-10's tests use the real `UiChannel` over `FakeRoom` instead. |
| 68 | V2-11 | `web/src/lib/ui-state.ts` | `emptyUiState()` and the `Required<Pick<…>>` store type lack `blocks`. Default it to `{}` and normalize a missing `blocks` to `{}`. `applyOp` already handles `/blocks/<id>/…` paths correctly: set, append, keyed upsert/remove by `key`/`id`, unkeyed remove = delete leaf, numeric list indexes. | The seq-1 snapshot always carries `blocks`, which may be `{}`. |
| 69 | V2-11 / V2-13 (console tools tab) | `web/src/components/console/lib/constants.ts` | Add toggles for `update_block`, `show_document`, `table_append`, `request_form` (`builtin_disabled` names). Each registers only when the panel has a matching block. | `lkap_agent.tools.builtin.BLOCK_TOOL_NAMES`. These names are deliberately **not** in `BUILTIN_TOOL_NAMES`. |
| 70 | Architect (note) | CONTRACTS-V2 §4.4 tool signatures | `update_block(block_id, patch)` and `table_append(block_id, row)` take the dict as a **JSON-object string**. A free-form `dict` parameter converts to a Gemini OBJECT with no `properties`, and Gemini rejects the whole tool list, which breaks every Gemini session. Python-side `UiChannel.set_block/patch_block` stay dict-typed. | Verified with `livekit.agents.llm._provider_format.google.to_fnc_ctx`, tested in `test_ui_blocks_tools.py`. |
| 71 | Architect (note) | `packs/base.py` | `on_block_action` is on a separate `BlockActionPack` Protocol, not on `Pack`. A structural Protocol can't carry a default method: adding it to `Pack` breaks `PACK: Pack = GenericPack()` (and the insurance and null packs) under mypy. The worker looks it up with `getattr`. A pack without it gets the default no-op (`ok: true, payload: {}`). | "Optional protocol method with a default no-op" (V2-10 card), implemented additively. |

### V2-10 → V2-11 wire contract (panel blocks)

All state still travels in the one `lkap.ui.state` envelope stream. It uses the same `seq` rules: seq 1 is the platform snapshot after `on_session_start`, every patch is `last + 1`, and a resnapshot follows every 50 patches or a `get_snapshot`.

- **`UiState.blocks`**: `{<BlockSpec.id>: <state object>}`. The seq-1 snapshot carries every block of the resolved layout, and so does every later snapshot. Keys are block ids; ids never contain `/`. `v` is `2`. Values are plain JSON:
  - `status` / `notes` / `checklist` / `activity` / `custom` → `{}`. The four envelope blocks render `state.status`, `state.progress`, `state.notes`, `state.checklist` and `state.activity`, exactly the props of `web/src/panels/generic/blocks.tsx`, and the existing tools (`set_status`, `push_note`, ...) keep writing those envelope fields.
  - `form` → `{schema: <JSON schema>, values: {}, status: "idle"|"requested"|"submitted", submitted_at: float|null}`. The key is `schema`, never `schema_`.
  - `document` → `{asset_id: str|null, url: str|null, page: int (1-based), highlights: [{page, bbox: [x0,y0,x1,y1], note}]}`. `bbox` is page-relative 0..1, origin top-left. `show_document` writes a note as `bbox [0,0,1,1]` (the whole page), and `url` is always `https://`.
  - `gallery` → `{asset_ids: [str], selected: str|null}`. Every `image/*` asset pushed through `push_asset` (for example `pin_frame`) is appended to every gallery block **in the same patch** as its `/assets` append.
  - `table` → `{columns: [{key, label, type: string|number|boolean|date}], rows: [{id, ...cells}], selected_row: str|null}`. Every row has a string `id`.
  - `transcript` → `{show_tools: bool}`. `video` → `{source: "agent_avatar"|"user_camera"|"user_screen"|"track:<sid>", muted: bool}`. `kb_citations` → `{items: [{chunk_id, filename, score, text}]}`.
  - Initial state: the model defaults, seeded by any `BlockSpec.config` key that names a state field (for example `config.columns` on a table, `config.source` on a video).
- **Patch paths the worker emits** (all ops are the existing `UiPatchOp`):
  - `set /blocks/<id>` (`set_block`, `show_document`, `request_form`'s "requested" state).
  - `set /blocks/<id>/<field>` (`update_block`, one op per field).
  - `append /blocks/<id>/rows` and, when new keys appear, `set /blocks/<id>/columns` first in the same patch (`table_append`).
  - `append /blocks/<gallery>/asset_ids` (with `/assets`).
  - `set /blocks/<id>/items` (citations; replaced, never appended).
  - Form submit is one patch with three ops: `set …/values`, `set …/status = "submitted"`, `set …/submitted_at`. Cancel is `set …/status = "idle"`.
  - Packs may also use `append`/`upsert`/`remove` (keyed by `id`/`key`) and numeric indexes under `/blocks`.
- **Form round trip**:
  1. The worker sets the block to `status: "requested"`, with `values` = prefill.
  2. The worker RPCs `lkap.ui.request {v:1, method: "form", payload: {block_id, schema, prefill}}` with a **15 s** caller-side timeout. The browser's handler sees `responseTimeout` ≈ 8 s, because the SDK subtracts its 7 s round-trip allowance: a 10 s timeout measured live arrived as 3 s. The browser should reveal the form and answer **at once** with `{ok: true, payload: {}}`.
  3. The user's values come back as `lkap.agent.action {v:1, action: "form_submit", payload: {block_id, values: {...}}}` → `{ok: true}`.
  4. Dismissal is `form_submit {block_id, cancelled: true}` (additive), or a `form` RPC result `{cancelled: true}`.
  5. For compatibility the worker also accepts a `form` RPC result `{values}`, but don't hold that RPC open waiting for the user.
  6. If the RPC fails, the wait continues, because the block state already shows the form. So a panel that renders a `requested` form from a snapshot after a reconnect can still submit.
  7. Worker-side timeout is 120 s. After it, the block stays `requested`. A late `form_submit` is still stored and recorded, and the model is prompted with it.
  8. `form_submit` errors (`ok: false`): unknown block id, a non-form block, or `values` not an object.
- **`request_form` field schema**: `{type: "object", properties: {<name>: {title: <label>, type, format?, enum?}}, required?: [...]}` with types `string|number|integer|boolean`, `date` → `{type: string, format: date}`, `email` → `{type: string, format: email}`, `select` → `{type: string, enum: [...]}`. Property order is field order.
- **Other UI requests**: `show_block {block_id}` (fire-and-forget; 10 s caller timeout, which the browser sees as about 3 s, measured live; sent after `show_document`). `navigate` is not emitted by any built-in yet.
- **`block_action {block_id, name, data}`** → the pack's `on_block_action(ctx, block_id, name, data)`. The result payload is returned. With no hook it returns `{ok: true, payload: {}}`. A missing `block_id` → `ok: false`. `rewind` / `inject_user_text` currently answer `ok: false, "unsupported action"` (V2-18).
- **Session events** (timeline):
  - `block_update {block_id, block_type, op: "set"|"patch"|"cite"|"form_requested"|"form_cancelled"}`, one per helper call, never with the state.
  - `form_submitted {block_id, values}`.

## Open — left by V2-11 (Panels v2 web)

V2-11 closes **#65** (consumed as `ConnectResponse.agent.panel` via `panelLayoutOf`, R-V2-7), **#68** (`blocks` in the store; `/blocks` ops replay vectors generated by the agent's `apply_patch_op`, `web/tests/ui-state-blocks.test.ts`) and **#69** (the four block-tool switches live in the panel composer, `BLOCK_TOOLS` in `constants.ts`). Edits outside its listed files are rows 73–75.

| # | Owner | File | Edit / hand-off | Why |
|---|---|---|---|---|
| V2-14-1 | **Closed by V2-19B** (2026-09-23): `_strip_titles` keeps keys of `properties`/`patternProperties`/`$defs`/`definitions` maps; both `.d.ts` carry `BlockSpec.title?: string \| null` (byte-identical); `BlockSpecV2`/`PanelLayoutV2` are now plain deprecated aliases in `layout.ts` (rename of the consumers logged as V2-19B-1). Was: **Ruled R-V2-15** → V2-19 (`export.py::_strip_titles` strips the property named `title`; fix + regen, delete the adapter) | `contracts/generated/ts/lkap-contracts.d.ts`, `web/src/contracts/lkap-contracts.d.ts` | `BlockSpec.title` is in the Python model and in `contracts/generated/schemas/PanelLayout.schema.json` / `BlockSpec.schema.json`, but both generated `.d.ts` files omit it (the interface has only `config`, `id`, `order`, `type`). V2-11 papers over it with `BlockSpecV2` / `PanelLayoutV2` in `web/src/panels/composite/layout.ts` (adapter item 1) — delete those once the export carries `title?: string \| null`. | Titles are authored in the composer and rendered as block headings. |
| V2-14-2 | WP-8 (informational; ratified by R-V2-15) | `web/src/components/session/session-room.tsx` | Three surgical edits: `panelPerform` sends `agentActionFor(action)` (so `form_submit` / `block_action` reach the agent as themselves; `ui_action` keeps its v1 shape), the layout is `panelLayoutFor(panel, agent)` (the composite panel's layout is per agent), and the imports. The video block reuses `useLocalTrackRef` from `agent-stage.tsx` and draws through `StageView`. | The card's "form block submits values through `perform`"; `PanelDefinition.layout` is static. |
| V2-14-3 | **Not done by V2-19B**, see V2-19B-4 (needs `api_models.py`, V2-19T's this round). **Ruled R-V2-15** → V2-19, low priority (`SessionDetailOut.panel` from the `agent_config_versions` snapshot at `session.config_version`) | `web/src/components/console/sessions/session-panel-tab.tsx` | `panelAgent` fills the now-required `AgentPublicOut.panel` from the agent's stored `config.panel` (`storedPanelLayout`). The "Panel at end of call" tab now renders `composite` agents through the composite panel instead of the generic fallback (test updated). It shows the agent's **current** layout, not the one at the session's `config_version`. | V2-12 made `panel` required; V2-11 registered `composite`. |
| V2-14-4 | WP-3 (informational; ratified by R-V2-15) | `editor/form-values.ts`, `lib/schemas.ts`, `editor/summary-rail.tsx`, `create/pack-card.tsx`, `tabs/panel-tab.tsx` | `config.panel` joined `agentConfigFormSchema` (README rule 2); `buildAgentUpdate` sends it and derives `ui_panel_id` from it. `PANEL_META` moved to `components/shared/panel-meta.ts` and `KNOWN_PANEL_IDS` is gone (F-30); the rail reads the form's `config.panel`, the pack card the pack's `default_panel`. `panel-tab.tsx` is a one-line re-export of the composer because `builtin-sections.tsx` imports it. | The composer owns `config.panel`. |
| V2-14-5 | **Closed by V2-19C** (2026-09-23): R-V2-16's optional one-liner applied — `session-room.tsx` passes `suppressAgentVideo` to `AgentStage` when a blocks-aware panel has a `video` block with `source: "agent_avatar"`, so the avatar track is decoded once (`tests/agent-stage.test.tsx`; `session-shell` tests unchanged) | UI_UX_SPEC-V2-AMENDMENTS §2.6, `web/src/components/session/session-layout.ts` | "When a `video` block is present in a `wide` layout the stage moves into the panel column" is **not done**: it's a WP-8 shell/layout change. The video block renders its track inside the panel today, and the stage stays where it is. Decide whether V2-11 or a WP-8 follow-up owns it. | Out of V2-11's files. |
| V2-14-6 | **Closed by V2-19B** (2026-09-23): `lkap_contracts.blocks` (`BLOCK_CONFIG_MODELS`, `validate_block_config`, `validate_panel_block_configs`), schemas `generated/schemas/BlockConfig_<type>.schema.json` (JSON only, not in the TS doc), `lkap_api.panels.block_config_issues` registered in `VALIDATORS` (422 at `panel.blocks[i].config.<key>`), both real pack manifests validate, vitest `web/tests/block-config-parity.test.ts`. Deviation: `custom.kind` is optional (the composer's Pack block has no field for it yet). Was: **Ruled R-V2-17** → V2-19 (`contracts/blocks.py` + `validate_block_config` via `VALIDATORS`; web parity test) | CONTRACTS-V2 §4.4 / R-V2-7 "`contracts/blocks.py`" | There is no per-block **config** schema in contracts yet. The composer's config forms are generated from `web/src/panels/blocks/catalog.ts::configFields`, which holds exactly the keys the worker honours today (`initial_block_state` seeds state from config keys that name a state field): table `columns`, document `url`/`page`, transcript `show_tools`, video `source`/`muted`. When `contracts/blocks.py` lands, generate `configFields` from it. | R-V2-7's "validation rejects any `config` key not in the block schema" has nothing to validate against yet. |

## Coordinator decisions

- **#25 (unscoped v1 routers: sessions, credentials, tools, knowledge bases):** assigned to **V2-02**, as one scoping pass after V2-06 and V2-08 merge (both touch those routers). Must land before any UI can create a second workspace, and before `tenant_guard` goes autouse (#13).
- **#47 (fleet-desired query needs the cross-workspace opt-out before `tenant_guard` goes autouse):** folded into V2-02's #25 scoping pass. That pass flips the guard, so it must mark this query at the same time.
- **#44 (fleet `restart` returns 501, needs a `restart_generation` contract field):** goes to the architect for a decision with the next batch.
- **#51 (qa.model credential in the worker):** ruled R-V2-6 (`qa_llm` resolved slot). Assigned to the V2-08 follow-up, which also takes the one-line switch in `agent/src/lkap_agent/qa.py` and the #50 rubric swap.
- **#54 (`describe_current_frame` in half-cascade; `declarative.py` docstring):** a small agent-side fix, deferred to **V2-19 integration** to avoid colliding with V2-10's in-flight agent edits. The rule (architect, recorded in PLAN-V2 §8): in half-cascade, vision tools follow the **realtime** path. The realtime model owns video input, `describe_current_frame` returns metadata only, and per-turn injection is skipped. The docstring should read "intersection with a private-range deny-list" (F-14).
- **#56 (`livekit-plugins-krisp` has no 1.8.2 release; it's independently versioned at 0.4.2):** deferred to **V2-19 integration**. Repin it, or flip it to `deferred` in the registry, then regenerate `agent/requirements/full.txt`. It only breaks the nightly full-image build, which can't run until the user adds a GitHub remote.
- **#57 (`test_tools.py::test_update_replaces_the_definition`, a real bug):** sent to **V2-06**, which owns `routers/tools.py` and is still running.
- **#58 (`test_health` head-revision test is order-dependent):** the head pin is being bumped by the V2-08 follow-up (`v2_010`). The order dependence goes to **V2-02's #25 scoping pass**, which owns `test_health.py`.
- **B1 (`/s/[slug]` bundle budget, for the architect):** UI_UX_SPEC §2 says "First Load JS ≤400 kB (currently 383 kB)". But 383 kB was Next's **Size** column when the spec was written; First Load was already 564 kB. Measured since then:
  - WP-8: Size 391 kB, First Load 572 kB.
  - WP-3: First Load 576 kB.
  - WP-7: First Load 580 kB.

  The architect should decide which metric the budget means and the real limit. WP-12/V2-19 then enforces it, e.g. with a CI check in `web.yml`.
  - **Closed by V2-19C (2026-09-23):** `web/scripts/check-bundle.mjs` + a `web.yml` step after Build. Scratch build: `/s/[slug]` First Load JS **592 kB** → pass; with `@xyflow/react` statically imported into the session route: **654 kB** → exit 1 (reverted; scratch copy only).
  - After V2-11: Size 65 kB, First Load 589 kB. **Ruled R-V2-18:** the metric is First Load JS, the budget ≤ 620 kB, nothing is cut now; V2-19 creates `web/scripts/check-bundle.mjs` (V2-11's card named it but it was never written) and runs it in `web.yml`.
- **P1 (the permission rule `Read(./**/credentials*)` blocks source paths, per asks #60 and WP-4's page route):** the user delegated the decision. Their deny rule stays unchanged (it guards real secret files). Instead, **no source path segment may start with `credentials`**:
  - The api router is now `routers/provider_keys.py`, renamed with `git mv`; the `/v1/credentials` API path is unchanged.
  - The credentials page is served at **`/console/keys`** (`web/src/app/console/keys/page.tsx`).
  - Names like `credential_tests/`, `test_credentials.py` and `credential-list.tsx` are fine, because the rule only matches segments that *start* with `credentials`.
  - All future packages must follow this.
- **A11Y — Closed by V2-19C (2026-09-23):** 0 critical / 0 serious across the full capture (see V2-19C in the section "Open — left by V2-19C" for the figures); R-V2-19 applied in `stage-view.tsx` with the requested test; `e2e/preview.spec.ts` green and a new `e2e/a11y.spec.ts` guards 16 routes. `StageView`'s `localPreview`/`posterUrl` escape is not done (logged there). Original entry: **A11Y (WP-10's axe run, 159 shots; results in `…/scratchpad/ui-audit/after-wp10/axe.json` and `INDEX.md`): assigned to WP-12 / V2-19 integration as one fix pass.** 0 critical, 13 serious, 124 moderate.
  - **Serious, `color-contrast`:**
    - `/console/sessions` swept-status chip (3.8:1) and `RelativeTime` `text-muted-foreground/70` (3.28:1), from WP-7.
    - Recording section "days" suffix on `bg-muted` (1.74:1), from WP-3.
    - Dark-mode destructive button on `--background` (4.06:1), from WP-0.
    - `StageView` caption while `reconnecting` (3.46:1), from WP-8. This is a **spec-vs-AA conflict for the architect**: UI_UX_SPEC §5.4's `opacity-60` dim. **Ruled R-V2-19:** dim the media (video, meter) only; the caption keeps full contrast.
  - **Serious, `aria-prohibited-attr`:** `aria-label` on the roleless sessions loading-skeleton `div`, from WP-7.
  - **Moderate:**
    - `page-has-heading-one` ×60: the session shell has no `h1`.
    - `region` ×40.
    - `landmark-unique` ×16.
    - `heading-order` ×8.
  - **Also for WP-12:**
    - `StageView` can't take a local video or poster outside a `Room`, so the preview can't show camera states. Needs a `localPreview`/`posterUrl` escape (WP-10's request).
    - `e2e/preview.spec.ts` must go green. The pre-call hydration bug it caught was fixed in commit 058116b.
- **#29 (owner password):** the **user** runs `python -m lkap_api.auth set-password --email owner@local` themselves. It sets a credential on their account, so no agent runs it. Stage L1 (login) waits on it.
- **Architect rulings R-V2-8 … R-V2-19 (Fable, 2026-09-23; PLAN-V2 §8) close the following.** V2-15-1 → R-V2-8 (`SessionSummaryIn += disposition, variables`; V2-16). V2-15-2 → R-V2-9 (worker's no-new-secret rule stands; api validates it; V2-16). V2-15-3 → R-V2-10 (node tool refs are **names**; V2-16). V2-15-4 → R-V2-11 (`effective_qa` helper: a `qa` node means QA is on; V2-16). V2-15-5 → R-V2-12 (`agents.mode` derived from `config.flow`; V2-16). V2-15-8 → R-V2-13 (prompt order `instructions → global → node`; V2-16). V2-15-7, the `flow_progress` block id and V2-15-6 → R-V2-14 (`main.py` hooks ratified and passed to V2-16; progress blocks by `config.kind`; V2-15-6 informational to V2-17). #72 → R-V2-15 (`export.py::_strip_titles` fix; V2-19), which also ratifies #73/#75 and sends #74's pinned-layout fix to V2-19. #76 → R-V2-16 (§2.6 sentence withdrawn; no code). #77 → R-V2-17 (`contracts/blocks.py`; V2-19). **B1** → R-V2-18 (metric = First Load JS, budget ≤ 620 kB, V2-19 writes `web/scripts/check-bundle.mjs` + `web.yml` step; nothing cut). **A11Y §5.4 conflict** → R-V2-19 (dim media only; WP-12/V2-19). The V2-16 card in PLAN-V2 §4 now lists the file grants these rulings need.
- **Architect rulings R-V2-20 … R-V2-25 (Fable, 2026-09-23; PLAN-V2 §8) close every V2-17 ask.** V2-17-2 + V2-17-4 → R-V2-20 (the `main.py` wiring as written, with: no `Deps` seam — `report_call`/`transfer_call` join `ConfigClient`; `TelephonySession` in `SessionContext.userdata["telephony"]`; `LKAP_SIP_ANSWER_TIMEOUT_S`=135 as a backstop behind the api's room deletion, `_abort_start(speak=False)`; reconnect grace 0 on SIP channels; #55 stands, `DispatchMetadata` not widened). V2-17-8 → R-V2-21 (`AgentConfig.telephony.transfer_targets: [{label, to}]` in `lkap_contracts/telephony.py`; `pack_settings` allowlist retired; editor form schema + list editor; policy validation in `telephony/validation.py`, never `flows/validation.py`). V2-17-5 → R-V2-22 (`ResolvedAgentConfig.variables`; flow `initial_variables`, prompt agents get a known-variables block; **R-V2-8 amended: `put_summary` merges `variables` — V2-16 must land the merge, send it the amended R-V2-8 text now**). V2-17-10 → R-V2-23 (default-deny per-workspace dialing policy in `workspaces.settings["telephony"]`, static premium/satellite block list, `sip:` hosts allowlisted, minute bucket + concurrency caps, one chokepoint, audit rows; **V2-19 implements before V2-20's live dial, V2-21 reviews with the adversarial tests**). V2-17-11 → R-V2-24 (sweep from `sweep_loop`, outbound-only ring timeout, open rows follow terminal sessions). V2-17-1/3/6/7/9 → R-V2-25 (models promoted to contracts, `types.ts` deleted, `TELEPHONY_TOOL_NAMES` + web toggles, CONTRACTS §3.4 amended, DTMF transport ratified with a V2-20 live check, `DtmfPack` to `packs/base.py`). Every implementation grant is in the V2-19 card's **"Telephony grants"** block (its own file list: run it as a separate V2-19 agent once V2-16 and V2-18 land — not before, since `main.py`, `config_service.py` and the editor schema are theirs until then); the V2-21 card lists the review items. `TELEPHONY-LIVE-TEST.md` keeps its V2-17-2 caveat until that block lands.

## Open — left by V2-13 (Web: connections, providers, avatars, editor slots)

| # | Owner | File | Edit / hand-off | Why |
|---|---|---|---|---|
| V2-13-0 | — (applied by this package, mechanical) | `web/src/components/console/lib/schemas.ts`, `web/src/components/console/agents/defaults.ts`, `web/src/components/console/agents/editor/form-values.ts` | Not in V2-13's exclusive file list (WP-3's), but the providers section's Advanced disclosure (VAD/Turn/NC) and avatar options card needed fields `agentEditorFormSchema` didn't have yet, per the editor README's own rule 2 ("a section that starts editing a field not in the schema yet … request it — a one-line schema addition"). Added `pipelineConfigSchema.{vad,turn_detection,noise_cancellation,avatar_options}` (all four already exist on the stored `PipelineConfig`; only the form was missing them), `avatarOptionsSchema`, `DEFAULT_AVATAR_OPTIONS`, and merged the default into `toFormValues`. `payloadPipeline`'s existing `{...stored, ...edited}` spread needed no change — the new fields flow through it automatically. Same pattern as asks #5/#6. Mechanical fallout fixed in the same edit: `tests/console-provider-slot.test.tsx`, `tests/console-zod-resolver.test.ts` and `tests/console-editor-form-values.test.ts` needed an `avatar_options` (and, in one already-in-flight case, `panel`) value added to their fixture literals — none of those three files are V2-13's either, but all three broke from this schema change and are one-line-per-literal fixups, the same "mechanical fixup" precedent asks #2–4/#40 use. |
| V2-13-1 | — (closed by this package) | `web/src/components/console/registry/registry-form.tsx` | Ask #36 (aws-transcribe-stt's `credentials` field shape) is closed with **no code change**: the field is already `FieldSpec(type="json")`, and `RegistryForm`'s existing generic JSON textarea (with a Format button) already gives a free-form JSON editor — nothing provider-specific was needed. One thing worth carrying forward: `ProviderRef.fields` lands in `agents.config` **unencrypted**, so a boto-style `Credentials` object pasted here is a plaintext secret in the stored agent config. The registry's own `help` text already steers toward "omit and use the ambient AWS credential chain," which is the safe default; V2-05 may want to say that more forcefully in `verified_note`/`notes`, but `RegistryForm` deliberately gives every field the same treatment (its contract: "no provider gets bespoke UI"). | Deferred to whichever package built the credentials UI for it (PLAN-V2 V2-13 card); that's this package's `registry/**`. |
| V2-13-2 | V2-05 | `contracts/src/lkap_contracts/providers.py` | No registry entry sets `FieldSpec.catalog_kind` on an avatar provider's id field today (confirmed: `bey-avatar`'s `avatar_id` and `simli-avatar`'s `simli_config.face_id` are both plain `type="string"`, even though both providers carry a working `catalog=CatalogSpec(kinds=["avatars"])`). This package's avatar picker (`registry-form.tsx`'s `CatalogFieldContext`/`catalogKindFor`) therefore falls back to a **name-convention heuristic** (`avatar_id`, `*.face_id`, `*.persona_id` → the catalog combobox) instead of an explicit per-field marker. It works for both wired vendors (bey, simli) and is applied uniformly (not vendor-specific code), but a one-line fix — setting `catalog_kind="avatars"` on those two fields — would make it exact instead of name-matched, and would extend automatically to any future avatar provider that doesn't happen to name its field `*_id`/`*face_id`/`*persona_id`. |
| V2-13-3 | **Closed by V2-19C** (2026-09-23): `CapabilityKind` gained `text-modality` (`TypeIcon`) and `cloud-only` (`CloudIcon`); `provider-row.tsx`'s `ExtraBadge` stand-in is deleted (`shared-primitives.test.tsx` covers both kinds) | `CapabilityBadge`'s `CapabilityKind` union / `CAPABILITY_BADGE_META` | UI_UX_SPEC-V2-AMENDMENTS §2.2 asks the Providers catalog row for `vision, text-modality, tools, no-key, cloud-only` badges. `vision`/`tools`/`no-key` already exist; `text-modality` and `cloud-only` don't, and `capability-badge.tsx` isn't in this package's exclusive file list (only `components/shared/capability-meta.ts`, a new file this package added for *connection* capability chips — a different concept). `web/src/components/console/providers/provider-row.tsx` renders a local `StatusChip`-based stand-in (`ExtraBadge`) for those two instead of editing WP-0's file. A one-line addition of two `CapabilityKind` members would let the Providers page use the real `CapabilityBadge` component and drop the stand-in. |
| V2-13-4 | Coordinator / V2-14 | `api/src/lkap_api/routers/` (no `storage-configs` router exists), `web/src/components/console/settings/settings-tabs.tsx` (Storage tab is a `ComingSoonTab`) | The Detail → Storage tab (UI_UX_SPEC-V2-AMENDMENTS §2.1: "pick or create a storage config; Test writes and deletes a 1 KB object") can't be built as specified: ask #24 already recorded that `GET/PUT /v1/connections/{id}/storage` was never implemented, and there is no `/v1/storage-configs` CRUD at all yet. `web/src/components/console/connections/storage-tab.tsx` ships a read-only placeholder (current `storage_config_id`, a note pointing at this gap) instead. Once a storage-configs API exists (V2-14's Settings → Storage), this tab should become the real picker + 1 KB write/delete test. |
| V2-13-5 | **Closed by V2-19C** (2026-09-23): the capture script also covers `/login`, `/console/analytics`, `/console/telephony` and `/console/keys`, and `axe.json` now records the first five offending nodes per finding | `web/scripts/ui-capture.mjs` | Not in this package's exclusive file list (not owned by any card), but it has no route for `/console/connections`, `/console/connections/new` or `/console/providers`, so a capture run before this change never screenshot them. Added `console-connections`, `console-connections-new`, `console-providers` to `captureConsole`'s `staticRoutes`, a `console-connection-detail` route via a newly discovered `ids.connectionId` (the workspace's default connection — **read-only navigation only**; the script never clicks Test/Rotate/Delete, so the user's live default connection is never mutated by a capture run), and an `ids.connectionId` lookup in `discoverIds`. Purely additive; no existing route or shot name changed. |
| V2-13-6 | **Closed by V2-19C** (2026-09-23): fixed with the A11Y entry — see the Coordinator decisions A11Y bullet | — | The connections/providers pages inherit the same three pre-existing, already-triaged moderate a11y findings as every other console page (`region`, `landmark-unique`, `heading-order` — see the WP-10 A11Y entry). This package fixed the `heading-order` instances its own new components introduced (four `<h3>`s promoted to `<h2>` in `connection-create-form.tsx`, `connection-overview.tsx`, `fleet-card.tsx` — each was the first heading after `PageHeader`'s `<h1>` with nothing in between). `region`/`landmark-unique` are the same systemic pattern WP-10 already assigned to WP-12/V2-19 as one fix pass; not re-fixed per-package here. |

Consumed, no action needed:
- **#48** (V2-04's fleet card hand-off): implemented in `web/src/components/console/connections/fleet-card.tsx` — desired-replicas stepper, Start/Stop/Restart (`restart` per R-V2-4), the instances table (including `gone` rows the endpoint already includes), `restart_requested_at` shown as "Restart requested … ago", and a warning when a pool mixes `external` and `supervisor` `managed_by` rows (PLAN-V2 §7's risk).
- **R-V2-2**: `web/src/components/console/registry/**` and `CAPABILITY_META` migrated off the `status`/`isMvp` alias onto `availability` + `installed_on` + `enabled` (`provider-meta.ts`'s `slotAvailability`/`connectionDisabledReason`/`isSelectableForCredentials`), with one nuance worth recording: **without a bound connection in view, the gate still falls back to the `isMvp` alias** rather than reading `installed_on` at all. The reason is `installed_on` has no meaning without a specific connection to test against, and the static registry export (`contracts/generated/providers.json`, generated with zero connections) always reports it as `[]` for every entry — treating an empty list as "not installed" with no connection in view would make every provider unselectable outside a live workspace with real connections, which broke WP-4's existing `ProviderSlotEditor`/`ProvidersTab`/credential-sheet tests when first tried. The connection-scoped gate (`ctx.connection` set) is where `installed_on` is actually authoritative, and that's what the providers section's slot cards use.

## Open — left by V2-06 (Credential tests, vendor catalogs, workspace enablement)

| # | Owner | File | Edit / hand-off | Why |
|---|---|---|---|---|
| 60 | **Closed** (coordinator + V2-06) | `api/src/lkap_api/routers/provider_keys.py` (renamed from `routers/credentials.py`, content unchanged, `git mv`; `/v1/credentials` API path unaffected) | Resolved two ways. First, the coordinator (on the user's delegation) renamed the file: the operator's `Read(./**/credentials*)` permission rule matches paths *starting* with `credentials`, so `provider_keys.py` is readable/editable while `routers/credentials.py` was not; imports fixed in `main.py`, `credential_tests/__init__.py`, `tests/test_credentials.py`, `tests/test_credential_tests.py`. Second, V2-06 then applied patch (1): `test_credential` now checks `credential_tests.has_adapter(spec)` + `is_cache_fresh(row.last_test_at)` first (returns `cached_result` on a hit), else decrypts and calls `credential_tests.run(spec, secrets, client)`, persisting `last_test_at/ok/message` on a real result before returning it, and only then falls back to the original `_TEST_CALLS` table unchanged. Verified: `uv run python -c "import lkap_api.main"` (with the test env vars `conftest.REQUIRED_ENV` sets) imports cleanly — no circular import; `pytest tests/test_credentials.py tests/test_credential_tests.py tests/test_tools.py` all green (64 passed); full api gate reported in the V2-06 report. Patch (2) (the ask-#25 tenant-scoping pass across all four routers) stays assigned to V2-02, untouched here per the coordinator's instruction. | Renamed 2026-09-23; router patch applied same day. |
| 61 | V2-05 | `contracts/src/lkap_contracts/providers.py` | D-V2-9 names 20 vendor adapters. The registry currently sets `catalog=`/`test=` for 13 of them, all implemented and tested in `api/src/lkap_api/catalogs/adapters.py`. Of the remaining seven (Google Gemini API, Cerebras, Perplexity, Deepgram, Polly, Azure Speech, D-ID), only **D-ID** has a working, tested adapter ready to wire (`did_avatars` in `ADAPTERS`, confirmed list API per research §2.2) — it just needs `catalog=CatalogSpec(adapter="did_avatars", kinds=["avatars"])` and `test="did_avatars"` added to the `did-avatar` entry. The other six have no adapter implementation at all yet. Flagging so V2-13/WP-4 don't expect a catalog picker for those six, and so whoever wires D-ID's registry fields gets a one-line change against code that's already tested. | `docs/research-v2/livekit-plugins-catalog.md` §1/§2.2 confirms list-APIs for all of these; `providers.py` is V2-05's exclusive file (mine are `catalogs/**`/`credential_tests/**`/three routers only). |
| 62 | — (informational) | `contracts/src/lkap_contracts/api_models.py` | `CatalogResponse` gained an additive `error` field (type `str \| None`, default `None`, so no existing consumer breaks) so `GET /v1/providers/{id}/catalog` can report *why* it fell back to a cached or static list without inventing an out-of-contract key. `ProvidersResponse` was also completed per the card: `v` now defaults to `2` and `providers: list[ProviderOut]` (was `list[ProviderSpec]`); `contracts/src/lkap_contracts/export.py::build_providers_document` and both `contracts/tests/test_export.py` assertions and `api/tests/test_health.py::test_providers_endpoint_lists_the_whole_registry` were updated to match (mechanical fixups, same pattern as asks #2-4); contracts + web types regenerated (`scripts/export_contracts.sh --generate`). | Explicitly granted by the task brief ("bumping it to 2 with `ProviderOut` is yours"); the `error` field is a direct, minimal consequence of the "never break the page" acceptance criterion with nowhere else to put the note. |

## Open — left by V2-09 (Images, compose, CI)

Renumbered 56–58 (not 48–50) when first written: this session's edit collided
with another package concurrently claiming 48/49/50 in a different section
(`## Open — left by V2-07 (Worker v2)`) — same race the whole log is exposed
to with several agents appending at once. Picking the number after the
highest one *visible in this file* isn't safe under concurrent writes;
whoever reconciles `_asks.md` at merge time should consider switching to a
prefix per package (e.g. `V2-09-1`) to remove the collision risk entirely.

| # | Owner | File | Edit / hand-off | Why |
|---|---|---|---|---|
| 56 | **Closed by V2-19B** (2026-09-23): `krisp-noise-cancellation` is `availability="deferred"` with a note; `agent/requirements/full.txt` regenerated (no krisp line); `agent/pyproject.toml` never pinned it | `contracts/src/lkap_contracts/providers.py` (`krisp-noise-cancellation`) | `livekit-plugins-krisp` has no `1.8.2`-tagged release on PyPI — confirmed live against `https://pypi.org/pypi/livekit-plugins-krisp/json` (2026-09-23): latest is `0.4.2`, versioned independently of `livekit-agents` core, not lockstepped. Every *other* `full`-image package resolves at `==1.8.2` (checked all 67, same method). `agent/requirements/full.txt` still pins it at `==1.8.2` per CONTRACTS-V2 §7's literal rule and PLAN-V2 §7's own risk-table design ("the build gate is the import check; failures ... flipped to deferred with a note") — this is that gate firing on the first real registry entry it was built to catch, not a generator bug. The nightly `full` image build (`.github/workflows/docker.yml`, `build-agent-full`) **will fail on this package** until someone with permission to edit the registry either (a) flips `krisp-noise-cancellation`'s `availability` to `"deferred"` with a note, or (b) special-cases its pin to a real installable version (`livekit-plugins-krisp` has its own SDK boundary — check whether `0.4.2` actually still targets `livekit-agents==1.8.2`'s plugin ABI before pinning it). Not something V2-09 can fix in its own files: `providers.py` is V2-05's. | Live PyPI verification during V2-09 build-gate testing (2026-09-23); `docs/v2/PLAN-V2.md` §7 risk row "full image native wheels fail (bithuman, Krisp, Azure Speech, awscrt)" already named Krisp as an expected risk. |
| 57 | V2-06 | `api/tests/test_tools.py::test_update_replaces_the_definition` | Reproduces in isolation (not flaky): `PUT /v1/tools/{id}` with a full valid body returns `422` then the test's `response.json()["definition"]` raises `KeyError: 'definition'` — the response is an error body, not the updated tool. Found incidentally while V2-09 ran the full api suite once (`uv run pytest -q -m "not live"`, no env overrides) to size `python.yml`'s gate; not investigated further since `routers/tools.py` is V2-06's file (ask #38 already names it, F-15). | Full-suite run during `.github/workflows/python.yml` authoring; 2 failures found (see #58), api otherwise 584 passed / 1 skipped. |
| 58 | V2-02 — **fixed by V2-02**: the test is now async (it was a sync test depending on the async autouse `_at_head` fixture), clears the `migration_head` cache and compares with a head computed afresh from `api/alembic` instead of a hard-coded revision; `_at_head` clears the cache too | `api/tests/test_health.py::test_migration_head_is_the_latest_v2_revision` | Failed only in the full-suite run (`584 passed` run), **passed** when run alone or with `test_tools.py`'s one test — looks order-dependent (shared cache/global state polluted by an earlier test module), not a deterministic bug. Not investigated further: `health.py` and its tests are V2-02's ask-#11 territory, and `.github/workflows/python.yml` runs the real suite as-is rather than masking this. | Same full-suite run as #57. |

| 63 | — (informational) | `api/tests/test_validation_connections.py`, `api/tests/test_tools.py`, `api/tests/test_credentials.py` | V2-03's `test_validation_connections.py` (untracked, so this rides along in whatever commit first picks the file up) had a local `blocked_provider_validator` fixture that registered-then-removed a throwaway stub validator purely to prove `config_service.VALIDATORS` works; V2-06 replaced it with an explicit `import lkap_api.catalogs` and updated both call sites' expected message to the real `catalogs.validation.disabled_provider_issues` text, since both validators would now fire and double the issue count. `test_tools.py`'s `_create_tool` helper now auto-attaches a `http-tool-secret` credential whenever the merged definition contains a `{{ secret.` placeholder and no `credential_id` override was given (F-15 would otherwise 422 every test that used the unmodified `HTTP_DEFINITION`, which has always had a `{{ secret.WEATHER_KEY }}` header); five call sites needed a one-line adjustment to keep the credential attached across create→update round trips. `test_credentials.py::test_credential_test_reports_not_implemented_providers` was moved from `bey-avatar` to `did-avatar`, since `bey-avatar` now has a real `credential_tests` adapter that ask #60's patch (now applied) routes it to (unlike `did-avatar`, whose `spec.test` stays unset). | Mechanical fallout of the workspace-enablement validator, F-15 and the shared adapter tree, same pattern as asks #2-4/#40. |
| 64 | — (informational, closes ask #57) | `api/tests/test_tools.py::test_update_replaces_the_definition` | Fixed. Ask #57's `KeyError: 'definition'` was this package's F-15 change: `update_tool` now 422s when the request's `definition` still carries `{{ secret.WEATHER_KEY }}` with no resolvable credential, and this test's inline `json={...}` body was constructed from the bare `HTTP_DEFINITION` (which has always had that placeholder) with no `credential_id`. Fix landed in the same edit as ask #63's `_create_tool` change: the PUT body now spreads `tool["definition"]` (which already carries the credential `_create_tool` attached) before overriding `method`. Reconfirmed green both alone (`uv run pytest tests/test_tools.py::test_update_replaces_the_definition`) and in the full suite. | V2-09 flagged it (ask #57) while sizing `python.yml`; V2-06 owns `routers/tools.py`. |

## Open — left by V2-15 (Flow runtime)

Numbered `V2-15-n` (the per-package prefix V2-09 proposed above) to avoid the concurrent-append collisions. The first three are design gaps the docs do not answer; they need an architect ruling before V2-16 builds on them.

| # | Owner | File | Edit / hand-off | Why |
|---|---|---|---|---|
| V2-15-1 | **Ruled R-V2-8** → V2-16 (option (a): `SessionSummaryIn` fields) | `contracts/.../api_models.py::SessionSummaryIn`, `api/.../routers/internal.py::put_summary` | No worker→api path exists for `sessions.disposition` / `sessions.variables` (the columns exist; `SessionSummaryIn` has neither field and nothing writes them). V2-15 records the final state as a durable session event `flow_ended {completed, current_node, path, variables, disposition, reason, webhook_event?, missing_required?}` (recorded before the summary is posted). Proposed: `SessionSummaryIn += disposition: str\|None, variables: dict` (the worker fills them from `FlowState`; the api writes the columns and puts both in `session.ended`), **or** the api reads the `flow_ended` event in `put_summary`. The worker change is a few lines either way. | CONTRACTS-V2 §1.4 names the columns but no contract fills them. |
| V2-15-2 | **Ruled R-V2-9** → V2-16 (fallback stands + save-time validation; `node_providers` is Phase 2) | `ResolvedAgentConfig`, `config_service.resolve_providers` | `AgentNode.providers` are `ProviderRef`s, and `resolved` is keyed by slot, so credentialed per-node overrides cannot reach the worker (R-V2-6: the worker never resolves credentials). V2-15 applies an override only when it needs no new secret: the same provider (and credential) as an already-resolved slot (`llm`/`workflow_llm`/`qa_llm`, `tts`) with the node's model and fields on top, or a `requires_credential=False` provider (Inference) resolved from the registry. Anything else logs a warning and keeps the session provider. Proposed: `ResolvedAgentConfig.node_providers: dict[node_id, dict["llm"\|"tts", ResolvedProvider]]`, filled by the api for cascaded flows, which `flow/providers.py` would prefer. | Same shape as ask #51 → R-V2-6. |
| V2-15-3 | **Ruled R-V2-10** → V2-16 (names, validated against the agent-level union) | `AgentNode.tools` / `GlobalNode.tools` | CONTRACTS §4.5 says node tools are "builtin names, tool ids, pack tool names", but `ToolDefinition` (what the worker receives) carries no id. The worker matches by **name**: built-in/block tool names, pack tool names, HTTP tool `name`, MCP server `name` (whole server). Proposed: the api stores or rewrites node tool references as names at resolve time. Also: a node may only use tools and KBs the agent resolves (`config.tools.tool_ids`, `config.knowledge.kb_ids`); the worker drops anything else with a warning. So either the builder restricts node pickers to the agent-level selections, or the api resolves their union. | The worker cannot map ids. |
| V2-15-4 | **Ruled R-V2-11** → V2-16 (`effective_qa`: a `qa` node means QA is on) | `config_service` flow validation | A `qa` node only enables QA when the api resolved `qa_llm` (R-V2-6: only when `qa.enabled`). Save-time validation should require `qa.enabled=true` when the flow has a `qa` node (or the resolver should treat a `qa` node as enabled). The node's `rubric_prompt` overrides `qa.rubric_prompt` at run time. | Otherwise a `qa` node is silently inert. |
| V2-15-5 | **Ruled R-V2-12** → V2-16 (`mode` derived from `config.flow` on save) | `routers/agents.py::update_agent` | `PUT /v1/agents/{id}` does not set `agents.mode`. The worker runs a flow whenever `config.flow` has nodes, whatever `mode` says. V2-16 should keep `mode` and `config.flow` consistent (mode=`prompt` ⇒ `flow=None`). | The runtime keys off `config.flow`. |
| V2-15-6 | V2-17 (informational; R-V2-14 confirms no ruling needed) | `agent/src/lkap_agent/flow/runtime.py::FlowServices.transfer` / `.initial_variables` | Transfer nodes call `FlowServices.transfer: (TransferNode, FlowState) -> Awaitable[bool]`. The default (`None`) records a `transfer {status: "unavailable"}` event and tells the model to keep helping. Outbound `CallCreate.variables` can seed `FlowServices.initial_variables`. Wiring both from the telephony module is a two-line change in `main._assemble`. | Telephony is V2-17's. |
| V2-15-8 | **Ruled R-V2-13** → V2-16 (option (b): `instructions → global → node`) | `flow/runtime.py::FlowRuntime.instructions_for`, WP-3 mode switch | The docs say "global prefix + node prompt" and do not say what happens to `AgentConfig.instructions` in flow mode. V2-15 uses the global node's instructions **instead of** `config.instructions` when a global node exists, and `config.instructions` as the global prefix when none exists. A prompt→flow switch that then adds a global node therefore drops the author's base prompt with no warning. Options: (a) keep this rule and have the builder migrate `config.instructions` into the new global node; (b) always prepend `config.instructions` → global → node. Either is a one-line change in `instructions_for`. | Undocumented precedence. |
| V2-15-7 | **Ruled R-V2-14** → hooks ratified; ownership passes to V2-16 (adds the `_assemble` flow test) | `agent/src/lkap_agent/main.py` | V2-15 edited `main.py` outside its card: an `is_flow(resolved)` branch in `_assemble` that builds `build_flow_agent(FlowServices(...))` instead of `PlatformAgent`, and `prepare_flow_resolved(...)` wrapped around `prepare_resolved(...)` in `run_session`. Without these, flow mode is unreachable. | Card lists `flow/**`, tests and `platform_agent.py` hooks only. |

## Open — left by V2-14 (Web: auth, team, keys, webhooks, settings, sessions v2)

| # | Owner | File | Edit / hand-off | Why |
|---|---|---|---|---|
| V2-14-1 | **Closed by V2-19C** (2026-09-23): an account menu (`shell/account-menu.tsx`) in the sidebar footer on every console screen (rail and mobile sheet): name and email, "Account settings", and **Sign out** through the shared `components/console/lib/sign-out.ts` (also used by `workspace-tab.tsx`). The break-glass token gets an explanatory line instead of a no-op sign-out (`tests/console-account-menu.test.tsx`) | none owned by this package | Neither file has an account/user menu slot for "Sign out". V2-14 put Sign out and the password-change form in `/console/settings?tab=workspace`'s new Account section (`components/console/settings/workspace-tab.tsx`) instead, which works but is a page navigation away from every other screen. A one-line ask, not a blocker: add a "Sign out" item to the sidebar footer or a header account menu, calling `POST /api/console/auth/logout` then `window.location.assign("/login")`. | No entry point existed to plug into; not a file this card owns. |
| V2-14-2 | — (informational) | `api/src/lkap_api/routers/workspaces.py` | No `GET /v1/workspaces/{id}` (single) exists — only `GET /v1/workspaces` (list) and `PUT /v1/workspaces/{id}`. `use-settings-queries.ts::useWorkspace` reads the list and selects the active one client-side rather than requesting a route that would 404. Flagging in case a future package wants the singular route for a cleaner fetch. | Discovered live: the console showed the Workspace tab stuck on "Loading…" until fixed. |
| V2-14-3 | — (informational) | none | `docs/v2/UI_UX_SPEC-V2-AMENDMENTS.md` §1 names `/invite/[token]` as the invite route, but the api's own `_invite_url` (`routers/workspaces.py`) builds `{web_base_url}/login?invite=<token>`, matching CONTRACTS-V2 §3.1's "signed in" flow and ask #37's instruction ("`/login?invite=<token>`"). V2-14 followed the api (the executable contract) and built invite acceptance into `/login` rather than adding a separate `/invite/[token]` page. Worth a one-line fix to the amendments doc so the two stop disagreeing. | Direct conflict between two docs; the api is what actually runs. |
| V2-14-4 | — (informational) | none (no router exists) | Settings' Storage and Danger-zone tabs render an honest "not available yet" card rather than functioning CRUD: `routers/workspaces.py` has no `DELETE /v1/workspaces/{id}` or ownership-transfer route, and there is no `/v1/storage-configs` (or `/v1/connections/{id}/storage`, ask #24) router — `storage_configs` is only reachable today through `PUT /v1/connections/{id}` (V2-13's surface). Nothing to build against yet; flagging so whoever adds either route also knows the Settings tab is the natural home for the workspace-level half of it. | CONTRACTS-V2 §1.2/§3.2 describe both actions; neither ships an endpoint in Phase 1. |
| V2-14-5 | — (informational) | `web/src/components/console/sessions/{session-model.ts,sessions-table.tsx,use-session-queries.ts}` | `GET /v1/sessions` now accepts `channel`/`connection_id`/`from`/`to` server-side (closed by ask #67), but V2-14 left `sessions-table.tsx` on WP-7's original client-side `filterSessions`/`paginate` over one 500-row fetch rather than switching to server paging. The read is still correct (just not maximally efficient past ~500 sessions in a workspace), and the switch touches `session-model.ts`'s pagination helpers and `tests/console-sessions-table.test.tsx`'s ~300 lines of client-filter assertions non-trivially. Deferred rather than risked under this card's time budget — a clean follow-up, not a design question. | Task instruction: "switch … only if … a clean change … otherwise report it." |
| V2-14-6 | V2-00 / coordinator (re-affirms open asks #26, #39) | `contracts/src/lkap_contracts/api_models.py` | Still not done: moving `LoginIn`/`WorkspaceOut`/`MemberOut`/`ApiKeyOut`/`AuditOut`/etc. (ask #26) and a real `WebhookEndpointCreated` with `secret: str` (ask #39) into contracts. V2-14 mirrors all of them by hand in `web/src/components/console/settings/api-types.ts` (clearly marked, header explains why, deleted once these land). | Confirms both asks are still needed; V2-14 is the first consumer that actually needed the shapes. |

## Open — left by V2-17 (Telephony core)

Numbered `V2-17-n`. Items 1–5 are hand-offs to files V2-17 does not own; 6–9 are for the architect (design gaps the docs do not answer, each with the interim V2-17 shipped). **#55 is answered** by V2-17's worker hook: `lkap_agent.telephony.TelephonySession` reports the SIP leg (`sip.phoneNumber`/`sip.trunkPhoneNumber`/`sip.callID`/`sip.trunkID`) to `POST /internal/v1/telephony/calls/report`, which fills `sessions.caller`, creates/links the inbound `calls` row and replaces the `sip_in-caller` placeholder identity; the `participant_joined` webhook does the same when webhooks are configured. Dispatch rules send `config_version: 0, participant_identity: ""` as #55 asked. It takes effect once V2-17-2 is applied. **V2-15-6 is answered** by `TelephonySession.flow_transfer` (wire it in V2-17-2).

| # | Owner | File | Edit / hand-off | Why |
|---|---|---|---|---|
| V2-17-1 | **Ruled R-V2-25** → V2-19 (promote into `api_models.py`; patterns in the new `lkap_contracts/telephony.py`; delete `types.ts`) | `contracts/src/lkap_contracts/api_models.py`, regenerate | Promote `TrunkCreate/TrunkUpdate/TrunkOut`, `DispatchRuleCreate/DispatchRuleOut`, `PhoneNumberCreate/PhoneNumberUpdate/PhoneNumberOut`, `CallTransferIn`, `CallDtmfIn/CallDtmfOut`, `CallReportIn`, `InternalTransferIn/Out` from `api/src/lkap_api/telephony/models.py` (field names final), then delete the hand-written `web/src/components/console/telephony/types.ts`. | CONTRACTS-V2 §3.4 names the routes but not these shapes; V2-17 does not own `api_models.py`, so the TS side is a hand-written stopgap. |
| V2-17-2 | **Ruled R-V2-20** → V2-19, first `main.py` edit after V2-16 and V2-18 land (as written, with four amendments: no `Deps` seam — `deps.config_client` carries the two calls; `TelephonySession` in `SessionContext.userdata["telephony"]`; `LKAP_SIP_ANSWER_TIMEOUT_S`=135 backstop + `_abort_start(speak=False)`; reconnect grace 0 on SIP) | `agent/src/lkap_agent/main.py` | Wire the worker hook (all functions exist and are tested in `tests/unit/test_telephony.py`). (a) `Deps += telephony_api_factory: Callable[[], TelephonyApi]`, default `lambda: HttpTelephonyApi(settings.api_base_url, settings.service_token)`. (b) `_assemble`, after `session_ctx` exists: `telephony = telephony_mod.session_for(room=ctx.room, resolved=resolved, pack_ctx=session_ctx, pack=pack, record_event=record_event or _noop_record_event, api=deps.telephony_api_factory())`; append `*(telephony.tools(config=resolved.config, shutdown=lambda r: ctx.shutdown(reason=r)) if telephony else [])` to `tools`; pass `transfer=telephony.flow_transfer if telephony else None` to `FlowServices`; carry `telephony` back to `run_session` (e.g. a third return value or an attribute on the agent). (c) `_start`, between `await ctx.connect()` and `deps.session_starter(...)`: `if resolved.channel == "sip_out" and await telephony_mod.wait_for_answer(ctx.room, timeout_s=60) is None: raise RuntimeError("callee never answered")` (the existing `_abort_start` path ends the job). (d) `run_session`, right after `await _start(...)` succeeds: `telephony.start()`. (e) The shutdown callback, before the summary: `await telephony.aclose(reason=<shutdown reason>)`, then the api client's `aclose()`. | Only `telephony.py` and the two tool files are V2-17's. Without this the calls still connect (dispatch rules and `CreateSIPParticipant` are api-side), but there is no worker caller/status report, no DTMF in/out, no `send_dtmf`/`transfer_call` tool, and an outbound agent may greet a phone that is still ringing (RoomIO links the SIP leg while `sip.callStatus=dialing`). |
| V2-17-3 | **Ruled R-V2-25** → V2-19 (`TELEPHONY_TOOL_NAMES` re-export; `TELEPHONY_TOOLS` in `constants.ts`, outside `BUILTIN_TOOLS`) | both | Export `lkap_agent.telephony.TELEPHONY_TOOL_NAMES` (`send_dtmf`, `transfer_call`) next to `BLOCK_TOOL_NAMES`, and add the two console toggles (`builtin_disabled` names). Both tools are registered only on SIP sessions: `send_dtmf` needs `capabilities.dtmf`, `transfer_call` needs a non-empty transfer allowlist (V2-17-8). | So the Tools tab can switch them off like every other built-in. |
| V2-17-4 | **Ruled R-V2-20** → V2-19 (fold into `ConfigClientProtocol`/`ConfigClient`; `HttpTelephonyApi` deleted; the config-client fake gains both methods) | `agent/src/lkap_agent/config_client.py` | Fold `report_call(body)` and `transfer_call(session_id, to, identity)` into `ConfigClientProtocol`/`ConfigClient`, then retire `lkap_agent.telephony.HttpTelephonyApi`. | V2-17 keeps its own small httpx client so it did not edit V2-07's file. |
| V2-17-5 | **Ruled R-V2-22** → V2-19 (`ResolvedAgentConfig.variables` from `sessions.variables`; flow → `initial_variables`, prompt → `apply_call_variables`; `put_summary` **merges** `variables` — that last line is V2-16's, running now) | `GET /internal/v1/sessions/{id}/resolved` | `POST /v1/calls` stores `CallCreate.variables` on `sessions.variables`, but nothing sends it to the worker, so today it is dead data (and `sessions.variables` is documented as flow *output*). Recommended: add `ResolvedAgentConfig.variables: dict` (filled from the session row for `sip_out`), and have `main._assemble` pass it as `FlowServices.initial_variables` (V2-15-6). The alternative is to stop storing it. | CONTRACTS-V2 §4.6 has `CallCreate.variables` with no consumer. |
| V2-17-6 | **Ruled R-V2-25** → done in CONTRACTS §3.4 (both routes listed, router `routers/calls.py`) | CONTRACTS-V2 §3.4 internal routes | V2-17 added two service-token routes in its own `routers/calls.py`: `POST /internal/v1/telephony/calls/report` (the worker's view of a leg; creates or links the inbound `calls` row, fills `sessions.caller`, forward-only status) and `POST /internal/v1/telephony/sessions/{id}/transfer` (the `transfer_call` tool's path to `SipService.transfer_sip_participant`, which is exactly ARCHITECTURE-V2 §2.6 "W→API transfer request"). Please list them in §3.4. | §3.4 names no worker telephony route, but §2.6 draws one. Webhooks alone do not work in dev (no public URL), and a webhook usually arrives before the worker's `sessions/start`. |
| V2-17-7 | **Ruled R-V2-25** → ratified; card wording amended; V2-20 verifies `participant is None` live, V2-21 falls back to an HMAC check if not | PLAN-V2 V2-17 card ("DTMF send via a worker RPC") | The server SDK (`livekit-api` 1.2.1 `RoomService`) has no participant RPC, and the api must not join rooms. `POST /v1/calls/{id}/dtmf` therefore sends a reliable `RoomService.SendData` packet on topic `lkap.telephony.dtmf`, `{"v":1,"op":"dtmf","digits":…,"call_id":…}`. The worker honours it only when the packet came from the server (`participant is None`) and plays it with `publish_dtmf`. There is no delivery acknowledgement: the route answers `queued: true`. | Card wording vs. the SDK. |
| V2-17-8 | **Ruled R-V2-21** → V2-19 (`AgentConfig.telephony: TelephonyConfig{transfer_targets: [{label, to}]}` in `lkap_contracts/telephony.py`; `pack_settings["transfer_targets"]` retired; editor list + form-schema addition; save-time policy validation in `telephony/validation.py`) | `AgentConfig` | Where do an agent's **transfer destinations** live? The `transfer_call` tool must never dial free text from the model (a caller could talk it into a premium-rate number). Interim: an allowlist in `config.pack_settings["transfer_targets"]` (`{"Sales": "+1…"}` or a list), and the tool is registered only when it is non-empty. Proposal: `AgentConfig.telephony: TelephonyConfig{transfer_targets: dict[str, str]}` in contracts, plus an editor section. Flow `transfer` nodes are unaffected (`node.to` is author-configured). | Neither CONTRACTS-V2 nor the card defines it. |
| V2-17-9 | **Ruled R-V2-25** → events listed in CONTRACTS §3.4; `DtmfPack` moves to `packs/base.py` (V2-19) | CONTRACTS-V2 §3.4 event types | New session event types from the worker: `sip_answered {participant_identity, from, to, call_id, trunk_id}`, plus `dtmf {direction: received\|sent, digits, source?}` and `transfer {to, ok, status, reason}` as listed. The pack hook `on_dtmf(ctx, digits) -> bool` sits on a separate structural `DtmfPack` Protocol, like `BlockActionPack` (asks #71); the `packs/base.py` owner may want to document it there. | Additive. |
| V2-17-10 | **Ruled R-V2-23** → V2-19 implements (default-deny `workspaces.settings["telephony"]` policy: `allowed_prefixes`, `allowed_sip_hosts`, `max_calls_per_min`, `max_concurrent_outbound`; static `BLOCKED_PREFIXES`; one `check_destination` chokepoint for dial, both transfers and save-time validation; `live_session_count` → `lkap_api/limits.py`; audit rows), V2-21 reviews + adversarial tests | `routers/calls.py`, `telephony/calls.py` | Toll-fraud surface: any `builder` (or an API key with `calls:write`) can dial any E.164 number through the workspace's trunk. There is no destination allowlist or country block, no per-workspace call rate or concurrency cap, and `AgentLimits.max_concurrent_sessions` is not checked for outbound calls. Decide the policy (e.g. an allowed-prefix list per trunk, plus a calls-per-minute bucket). | Phase 1 relies on the carrier's own fraud controls. |
| V2-17-11 | **Ruled R-V2-24** → V2-19 (no jobs move; `sweep_stuck_calls` from `sweep_loop`: outbound `dialing`/`ringing` older than 210 s by `coalesce(calls.started_at, sessions.created_at)` → `failed`; open rows follow a terminal session; inbound rows never ring-swept) | `telephony/calls.py::run_dial` | The dial runs as a FastAPI background task in the api process. An api restart mid-dial (e.g. `--reload`) leaves the call `dialing` until a webhook, the worker's report or `room_finished` closes it; nothing sweeps it. Move the dial onto the jobs system (idempotent already: `started_at` guards a re-run), or add a sweep that fails `dialing` calls older than the ring timeout plus a margin. | The inline jobs backend retries, which is safe only because of the `started_at` guard. |

## Open — left by V2-16 (Flow API + builder UI)

Numbered `V2-16-n`. V2-16 implemented R-V2-8 (with the R-V2-22 merge), R-V2-9…R-V2-14 inside its grants; the rows below are the edits outside them and the hand-offs.

| # | Owner | File | Edit / hand-off | Why |
|---|---|---|---|---|
| V2-16-1 | — (applied by this package, mechanical; precedent #75 / V2-13-0) | `web/src/components/console/lib/schemas.ts`, `web/src/components/console/agents/editor/form-values.ts`, `editor/extensions.ts` | `config.flow` joined `agentConfigFormSchema` as `z.custom<FlowSpec \| null>().optional()` (lax: the builder checks the graph itself); `toFormValues` fills it, `buildAgentUpdate` sends it when present. One append-only entry (`flowBuilderExtension`) in `extensions.ts`. No existing fixture needed a change (the field is optional). | Editor README rule 2: the flow section edits `config.flow`, and R-V2-12 derives `mode` from it, so the Save payload must carry it. |
| V2-16-2 | **Closed by V2-19B** (2026-09-23): `ValidationContext.pack_tool_names` (config_service: the field + a `pack_tool_names` kwarg on `validate_in_db` only); `flows.pack_tool_names_for(pack_id)`; `routers/agents.py::validate_stored_config` and `routers/flows.py` pass the agent's pack; unknown pack → none, `pack_id=None` → the installed-pack union | `api/src/lkap_api/config_service.py::ValidationContext`, `validate_in_db` | The flow validator's allowed tool names (R-V2-10) include **every installed pack's** `tool_names`, because `ValidationContext` doesn't know the agent's pack. A name from another installed pack passes validation and the worker drops it with a warning. Proposed: `ValidationContext.pack_tool_names: frozenset[str] \| None` (or `pack_id`), filled by `validate_in_db` from the agent's pack, and used by `lkap_api.flows.validation.allowed_tool_names`. The web pickers already use the agent's own pack. | The V2-16 grant allowed exactly one new `ValidationContext` field (`tool_names_by_id`). |
| V2-16-3 | **Closed by V2-19B** (2026-09-23): (partly) `lkap_contracts.tools` holds `BUILTIN_TOOL_NAMES`, `VISION_TOOL_NAMES`, `BLOCK_TOOL_NAMES`, `UPDATABLE_BLOCK_TYPES`, `BLOCK_TOOL_TYPES`, exported as `contracts/generated/builtin_tools.json`; the api imports them; `agent/tests/unit/test_tool_names_parity.py` pins the worker's copies to them. The agent and web import swaps are V2-19B-2/-3 | `api/src/lkap_api/flows/validation.py` (`BUILTIN_TOOL_NAMES`, `BLOCK_TOOL_TYPES`), `web/src/components/console/flow/tool-options.ts` (`BLOCK_TOOL_TYPES`) | Both mirror `agent/src/lkap_agent/tools/builtin/__init__.py` (`BUILTIN_TOOL_NAMES`, `BLOCK_TOOL_NAMES`, and the block types each needs: `update_block` for any of document/gallery/table/transcript/video/kb_citations/custom, `show_document` → document, `table_append` → table, `request_form` → form). Proposed: move the names and the block-type map into `lkap_contracts` (e.g. `lkap_contracts.tools`) and export them, so the agent, the api and the web share one list. | Three hand-kept copies drift. |
| V2-16-4 | **Closed by V2-19B** (2026-09-23): `qa/scorer.py` uses `effective_qa(config)` for `enabled` and `rubric_prompt` (test: a flow QA node with `qa.enabled=false` is re-scored with the node's rubric) | `api/src/lkap_api/qa/scorer.py:140` | The api's re-score path (`POST /v1/sessions/{id}/qa`) still checks `config.qa.enabled`. Under R-V2-11 it should check `effective_qa(config).enabled` (and use its `rubric_prompt`), or a flow with a QA node and `qa.enabled=false` is scored by the worker but skipped on re-score. | R-V2-11: one definition everywhere. |
| V2-16-5 | **Resolved by V2-19T** (2026-09-23): `instructions` is `z.string()` plus a `superRefine` on `agentConfigFormSchema` that requires it only when `config.flow` has no nodes (coordinator's predicate; a flow-mode agent whose graph is still empty keeps the rule). Tests: `web/tests/console-zod-resolver.test.ts` ("lets a flow agent … save empty instructions", "still requires instructions for a prompt agent", "requires instructions when the flow is empty") | `web/src/components/console/lib/schemas.ts` (`instructions: z.string().min(1, …)`) | R-V2-13 lets a flow author empty `instructions` to use the global node alone; the api only warns, but the editor's zod rule refuses an empty value. Suggest: allow empty when `mode === "flow"` (a `superRefine` on `agentEditorFormSchema`). | Spec vs form rule. |
| V2-16-6 | WP-5 (informational) | `web/src/components/console/agents/tabs/instructions-tab.tsx` | R-V2-13's "Base instructions (apply to every node)" label is shown as a callout above WP-5's editor in flow mode (`components/console/flow/flow-instructions.tsx`, applied with a `sectionPatches` entry that wraps the section's component), and as the flow section's intro line. The nav label and WP-5's own heading stay "Instructions & voice" / "Instructions". If the heading itself should change in flow mode, that's a one-line change in WP-5's file. | Not V2-16's file. |
| V2-16-7 | — (informational; a necessary consequence of R-V2-11) | `api/src/lkap_api/routers/internal.py::_credential_ids` | Uses `effective_qa(config)` instead of `config.qa.enabled` when collecting the `qa.model` credential. Otherwise a flow QA node with `qa.enabled=false` and a vendor-key `qa.model` would resolve `qa_llm` with no secret. The grant named only `put_summary` in this file. | R-V2-11 acceptance ("a `qa` node with `qa.enabled=false` resolves `qa_llm`"). |
| V2-16-8 | — (informational) | `api/src/lkap_api/routers/flows.py`, `api/src/lkap_api/routers/agents.py` | (a) The flows router has no prefix, so the draft-validate route keeps the CONTRACTS path `POST /v1/agents/{id}/flow/validate`. Its body is read as a raw object: structural errors come back as `ValidationResult.issues` with a per-node/edge path, not a pydantic 422. Only flow-scoped findings (`flow.*`, `qa.enabled`) are returned. (b) `_raise_if_invalid` now also puts `issues` (with paths) in every config 422's `details` (additive), so a failed save dots the offending node. (c) `restore_version` also re-derives `mode` (R-V2-12). | So the builder can address findings. |
| V2-16-9 | **Closed by V2-19B** (2026-09-23): `_snapshot_version(..., created_by=ctx.actor.id)` at create/update/restore (user id, API-key id or `break-glass`); no migration needed, the column exists | `api/src/lkap_api/routers/agents.py::_snapshot_version` | `agent_config_versions.created_by` is never written, so the history sheet can't show who saved a version. | Contract field unused. |

## Open — left by V2-18 (Text test mode + widget)

Numbered `V2-18-n`. Foreign-file edits this package made (all small, per the card's own allowance), plus follow-ups.

| # | Owner | File | Edit / hand-off | Why |
|---|---|---|---|---|
| V2-18-1 | — (small hook, applied by this package) | `agent/src/lkap_agent/ui/channel.py` | Added `OnTextAction` callback type, an `on_text_action` param/attribute on `UiChannel.__init__`/`.bind()`, and a dispatch branch in `_dispatch_agent_action` for `rewind`/`inject_user_text` (returns `AgentActionResult(ok=False, "… is not supported")` with nothing bound, matching the existing `set_video_source` pattern). ~15 lines. | `rewind`/`inject_user_text` are first-class `AgentAction`s (CONTRACTS-V2 §4.4) delivered over the RPC `UiChannel` already terminates; routing them through the generic `ui_action` callback instead would misrepresent the contract. Not on the card's pre-listed hook list, but the smallest correct place to add it. |
| V2-18-2 | — (small hook) | `agent/src/lkap_agent/main.py` | One `if is_text_channel(resolved): ui.bind(on_text_action=...)` branch in `_assemble`, right after `ui = deps.ui_channel_factory(...)`, closing over `plan.session` and calling `lkap_agent.text_mode.handle_agent_action`. Kept separate from V2-16's `is_flow`/`prepare_flow_resolved` hooks in the same function. | The card's own allowed-hooks list named this file explicitly. |
| V2-18-3 | — (updated a leftover placeholder test) | `agent/tests/unit/test_ui_blocks.py::test_v2_18_actions_are_reported_unsupported` | V2-10 left this test asserting `rewind`/`inject_user_text` fall into the generic "unsupported action" branch with `"unsupported"` in the error text — a placeholder documenting pre-V2-18 behaviour, named after this package. Renamed and updated the assertion to `"is not supported"` (V2-18-1's wording) now that both actions are recognized (just unbound outside a `channel="text"` session). | The test's own name said it was V2-18's to update. |
| V2-18-4 | — (small hook, allowed) | `web/src/middleware.ts` | Made `middleware` `async`; widened `matcher` to include `/s/:path*`; added the embed CSP branch (`frame-ancestors`, fetching `GET /v1/agents/{slug}/embed-policy`, fail-closed to `'none'`). `/console/**` behaviour is unchanged (`consoleAuthRedirect`, extracted verbatim from the old body). | Card's allowed-hooks list named this file for the CSP header. |
| V2-18-5 | — (mechanical follow-on of V2-18-4) | `web/tests/middleware.test.ts` (V2-14's file) | `middleware` becoming `async` meant its three existing call sites (`middleware(request(...))`) needed `await`; added `await` and `async` to those three `it()` callbacks, no assertion changed. Added a new `describe("embed CSP middleware …")` block alongside (new tests only, nothing existing removed). | Unavoidable consequence of V2-18-4; the file isn't V2-18's, but the break was mechanical and the fix is one line ×3. |
| V2-18-6 | — (allowed hook) | `web/src/app/(session)/s/[slug]/page.tsx` | Read `embed`/`channel` from `searchParams`; when `embed === "1"`, renders `EmbedLayout` (V2-18's own file) instead of `SessionExperience`. No change to the non-embed path. | Card's own allowed-hooks list ("editor's Test button" aside — this is the other named page-level hook: `page.tsx` is the thin dispatcher `embed-layout.tsx` needs). |
| V2-18-7 | — (append-only, shared by design) | `web/src/components/console/agents/editor/extensions.ts` | Added one import (`testChatExtension`) and one array entry, after V2-16's `flowBuilderExtension`. Nothing else touched. | The file's own docstring names V2-18 as an expected future appender. |
| V2-18-8 | V2-19 / architect | `contracts/src/lkap_contracts/api_models.py::AgentPublicOut` | Chose **not** to add `allowed_origins` there: `embed_policy` (`api/src/lkap_api/routers/text_sessions.py`) is a small local `EmbedPolicy` Pydantic model instead, so the CSP-header need doesn't touch a frozen public contract mid-wave. If a future package wants `allowed_origins` on `AgentPublicOut` anyway (e.g. so the console preview page or an SDK can show it without a second endpoint), that's a one-field additive contract change plus a `to_public` line, following the R-V2-7/R-V2-8 precedent. | Documented alternative (advisor recommendation), not a code change. |
| V2-18-9 | **Closed by V2-19C** (2026-09-23): `embed?: boolean` threads `EmbedSession` → `SessionExperience` → `LiveSession` → `SessionRoom` → `SessionShell` / `SessionControls`: no top strip (the h1 stays, sr-only), and a compact control bar restyled from the wrapper (`CONTROL_BAR_COMPACT`; the vendored bar is untouched). Preview scene `session&embed=1`; tests in `session-shell.test.tsx` | `web/src/components/session/session-shell.tsx` / `session-room.tsx` | The voice half of `/s/[slug]?embed=1` (`components/session/embed/embed-session.tsx`) reuses `SessionExperience` unmodified inside a `h-dvh overflow-hidden` wrapper — it does **not** get UI_UX_SPEC §2.6's "no top strip, compact control bar": those live in `SessionShell`/`SessionRoom` (WP-8), which have no `embed` prop to ask for a trimmed chrome. Proposed: an optional `embed?: boolean` prop threaded from `SessionExperience` → `SessionRoom` → `SessionShell` that hides the top strip and compacts the control bar. Text-mode embed (`channel=text`) is unaffected — it never touches `SessionShell` at all (a purpose-built `TextChat`, `components/session/embed/text-chat.tsx`). | Out of this package's file ownership; a real UI change to WP-8's files. |
| V2-18-10 | Coordinator / V2-19 (decision needed) | `api/src/lkap_api/routers/connect.py` (or `text_sessions.py`) | The widget's iframe (`/s/[slug]?embed=1`) calls `connect`/`text-sessions` carrying the **platform's own** `Origin` (the page the iframe loaded, same-origin to the api's web app), not the third-party site that embedded it — so a widget session lands as `channel="web"`/`"text"` with `caller` empty, never the contract's `channel="widget"` with `caller.origin` set (CONTRACTS-V2 §1: "widget: caller JSON with origin"). Needs either a way for the embed page to learn its embedder's origin (the `postMessage` bridge, `components/session/embed/embed-bridge.ts`, could carry it from `widget.js`, which does know `window.location.origin` of the host page) and forward it as `participant_metadata` or a new `ConnectRequest` field, or a ruling that `channel="widget"` is Phase 2. | Out of scope for the acceptance criteria (CSP header + 403 + iframe + rewind), which say nothing about the `widget` channel value itself; flagging so `channel="widget"` doesn't stay silently unreachable. |
| V2-18-11 | V2-20 (live verification) | `agent/src/lkap_agent/text_mode.py` (module docstring) | Verified against `livekit-plugins-google`'s `RealtimeSession.update_chat_ctx`/`_sync_chat_ctx`: Gemini Live logs `"Gemini Live does not support removing messages"` and does not actually delete anything remotely — so `rewind`/an edit's truncation on a **half-cascade text session bound to Gemini Live** is best-effort: the browser's transcript and turn numbering are correct (both `session.history` and the local `Agent` object are truncated), but the remote model may still recall the dropped turns. No workaround exists short of not using Gemini Live there. Tracked as a live-verification finding (PLAN-V2 §6 stage L12c), not a bug to fix here. | livekit-agents 1.8.2, `livekit/plugins/google/realtime/realtime_api.py:698`. |
| V2-18-12 | — (informational, no action) | `web/src/components/console/agents/editor/sections/limits-section.tsx` | The card lists "an `allowed_origins` editor" in scope; it was already built (`OriginsEditor`, wired to `AgentLimits`/`allowed_origins` in the Limits section) before this package started, presumably by V2-13/WP-4's slot work. The "Add to website" dialog links to `?section=limits` rather than duplicating it. | No code needed. |
| V2-18-13 | V2-19 (Playwright infra) | `web/e2e/text-chat.spec.ts` (new file, `test.fixme`) | "Text chat round trip with the fake worker" needs a fake/mock LiveKit worker or server reachable from a real browser; `web/e2e/` has none today (only `login-console-smoke.spec.ts`, which never opens a room), and this package's hard rules forbid starting a real LiveKit worker. Proposed home: extend V2-19's `testing/lkap_testing` shared-fakes package (its card already covers "shared fakes … used by agent and packs tests") with a browser-facing fake, or stand up `livekit-server --dev` + a scripted worker double for e2e specifically. | Genuinely missing infrastructure, not a V2-18-sized addition. |

## Open — left by V2-19B (integration: backend, contracts, docs)

Numbered `V2-19B-n`. Everything V2-19B closed is marked **Closed by V2-19B** in its original row (#18, #50, #54, #56, #67, V2-14-1 in the V2-11 table [#72], V2-14-6 [#77], V2-16-2, V2-16-3 (partly), V2-16-4, V2-16-9) plus F-17 and F-18 below.

| # | Owner | File | Edit / hand-off | Why |
|---|---|---|---|---|
| V2-19B-1 | **Closed by V2-19C** (2026-09-23): every importer uses `BlockSpec` / `PanelLayout` from `@/contracts/lkap-contracts`; both aliases are deleted from `panels/composite/layout.ts` (the other listed files never used them) | `web/src/panels/blocks/{index.tsx,catalog.ts,types.ts,frame.tsx}`, `blocks/__fixtures__/index.ts`, `components/preview/{scenes,block-scenes}.tsx`, `console/agents/panel-section/{composer-model.ts,panel-composer.tsx}`, `console/sessions/session-panel-tab.tsx`, `session/session-room.tsx`, `lib/livekit.ts`, `panels/README.md` | Mechanical: import `BlockSpec` / `PanelLayout` from `@/contracts/lkap-contracts` instead of `BlockSpecV2` / `PanelLayoutV2`, then delete the two deprecated aliases in `panels/composite/layout.ts`. They are now plain aliases (no adapter logic), so nothing breaks meanwhile. | R-V2-15; V2-19B could edit only `layout.ts` and its test. |
| V2-19B-2 | **Closed by V2-19T** for `tools/builtin/__init__.py` (`BUILTIN_TOOL_NAMES` / `BLOCK_TOOL_NAMES` now imported from `lkap_contracts.tools`, re-exports kept). `tools/builtin/update_block.py::UPDATABLE_BLOCK_TYPES` stays local (not in V2-19T's grant; `test_tool_names_parity.py` pins it): see V2-19T-3 | `agent/src/lkap_agent/tools/builtin/__init__.py`, `tools/builtin/update_block.py` | Replace the local `BUILTIN_TOOL_NAMES` / `BLOCK_TOOL_NAMES` tuples (and `UPDATABLE_BLOCK_TYPES`) with imports from `lkap_contracts.tools` (keep the re-exports). `agent/tests/unit/test_tool_names_parity.py` then becomes trivially true and can stay. | V2-16-3 single source. |
| V2-19B-3 | **Closed by V2-19C** (2026-09-23): the web image builds from `web/` alone, so the JSON is not imported at runtime. `flow/tool-options.ts` lost its duplicate `BLOCK_TOOL_TYPES` (it reads `panels/blocks/catalog.ts`'s) and exports `VISION_TOOL_NAMES`; `tests/tool-names-parity.test.ts` pins `catalog.ts` (`BLOCK_TOOL_TYPES`, `UPDATABLE_BLOCK_TYPES`), `tool-options.ts` and `lib/constants.ts` (`BUILTIN_TOOLS` + `http_request`, `BLOCK_TOOLS`) to `contracts/generated/builtin_tools.json` | `web/src/components/console/flow/tool-options.ts`, `web/src/panels/blocks/catalog.ts` (`BLOCK_TOOL_TYPES`, `UPDATABLE_BLOCK_TYPES`) | Read the names from `contracts/generated/builtin_tools.json` (`builtin_tool_names`, `vision_tool_names`, `block_tool_names`, `block_tool_types`), or add a vitest that pins the hand-kept copies to it (the `block-config-parity.test.ts` pattern). | V2-16-3 single source. |
| V2-19B-4 | V2-19T (`api_models.py`) → then any api/web owner | `contracts/src/lkap_contracts/api_models.py::SessionDetailOut`, `api/src/lkap_api/routers/sessions.py`, `web/src/components/console/sessions/session-panel-tab.tsx` | #74 (R-V2-15): add `SessionDetailOut.panel: PanelLayout \| None = None`; `GET /v1/sessions/{id}` fills it with `panels.effective_layout` over the `agent_config_versions` row at `session.config_version` (fallback: current config); the tab prefers it over `storedPanelLayout`. Not started: `api_models.py` is V2-19T's this round. | Low priority per the ruling. |
| V2-19B-5 | **Closed by V2-19T** (#66): `NoopUiChannel` gained no-op `set_block`, `patch_block`, `request_form` (→ `None`) and `cite`; `test_main.py::test_noop_collaborators_satisfy_the_pack_protocols` asserts it covers every `packs.base.UiChannel` member | `agent/src/lkap_agent/main.py::NoopUiChannel` | #66, still open: add no-op `set_block`, `patch_block`, `request_form` (returns `None`) and `cite` so it conforms to `packs.base.UiChannel`. | `main.py` is V2-19T's this round. |
| V2-19B-6 | V2-00 / coordinator | `contracts/src/lkap_contracts/api_models.py` | #26, #39 and V2-14-6 stay open: moving the auth/team models and a `WebhookEndpointCreated{secret}` into contracts needs `api_models.py` (V2-19T's this round). | Ownership. |
| V2-19B-7 | Coordinator (informational) | `agent/pyproject.toml`, `packs/pyproject.toml`, their `uv.lock`, `agent/Dockerfile`, `.github/workflows/python.yml` | F-18: new package `testing/` (`lkap-testing`, src `lkap_testing`) is a dev-group path dependency of `agent` and `packs`; both `fake_ctx.py` files are now shims (the agent one keeps its `FakeRoom` + `structlog` defaults). `agent/Dockerfile`'s `uv sync` gained `--no-install-package lkap-testing` (the build context has no `../testing`; **unverified**, Docker not running). `python.yml` gains `testing` in the matrix and `testing/**` in the paths. | Files outside the V2-19 card's list, needed for the package to exist. |
| V2-19B-8 | Coordinator (informational) | `api/src/lkap_api/fleet/registry.py`, `routers/fleet_internal.py`, `api/tests/test_fleet.py` | F-17: the worker already reported `pack_ids` (`main.py` passes them to `WorkerRegistration`, no agent edit needed). The api now compares them with its own discovered packs and logs `worker_pack_mismatch {missing_on_worker, unknown_to_api}`; an empty report (supervisor pre-registration) is not compared. Log only; surfacing it in `FleetStatus`/`/v1/health` needs `api_models.py`. | Card F-17. |
| V2-19B-9 | Coordinator (informational) | `api/tests/conftest.py`, `api/tests/test_kb_seed.py` | api suite speed: the last run took 22 m 31 s, of which **1211 s** was `test_kb_seed.py::test_seed_config_populates_agent_config_kb_ids` loading the real fastembed model (a ~90 MB download over the phone hotspot, into a fresh tmp dir each run). The test now stubs `resolve_embedder` with `FakeEmbedder`, and a session-scoped autouse conftest fixture makes `FastEmbedEmbedder._get_model` raise, so no unit test can hit the network that way again. Full suite now **2 m 16 s** (845 passed). Nothing else is slow (next: 6 s). CI's 20-minute job timeout would have failed on the old behaviour. | Card item 8. |
| V2-19B-10 | V2-05 / coordinator (not done, deliberately) | `contracts/src/lkap_contracts/providers.py` | #61 (wire D-ID `catalog=`/`test="did_avatars"`) and V2-13-2 (`catalog_kind="avatars"` on bey/simli id fields) stay open. #61 would flip `api/tests/test_credentials.py::test_credential_test_reports_not_implemented_providers`, which uses `did-avatar` as its "no adapter" example (#63); V2-13-2 changes a field's type semantics the web's heuristic already covers. Both are one-line registry edits for whoever takes them. | Scope. |
| V2-19B-12 | **Closed by V2-19T**: both fixtures (and the third, `form()` helper) carry `telephony: {transfer_targets: []}`; web gate green | `web/tests/console-zod-resolver.test.ts` | Seen at V2-19B's web gate: `pnpm typecheck` (2 errors, lines 35 and 75) and 5 vitest cases fail because `agentConfigFormSchema` gained `telephony` (V2-19T, `lib/schemas.ts`) and the two form fixtures lack it. Add `telephony: {transfer_targets: []}` to both fixtures. The precedent is V2-13-0: the schema change carries its fixture fixups. | Not V2-19B's file or change. |
| V2-19B-11 | Coordinator (open decisions, untouched) | — | V2-18-10 (`channel="widget"` unreachable) and V2-18-13 (a browser-facing fake worker for Playwright) are not V2-19B's: the first needs a ruling, the second is web e2e infrastructure, and `testing/lkap_testing` is Python-only today. #35 (Hedra), #38 (settings fold) and #53 (drop `LIVEKIT_AGENT_NAME` from the worker env) are unchanged. | Triage. |

## Open — left by V2-19C (web integration and accessibility)

Numbered `V2-19C-n`. Closed by V2-19C, marked in their rows: the A11Y entry and B1 (Coordinator decisions), V2-13-3, V2-13-5, V2-13-6, V2-14-1 (V2-14 table), V2-14-5 (V2-11 table, R-V2-16), V2-18-9, V2-19B-1, V2-19B-3.

**Axe** (`scripts/ui-capture.mjs`, desktop + mobile, light + dark, every console route, the session routes and every preview scene): **before** (the pre-change `before-v2-19c` run, 234 shots) critical 0 / serious 12 / moderate 194 / minor 8 — `color-contrast` ×9, `document-title` ×2 and `html-has-lang` ×1 (capture timing, see below), `page-has-heading-one` ×112, `region` ×49, `landmark-unique` ×24, `heading-order` ×8, `landmark-one-main` ×1; plus, found after it on the newly captured `/login`: critical `aria-allowed-attr` ×2. **After** (`after-v2-19c`, 254 shots incl. `/login`, analytics, telephony, keys): **critical 0 / serious 0 / moderate 0 / minor 8**. The 8 minors are `image-redundant-alt` on the gallery block (a `figcaption` repeats the `alt`), left as is: the caption is the visible label and `panel-blocks.test.tsx` pins the alt. The three timing findings were axe running on a streamed page before its `<head>` settled; the capture script now waits for `document.title` and `<html lang>` before axe.

| # | Owner | File | Edit / hand-off | Why |
|---|---|---|---|---|
| V2-19C-1 | **Resolved by V2-19T** with V2-16-5 (the predicate is "`config.flow` has nodes", as the coordinator specified, rather than `mode === "flow"`) | `web/src/components/console/lib/schemas.ts` | V2-16-5, decided: allow an empty `instructions` when the form's mode is `flow` (R-V2-13: the global node alone may carry the prompt) — a `superRefine` on `agentEditorFormSchema` that keeps `min(1)` for prompt mode. The api already only warns. | V2-16-5 asked WP-12/V2-19 to decide; the file is V2-19T's. |
| V2-19C-2 | V2-19T (`telephony/**` this round) | `web/src/components/console/telephony/{calls,trunks,rules,numbers}-section.tsx` | Loading skeletons: wrap them in `SkeletonRows` / `LoadingRegion` from the new `components/shared/loading-state.tsx` (a `role="status"` region with a real sr-only label), like every other console list now. Error copy: "Couldn't load … — <api message>". Axe passes today; this is consistency. | V2-19C's consistency sweep; not V2-19C's files. |
| V2-19C-3 | WP-8 follow-up (not done) | `web/src/components/session/stage-view.tsx` | WP-10's request (A11Y entry): a `localPreview` / `posterUrl` escape so the preview route can show camera states without a `Room`. Not started. | Out of the accessibility scope; no finding depends on it. |
| V2-19C-4 | Coordinator (unchanged) | `web/e2e/text-chat.spec.ts` | Stays `test.fixme`: V2-18-13's browser-facing fake worker is still infrastructure nobody owns (V2-19B-11). | Not a small fake. |
| V2-19C-5 | — (informational) | `web/src/components/console/agents/editor/editor-shell.tsx`, `web/tests/console-editor-shell.test.tsx` | The editor's active-section region is now named "<Section> section" (e.g. "Providers section"): the section components reuse the bare label for their own `<section>`, which axe flagged as `landmark-unique`. Seven test queries updated to the new name. | Accessibility. |
| V2-19C-6 | — (informational) | `web/src/components/console/shell/breadcrumb-context.tsx` + four pages | Depth-2 pages (`agents/new`, `connections/new`, connection detail, knowledge-base detail) set the top bar's trail with `<ConsoleBreadcrumbs>` instead of a second breadcrumb inside `PageHeader`: two `nav "breadcrumb"` landmarks per page were `landmark-unique`. `PageHeader`'s `breadcrumbs` prop still exists (the preview's primitives scene uses it). | Accessibility; one trail per page. |
| V2-19C-7 | — (informational) | `web/src/app/globals.css` | Dark `--destructive` oklch(0.66 → 0.72, 0.19, 25): the destructive button's text on its own `destructive/20` tint over `--card` was 4.07:1, now 4.85:1 (4.6:1 over `--popover`); `destructive-foreground` on `destructive` rises to 7.3:1. `pnpm check:contrast` passes. | WP-0 token; serious `color-contrast`. |
| V2-19C-8 | V2-14 follow-up (unchanged) | `web/src/components/console/sessions/*` | V2-14-5 (server-side paging for `/console/sessions`) is still open; not touched. | Scope. |

## Open — left by V2-19T (telephony block of V2-19, rulings R-V2-20 … R-V2-25)

Numbered `V2-19T-n`. R-V2-20 … R-V2-25 are implemented inside the card's "Telephony grants"; the rows below are the edits outside them and the hand-offs.

| # | Owner | File | Edit / hand-off | Why |
|---|---|---|---|---|
| V2-19T-1 | — (applied, additive; V2-19B's file) | `contracts/src/lkap_contracts/export.py` | Added the promoted telephony models to `EXPORTED_MODELS` under the existing `# webhooks and telephony` comment (`TransferTarget`, `TelephonyConfig`, `Trunk*`, `DispatchRule*`, `PhoneNumber*`, `CallTransferIn`, `CallDtmfIn/Out`, `CallReportIn`, `InternalTransferIn/Out`) plus `TrunkPage` / `DispatchRulePage` / `PhoneNumberPage` in the page block, and one import line. Nothing else in the file changed; the file was re-read immediately before the edit. | R-V2-25's acceptance (`types.ts` deleted, `web/src/contracts` byte-identical) is unreachable without the entries. |
| V2-19T-2 | — (applied, import line only) | `api/src/lkap_api/routers/connect.py` | `live_session_count` moved to the new `lkap_api/limits.py`; `connect.py` re-imports it as `live_session_count as live_session_count` so V2-18's `routers/text_sessions.py` keeps importing it from there unchanged. | R-V2-23 (`telephony/calls.py` never imports a router). |
| V2-19T-3 | Next owner of `agent/src/lkap_agent/tools/builtin/{update_block,transfer_call}.py` | those two files | (a) `update_block.py::UPDATABLE_BLOCK_TYPES` → import `lkap_contracts.tools.UPDATABLE_BLOCK_TYPES` (typed `frozenset[BlockType]` locally; needs a cast). (b) `transfer_call.py`'s module docstring still says the allowlist is `pack_settings["transfer_targets"]`; it is `config.telephony.transfer_targets` now (the code path is already right: the tool receives the targets from `telephony.build_telephony_tools`). | Not in V2-19T's grant. |
| V2-19T-4 | Architect (arithmetic) | `api/src/lkap_api/telephony/calls.py::STUCK_DIAL_AFTER_S` | R-V2-24 writes "`MAX_RING_S + DIAL_MARGIN_S + 60` = 210 s", but with `MAX_RING_S=120`, `DIAL_MARGIN_S=15` (R-V2-20's own "135 = MAX_RING_S + DIAL_MARGIN_S") that is **195 s**. V2-19T implemented the formula (195 s); the acceptance cases (4-minute-old rows) pass either way. Change the constant if 210 was meant. | Ruling text vs formula. |
| V2-19T-5 | V2-21 (review) | `api/src/lkap_api/routers/calls.py` | (a) Audit: successful dials/transfers write `call.placed` / `call.transferred`; the worker route also writes `call.transfer_refused` / `call.transfer_failed`. A **refused public** `POST /v1/calls` or `/transfer` (422/429) writes no row, because the error rolls back the request transaction; recording refusals would need a second session. (b) The per-workspace call bucket is enforced even when `LKAP_RATE_LIMIT_ENABLED=false` (treated as a toll-fraud control, not a convenience limit). (c) The agent's `max_concurrent_sessions` cap on dial answers 429 `calls_busy` (not `agent_busy`), per the ruling's error list. Confirm or rule otherwise. | Policy choices the ruling left open. |
| V2-19T-6 | WP-3 / panel-section owner | `web/src/components/console/lib/schemas.ts` (`capabilitiesConfigSchema`) + the Panel & capabilities section | `capabilities.dtmf` is preserved on save (`buildAgentUpdate` merges stored capabilities) but no editor control sets it, so `send_dtmf` and keypad-to-model need `PUT /v1/agents/{id}` today (TELEPHONY-LIVE-TEST §2.4 says so). A "Keypad input (phone calls)" switch next to the other capabilities would close it. | Out of this round's scope. |
| V2-19T-7 | V2-19B / next `api_models.py` owner | `contracts/src/lkap_contracts/api_models.py` | V2-19B-4 (#74 `SessionDetailOut.panel`) and V2-19B-6 (#26/#39/V2-14-6 auth/team/webhook models) were **not** done: both are outside R-V2-20…25 and the file is free again after this round. | Hand-back. |
| V2-19T-8 | Coordinator (deploy note) | live DB `workspaces.settings` | After this lands, the live `default` workspace has `settings = {}`, so every outbound call and transfer is refused until an admin sets `settings.telephony.allowed_prefixes` (Console → Telephony → Outbound dialing policy). TELEPHONY-LIVE-TEST §2 step 0 covers it for V2-20. No agent in the live DB has transfer nodes or targets (checked read-only), so no saved agent starts failing validation. | R-V2-23 default deny. |
| V2-19T-9 | — (applied, one line each) | `docs/v2/CONTRACTS-V2.md` §4.3; `web/tests/console-editor-form-values.test.ts` | CONTRACTS §4.3's `ResolvedAgentConfig` snippet gained `variables: dict[str, Any] = {}` (the spec lagged R-V2-22; §4.6 already referenced it). The form-values test file gained a `config.telephony` describe block (R-V2-21's round-trip acceptance); precedent V2-13-0. | Spec consistency; acceptance test. |

## Open — left by V2-20 (live verification)

Evidence for each item is in `docs/v2/LIVE-RESULTS.md`.

| # | Issue | Repro | Suggested owner / fix |
|---|---|---|---|
| V2-20-1 | **Resolved by V2-20F** (2026-09-23) | `webhooks/events.py::KNOWN_EVENTS` advertised `session.started`, `session.qa_completed`, `call.started` and `call.ended`, and the console's picker offered them, but nothing called `webhooks.service.emit` for any of them. Only `SESSION_ENDED` (`routers/internal.py::put_summary`) and `RECORDING_READY` (`recordings/job.py`) were emitted. | api now emits `session.started` from `routers/connect.py::connect` (the common browser/widget/test path) and `routers/internal.py::start_session` (the worker-discovered-room path, e.g. inbound SIP), and `session.qa_completed` from `routers/internal.py::put_qa` (worker verdict) and `qa/scorer.py::score_session` (the api re-score job) — both `done` and `failed` outcomes, `skip` emits nothing. All four sites commit before calling `webhooks.emit` (ask #40's rule: it opens its own connection). `call.started`/`call.ended` were **removed from `KNOWN_EVENTS`** and the web picker instead of faked: every call-status transition goes through one `advance()` chokepoint in `api/src/lkap_api/telephony/calls.py`, which is V2-21's exclusive file this wave — see "Open — left by V2-20F" below for the one-line follow-up once it reopens. Tests: `api/tests/test_connect_limits.py::test_connect_emits_session_started` (+ the "no matching subscription" case), `test_qa.py` (3 new re-score cases), `test_qa_v2.py` (2 new worker-path cases). |
| V2-20-2 | **Resolved by V2-20F** (2026-09-23) | `connections/bundle.py::api_base_url()` built the worker callback URL as `http://127.0.0.1:{settings.port}`, and `settings.port` came from `PORT` (default 8080), not the port uvicorn actually bound — supervised subprocess workers could silently register against the wrong api on a host running more than one. | `api/src/lkap_api/settings.py` gained an explicit `LKAP_API_BASE_URL` field and a `worker_callback_base_url` property with precedence `LKAP_API_BASE_URL` (new, authoritative) → `LKAP_PUBLIC_BASE_URL` → the `PORT`-derived guess, plus `worker_callback_url_is_derived`. `connections/bundle.py` itself is V2-21's exclusive file this wave, so the override lives one layer up: `routers/internal.py::connection_worker_env` overwrites `env.env["LKAP_API_BASE_URL"]` with the new property and logs a WARNING (`worker_callback_url_derived_from_port`) whenever it's a guess; `main.py::_log_worker_callback_url` logs the same at api startup. The worker also now logs `api_base_url` at its own startup (`agent/src/lkap_agent/main.py::install_worker_registration`) — the "clear startup log line" from both ends. A one-line ask to fold this into `bundle.api_base_url()` directly is filed below for whoever next owns `connections/**`. Tests: `api/tests/test_settings.py` (3 precedence cases), `test_internal_connections.py` (2 cases), `test_main.py` (3 cases, calling the helper directly since the `app`/`client` test fixtures never run the real ASGI lifespan). |
| V2-20-3 | **Resolved by V2-20F** (2026-09-23) | When `recording/start` failed (422 `NoEgressStorageError`, no S3 storage), the worker recorded a `recording` session *event* with `status: failed`, but `sessions.recording_status` stayed `none` and no reason was stored anywhere queryable. | New nullable column `sessions.recording_error` (migration `v2_011_recording_error`; rehearsed up→down→up on a `.backup` scratch copy of the live DB — **not applied to `api/data/lkap.db`**, the coordinator applies it). `recordings/finalize.py::apply_egress_result` gained an `error` param (cleared on any non-`failed` status so a stale reason never lingers); the worker's `_Recording._start()` (`agent/src/lkap_agent/main.py`) now also posts `SessionRecordingIn(egress_id="", status="failed", error=...)` to the existing `POST /internal/v1/sessions/{id}/recording` fallback on a `RecordingUnavailableError`, mirroring the shutdown-time report path. Surfaced as `RecordingOut.error` on the session detail Recording tab (shows the reason instead of the generic empty state) and `SessionOut.recording_status` on the sessions list (a danger "Recording failed" chip next to the session's own status chip, `sessions-table.tsx::SessionStatusChips`). Tests: `api/tests/test_internal_connections.py` (2 cases), `agent/tests/unit/test_run_session_v2.py` (extended), `web/tests/console-sessions-v2-extension.test.tsx` (2 cases), `console-sessions-table.test.tsx` (2 cases). |
| V2-20-4 | **Resolved by V2-20F** (2026-09-23) | `contracts/pricing.py` had no rows for `livekit-inference-stt`, `livekit-inference-llm` or `livekit-inference-tts` — the default pipeline of every pack — so every default session's `cost_usd` was always `null`. | Added 14 `Price` entries, one per model the registry actually exposes for those three provider ids (`contracts/src/lkap_contracts/providers.py`'s `ModelSpec` lists), sourced from https://www.livekit.com/pricing/inference (fetched 2026-09-23) at its "Build" tier (the entry-level, no-volume-discount rate), converted from the page's own $/minute (STT) or $/million-tokens-or-characters (LLM/TTS) figures at import time via `Decimal` division so the source number stays visible in the diff. Left unpriced, per this module's own "never guess" rule: `openai/gpt-oss-120b` (the page lists two different rates depending on which backend LiveKit routes to — Baseten vs. Groq — and neither can be attributed to our registry id with confidence) and `inworld/inworld-tts-2` (the page has no matching "TTS 2" line item, only "Realtime TTS 1.5/2.0" variants). The console already never rendered `$0` for an unpriced line (`note="no price"` shown verbatim in `cost-tab.tsx`, `"—"` for a null total) — no UI change was needed for that half of the ask. Contracts regenerated; `web/src/contracts` byte-identical (the price *table* isn't part of the exported JSON Schema/TS surface, only the `Price` model's fields, which didn't change). Tests: `contracts/tests/test_v2.py` (16 new cases: one per priced model, plus the two deliberately-unpriced ones). |
| V2-20-5 | **Resolved by V2-20F** (2026-09-23) | Viewer UI gating. A `viewer` sees **New agent** (link) and **New connection** (link), can open `/console/agents/new`, and sees an enabled **Publish** control in the editor. The editor fires `POST /agents/{id}/validate` on load, which is 403 for a viewer (twice). The server refuses every write, so this is not a security hole, but it's noisy and confusing. | New shared helpers — `components/console/lib/roles.ts` (`roleAtLeast`, `useWriteAccess(min)`, reading the active workspace membership's role; `min` defaults to `"builder"`, the floor `auth/roles.py::ROUTE_POLICY` sets for agents/tools/knowledge-bases/flows/sessions, with `"admin"` passed explicitly wherever the table sets a stricter floor: connections, providers, credentials/keys, telephony trunks/numbers/rules, workspace api-keys), `components/shared/gated-button.tsx` (a `Button` that disables with a Radix tooltip explaining why — the first use of `Tooltip` outside the shell, given its own `TooltipProvider` so it works standalone), `components/shared/new-resource-button.tsx` and `require-write.tsx` (page-level gates for server-component pages, since a `next/link` `Link` can't be `disabled` the way a button can). Applied across agents (list + create flow + editor Save/Publish/Unpublish/Delete, and the on-load `validate` call now skips for non-writers), tools (list + per-row edit/delete, + the HTTP tool credential picker at the stricter `admin` floor per REVIEW-V2 R2-03/V2-21-2), knowledge (create/delete KB, upload/delete/retry documents — the drop-zone itself becomes non-interactive), connections (list + row menu), providers (enable switch + credential sheet), keys/credentials (add/rotate/rename/delete), telephony (numbers/trunks/rules add+delete; calls and the already-admin-gated dialing policy untouched), settings (api-keys tab; the whole webhooks tab, since `/v1/webhooks` needs `admin` even to read), and the flow builder's version-restore. Tests: `web/tests/console-roles.test.ts` (new, `roleAtLeast` ordering), `console-agents-list.test.tsx` (viewer sees a disabled button, not a link), `console-telephony.test.tsx` (builder vs. admin on trunks/numbers/rules), plus every pre-existing console component test that renders a gated control needed an `auth/me` fixture added to its fetch mock (none previously mocked it) — updated in place rather than skipped, so the suite still proves the *enabled* path for a privileged role, not just that nothing crashes. |

## Open — left by V2-20F (five V2-20 fixes: webhooks, worker callback url, recording failure, Inference pricing, viewer gating)

| # | Owner | File | Edit / hand-off | Why |
|---|---|---|---|---|
| 75 | **V2-22** (ruling R-V2-36; it owns `connections/**`) | `api/src/lkap_api/connections/bundle.py::api_base_url()` | Change `return f"http://127.0.0.1:{settings.port}"` to consult `settings.worker_callback_base_url` (new property, `api/src/lkap_api/settings.py`, V2-20-2) instead of re-deriving the same broken guess: `if settings.api_base_url: return settings.api_base_url.rstrip("/")`, then the existing `public_base_url` check, then the `PORT` fallback — i.e. the function becomes `return settings.worker_callback_base_url`. Once this lands, `routers/internal.py::connection_worker_env`'s override of `env.env["LKAP_API_BASE_URL"]` (added as a stopgap by V2-20F, since `bundle.py` was off-limits this wave) becomes a redundant no-op and can be deleted. | `bundle.py` is V2-21's exclusive file this wave (connections/**), so V2-20F could not make the real fix there. |
| 76 | **Coordinator: a separate follow-up, not V2-22** (ruling R-V2-36: a feature, not hardening) | `api/src/lkap_api/telephony/calls.py::advance()` | Emit `webhooks.CALL_STARTED` the first time `advance()` moves a call into `"answered"`, and `webhooks.CALL_ENDED` the first time it moves into `TERMINAL` — `advance()` already knows whether this call is a genuine forward transition (it no-ops otherwise) and is the single chokepoint every call-status change goes through (`run_dial`, `apply_report`, `hangup`, `transfer`, `sweep_stuck_calls`), so this is the one place to add both events without touching every call site. Re-add `CALL_STARTED`/`CALL_ENDED` to `webhooks/events.py::KNOWN_EVENTS` and `web/src/components/console/settings/api-types.ts::KNOWN_WEBHOOK_EVENTS`/`WEBHOOK_EVENT_LABEL` in the same change (both already have the string constants, just commented out of the advertised list). Mind ask #40's commit-then-emit rule: `advance()` runs inside callers' open transactions today, so the emit call needs to happen after their `db.commit()`, not inside `advance()` itself — passing a list of `(event_type, data)` tuples back out to each caller is probably cleanest. | V2-20-1: `call.started`/`call.ended` are named in `ARCHITECTURE-V2.md` §1 but have never had an emitter; V2-20F removed them from the advertised list rather than fake it, since `telephony/**` was off-limits this wave. |
| 77 | **Decided by ruling R-V2-36** (V2-21-1 and V2-21-3 → V2-22) | REVIEW-V2 / this file | V2-21's own review (below) left three items addressed to "V2-20F" (webhook double-delivery on `recording.ready`, HTTP-tool credential-picker role gating, a `connect` concurrency race) that were **not** part of this pass's five-item assignment and were left untouched to stay in scope — except the credential-picker gating, which overlapped V2-20-5's RBAC work and was folded in there (see `web/src/components/console/tools/http-tool-editor-dialog.tsx`). The other two (`V2-21-1`, `V2-21-3`) still need an owner. | Scope boundary, recorded so they aren't silently dropped. |

## Open — for V2-21 (security review), logged by the coordinator

| # | Source | Issue |
|---|---|---|
| S2 | V2-04 | Dev launch config (`.claude/launch.json`, outside the repo) passes `LIVEKIT_API_SECRET`, `LKAP_MASTER_KEY` and the service/admin tokens as `export` args on the command line, so any local user can read them with `ps`. Dev-only, but the RUNBOOK and dev tooling should document a way to put them in the launch config's environment, or an env file the user creates, instead of args. Also confirm prod never passes secrets as argv (the supervisor's docker backend already uses container env only). |
| S1 | V2-03 | `POST /v1/connections/test` and connection update dial any `ws(s)://` or `http(s)://` URL an admin enters: an SSRF surface. Private ranges can't simply be blocked, because live stage L14 targets a self-hosted `ws://localhost:7880`. Decide the policy (e.g. an allowlist setting, a private-range opt-in flag, or admin-only plus audit) and add tests. |

## Open — left by V2-21 (security review; punch list in `docs/v2/REVIEW-V2.md`)

| # | Owner | File | Edit / hand-off | Why |
|---|---|---|---|---|
| V2-21-1 | **V2-22** (ruling R-V2-36, 2026-09-24; V2-20F declined it in #77) | `recordings/finalize.py`, `routers/internal.py` (recording fallback), `recordings/job.py` | Schedule the finalise job only when `recording_status` changes to `ready`, or derive the outbound `event_id` from `(session_id, "recording.ready")`. Today both the `egress_ended` webhook and the worker fallback enqueue it, so consumers get two `recording.ready` deliveries with different `X-LKAP-Event-Id`s. | REVIEW-V2 R2-20. |
| V2-21-2 | **Tool editor part closed by V2-20F** (#77); the slot editor part → **V2-22** (ruling R-V2-33) | `web/src/components/console/tools/http-tool-editor-dialog.tsx` (done); `web/src/components/console/agents/providers-section/**` | Binding a credential to an HTTP tool now needs `admin` + `providers:write`, and only `http-tool-secret` credentials are accepted (REVIEW-V2 R2-03). Still open: builders can keep or clear provider endpoint fields (`base_url` …) in the slot editor but not set them, so gate those fields too. | UI polish; the server already refuses these. |
| V2-21-3 | **V2-22** (ruling R-V2-34) | `lkap_api/limits.py` (new `reserve_session_slot`), `routers/connect.py`, `routers/text_sessions.py`, `routers/calls.py`, `telephony/calls.py::prepare_outbound_call` | Make count-and-insert atomic per agent (and per workspace for the outbound cap): a per-process lock held through the route's commit plus `FOR UPDATE` on Postgres; RUNBOOK: one api process on SQLite. Measured: a burst of 50 against `max_concurrent_sessions=5` admitted 14. | REVIEW-V2 R2-15; PLAN-V2 R-V2-34. |
| S1 | — | — | **Closed by V2-21:** `lkap_api/net_guard.py`, with the dev allowlist `LKAP_NET_ALLOW_PRIVATE_HOSTS` (default loopback in dev, nothing in prod). Fable to ratify (REVIEW-V2 F-7). | |
| S2 | User (launch.json) | `.claude/launch.json` | **Repo side closed by V2-21:** `keys rotate` reads the env, `smoke_v2.sh` keeps secrets off curl argv, and both supervisor backends are env-only (tested). The launch.json change is proposed in REVIEW-V2 §7; the user applies it. | |

## Open — planning issues found by V2-00

(none currently open in this section — #8 closed below)

## Closed

| # | Resolution |
|---|---|
| 7 | Resolved by ruling **R-V2-1** (PLAN-V2 §8): the alias is `available and worker_image == "slim"`; `verification` gates nothing. V2-00 now sets `verification` honestly (the four live-verified providers) and `worker_image="slim"` on all 18 v1 entries, so V2-05's flip is a no-op for `status` and no consumer changes in waves 0-1. |
| 8 | Closed by V2-05: `GEMINI_LIVE_VOICES` is now the full 30-name `Voice` literal (`contracts/src/lkap_contracts/providers.py`), source-verified against `livekit.plugins.google.realtime` (`realtime/api_proto.py`) rather than copied from the catalog doc unread. |
| 9 | Resolved by ruling **R-V2-2**: V2-00's "owns and updates" wording is withdrawn. `api/config_service.py` and `api/packs.py` → V2-03; `web/src/components/console/registry/**` and `CAPABILITY_META` → V2-13. V2-00 touched none of them. |
| 10 | Ruling **R-V2-3b** applied by V2-00: `UiRequest` payload keys documented in `ui_protocol.py` (`open_dialog {dialog, params?}`, `focus {target}`, `request_video_source {source}`, `toast {message, tone?}`), and `agent/tests/unit/test_ui_channel.py` now calls `request_ui("open_dialog", {"dialog": "packet"})`. No other `{"id": ...}` call site exists under `agent/` or `packs/`. |

| # | Applied by | File | Edit | Why |
|---|---|---|---|---|
| 1 | V2-00 (granted by the coordinator, 2026-09-20) | `agent/src/lkap_agent/providers/factory.py` | Add `"vad"`, `"turn_detection"`, `"noise_cancellation"` to `SLOT_KINDS` (kind mapping only; construction stays V2-07's) | `ProviderSlot` gained the three slots (CONTRACTS-V2 §4.3); the tripwire `agent/tests/unit/test_factory.py::test_slot_kinds_covers_every_resolved_slot` fires as designed |
| 2 | V2-00 (granted by the coordinator, 2026-09-20) | `api/tests/test_agents.py::test_validate_endpoint_reports_warnings` | Expect `{"ok": True, "errors": [], "warnings": [], "issues": []}` | `ValidationResult` gained `issues[]` (CONTRACTS-V2 §"Conventions", UI_UX_SPEC §7.14) |
| 3 | V2-00 (granted by the coordinator, 2026-09-20) | `api/tests/test_credentials.py::test_credential_test_reports_not_implemented_providers` | Replaced the exact-JSON match with a subset check on `ok`/`message`, so later additive fields cannot break it again | `CredentialTestResult` gained `checked_at`/`catalog_preview` (CONTRACTS-V2 §3.4) |
| 4 | V2-00 (granted by the coordinator, 2026-09-20) | `packs/tests/insurance_claim/fixtures/insurance_ui_state_lkap_{blank,auto,flood}.json` | Regenerate: `UiState` now dumps `"v": 2` and `"blocks": {}` | `UiState` v2 (CONTRACTS-V2 §4.4) |
| 23 | Closed by V2-12 | `lkap_api.recordings` (new package) | `start_recording(db, session, connection, factory)` implemented per the fixed signature: resolves `RecordingConfig`/`ConnectionCapabilities.egress_enabled`/an S3-compatible `storage_configs` row (`recordings/storage.py`; a `local`-kind row 422s — Egress uploads from LiveKit's own service, never through this api process), starts a `RoomCompositeEgress` (OGG, audio-only) with the connection's LiveKit client, and stores `recording_object_key` **at start** (deterministic, prefix-aware so a later signed URL reconstructs the same S3 key). Finalisation (duration + `session_costs` egress line) is shared between the `egress_ended` webhook (a **second** handler registered on the same event name, `connections/webhooks.py` untouched) and the worker's `POST /internal/v1/sessions/{id}/recording` fallback (ask #49). | PLAN-V2 V2-12. |
| 41 | Closed by V2-12 | `lkap_api.costs` (new package), `jobs/kinds.RECORDING_FINALIZE` | Cost lines computed from `sessions.usage` by mapping each `ModelUsage` entry to the *pipeline slot* that produced it (`costs/mapping.py`) rather than the vendor plugin's own free-text `.provider` string (verified there is no stable mapping from one to the other). Only priced lines persist to `session_costs` (its `unit_price_usd`/`cost_usd` are `NOT NULL`, no `note` column); unpriced lines are recomputed live on every read with `note="no price"`, never stored, never `0`. `RECORDING_FINALIZE` added to `jobs/kinds.py` per this ask's own suggestion; its job row is inserted directly on the producer's open session (never via `JobsService.enqueue`, which would deadlock SQLite the same way ask #40 describes) and consumed by a `recordings.job` handler that calls `webhooks.emit(RECORDING_READY, ...)` on its own connection. Local-backend recording playback is moot in practice: Egress cannot write to `local` storage at all (see ask #23's closure), so that half of this ask's suggestion (a `/internal/v1/storage/local/{key}` route) was not built. | PLAN-V2 V2-12. |
| 40 | Partially closed by V2-12 | `api/src/lkap_api/routers/internal.py::put_summary` | The cost half: `costs.cost_session(db, session)` now runs before the commit, and `webhooks.emit(..., SESSION_ENDED, ...)` after it (commit-then-call, per this ask's own rule). The QA half is **superseded** by R-V2-5/R-V2-6 (the worker scores QA itself and posts `PUT .../qa`, added concurrently by the V2-08 follow-up) — V2-12 did not add a `qa.enqueue_for_session` call to `put_summary`. `POST /internal/v1/sessions/{id}/metrics` (latency) and `POST /internal/v1/sessions/{id}/recording` (worker-side Egress finalisation fallback) were also added here per the coordinator's direct ask #49 grant (V2-07 had landed calling both with no route to receive them). | Ask #49, PLAN-V2 V2-12. |
| 65 | Closed by V2-12 (R-V2-7) | `contracts/src/lkap_contracts/api_models.py` (`AgentPublicOut.panel`), `api/src/lkap_api/panels.py` (new), `routers/connect.py`, `routers/agents.py::to_public`, `routers/internal.py::_build_resolved` | `AgentPublicOut.panel: PanelLayout` added and regenerated (contracts/web types); `lkap_api.panels.effective_layout(agent, pack)` is the one function both `connect` and `/internal/v1/sessions/{id}/resolved` call — `config.panel` when it differs from `PanelLayout()`'s own default (i.e. was ever customized), else `pack.default_panel`, else the bare default. `to_public`'s `ui_panel_id` and `ConnectResponse.uiPanelId` now mirror `panel.panel_id` (e.g. `"composite"`) rather than the stored `agents.ui_panel_id` column (still `"generic"` for pack-seeded agents) — this is what the ruling means by "stays as a mirror … for one release". Fixed the two pre-existing tests this changed the response shape of (`test_agents.py::test_public_get_returns_the_public_projection`, `test_connect.py::test_connect_returns_token_source_compatible_fields`) and `contracts/tests/test_models.py`'s two hand-built `AgentPublicOut`/`ConnectResponse` fixtures, plus `api/tests/conftest.py`'s `GENERIC_MANIFEST` fake pack (it had no `default_panel`, unlike the real `packs.generic` manifest it stands in for — every `admin_client`-based test resolves through this fake). | R-V2-7 (PLAN-V2 §8, asks #65 origin), architect instruction 2026-09-23. |
| 66 | Closed by V2-12 (incident) | `api/src/lkap_api/sessions_sweep.py`, `api/tests/test_app_imports.py` (new) | A module-scope `from lkap_api.recordings.storage import resolve_egress_target` in `sessions_sweep.py` (`connections/webhooks.py` imports this module for `NEVER_STARTED`; `lkap_api.recordings`'s package `__init__` imports `connections.webhooks` back via `finalize.py`) crashed `import lkap_api.main` mid-initialisation and took the dev api down. Fixed by deferring that import inside `sweep_recording_retention` (call time, long after app startup). Added `test_app_imports.py::test_lkap_api_main_imports_cleanly_in_a_fresh_interpreter` (a subprocess cold-import) so a reintroduced cycle fails CI instead of only a live `--reload` worker. | Coordinator-reported outage, 2026-09-23. |
| 67 | Closed by V2-12 (WP-7 gap report) | `api/src/lkap_api/routers/sessions.py` | `GET /v1/sessions` now sets `channel`/`connection_id`/`cost_usd`/`disposition` on every row and accepts `channel`/`connection_id`/`from`/`to` filters (`limit` capped at 200) per CONTRACTS-V2 §3.4; `GET /v1/sessions/{id}` now fills `recording`/`cost`/`latency`/`qa`. Turn count: `PUT .../summary` folds `latency.turns` into `usage.turns` (`internal.py::_merge_turn_count`) since `AgentSessionUsage` carries no turn count of its own — the console's `usageTurns()` helper already looks for that key. | WP-7 gap report, 2026-09-23. |
