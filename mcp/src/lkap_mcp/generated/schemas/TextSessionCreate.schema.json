{
  "$id": "lkap-contracts/TextSessionCreate.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "``POST /v1/agents/{id_or_slug}/text-sessions`` (R-V5-10: a ``ConnectRequest`` plus the zone).",
  "properties": {
    "participant_identity": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Participant Identity"
    },
    "participant_metadata": {
      "additionalProperties": {
        "type": "string"
      },
      "default": {},
      "description": "Caller attributes (at most 2 KB). `timezone` = the browser's IANA timezone; keys starting with `lkap.` are reserved and dropped.",
      "title": "Participant Metadata",
      "type": "object"
    },
    "participant_name": {
      "default": "Guest",
      "title": "Participant Name",
      "type": "string"
    },
    "timezone": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "The browser's IANA timezone (`Intl.DateTimeFormat().resolvedOptions().timeZone`); an unknown name is ignored. Wins over `participant_metadata.timezone`.",
      "title": "Timezone"
    }
  },
  "title": "TextSessionCreate",
  "type": "object"
}
