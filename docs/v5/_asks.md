# LKAP v5 — cross-package asks

Format (v3/v4): **owner package** — file — the exact edit — who asked — status. The coordinator's decisions are binding; design questions go to Fable and land as `R-V5-n` in `PLAN-V5.md` §7. Never a key value, a hostname from the environment, a user name or a machine path.

## Coordinator decisions

- R-V5-1 (V5-08): barge-in cancels `request`-method requests only; `request_choice` added to `_REALTIME_SILENT_BUILTINS`; neutral late-submit wording. See `PLAN-V5.md` §7.

## Open

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 1 | V5-15, V5-19 | `agent/src/lkap_agent/platform_agent.py` (`_REALTIME_SILENT_BUILTINS`) | Add `request_consent` / `request_upload` when those tools land (R-V5-1 item 2) | Fable | Open — inherited |
| 2 | coordinator (api `provider_keys.py` owner) | `api/src/lkap_api/provider_keys.py` (`GET /v1/credentials`, update/test/delete) | Hide Composio connection rows (`tool-provider-account`) from the credentials list and refuse update/test/delete on them (today a Keys-page Delete removes one without the Composio delete or pausing its tools); flip the pinning test in `api/tests/test_tool_providers.py` | V5-18 | Open |
| 3 | coordinator (api `provider_keys.py` owner) | `api/src/lkap_api/provider_keys.py` (`update_credential`) | Clear the last-test fields when the secret changes, so Validate right after Rotate does not answer from the old key's 10-minute cache (all providers) | V5-18 | Open |
| 5 | V5-47 | `api` `tools` table, Composio adapter | The `tools.kind` check allows only `http`/`mcp`: the `provider` kind needs a migration; Composio marks `/api/v3.1/mcp/servers` deprecated in favour of Tool Router sessions — prefer `tool_router/session` or confirm; Tool Router allow/deny field shapes unverified | V5-18 | **Done** (V5-47, in-branch): migration `v5_010_tool_provider_kind`; both app server and tool finder use `tool_router/session` (the MCP server API is marked deprecated in the reference index); allow/deny shapes `{"enable": [...]}` / `{"disable": [...]}` verified — `docs/v5/_briefs/v5-47-live.md` |
| 6 | coordinator (from V5-03) | `web/src/panels/registry.ts` | `PanelAction` (and `PanelFormSubmitAction`, or a new `PanelBlockSubmitAction`) has no `"block_submit"` member, though `AgentAction.action` has carried it since V5-02 and `agentActionFor` already forwards any action it doesn't special-case unchanged. `web/src/panels/composite/use-block-request.ts` sends it today via `perform({action: "block_submit", ...} as unknown as PanelAction)` (documented at the cast) — correct at runtime, but the type gap should close. Add `PanelBlockSubmitAction {action: "block_submit"; payload: {block_id: string; values: Record<string, unknown>} | {block_id: string; cancelled: true}}` to the `PanelAction` union and drop the cast in `use-block-request.ts`. | V5-03 | **Done** (coordinator, 2026-09-25): applied; the cast in `use-block-request.ts` is gone |
| 7 | coordinator (from V5-03) | `web/src/panels/composite/index.tsx` (docblock) and `web/src/panels/README.md` | Both still describe the composite panel's `lkap.ui.request` handling as `form`, `show_block`, `focus`, `navigate` only; V5-03 added `request` alongside `form` in `composite/requests.ts` (one handler, two method names) but neither doc comment was touched (not exclusive files of this card). Update the method lists to include `request`. | V5-03 | **Done** (coordinator, 2026-09-25): both docs list `request` |
| 16 | coordinator (after V4-13 merges; filed as #6 by V5-22) | `web/src/components/console/tools/tool-row.tsx` and `tools-list.tsx`'s Kind column (`tool.kind === "http" ? "HTTP" : "MCP"`) | On `tool.kind === "provider"` (V5-47's `ProviderToolDefinition`), render an "App" `StatusChip` with the connected app's logo instead of the "HTTP"/"MCP" label — the logo comes from the toolkit behind `definition.connection_id`, not carried on the tool itself, so this needs a lookup (`GET /v1/tool-providers/composio/connections`) added alongside. V5-22 could not make this edit: `tool-row.tsx` was V4-13's file while V5-22 ran (docs/v5/COMPOSIO.md §6, D-V5-C13 says "the tools list shows an 'App' chip … this package edits `tool-row.tsx` only after V4-13 merges, else files an ask") | V5-22 | **Done** (V5-48): `tools-list.tsx`'s Kind column (the file this card owns) now renders an "App" `StatusChip` with a `VendorMark` keyed to the connected app's name, looked up via `useToolProviderConnections()` by `definition.connection_id`, falling back to the tool's own `toolkit` field when the connection is gone. Real vendor **logos** (not a monogram mark) were not added: no lookup in this codebase resolves a toolkit slug to its `ToolkitOut.logo` URL without fetching the toolkit list (`GET .../toolkits`, paged/searched — no by-slug batch route), and the console's established pattern for a compact list row without a fetched image is `VendorMark` (used for every other vendor identity in this list-page context: providers, registry, agents). `tool-row.tsx` (the agent editor's own tool row) was **not** touched: it isn't in this card's exclusive files, and its `requestSummary` already labels a provider tool ("App action · toolkit", ask #20) without a Kind column of its own. |
| 17 | coordinator (V4-15 owns `db/models.py`) | `api/src/lkap_api/db/models.py` (`Tool.__table_args__`) | `CheckConstraint("kind IN ('http','mcp','provider')", name="kind_valid")` — the model mirror of migration `v5_010_tool_provider_kind`; without it `create_all` test databases refuse `provider` rows | V5-47 | Applied in-branch (one line); keep it through the V4-15 merge |
| 18 | coordinator (V4-15 owns `api_models.py` additively) | `contracts/src/lkap_contracts/api_models.py` (`ToolCreate.kind`) | `Literal["http", "mcp", "provider"]`; without it `GET /v1/tools` fails once a `provider` row exists (`ToolOut(kind=...)`) | V5-47 | Applied in-branch in the first contracts commit (one line) |
| 19 | coordinator (V5-07/V5-06 own `main.py` hunks this wave) | `agent/src/lkap_agent/main.py` (`_assemble`, the `declarative_tools_builder` call) | `[t for t in resolved.tools if t.kind in ("http", "provider")]` so connected-app actions reach the builder (it dispatches `provider` to `tools/provider.py`) | V5-47 | Applied in-branch (one line + comment); rebase V5-07/V5-06 over it |
| 20 | coordinator (V4-13 / V5-48 own the console files) | `web/src/components/console/agents/defaults.ts` (`DEFAULT_TOOLS.apps`), `web/src/components/console/tools/tool-row.tsx` (`requestSummary`) | Keep `pnpm typecheck` green with the regenerated `.d.ts`: `DEFAULT_TOOLS` gains the `apps` default (`mode: "off"` …); `requestSummary` returns "App action · <app>" for a `provider` definition (it has no url) | V5-47 | Applied in-branch in the first contracts commit; V5-48 may restyle |
| 21 | coordinator (V4-15 owns `routers/agents.py`) | `api/src/lkap_api/routers/agents.py` (`restore_version`) | Also call `apps.on_save(db, ctx, row, config)` when a restored config is written, so restoring a version with another `tools.apps.mode` provisions or removes the tool finder / app server like a save does (today a restore leaves the Composio side as it was until the next save) | V5-47 | Open |
| 22 | coordinator (V4-15 owns `routers/internal.py`) | `api/src/lkap_api/routers/internal.py` (`_build_resolved`) | Only if the live check shows Composio needs `connected_account_id` when a subject has two accounts for one app: add each `provider` tool's `connection_id` to `wanted`, and set `ProviderToolDefinition.connected_account_id` from that connection bag's `connected_account_id` before `resolve_tool_definition` (the contract field exists; the worker already sends it when set) | V5-47 | Open — conditional on `v5-47-live.md` step 5 |
| 23 | V5-46 (owns `mcp/src/lkap_mcp/docs/guide.md`) | `mcp/src/lkap_mcp/docs/guide.md` | One line under Apps: "`agent_apps_mode` chooses how an agent uses connected apps (picked actions, an app server, or a tool finder); recipe `attach-app-actions`" (COMPOSIO.md §7 "`lkap_guide` gains one line") | V5-47 | Open |
| 24 | V4-15 (costs owner: `api/src/lkap_api/costs/**`, `routers/internal.py`, `jobs/rollup.py`) | the session cost lines | D-V5-C12 (V5-47 scope, deferred to the costs owner): count one `requests` usage line under provider `composio` per `provider` tool call and per tool-finder meta call — today the count is the session's `tool_call_ended` events whose `tool` is a `provider` row's name or one of the `composio_tool_finder` / `composio_app_server` row's `allowed_tools`; priced from the free tier's zero with the managed-app surcharge as `tier_note`, else shown as "no price" | V5-47 | Open |

## Open — left by V5-01 (asks renumbered #1–#8 → #8–#15 at merge; Knowledge ingest: chunking with locators, MarkItDown, model names to settings, per-KB dimension)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 8 | coordinator | `api/alembic/versions/v5_001_knowledge_p0.py` | **Chain note.** §0.3 chains every V5 migration after the costs package's `v4_003`, which has not landed. `v5_001` has `down_revision = "v4_002_provider_models"` (the current head). If a `v4_003` lands first, set `down_revision` to it and re-run `tests/test_migration_v5_001.py` (its `BEFORE` constant moves too) and the rehearsal. | V5-01 | Open (re-chain at merge if needed) |
| 9 | coordinator (no owner) | `api/alembic/env.py` | Added `include_object` so autogenerate and `alembic check` skip the objects `v5_001` creates outside the ORM: the SQLite FTS5 table `kb_chunks_fts` and its shadow tables, the Postgres generated column `kb_chunks.tsv` and its index `ix_kb_chunks_tsv`. Without it, `tests/test_migrations.py::test_upgrade_head_matches_the_models_exactly` fails. | V5-01 | **Applied by V5-01**, please confirm |
| 10 | coordinator (no owner) | `api/tests/test_health.py` | `test_migration_head_matches_the_script_directory` allows head prefixes `("v2_", "v3_", "v4_")`; added `"v5_"`. Any V5 migration needs it. | V5-01 | **Applied by V5-01**, please confirm |
| 11 | coordinator (no owner) | `mcp/uv.lock` | Re-locked: `mcp` dev-depends on `lkap-api` (editable), so the new `markitdown[docx,pptx,xlsx]==0.1.8` pin adds its transitive packages there too. `onnxruntime` forks on Windows only (magika's marker); Linux and macOS stay on 1.30.0. | V5-01 | **Applied by V5-01**, please confirm |
| 12 | contracts owner (V4-12 until it merges; then the next contracts package, e.g. V5-04 or V5-06) | `contracts/src/lkap_contracts/api_models.py` | Additive fields the api now returns through api-local subclasses in `routers/knowledge.py` (so the console and MCP types can see them): `KbOut += dimension: int \| None = None, embedder_model: str \| None = None, chunking: dict[str, int] \| None = None`; `KbDocumentOut += progress: float \| None = None`; new `KbEvalIn {question, expected_document_id?, expected_text?, tags[]}` (at least one expected), `KbEvalOut(KbEvalIn) {id, created_at}`, `KbEvalSetIn {items[] ≤ 500}`, `KbEvalSetOut {items, total}`; `KbReindexIn {document_ids?}`, `KbReindexOut {queued[], skipped[{document_id, filename, reason: source_not_stored\|ingest_in_progress}]}`. `KbHit.meta` is V5-04's (its card). When they land, `routers/knowledge.py` drops the local classes and imports these. | V5-01 | Open |
| 13 | V5-04 | `api/src/lkap_api/kb/search.py`, `routers/knowledge.py` | Facts V5-04 builds on: (a) the lexical index exists from `v5_001`: SQLite `kb_chunks_fts(chunk_id, kb_id UNINDEXED, text)` with `porter unicode61 remove_diacritics 2` — query `WHERE kb_chunks_fts MATCH 'text:(...)'` (always scope the column: `chunk_id` is indexed too, for the delete trigger) and filter `kb_id`; Postgres `kb_chunks.tsv` = `to_tsvector('english', text)` — query `plainto_tsquery('english', :q)`. The test suite builds its schema with `create_all`, so lexical tests must create the FTS table (reuse the migration's `_SQLITE_FTS_UP`). (b) The embedder check is in the router (`check_kb_embedder` → 422); the card's "skip a mismatched KB with a warning" replaces it inside `KnowledgeService`. (c) `KbChunk.meta` carries `heading_path`, `page`, `char_start`, `char_end` for chunks ingested or re-indexed after V5-01; older chunks carry `filename` only. (d) `LKAP_RERANK_MODEL` is `Settings.rerank_model`. | V5-01 | FYI |
| 14 | V5-05 | `api/src/lkap_api/kb/evals.py` | `kb_evals` rows: `id, kb_id, question, expected_document_id (no FK), expected_text, tags (JSON list), ordinal (the PUT order), created_at`; read them with `ORDER BY ordinal`. An `expected_document_id` whose document was deleted after the PUT is the "skipped" case. | V5-01 | FYI |
| 15 | V5-13 (re-embedding) | `api/src/lkap_api/routers/knowledge.py` | `POST /v1/knowledge-bases/{id}/reindex` (V5-01) re-extracts and re-chunks stored documents through the existing `KB_INGEST` job with the **current** embedder, and is refused per document when the KB's recorded embedder differs. Documents seeded from packs/templates were never written to storage and are skipped (`source_not_stored`); the Demo agents' seeded KBs therefore keep their pre-V5-01 chunks until V5-13's `kb_reindex` (or a re-seed) can read the seed files. | V5-01 | FYI |

## Open — left by V5-04 (knowledge search: fan-out, hybrid, rerank, score floor, single writer)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 25 | coordinator (no owner; `jobs/arq_worker.py`) | `api/src/lkap_api/jobs/arq_worker.py` (`_startup`) | **Pre-existing, blocks D-V5-12 in prod.** The `arq` worker imports no handler module, so its registry holds only `usage_daily_rollup` (verified: `import lkap_api.jobs.arq_worker; registered_kinds()` → `['usage_daily_rollup']`). With `LKAP_JOBS_BACKEND=arq` every `kb_ingest`, `kb_delete`, `webhook_delivery`, `qa_scoring`, `recording_finalize`, sweep and call-event job is marked `failed` "no handler registered for kind". Suggested: import the handler modules at the top of `arq_worker.py` (`kb.ingest`, `kb.jobs`, `webhooks.delivery`, `qa.job`, `recordings.job`, `sessions_sweep`, `telephony.calls`, `fleet.sweep`); plus a test that `registered_kinds()` after importing `arq_worker` contains every kind in `jobs/kinds.py` that has a handler. Until this lands, "only the jobs process writes LanceDB" holds in dev (`inline`) but prod ingest and delete do not run at all. | V5-04 | **Done** (integ-db: `jobs/handlers.py::load_all_handlers` + the guard test) |
| 26 | coordinator (Fable; card wording) | `docs/v5/PLAN-V5.md` (V5-04 card, Scope "Single writer") | **Deviation, please ratify.** The card says the delete routes "mark the row deleting and enqueue `KB_DELETE`". Neither table can say "deleting" without a migration (`kb_documents.status` has `CHECK IN ('pending','ready','failed')`, `knowledge_bases` has no status) in `db/models.py`, which V5-04 does not own, and §0.3 has no V5-04 migration. Shipped instead: the route deletes the SQL rows (cascade: chunks, evals; the `v5_001` triggers drop the lexical rows), commits, then enqueues `kb_delete {kb_id, document_id\|null}`; the handler (`kb/jobs.py`) deletes any chunk row an in-flight ingest left, then the vectors, then `optimize`. The SQL rows are the source of truth and every search joins its hits to `kb_chunks`, so orphan vectors are never returned while the job is pending; the document disappears from listings at once. Same effect as the card, no schema change. Also: the card's `routers/internal.py` clause was moot — the `kb/search` handler is `internal_search_kb` in `routers/knowledge.py`; `internal.py` is untouched. | V5-04 | **Ratified** (coordinator): immediate row delete + `kb_delete` vector job is equivalent to a "deleting" state without a migration |
| 27 | contracts owner (next contracts package, e.g. V5-06; with #12) | `contracts/src/lkap_contracts/api_models.py` | Additive fields V5-04 serves through api-local subclasses (`kb/service.py`: `KnowledgeHit`, `KnowledgeSearchWarning`, `KnowledgeSearchResponse`; `routers/knowledge.py`: `KnowledgeSearchIn`, `InternalKnowledgeSearchIn`): `KbHit += kb_id: str, meta: dict[str, Any] = {}, vector_score: float \| None, lexical_rank: int \| None, fused_score: float \| None, rerank_score: float \| None, score_source: Literal["vector","fused","rerank"] = "vector"`; `KbSearchRequest` and `InternalKbSearchRequest += mode: Literal["vector","hybrid"] = "vector", rerank: Literal["none","local"] = "none", min_score: float \| None (ge 0, le 1)`; `KbSearchResponse += mode, rerank, min_score, dropped: int = 0, warnings: list[KbSearchWarning] = [], timings_ms: dict[str, float] = {}`; new `KbSearchWarning {code: Literal["kb_not_found","kb_embedder_mismatch","kb_timeout","kb_error","lexical_unavailable","rerank_failed"], message, kb_id?}`. V5-06's `config_client.kb_search` needs the request fields from contracts. When they land, the api drops its local classes and imports these. | V5-04 | Open |
| 28 | coordinator | `api/tests/test_kb_knowledge_p0.py` (`test_search_on_a_kb_built_by_another_embedder_is_a_422_naming_it`) | **Touched outside the card's exclusive files; please ratify.** V5-01's test pinned the 422 on both search routes; #13 (b) and the card move the worker route to "skip with a warning". The admin half is unchanged (the single-KB test search still answers 422 `kb_embedder_mismatch`); the internal half now asserts 200, no hits, and one `kb_embedder_mismatch` warning naming the KB. The gate would be red without it. | V5-04 | **Ratified** (coordinator): the worker-route half expects skip-with-warning (V5-04 behaviour) |
| 29 | V5-13 (closes the two-writer risk) or the `routers/agents.py` owner | `api/src/lkap_api/routers/agents.py` (`_seed_config` → `import_pack_kb_seeds` / `import_kb_seeds`) | Creating an agent from a starter with knowledge seeds still ingests the seed files into LanceDB **in the api process** (`kb/seed.py` → `ingest_into_session`), the one remaining api-side LanceDB writer after V5-04. Options: enqueue `kb_ingest` per seed file (the seed bytes would need to go to storage first, which also fixes #15's `source_not_stored`), or accept it until pgvector (V5-13) makes the question moot. The prod compose comment on `api-data` names this exception. | V5-04 | **Done by V4-18** (R-V4-65): seeds are staged as `pending` documents with their bytes in storage, and one `kb_ingest` job per file is enqueued after the agent row commits; `kb/seed.py` no longer calls `ingest_into_session`. Seeds staged from now on also have a stored source for `reindex` (#15). The compose comment follow-up is v4 ask #182 |
| 30 | V5-06, V5-05, V5-08 | `api/src/lkap_api/kb/service.py` | FYI. (a) `KnowledgeService(session, store=, embedder=, reranker=).search(kb_ids, query, k, *, min_score, rerank, mode, workspace_id=None)` returns `KnowledgeSearchResponse`; the query-embedding LRU (`kb/search.py::QUERY_CACHE`, 512) is process-wide and keyed by embedder model. (b) `score` is the deciding stage on [0, 1]: cosine (vector), RRF / its maximum (hybrid), sigmoid of the cross-encoder logit (rerank), so V5-06's `min_score ∈ [0,1]` validator matches. The fused score is rank-derived (the best hit of any query is near 1.0), so in `hybrid` mode without rerank a floor only trims the tail; it judges relevance only in `vector` mode or with `rerank="local"` — decide that before defaulting agents to `mode="hybrid"` with a floor. (c) Rerank cost on 256-token chunks: p50 95 ms for 10 passages, 193 ms for 20 (`_briefs/v5-04-live.md`); the pool is `RERANK_POOL = 20` — if V5-06 reranks inside a voice turn it should ask for a `rerank_pool` option (10). (d) Each hit carries `meta` (locators) and `kb_id` for V5-08's `KbCitation`. (e) Lexical queries OR the query's letter/digit tokens (FTS5 `bm25`, Postgres `to_tsquery('english', 'a \| b')` + `ts_rank_cd`); the Postgres branch is exercised only by the CI `test-postgres` job (no Docker locally). | V5-04 | FYI |

## Open — left by V5-07 (asks renumbered #25–#30 → #31–#36 at merge; conversation tuning: typed turn handling, presets, detector, telephony noise cancellation)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 31 | coordinator (agent dependency owner; V4-14 owned `agent/pyproject.toml`, `agent/uv.lock`, `agent/requirements/**`) | `agent/pyproject.toml`, `agent/uv.lock`, `agent/requirements/full.txt`; then `contracts/src/lkap_contracts/providers.py` (`legacy-noise-cancellation.availability`) and `agent/tests/fixtures/plugin_signatures.json` | **Phone noise cancellation cannot run yet: neither Cloud filter is in the worker image.** V5-07 read both packages (not installed; read from PyPI wheels in a scratch venv): `livekit-plugins-noise-cancellation==0.3.2` (requires `livekit>=0.21.3`; `NC()`, `BVC()`, `BVCTelephony()` return `rtc.NoiseCancellationOptions`) and `livekit-plugins-krisp==0.4.2` (requires `livekit-agents>=1.8.2` and a native `livekit-plugins-krisp-internal==0.2.0` wheel; `voice_isolation_telephony(*, auth_provider, noise_suppression_level)`). The registry now names both (`python_class`/`telephony_variant`) but keeps them `deferred`. Suggested: add `livekit-plugins-noise-cancellation==0.3.2` to the full image (it needs no key; billing goes through the Cloud project), import-check it on linux x86_64/aarch64, flip `legacy-noise-cancellation` to `available`, regenerate the signature snapshot. The worker then turns it on for `telephony` phone calls on Cloud connections (D-V5-30) with no further code. Until then `agent_validate` warns "tunes turn-taking only". | V5-07 | **Ruled: R-V5-3** — kept deferred; the pin and the BVCTelephony flip move to V5-32 |
| 32 | coordinator (ratify; V5-07 edited beyond "the NC entries") | `contracts/src/lkap_contracts/providers.py` (`ProviderSpec`, `_full`) | Two additive `ProviderSpec` fields the card's "`telephony_variant` field and a price note" need: `telephony_variant: str \| None = None` and `price_note: str \| None = None` (both default unset; only the two Cloud NC entries set them), plus the two matching `_full(...)` kwargs. The generated `ProviderSpec`/`ProviderOut` schemas and `.d.ts` carry them. `legacy-noise-cancellation` is relabelled "Noise Cancellation (LiveKit Cloud)" with its verified class path (`livekit.plugins.noise_cancellation.BVC`) replacing `UNVERIFIED`. | V5-07 | **Ratified** (coordinator, V5-07 merge) |
| 33 | coordinator (ratify; V5-07 owns only "the two validators" in `config_service.py`) | `api/src/lkap_api/config_service.py` (`validate`, `knowledge_auto_inject_issues`) | Two edits the type change forces: (a) `validate()` iterates `turn_handling_dict(pipeline.turn_handling)` (the field is now `TurnHandlingOptions \| dict`) and adds one warning at `pipeline.turn_handling.<key path>` when a saved value does not match the typed model (the contract keeps such a dict for compatibility, so the save still succeeds); (b) the auto-inject tip reads `preemptive_generation` from the preset-expanded dict (`resolve_turn_handling`), so `snappy` (which enables it) gets the "discards the preemptive reply" tip. | V5-07 | **Ratified** (coordinator, V5-07 merge) |
| 34 | coordinator (ratify; V5-11 owns the console defaults next) | `web/src/components/console/agents/defaults.ts` (`DEFAULT_VOICE`) | `ambient_sound: "none"`, so `pnpm typecheck` stays green with the regenerated `.d.ts` (`Required<VoiceConfig>`), the same pattern as #20. V5-11 adds the control. | V5-07 | **Ratified** (coordinator, V5-07 merge) |
| 35 | doc owner (`docs/CONTRACTS.md`) | `docs/CONTRACTS.md` (the `PipelineConfig` / `VoiceConfig` listing, ~l.469) | `turn_handling` is now `TurnHandlingOptions \| dict` (typed mirror of the 1.8.3 TypedDict, extra keys pass through, only set keys stored); add `conversation_preset: Literal["patient","balanced","snappy","telephony","custom"] = "custom"` (expanded by `turn_handling.resolve_turn_handling` at session build, never stored expanded), `turn_detector: TurnDetectorSettings \| None = None` (`{mode: hosted\|local, unlikely_threshold: 0..1}`) and `VoiceConfig.ambient_sound: str = "none"` (a `BuiltinAudioClip` name lower-cased, or `asset:<id>`, which the worker does not play yet); `ProviderSpec.telephony_variant` / `price_note`. | V5-07 | Open |
| 36 | V5-11 (console Conversation section) | `web/src/components/console/agents/editor/sections/conversation-section.tsx` | Facts to build on: the pinned preset values are `lkap_contracts.turn_handling.CONVERSATION_PRESETS` (balanced = SDK defaults: wait 0.5–3.0 s, interrupt after 0.5 s of speech; patient 1.0–5.0 s; snappy 0.3–2.0 s plus early replies; telephony 0.5–4.0 s and 0.8 s to interrupt); a named preset replaces only `endpointing`, `interruption` and `preemptive_generation` and keeps other stored keys (`user_turn_limit`); `voice.allow_interruptions` still decides `interruption.enabled`. The NC price line is `ProviderSpec.price_note` of `legacy-noise-cancellation`. Uploaded ambient clips (`asset:<id>`) validate but are not played by the worker yet. | V5-07 | FYI |

## Open — left by V5-08 (asks renumbered #25–#32 → #37–#44 at merge; block quartet and citation preview; numbers provisional, the coordinator renumbers)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 37 | coordinator (ratify) → V5-12 | `web/src/panels/blocks/index.tsx` (`BLOCK_COMPONENTS`), `web/src/components/console/agents/panel-section/panel-composer.tsx` (`BLOCK_ICONS`), `web/src/components/console/lib/schemas.ts` (`BLOCK_TYPE_VALUES`), `web/src/components/console/lib/constants.ts` (`BLOCK_TOOLS`), `web/src/components/console/agents/panel-section/composer-model.ts` (`TOOL_NEEDS`), `web/src/panels/blocks/__fixtures__/{choices,details,markdown,steps}.json` + `index.ts` + `layout.json`, `web/tests/panel-block-fixtures.test.ts` (`SCHEMA_FILE`), `web/tests/preview-scenes.test.tsx` (25 → 33 combos) | **Applied in-branch in V5-08's contracts commit; keep through the merge.** The §0.1 "new block types" rule names only `catalog.ts`, but `BlockType` and `BlockToolName` key exhaustive `Record`s and a zod enum in these files, and `tool-names-parity.test.ts` pins `BLOCK_TOOLS` to `builtin_tools.json`, so `pnpm typecheck`/`pnpm test` were red without them. Each edit is the minimum: a "not shown here yet" placeholder renderer for the four types, one icon each, the enum values, five toggle rows and five "add a … block first" hints, state fixtures that pass the schema check (V5-12 extends them), the scene count. `catalog.ts` also gained a `list` config-field kind for `details.fields` / `steps.steps` (the composer shows only the label until V5-12 adds an editor). Suggested: amend PLAN-V5 §0.1 to list these files for the next contracts package that adds a `BlockType` (V5-15 `consent`, V5-19 `upload`, V5-32 `handoff`, V5-43 `link`/`slots`/`cards`). | V5-08 | **Ratified** (coordinator, V5-08 merge) |
| 38 | coordinator (ratify) | `agent/src/lkap_agent/tools/builtin/update_block.py` (`UPDATABLE_BLOCK_TYPES`), `agent/src/lkap_agent/ui/channel.py`, `agent/src/lkap_agent/flow/runtime.py` (import line), `agent/src/lkap_agent/platform_agent.py` | **Touched beyond the card's literal list; please ratify.** (a) `update_block.py` keeps the worker's copy of `UPDATABLE_BLOCK_TYPES`, pinned to the contract by `test_tool_names_parity.py`: += `details`, `markdown`, `steps`. (b) `ui/channel.py` is under `ui/**`, which §2's wave text gives V5-08, though the card lists only `ui/blocks.py`: `cite` carries the locators, a public `submit_block(block_id, values)` (for `resolve_choice`), a generic submit writes answer keys that name state fields (`selected`), and `block_action {name: "open_citation"}` on a `kb_citations` block is handled there before the pack callback. (c) `flow/runtime.py`: the one `publish_progress` hunk plus its import line. (d) `platform_agent.py`: there was no `user_state` handler in this file (the only one is `main.py`'s idle timer), so hunk 1 is a new synchronous `PlatformAgent.on_user_state_changed` that `_init_blocks` registers on `ctx.session` once per session (guarded by a `userdata` key, so flow nodes do not register it twice); `main.py` is untouched. Hunk 2 (`_REALTIME_SILENT_BUILTINS`) and the neutral late-submit wording are as R-V5-1 says. | V5-08 | **Ratified** (coordinator, V5-08 merge) |
| 39 | V5-19 (session assets) + V5-12 (renderer) | `agent/src/lkap_agent/ui/blocks.py::open_citation`; the `kb_citations` renderer | The card's fallback ("until V5-19 the handler opens the console's document URL via `navigate`") cannot be built: there is no console or api URL that shows one KB document, the worker knows no public web URL (only `api_base_url`), and `navigate` accepts absolute http(s) only. So `open_citation` opens the page in a `document` block **when the session holds the document as an asset whose `AssetRef.meta.document_id` is the citation's `document_id`**, and otherwise answers `{opened: false, reason: "no_source" \| "no_document_block" \| "unknown_citation", citation}`. V5-19: push a cited KB document through the session-asset path with `meta={"document_id": <kb document id>}` and the handler needs no change. V5-12: send `block_action {name: "open_citation", data: {chunk_id}}` on tap and, on `opened: false`, show the returned passage (filename, page, heading path, text) in a dialog. | V5-08 | **Ruled: R-V5-5** — open from a session asset with `meta.document_id`, else return the citation; V5-19 adds `assets/from-document` |
| 40 | coordinator (ruling) | `api/src/lkap_api/config_service.py` (`choices_on_phone_issues`) | "Warn when a `choices` block exists on a `sip_*`-only agent": validators see only `AgentConfig`, which has no channel list (phone numbers and dispatch rules live in their own tables). Shipped heuristic: an agent counts as set up for phone calls when `capabilities.dtmf` is on or `telephony.transfer_targets` is non-empty; then each `choices` block gets a warning at `panel.blocks[i]`. If a stricter rule is wanted, add the agent's phone-number / dispatch-rule binding to `ValidationContext` (filled in `validation_context_for`) and warn only when the agent has one. | V5-08 | **Ruled: R-V5-2** — heuristic accepted as a "Tip:" (web-only choices; spoken fallback on phone) |
| 41 | V5-06 / V5-04 | `agent/src/lkap_agent/ui/channel.py::cite` | FYI for the locator handoff (V5-06 card): `cite` maps `KbHit.document_id` and, via `getattr(hit, "meta", None)`, `page`, `heading_path`, `char_start`, `char_end` onto `KbCitation`. Once V5-04 adds `KbHit.meta`, `PlatformAgent._cite` and `search_knowledge` need no change; unset locators stay off the wire. | V5-08 | FYI |
| 42 | coordinator (card wording) | `docs/v5/PLAN-V5.md` (V5-08 card) | Stale paths in the card: `docs/CONTRACTS.md §4` → the block contract lives in `docs/v2/CONTRACTS-V2.md §4.4` (V5-02 edited it there too; V5-08 did the same); `mcp/src/lkap_mcp/docs/concepts/panels.md` → `panels-and-blocks.md`. | V5-08 | **Done** (Fable, R-V5-7 pass): V5-08 and V5-19 card paths fixed |
| 43 | V5-12 | `web/src/panels/blocks/catalog.ts` (`BLOCK_TOOL_TYPES`), `composer-model.ts::blockToolStatus`, `web/src/components/console/flow/tool-options.ts` | `BLOCK_TOOL_TYPES["set_steps"] = {"steps"}` is coarse on purpose (the contract keeps one type set per tool); the worker registers `set_steps` only for a `steps` block whose `config.source` is not `"flow"`. The composer's block-tool switch and the flow node tool picker should apply the same rule, so a panel with only a flow-driven steps block does not offer `set_steps`. Also for the `choices` renderer: a `block_submit` whose ids are not options of the block is not stored, so the block can read `status: "submitted"` with `selected: []` (the tool reports "did not pick"); render that as "no answer". | V5-08 | Open |
| 44 | next owner of `packs/src/packs/base.py` (+ `main.py` `NoopUiChannel`) | `packs.base.UiChannel` | Optional: add `submit_block(block_id, values) -> bool` to the protocol and the no-op channel. `resolve_choice` reads it with `getattr` and falls back to patching the block, so nothing breaks without it. | V5-08 | Open |

## Open — left by V5-48 (asks renumbered #25–#27 → #45–#47 at merge; console: Connected apps card on agents)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 45 | coordinator (V4-13 owns `schemas.ts` this wave) | `web/src/components/console/lib/schemas.ts` (`toolsConfigSchema`) | **Applied in-branch, needs ratification** (same pattern as ask #20). `toolsConfigSchema` had no `apps` field, and `z.object` strips unknown keys on every `zodResolver` parse (the file's own header warning) — the Connected apps card's mode picker would post `config.tools.apps.mode` into the form, `zodResolver` would silently drop it before `onSubmit`, and nothing would ever save. Added `apps: z.custom<AppsMode>().optional()` (mirrors `builtin_execution`'s existing passthrough for the same "no JSON-Schema-to-zod step" reason); `.optional()` rather than required, so the several existing test fixtures across the suite that build a `tools` value by hand (predating V5-47) don't all need `apps` added too — `tests/console-zod-resolver.test.ts` failed typecheck without it. `DEFAULT_TOOLS.apps` (`agents/defaults.ts`) already supplies a concrete value in the real editor. | V5-48 | **Ratified** (coordinator, V5-48 merge) |
| 46 | coordinator → `tool-row.tsx` owner | `web/src/components/console/tools/tool-row.tsx` (the Edit button: `tool.definition.kind === "http" ? HttpToolEditorDialog : McpToolEditorDialog`) | **Found while wiring the Kind column's App chip (ask #16), not fixed here** (`tool-row.tsx` isn't in V5-48's exclusive files). A `provider`-kind tool row (a materialised connected-app action) falls into the `else` branch and opens `McpToolEditorDialog` on Edit — wrong dialog for a `ProviderToolDefinition` (no `url`/`headers` to speak of; it would likely error or silently do nothing useful). `tools-list.tsx`'s own `ToolActions` has the same branch and the same gap. Composio-origin **MCP server/router** rows are now read-only (V5-48, no Edit button at all), but individual `provider` action rows are not — they still show a (broken) Edit button. Needs either a dedicated provider-tool editor (rename/description/"Refresh schema" per D-V5-C8) or, until one exists, hiding Edit for `provider` rows the same way origin-tagged MCP rows are hidden. | V5-48 | **Ruled** R-V5-8: Edit hidden on `provider` rows in-branch now; the editor is V5-50 (Sonnet) Interim applied in integ-db: Edit hidden on provider rows. |
| 47 | coordinator (Fable ruling) | `web/src/components/console/agents/tabs/connected-apps-card.tsx` (`seedDestructive`) | **A design gap, not a bug fix candidate without a ruling.** D-V5-C7 says a destructive action's checkbox in "Actions the agent may take" starts unchecked; the only field is `AppsMode.denied_actions` (a deny-list — presence = blocked), so "starts unchecked" is implemented by adding a newly-seen destructive action's slug to `denied_actions` the first time the card sees it each mount. There is no persisted "the builder explicitly reviewed and re-allowed this one" marker distinct from "never reviewed", so re-opening an already-configured agent that previously had a destructive action explicitly un-denied (allowed) re-seeds it back into `denied_actions` on every mount — quietly (`shouldDirty: false`, so opening the tab alone shows no unsaved-changes prompt), but a save made for an unrelated reason while that state is loaded would persist the re-denial. Needs a ruling on where "reviewed" lives (a separate `reviewed_actions` field on `AppsMode`? seed server-side at materialisation instead of client-side on every render?) before a real fix. | V5-48 | **Ruled** R-V5-9: `AppsMode.reviewed_actions` + server-side `effective_denied_actions` (V5-49, Opus); the card stops seeding (V5-50, Sonnet) |

## Open — left by V5-49 (numbers provisional, the coordinator renumbers; reviewed_actions and the server-side default deny)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 48 | coordinator (the card names `tools/apps.py`) | `mcp/src/lkap_mcp/tools/agents.py` (`agent_apps_mode`), `mcp/tests/test_docs_lint.py` (the tool's pinned argument set), `mcp/tests/test_tools_apps.py` (one plan-only test), `mcp/src/lkap_mcp/docs/concepts/apps.md` (one paragraph) | **Applied in-branch, needs ratification.** The card's exclusive MCP file is `tools/apps.py`, but `agent_apps_mode` lives in `tools/agents.py` (V5-47 owned both). The `reviewed_actions` argument (upper-cased like `denied_actions`) went there; the docs-lint test pins every tool's argument names, so it had to list the new one; the docs paragraph says a destructive action stays blocked until reviewed and to ask the user first. No other wave-3 card touches these files. | V5-49 | **Ratified** (coordinator, V5-49 merge) |
| 49 | coordinator → `routers/internal.py` owner | `api/src/lkap_api/routers/internal.py` (`_build_resolved`: one call) | **Applied in-branch in its own commit, needs ratification** (same pattern as ask #45). The card's "resolve step" has no exclusive file: `_build_resolved` now passes each tool definition through `provisioning.apply_denied_actions(definition, config.tools.apps)` before `resolve_tool_definition`. It drops unreviewed destructive actions from an **app server** row's `allowed_tools`/`tool_options`, so a row provisioned before V5-49 (whose Composio session may still preload such an action) never offers it to the worker, even before the agent is saved again. A tool finder row is untouched: its deny list lives in the Composio session and is refreshed on the next save. Dropping this commit leaves only that pre-V5-49 app-server gap; everything else in V5-49 stands without it. | V5-49 | **Ratified** (coordinator, V5-49 merge) |
| 50 | coordinator (live check) | `api/src/lkap_api/tool_providers/service.py` (`destructive_actions`) | **Verify at implementation, not verifiable offline.** A tool finder's scope is every destructive action in its apps' catalogues, found by paging the adapter's `GET /tools?toolkit_slug=…` at `limit=50` (the console picker's page size, so the two share cache entries) up to 20 pages (1000 actions per app; beyond that the scan stops and logs `apps_destructive_scan_truncated`). Unconfirmed against the real API: that `next_cursor` pages through the whole toolkit, and that a Tool Router session accepts a long `tools.<toolkit>.disable` list (a GitHub-sized toolkit can have many destructive actions). A catalogue call that fails fails the save (fail closed). Worth checking in the V5-47 live check's router re-run the card names. | V5-49 | Open |
| 51 | V5-50 (Sonnet) | `web/src/components/console/agents/tabs/connected-apps-card.tsx` | **For V5-50, no action here.** (a) Until V5-50 lands the card never writes `reviewed_actions`, so from the console every destructive action in scope stays blocked (ticking one only removes it from `denied_actions`, which no longer allows it); `reviewed_actions` is reachable through the api and `agent_apps_mode` only. That is the seed's intended outcome, minus the re-denial bug of ask #47. (b) The validator warning at `tools.apps.denied_actions` names only the destructive actions the workspace has **picked** for the agent's apps (it has no vault or catalogue access); a tool finder's other destructive catalogue actions are blocked by provisioning without being named, so the card's "Blocked until you review it" badge should come from the catalogue listing (`AppActionOut.risk`), not from the warning. (c) An app server whose picked actions are all unreviewed destructive ones is now refused on save with a 422 at `tools.apps.denied_actions` naming them (`details.unreviewed`). | V5-49 | Open |

## Open — left by V5-50 (numbers provisional, the coordinator renumbers; console: the provider-tool editor and the review semantics)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 52 | contracts owner (next contracts package touching `tool_providers.py`) | `contracts/src/lkap_contracts/tool_providers.py` (or wherever the owner puts response-only api models), generated files | **api-local model, hand-mirrored.** `SchemaRefreshOut` (`api/src/lkap_api/tool_providers/materialise.py`) is a plain `BaseModel`, not a `lkap_contracts` model, so `lkap_contracts.export` never emits it and it isn't in `lkap-contracts.d.ts`. `provider-tool-editor-dialog.tsx`'s Refresh-schema flow needs its shape, so `web/src/components/console/lib/api-hooks.ts` hand-mirrors it as `ProviderToolSchemaRefreshOut` (same pattern as `ToolkitListParams` in the same file, and the same situation asks #12/#27 flagged for `KbOut`'s extra fields and `KbHit`). Promoting it to a real contract model would let the console import the generated type instead. | V5-50 | Open |
| 53 | coordinator (Fable ruling if contested) | `web/src/components/console/agents/tabs/connected-apps-card.tsx` (`handleActionsAdded`) | **Deviation from the card's literal wording, flagging for ratification.** The V5-50 card says "remove … every `shouldDirty: false` write"; R-V5-9's own sentence ("no `shouldDirty: false` writes remain") is in the paragraph about the destructive-action seed specifically. This PR removes that seed (`seedDestructive`/`seenDestructive`) entirely — no `shouldDirty: false` write is left in the reviewed/denied path. One other `shouldDirty: false` write remains, unrelated to R-V5-9: `handleActionsAdded`'s merge of `tools_created`/`tools_existing` into `tool_ids` after `ActionsDialog`'s "Add as tools" — that reflects an attach the server already made as its own config save (`materialise.attach_tools`), not a new edit, and flipping it to `shouldDirty: true` would pop an "unsaved changes" prompt for a save that already happened, with no test currently depending on either value. The card's own acceptance bullet ("opening the tab alone leaves the form clean") is about mount, which this write doesn't touch — it only runs after a deliberate "Add as tools" click. Left as `shouldDirty: false`; flagging in case the coordinator wants it flipped anyway. | V5-50 | Open |
| 54 | V5-50 (no action needed unless ratified otherwise) | `web/src/components/console/lib/schemas.ts` | **No edit made — verified unnecessary.** The card's spec says "`lib/schemas.ts` must carry `reviewed_actions` in the apps schema so it isn't stripped on save." `toolsConfigSchema.apps` is already `z.custom<AppsMode>().optional()` (ask #45) — a bare `z.custom()` with no predicate has no shape to strip keys from, so `reviewed_actions` (added to the generated `AppsMode` type by V5-49) already survives a `zodResolver` parse untouched; confirmed with a standalone zod repro and pinned in `console-connected-apps.test.tsx`'s "tools.apps survives the zod parse" test (extended to assert `reviewed_actions` round-trips). `buildAgentUpdate` (`agents/editor/form-values.ts`) also spreads `edited.tools` wholesale (`{...stored.tools, ...edited.tools}`), so the field reaches the PUT body too. | V5-50 | FYI |

## Done

- #4 (V5-18 → V5-22): Keys page (`web/src/components/console/registry/credential-list.tsx`) now filters out `provider_id: "tool-provider-account"` rows client-side (row-level fix; the server-side refusal in ask #2 is still open) and gives the Composio row its own status chip (`useComposioStatus`, shared with Tools → Apps) plus Validate/Rotate/Disable/Remove key actions in place of the generic Test/Rotate/Rename/Delete menu. The Providers page (`web/src/components/console/providers/providers-catalog.tsx`) now has a `tool_provider` tab ("Connected apps"); `provider-row.tsx` renders that kind read-only with a "Manage in Tools → Apps" link rather than the generic enable/key-badge controls, because the generic `PUT /v1/providers/{id}/settings` toggle does not pause tools the way `POST /v1/tool-providers/composio/enable|disable` does (docs/v5/COMPOSIO.md §4's `set_enabled`) — wiring the generic Switch to a tool-provider kind would have let a builder disable Composio without pausing its tools.
