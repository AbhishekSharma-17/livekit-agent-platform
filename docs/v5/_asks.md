# LKAP v5 — cross-package asks

Format (v3/v4): **owner package** — file — the exact edit — who asked — status. The coordinator's decisions are binding; design questions go to Fable and land as `R-V5-n` in `PLAN-V5.md` §7. Never a key value, a hostname from the environment, a user name or a machine path.

## Coordinator decisions

- R-V5-1 (V5-08): barge-in cancels `request`-method requests only; `request_choice` added to `_REALTIME_SILENT_BUILTINS`; neutral late-submit wording. See `PLAN-V5.md` §7.

## Open

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 1 | V5-15, V5-19 | `agent/src/lkap_agent/platform_agent.py` (`_REALTIME_SILENT_BUILTINS`) | Add `request_consent` / `request_upload` when those tools land (R-V5-1 item 2) | Fable | Open — inherited |
| 2 | coordinator (api `provider_keys.py` owner) | `api/src/lkap_api/provider_keys.py` (`GET /v1/credentials`, update/test/delete) | Hide Composio connection rows (`tool-provider-account`) from the credentials list and refuse update/test/delete on them (today a Keys-page Delete removes one without the Composio delete or pausing its tools); flip the pinning test in `api/tests/test_tool_providers.py` | V5-18 | Open |
| 3 | coordinator (api `provider_keys.py` owner) | `api/src/lkap_api/provider_keys.py` (`update_credential`) | Clear the last-test fields when the secret changes, so Validate right after Rotate does not answer from the old key's 10-minute cache (all providers) | V5-18 | Open |
| 5 | V5-47 | `api` `tools` table, Composio adapter | The `tools.kind` check allows only `http`/`mcp`: the `provider` kind needs a migration; Composio marks `/api/v3.1/mcp/servers` deprecated in favour of Tool Router sessions — prefer `tool_router/session` or confirm; Tool Router allow/deny field shapes unverified | V5-18 | **Done** (V5-47, in-branch): migration `v5_010_tool_provider_kind`; both app server and tool finder use `tool_router/session` (the MCP server API is marked deprecated in the reference index); allow/deny shapes `{"enable": [...]}` / `{"disable": [...]}` verified — `docs/v5/_briefs/v5-47-live.md` |
| 6 | coordinator (from V5-03) | `web/src/panels/registry.ts` | `PanelAction` (and `PanelFormSubmitAction`, or a new `PanelBlockSubmitAction`) has no `"block_submit"` member, though `AgentAction.action` has carried it since V5-02 and `agentActionFor` already forwards any action it doesn't special-case unchanged. `web/src/panels/composite/use-block-request.ts` sends it today via `perform({action: "block_submit", ...} as unknown as PanelAction)` (documented at the cast) — correct at runtime, but the type gap should close. Add `PanelBlockSubmitAction {action: "block_submit"; payload: {block_id: string; values: Record<string, unknown>} | {block_id: string; cancelled: true}}` to the `PanelAction` union and drop the cast in `use-block-request.ts`. | V5-03 | **Done** (coordinator, 2026-09-25): applied; the cast in `use-block-request.ts` is gone |
| 7 | coordinator (from V5-03) | `web/src/panels/composite/index.tsx` (docblock) and `web/src/panels/README.md` | Both still describe the composite panel's `lkap.ui.request` handling as `form`, `show_block`, `focus`, `navigate` only; V5-03 added `request` alongside `form` in `composite/requests.ts` (one handler, two method names) but neither doc comment was touched (not exclusive files of this card). Update the method lists to include `request`. | V5-03 | **Done** (coordinator, 2026-09-25): both docs list `request` |
| 16 | coordinator (after V4-13 merges; filed as #6 by V5-22) | `web/src/components/console/tools/tool-row.tsx` and `tools-list.tsx`'s Kind column (`tool.kind === "http" ? "HTTP" : "MCP"`) | On `tool.kind === "provider"` (V5-47's `ProviderToolDefinition`), render an "App" `StatusChip` with the connected app's logo instead of the "HTTP"/"MCP" label — the logo comes from the toolkit behind `definition.connection_id`, not carried on the tool itself, so this needs a lookup (`GET /v1/tool-providers/composio/connections`) added alongside. V5-22 could not make this edit: `tool-row.tsx` was V4-13's file while V5-22 ran (docs/v5/COMPOSIO.md §6, D-V5-C13 says "the tools list shows an 'App' chip … this package edits `tool-row.tsx` only after V4-13 merges, else files an ask") | V5-22 | Open |
| 17 | coordinator (V4-15 owns `db/models.py`) | `api/src/lkap_api/db/models.py` (`Tool.__table_args__`) | `CheckConstraint("kind IN ('http','mcp','provider')", name="kind_valid")` — the model mirror of migration `v5_010_tool_provider_kind`; without it `create_all` test databases refuse `provider` rows | V5-47 | Applied in-branch (one line); keep it through the V4-15 merge |
| 18 | coordinator (V4-15 owns `api_models.py` additively) | `contracts/src/lkap_contracts/api_models.py` (`ToolCreate.kind`) | `Literal["http", "mcp", "provider"]`; without it `GET /v1/tools` fails once a `provider` row exists (`ToolOut(kind=...)`) | V5-47 | Applied in-branch in the first contracts commit (one line) |
| 19 | coordinator (V5-07/V5-06 own `main.py` hunks this wave) | `agent/src/lkap_agent/main.py` (`_assemble`, the `declarative_tools_builder` call) | `[t for t in resolved.tools if t.kind in ("http", "provider")]` so connected-app actions reach the builder (it dispatches `provider` to `tools/provider.py`) | V5-47 | Applied in-branch (one line + comment); rebase V5-07/V5-06 over it |
| 20 | coordinator (V4-13 / V5-48 own the console files) | `web/src/components/console/agents/defaults.ts` (`DEFAULT_TOOLS.apps`), `web/src/components/console/tools/tool-row.tsx` (`requestSummary`) | Keep `pnpm typecheck` green with the regenerated `.d.ts`: `DEFAULT_TOOLS` gains the `apps` default (`mode: "off"` …); `requestSummary` returns "App action · <app>" for a `provider` definition (it has no url) | V5-47 | Applied in-branch in the first contracts commit; V5-48 may restyle |
| 21 | coordinator (V4-15 owns `routers/agents.py`) | `api/src/lkap_api/routers/agents.py` (`restore_version`) | Also call `apps.on_save(db, ctx, row, config)` when a restored config is written, so restoring a version with another `tools.apps.mode` provisions or removes the tool finder / app server like a save does (today a restore leaves the Composio side as it was until the next save) | V5-47 | Open |
| 22 | coordinator (V4-15 owns `routers/internal.py`) | `api/src/lkap_api/routers/internal.py` (`_build_resolved`) | Only if the live check shows Composio needs `connected_account_id` when a subject has two accounts for one app: add each `provider` tool's `connection_id` to `wanted`, and set `ProviderToolDefinition.connected_account_id` from that connection bag's `connected_account_id` before `resolve_tool_definition` (the contract field exists; the worker already sends it when set) | V5-47 | Open — conditional on `v5-47-live.md` step 5 |
| 23 | V5-46 (owns `mcp/src/lkap_mcp/docs/guide.md`) | `mcp/src/lkap_mcp/docs/guide.md` | One line under Apps: "`agent_apps_mode` chooses how an agent uses connected apps (picked actions, an app server, or a tool finder); recipe `attach-app-actions`" (COMPOSIO.md §7 "`lkap_guide` gains one line") | V5-47 | Open |
| 24 | V4-15 (costs owner: `api/src/lkap_api/costs/**`, `routers/internal.py`, `jobs/rollup.py`) | the session cost lines | D-V5-C12 (V5-47 scope, deferred to the costs owner): count one `requests` usage line under provider `composio` per `provider` tool call and per tool-finder meta call — today the count is the session's `tool_call_ended` events whose `tool` is a `provider` row's name or one of the `composio_tool_finder` / `composio_app_server` row's `allowed_tools`; priced from the free tier's zero with the managed-app surcharge as `tier_note`, else shown as "no price" | V5-47 | Open |

## Open — left by V5-01 (asks renumbered #1–#8 → #8–#15 at merge; Knowledge ingest: chunking with locators, MarkItDown, model names to settings, per-KB dimension)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 8 | coordinator | `api/alembic/versions/v5_001_knowledge_p0.py` | **Chain note.** §0.3 chains every V5 migration after the costs package's `v4_003`, which has not landed. `v5_001` has `down_revision = "v4_002_provider_models"` (the current head). If a `v4_003` lands first, set `down_revision` to it and re-run `tests/test_migration_v5_001.py` (its `BEFORE` constant moves too) and the rehearsal. | V5-01 | Open (re-chain at merge if needed) |
| 9 | coordinator (no owner) | `api/alembic/env.py` | Added `include_object` so autogenerate and `alembic check` skip the objects `v5_001` creates outside the ORM: the SQLite FTS5 table `kb_chunks_fts` and its shadow tables, the Postgres generated column `kb_chunks.tsv` and its index `ix_kb_chunks_tsv`. Without it, `tests/test_migrations.py::test_upgrade_head_matches_the_models_exactly` fails. | V5-01 | **Applied by V5-01**, please confirm |
| 10 | coordinator (no owner) | `api/tests/test_health.py` | `test_migration_head_matches_the_script_directory` allows head prefixes `("v2_", "v3_", "v4_")`; added `"v5_"`. Any V5 migration needs it. | V5-01 | **Applied by V5-01**, please confirm |
| 11 | coordinator (no owner) | `mcp/uv.lock` | Re-locked: `mcp` dev-depends on `lkap-api` (editable), so the new `markitdown[docx,pptx,xlsx]==0.1.8` pin adds its transitive packages there too. `onnxruntime` forks on Windows only (magika's marker); Linux and macOS stay on 1.30.0. | V5-01 | **Applied by V5-01**, please confirm |
| 12 | contracts owner (V4-12 until it merges; then the next contracts package, e.g. V5-04 or V5-06) | `contracts/src/lkap_contracts/api_models.py` | Additive fields the api now returns through api-local subclasses in `routers/knowledge.py` (so the console and MCP types can see them): `KbOut += dimension: int \| None = None, embedder_model: str \| None = None, chunking: dict[str, int] \| None = None`; `KbDocumentOut += progress: float \| None = None`; new `KbEvalIn {question, expected_document_id?, expected_text?, tags[]}` (at least one expected), `KbEvalOut(KbEvalIn) {id, created_at}`, `KbEvalSetIn {items[] ≤ 500}`, `KbEvalSetOut {items, total}`; `KbReindexIn {document_ids?}`, `KbReindexOut {queued[], skipped[{document_id, filename, reason: source_not_stored\|ingest_in_progress}]}`. `KbHit.meta` is V5-04's (its card). When they land, `routers/knowledge.py` drops the local classes and imports these. | V5-01 | Open |
| 13 | V5-04 | `api/src/lkap_api/kb/search.py`, `routers/knowledge.py` | Facts V5-04 builds on: (a) the lexical index exists from `v5_001`: SQLite `kb_chunks_fts(chunk_id, kb_id UNINDEXED, text)` with `porter unicode61 remove_diacritics 2` — query `WHERE kb_chunks_fts MATCH 'text:(...)'` (always scope the column: `chunk_id` is indexed too, for the delete trigger) and filter `kb_id`; Postgres `kb_chunks.tsv` = `to_tsvector('english', text)` — query `plainto_tsquery('english', :q)`. The test suite builds its schema with `create_all`, so lexical tests must create the FTS table (reuse the migration's `_SQLITE_FTS_UP`). (b) The embedder check is in the router (`check_kb_embedder` → 422); the card's "skip a mismatched KB with a warning" replaces it inside `KnowledgeService`. (c) `KbChunk.meta` carries `heading_path`, `page`, `char_start`, `char_end` for chunks ingested or re-indexed after V5-01; older chunks carry `filename` only. (d) `LKAP_RERANK_MODEL` is `Settings.rerank_model`. | V5-01 | FYI |
| 14 | V5-05 | `api/src/lkap_api/kb/evals.py` | `kb_evals` rows: `id, kb_id, question, expected_document_id (no FK), expected_text, tags (JSON list), ordinal (the PUT order), created_at`; read them with `ORDER BY ordinal`. An `expected_document_id` whose document was deleted after the PUT is the "skipped" case. | V5-01 | FYI |
| 15 | V5-13 (re-embedding) | `api/src/lkap_api/routers/knowledge.py` | `POST /v1/knowledge-bases/{id}/reindex` (V5-01) re-extracts and re-chunks stored documents through the existing `KB_INGEST` job with the **current** embedder, and is refused per document when the KB's recorded embedder differs. Documents seeded from packs/templates were never written to storage and are skipped (`source_not_stored`); the Demo agents' seeded KBs therefore keep their pre-V5-01 chunks until V5-13's `kb_reindex` (or a re-seed) can read the seed files. | V5-01 | FYI |

## Open — left by V5-08 (block quartet and citation preview; numbers provisional, the coordinator renumbers)

| # | Owner | File | Edit | Asked by | Status |
|---|---|---|---|---|---|
| 25 | coordinator (ratify) → V5-12 | `web/src/panels/blocks/index.tsx` (`BLOCK_COMPONENTS`), `web/src/components/console/agents/panel-section/panel-composer.tsx` (`BLOCK_ICONS`), `web/src/components/console/lib/schemas.ts` (`BLOCK_TYPE_VALUES`), `web/src/components/console/lib/constants.ts` (`BLOCK_TOOLS`), `web/src/components/console/agents/panel-section/composer-model.ts` (`TOOL_NEEDS`), `web/src/panels/blocks/__fixtures__/{choices,details,markdown,steps}.json` + `index.ts` + `layout.json`, `web/tests/panel-block-fixtures.test.ts` (`SCHEMA_FILE`), `web/tests/preview-scenes.test.tsx` (25 → 33 combos) | **Applied in-branch in V5-08's contracts commit; keep through the merge.** The §0.1 "new block types" rule names only `catalog.ts`, but `BlockType` and `BlockToolName` key exhaustive `Record`s and a zod enum in these files, and `tool-names-parity.test.ts` pins `BLOCK_TOOLS` to `builtin_tools.json`, so `pnpm typecheck`/`pnpm test` were red without them. Each edit is the minimum: a "not shown here yet" placeholder renderer for the four types, one icon each, the enum values, five toggle rows and five "add a … block first" hints, state fixtures that pass the schema check (V5-12 extends them), the scene count. `catalog.ts` also gained a `list` config-field kind for `details.fields` / `steps.steps` (the composer shows only the label until V5-12 adds an editor). Suggested: amend PLAN-V5 §0.1 to list these files for the next contracts package that adds a `BlockType` (V5-15 `consent`, V5-19 `upload`, V5-32 `handoff`, V5-43 `link`/`slots`/`cards`). | V5-08 | Applied in-branch — ratify |
| 26 | coordinator (ratify) | `agent/src/lkap_agent/tools/builtin/update_block.py` (`UPDATABLE_BLOCK_TYPES`), `agent/src/lkap_agent/ui/channel.py`, `agent/src/lkap_agent/flow/runtime.py` (import line), `agent/src/lkap_agent/platform_agent.py` | **Touched beyond the card's literal list; please ratify.** (a) `update_block.py` keeps the worker's copy of `UPDATABLE_BLOCK_TYPES`, pinned to the contract by `test_tool_names_parity.py`: += `details`, `markdown`, `steps`. (b) `ui/channel.py` is under `ui/**`, which §2's wave text gives V5-08, though the card lists only `ui/blocks.py`: `cite` carries the locators, a public `submit_block(block_id, values)` (for `resolve_choice`), a generic submit writes answer keys that name state fields (`selected`), and `block_action {name: "open_citation"}` on a `kb_citations` block is handled there before the pack callback. (c) `flow/runtime.py`: the one `publish_progress` hunk plus its import line. (d) `platform_agent.py`: there was no `user_state` handler in this file (the only one is `main.py`'s idle timer), so hunk 1 is a new synchronous `PlatformAgent.on_user_state_changed` that `_init_blocks` registers on `ctx.session` once per session (guarded by a `userdata` key, so flow nodes do not register it twice); `main.py` is untouched. Hunk 2 (`_REALTIME_SILENT_BUILTINS`) and the neutral late-submit wording are as R-V5-1 says. | V5-08 | Open |
| 27 | V5-19 (session assets) + V5-12 (renderer) | `agent/src/lkap_agent/ui/blocks.py::open_citation`; the `kb_citations` renderer | The card's fallback ("until V5-19 the handler opens the console's document URL via `navigate`") cannot be built: there is no console or api URL that shows one KB document, the worker knows no public web URL (only `api_base_url`), and `navigate` accepts absolute http(s) only. So `open_citation` opens the page in a `document` block **when the session holds the document as an asset whose `AssetRef.meta.document_id` is the citation's `document_id`**, and otherwise answers `{opened: false, reason: "no_source" \| "no_document_block" \| "unknown_citation", citation}`. V5-19: push a cited KB document through the session-asset path with `meta={"document_id": <kb document id>}` and the handler needs no change. V5-12: send `block_action {name: "open_citation", data: {chunk_id}}` on tap and, on `opened: false`, show the returned passage (filename, page, heading path, text) in a dialog. | V5-08 | Open |
| 28 | coordinator (ruling) | `api/src/lkap_api/config_service.py` (`choices_on_phone_issues`) | "Warn when a `choices` block exists on a `sip_*`-only agent": validators see only `AgentConfig`, which has no channel list (phone numbers and dispatch rules live in their own tables). Shipped heuristic: an agent counts as set up for phone calls when `capabilities.dtmf` is on or `telephony.transfer_targets` is non-empty; then each `choices` block gets a warning at `panel.blocks[i]`. If a stricter rule is wanted, add the agent's phone-number / dispatch-rule binding to `ValidationContext` (filled in `validation_context_for`) and warn only when the agent has one. | V5-08 | Open — ruling |
| 29 | V5-06 / V5-04 | `agent/src/lkap_agent/ui/channel.py::cite` | FYI for the locator handoff (V5-06 card): `cite` maps `KbHit.document_id` and, via `getattr(hit, "meta", None)`, `page`, `heading_path`, `char_start`, `char_end` onto `KbCitation`. Once V5-04 adds `KbHit.meta`, `PlatformAgent._cite` and `search_knowledge` need no change; unset locators stay off the wire. | V5-08 | FYI |
| 30 | coordinator (card wording) | `docs/v5/PLAN-V5.md` (V5-08 card) | Stale paths in the card: `docs/CONTRACTS.md §4` → the block contract lives in `docs/v2/CONTRACTS-V2.md §4.4` (V5-02 edited it there too; V5-08 did the same); `mcp/src/lkap_mcp/docs/concepts/panels.md` → `panels-and-blocks.md`. | V5-08 | Open |
| 31 | V5-12 | `web/src/panels/blocks/catalog.ts` (`BLOCK_TOOL_TYPES`), `composer-model.ts::blockToolStatus`, `web/src/components/console/flow/tool-options.ts` | `BLOCK_TOOL_TYPES["set_steps"] = {"steps"}` is coarse on purpose (the contract keeps one type set per tool); the worker registers `set_steps` only for a `steps` block whose `config.source` is not `"flow"`. The composer's block-tool switch and the flow node tool picker should apply the same rule, so a panel with only a flow-driven steps block does not offer `set_steps`. Also for the `choices` renderer: a `block_submit` whose ids are not options of the block is not stored, so the block can read `status: "submitted"` with `selected: []` (the tool reports "did not pick"); render that as "no answer". | V5-08 | Open |
| 32 | next owner of `packs/src/packs/base.py` (+ `main.py` `NoopUiChannel`) | `packs.base.UiChannel` | Optional: add `submit_block(block_id, values) -> bool` to the protocol and the no-op channel. `resolve_choice` reads it with `getattr` and falls back to patching the block, so nothing breaks without it. | V5-08 | Open |

## Done

- #4 (V5-18 → V5-22): Keys page (`web/src/components/console/registry/credential-list.tsx`) now filters out `provider_id: "tool-provider-account"` rows client-side (row-level fix; the server-side refusal in ask #2 is still open) and gives the Composio row its own status chip (`useComposioStatus`, shared with Tools → Apps) plus Validate/Rotate/Disable/Remove key actions in place of the generic Test/Rotate/Rename/Delete menu. The Providers page (`web/src/components/console/providers/providers-catalog.tsx`) now has a `tool_provider` tab ("Connected apps"); `provider-row.tsx` renders that kind read-only with a "Manage in Tools → Apps" link rather than the generic enable/key-badge controls, because the generic `PUT /v1/providers/{id}/settings` toggle does not pause tools the way `POST /v1/tool-providers/composio/enable|disable` does (docs/v5/COMPOSIO.md §4's `set_enabled`) — wiring the generic Switch to a tool-provider kind would have let a builder disable Composio without pausing its tools.
