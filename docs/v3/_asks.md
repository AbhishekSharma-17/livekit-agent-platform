# v3 cross-package asks

One line per change a package needs in a file it does not own (`PLAN-V3.md` "Exclusive ownership"). Format: **owner package** — file — the exact edit — who asked — status. The coordinator's decisions in the last section are binding. Rulings that come out of an ask are recorded in `PLAN-V3.md` §8 and referenced here by id.

Created by the architect (2026-09-24); V3-01 owns this file's structure from wave 1 on.

## Grants recorded by the architect (pre-approved edits outside a package's tree)

| # | Owner | File | Edit | Why |
|---|---|---|---|---|
| G1 | V3-00 | `web/src/components/console/settings/api-types.ts` | add `"audit:read"` to `SCOPES` (one line) | the scope must exist in the console before V3-04 builds the presets |
| G2 | V3-01 | `scripts/export_contracts.sh` | one block copying `contracts/generated/{providers.json,builtin_tools.json,schemas/}` into `mcp/src/lkap_mcp/generated/` | contracts gate keeps the MCP's generated resources in sync (D-V3-10) |
| G3 | V3-05 | `.github/workflows/python.yml`, `.github/workflows/contracts.yml` | `mcp` in the matrix; the generated dir in the diff paths | `.github/**` was V2-09's; granted for these two files |
| G4 | V3-08 | `scripts/export_contracts.sh` | one block copying `mcp/src/lkap_mcp/docs/recipes/*.md` into `mcp/claude-plugin/skills/lkap/recipes/` | V3-01 owns the script; one source for recipes (R-V3-16) |
| G6 | V3-08 → V3-04 | `web/src/components/console/settings/connect-agent-dialog.tsx` | **superseded by G8 (R-V3-26)** — the strings live in `snippets.ts` and V3-04 is not in wave 2. Original: V3-08 files the exact step-3 lines (skill install, Codex `AGENTS.md` note) as an ask; V3-04 applies them | V3-04 owns the dialog |
| G5 | V3-02 | `mcp/src/lkap_mcp/server.py` | **none** — chat tools register through `TOOL_MODULES`; if the hook is missing when V3-02 starts, file an ask, do not edit | keeps `server.py` exclusive to V3-01 |
| G7 | V3-06 | `web/src/components/console/settings/snippets.ts` | replace the "best-effort placeholder" comment above `codexSnippet`'s remote form with "verified against codex-cli 0.153.4 (R-V3-25)" — comment only, no string change | R-V3-25; the snippet test stays pinned |
| G8 | V3-08 | `web/src/components/console/settings/snippets.ts` (`skillInstallLine`, `CODEX_AGENTS_NOTE`, `ASK_AGENT_LINE`), `web/tests/console-settings-snippets.test.ts` (their pins) | set the three step-3 strings to the verified install command, the `AGENTS.md` note and the "ask your agent" line; run the web gate; nothing else in `web/` | R-V3-26 replaces G6 (V3-04 is not in wave 2) |
| G9 | V3-06F | `mcp/src/lkap_mcp/server.py` | add `registry_factory: Callable[[ServerContext], Registry] = Registry` to `build_server` and use it for the registry; nothing else | R-V3-30: `http.build_session_server` calls `build_server` instead of mirroring it |
| G10 | V3-06F | `web/Dockerfile`, `.github/workflows/docker.yml` | the `ARG`/`ENV NEXT_PUBLIC_LKAP_MCP_PUBLIC_URL` pair (two lines + comment) under the `NEXT_PUBLIC_API_BASE_URL` pair; the `web` build-arg line in `docker.yml` gains `NEXT_PUBLIC_LKAP_MCP_PUBLIC_URL=http://localhost:8090/mcp` | R-V3-31 |
| G11 | V3-06F | `mcp/pyproject.toml`, `mcp/uv.lock` | declare `starlette` and `uvicorn` (ranges from the locked versions), `uv lock` | R-V3-32 |
| G12 | V3-06F | `docs/RUNBOOK.md` (intro line + §5 build line), `deploy/README.md` (§4 build line) | "Section 20 is v3 (Remote MCP, V3-06)"; `docker build -f mcp/Dockerfile -t lkap-mcp .` next to the supervisor line | R-V3-34 |
 |
## Open — left by V3-00 (api + contracts additions)

| # | Owner | File | Edit | Why | Status |
|---|---|---|---|---|---|
| V3-00-1 | V3-00 (announced) | `web/src/components/console/settings/api-types.ts` | G1 needed **two** lines: `"audit:read"` in the `Scope` union as well as in `SCOPES` (`SCOPES: Scope[]` does not typecheck otherwise) | grant G1 | done; `pnpm typecheck` + `pnpm test` green |
| V3-00-2 | V3-00 (announced) | `contracts/src/lkap_contracts/export.py` | one line: `"KbImportIn": api_models.KbImportIn` in `EXPORTED_MODELS` | the card's "`KbImportIn` … exported" needs the registration; regen adds `schemas/KbImportIn.schema.json` and the `.d.ts` interface only | done |
| V3-00-3 | V3-00 (announced) | `api/tests/test_health.py` | `expected.startswith("v2_")` → `startswith(("v2_", "v3_"))` in `test_migration_head_matches_the_script_directory` | the new head `v3_001_agent_keys` failed the hard-coded prefix | done |
| V3-00-4 | coordinator | `api/src/lkap_api/main.py` | AGENT-ACCESS §7 last row: mention API keys and the `X-LKAP-Client` header in the OpenAPI auth `description` (docstring only) | §7 lists it, the V3-00 card's exclusive files do not; the route descriptions of `/v1/audit`, `/v1/api-keys` and `/self` already document it | done by V3-05 (assigned in its brief): `DESCRIPTION` gains an "Authentication" section (cookie, API keys + scopes + `GET /v1/api-keys/self` + agent keys, admin token, `X-Workspace`, `X-LKAP-Client` attribution); the stale "admin surface (`X-Admin-Token`)" bullet is reworded. Text only; api gate green (1109 passed, 1 skipped) |
| V3-00-5 | coordinator | `api/data/lkap.db` | apply `v3_001_agent_keys` after a `.backup` (`docs/v3/_briefs/migration-rehearsal-v3.md`) | the `--reload` api on :8080 already runs the new `ApiKey` model: every `Bearer lkap_…` request and the API keys list 500 (`no such column: api_keys.kind`) until it is applied | done — `alembic_version` in `api/data/lkap.db` reads `v3_001_agent_keys` (architect check, 2026-09-24) |
| V3-00-6 | CI | `.github/workflows/python.yml` (existing `test-postgres` job) | none: the Postgres rehearsal is that job's migration chain + full api suite against `postgres:16` | no Docker locally; the card asks for a Postgres rehearsal | open until the repo has a remote |

## Open — left by V3-01 (MCP server core)

| # | Owner | File | Edit | Why | Status |
|---|---|---|---|---|---|
| V3-01-1 | V3-02 (announced) | `mcp/src/lkap_mcp/chat/tools.py` | **The hook contract.** `server.TOOL_MODULES` is a list of dotted module paths and already contains `"lkap_mcp.chat.tools"` (imported only if it exists; an import error *inside* it still fails). The module exposes `def register(registry: lkap_mcp.registry.Registry) -> None` and declares each tool with `registry.register(fn, scopes={"agents:write"}, annotations=WRITE, data="Chat")` or the `@registry.tool(...)` decorator (`READ`/`WRITE`/`IDEMPOTENT_WRITE`/`DESTRUCTIVE` from `registry`). `fn` is an `async def` with typed keyword parameters returning `lkap_mcp.results.ToolResult` (`ToolResult.success(data)`, `ToolResult.fail(code, message, next_steps=…)`; wrap replies in `results.untrusted(text, source)`). `registry.ctx` is the `ServerContext` (`.settings` incl. `max_chats`, `.client` = `LkapClient` with `get/post/put/delete/items`, `.identity`, `.allows(scope)`); `registry.on_shutdown(async_fn)` closes open chats. Every call is attributed, sanitized and exception-safe in the wrapper, so tools may raise `ApiFailure`. Tests build the server with `lkap_mcp.server.build_server(settings, client=LkapClient(settings, api_key=raw, transport=httpx.ASGITransport(app)))`; see `mcp/tests/conftest.py`. The scope gate is lazy (first `tools/list`/`tools/call`), so a tool is visible only to keys whose scopes cover it. | V3-02 registers chat tools without editing `server.py` (G5) | closed (V3-02 uses it, V3-02-2) |
| V3-01-2 | V3-03 (announced) | `mcp/src/lkap_mcp/docs/**` | Integration with V3-03's own loader (`lkap_mcp.docs`): `prompts.py` renders every prompt through `lkap_mcp.docs.prompt(name).render(**args)` (the frontmatter concepts/recipes are inlined there) and falls back to V3-01's recipe+concept composition only when that raises; `lkap_mcp.content` (V3-01) reads the same `docs/` and `generated/` files but tolerates missing ones (placeholder guide, contracts fallbacks for `generated/`), and the discovery tools/resources use it. Nothing for V3-03 to change. `lkap_mcp.catalog.all_tool_names()` / `declared_specs()` give every declared tool (V3-01's 56 + V3-02's 4) for doc-lint without an api. | one source for prompts; resources survive partial content | open (info) |
| V3-01-3 | V3-03 | `mcp/src/lkap_mcp/docs/__init__.py`, `mcp/tests/test_docs_lint.py` | The shared `mcp/` gate fails on V3-03's files only: `mypy --strict` `no-any-return` at `docs/__init__.py` lines 244, 249, 266 (`json.loads(...)` returned as `dict[str, Any]`; bind it to an annotated local first), and `ruff check .` on `test_docs_lint.py` (F821 undefined `Any` ~l.808, E501 ~l.790/850, I001 ~l.86). V3-01's files pass `ruff check`, `ruff format --check` and `mypy --strict` on their own. | package gate | closed (V3-03 fixed both; package gate green 2026-09-24) |
| V3-01-4 | V3-05 | `.github/workflows/python.yml` (G3) | Add `mcp` to the matrix. Notes for the job: the dev group pulls `lkap-api`, `lkap-packs` and `lkap-testing` as path deps (tests boot the real api in-process), and `tests/conftest.py` appends `api/tests` to `sys.path` for `auth_helpers` and `connection_fakes`, so the checkout must include `api/`. `tests/test_stdio.py` spawns `python -m lkap_mcp` (no network: the api url is a closed loopback port). | V3-01 does not own `.github/**` | done (V3-05): `mcp` in the matrix, `mcp/**` in the trigger paths, a comment with these notes; `uv lock --check` clean |
| V3-01-5 | V3-06 (announced) | `mcp/src/lkap_mcp/http.py`, `__main__.py` | What V3-01 left for HTTP mode: `McpSettings` already has `transport` (`LKAP_MCP_TRANSPORT=http` switches `file:` refs to `ref_unavailable_in_http_mode`, `kb_add_document(file_path=)` and `webhook_create` to refusals), `http_host`/`http_port`/`public_url`/`max_sessions_per_key`/`calls_per_min`. A per-session server is `build_server(settings, client=LkapClient(settings, api_key=<bearer>))`; registration is lazy per server instance (first `tools/list`/`tools/call`/`resources/read`), so one server per MCP session gives per-session scope-gated tool lists without the union fallback. `server.aclose()` runs the shutdown hooks (chats) and closes the client. `__main__.py` has an `--http` flag stub that exits 2 with "not available in this build yet". | V3-06 builds on it | closed (V3-06: one `LkapServer` per MCP session, see V3-06-1) |
| V3-01-6 | architect / V3-07 | — (design note) | `lkap_delete(kind="agent", confirm=true)` relays the api's 409 only when the agent still has sessions (or `purge=true` on an unarchived agent); a session-less, unarchived agent is deleted (204), which is the api's documented rule (`DELETE /v1/agents/{id}`). V3-07 step 5 expects "confirm → 409 (not archived)"; after a test chat the agent has a session, so the 409 holds there. The MCP adds no extra "archive first" gate. Tests cover both 409 paths. | acceptance wording vs api semantics | ruled — R-V3-21 (V3-07 step 5 now archives then `purge=true`) |
| V3-01-7 | api (Phase 2) | `api/src/lkap_api/routers/workspaces.py` | `GET /v1/audit` has only `limit`/`offset`; the `activity` tool filters `mine`/`since`/`action_prefix` client-side over at most 2,000 rows. Server-side `actor_id`/`since`/`action` filters would make it exact. | efficiency | ruled — R-V3-22 (Phase 2 backlog; the tool names the window when exhausted) |
| V3-01-8 | V3-01 (announced) | — | Deviations from the card/§4 wording, all deliberate: (a) `TOOL_MODULES` is `list[str]` of dotted paths (lazy, optional import) rather than `list[ModuleType]`, so V3-02's module could be listed before it existed and there is no import cycle; (b) `ToolResult.data` is `Any` (FastMCP validates structured output against the return model, and redaction/`Untrusted` wrapping rewrite fields), and each tool names its data model in `_meta["lkap/data"]`; (c) `session_list` uses `since`/`until` (`from` is a Python keyword; sent as the api's `from`/`to`); (d) the blocked-destination hint keys off the api's `details.reason="blocked_destination"` (the api's code is `unprocessable_entity`); (e) values shorter than 8 characters are not scrubbed from free text (key-based redaction still covers them); (f) mixed read/write tools (`connection_fleet`, `agent_versions`, `agent_limits`, `api_request`) present as read-only (`readOnlyHint=true`) to keys without their write scope and refuse the write path with `forbidden`. | record | ruled — R-V3-23 ratifies (a)–(f); V3-01 card amended |

## Open — left by V3-02 (test chat over the room)

| # | Owner | File | Edit | Why | Status |
|---|---|---|---|---|---|
| V3-02-1 | V3-06 | `mcp/src/lkap_mcp/http.py` | none in V3-02's tree. Contract for HTTP mode: chats are owned by `lkap_mcp.chat.tools.OWNER_KEY(ctx)` (a replaceable module hook). Default: `"stdio"` in stdio mode; in HTTP mode `f"session:{mcp-session-id header}"` from `request_ctx.get().request`, falling back to `id(session)` only when no header is reachable (unsafe across GC: an id can be reused by a later session of another key). V3-06 **must** set `chat_tools.OWNER_KEY` from its own session registry (the 32-byte id bound to the key hash) and, when an MCP session ends, call `await chat_tools.manager_for(registry).close_owner(owner)`. Limits already enforced by `ChatManager`: `LKAP_MCP_MAX_CHATS` (3) per owner, `max_total=20` per process, 5 min idle | §9.1 "chats are keyed by session and die with it", §9.3 "3 per session, 20 per process" | ruled — R-V3-24 (V3-06: registry id owner, fail-closed, `close_owner` on every session end) |
| V3-02-2 | V3-01 | none | nothing needed: `server.TOOL_MODULES` already lists `lkap_mcp.chat.tools`, `register(registry)` + `registry.on_shutdown(...)` (a wrapper that runs `manager.close_all()` and drops the manager) are used as documented; `livekit==1.1.18` and `lkap-testing` are already in `mcp/pyproject.toml` | G5 | closed |

## Open — left by V3-03 (docs, recipes, prompts, doc-lint)

| # | Owner | File | Edit | Why | Status |
|---|---|---|---|---|---|
| V3-03-1 | V3-03 (announced) | `mcp/src/lkap_mcp/docs/blocks.md` (new) | Added the "blocks catalog lines" the card lists as a deliverable, in the exact `- \`type\` — description` format `lkap_mcp.content._block_lines()` (V3-01) already parses. Confirmed: `content.block_catalog()` now returns V3-03's descriptions instead of the empty-string fallback (spot-checked via `uv run python -c "from lkap_mcp import content; print(content.block_catalog()[0])"` in `mcp/`). | one source for block descriptions; no code change needed on either side | done |
| V3-03-2 | ack of V3-01-2/V3-01-3 | — | Confirming both from V3-03's side: (a) `lkap_mcp.docs` (this package's loader) and `lkap_mcp.content`/`prompts.py` (V3-01) turned out to overlap in purpose but not in code — V3-01 built its own tolerant reader independently and wired `prompts.py` directly to `lkap_mcp.docs.prompt(name).render(...)` as designed; both are now exercised by the shared test suite (423 tests green) and left as-is, no consolidation needed. (b) The three field-name deviations V3-01-8 records (`session_list.since/until`, `call_list.direction/session_id`, `chat_rewind.timeout_s`) are now reflected in `test_docs_lint.py`'s `MCP_TOOLS` and in the two docs that named the old `session_list`/`call_list` fields (`concepts/sessions-and-test-chat.md`, `concepts/telephony.md`, `recipes/diagnose-a-session.md`); `test_hardcoded_tool_fields_match_registry_signatures_once_v301_lands` now passes field-for-field against `lkap_mcp.catalog.declared_specs()` for all 60 tools. | keeping this file the single place both directions of an ask are visible | done |

## Open — left by V3-04 (console: AI agents tab)

| # | Owner | File | Edit | Why | Status |
|---|---|---|---|---|---|
| V3-04-1 | V3-06 or V3-08 | `web/src/components/console/settings/snippets.ts` (`codexSnippet`'s remote form) | Verify the Codex CLI remote-MCP header field name against whichever Codex version is installed at build time (§9.4: "`http_headers` map, or `bearer_token_env_var` with the key exported in the shell") and correct `codexSnippet` if it has moved. V3-04 shipped `[mcp_servers.lkap.http_headers]` / `Authorization = "Bearer <key>"` as the more directly testable literal form (no shell env var needed) — this is a best-effort placeholder, not a verified-against-Codex value, since remote mode (V3-06) hasn't landed yet and there is nothing running to check it against. | §5/§9.4 ask V3-04 to verify this "at build time"; that verification needs the remote service and an installed Codex CLI, neither of which exist in this wave. `console-settings-snippets.test.ts` pins the current text so a future change is a deliberate diff. | ruled — R-V3-25: verified against codex-cli 0.153.4, the shipped `http_headers` form stays; V3-06 documents the `bearer_token_env_var` alternative (G7 for the comment) |
| V3-04-2 | V3-08 (G6, reversed direction) | `web/src/components/console/settings/snippets.ts` (`skillInstallLine`, `CODEX_AGENTS_NOTE`, `ASK_AGENT_LINE`) | V3-08 hasn't landed yet, so step 3's copy (the skill install line, the Codex `AGENTS.md` note, "ask your agent" prompt) was written directly from `AGENT-ACCESS.md` §5/§10's own wording rather than waiting for V3-08's G6 ask. If V3-08 wants different phrasing (e.g. the plugin form's install command instead of `scripts/install_claude_skill.sh`), file the exact lines here and V3-04's `snippets.ts` will take them — the strings are isolated in one module precisely so this swap is a one-file change. | G6 says V3-08 → V3-04; V3-04 ran first (wave 1) so it used the doc's own text as a placeholder instead of blocking on a wave-2 package. | ruled — R-V3-26: V3-08 edits the three constants itself (G8) |
| V3-04-3 | coordinator / architect | — (no file; a design note) | `AGENT-ACCESS.md` §5 says the Connect dialog is "≤ 640 px"; the shipped dialog uses `DialogContent size="lg"` (768 px, `sm:max-w-3xl`) instead, because the snippet code blocks (multi-line `claude mcp add …`, TOML, JSON) and the client/preset radio-card grids read poorly at 640 px on desktop. Every step still fits and scrolls correctly at `md` (512 px) if a narrower width is preferred — it's a one-line prop change (`size="md"`) in `connect-agent-dialog.tsx` if the architect wants the letter of §5 followed instead. | flagging the deviation rather than silently keeping it | ruled — R-V3-27: `lg` stands, §5 amended |

## Open — left by V3-05 (tests, packaging, CI)

| # | Owner | File | Edit | Why | Status |
|---|---|---|---|---|---|
| V3-05-1 | V3-05 (announced) | `mcp/tests/conftest.py` (V3-01's) | Added a 6-line `pytest_addoption` hook registering `--snapshot-update` (nothing else changed). The card names the flag, and pytest registers options only from an initial `conftest.py` or a plugin, never from a test module. `test_catalog_snapshot.py` reads it through `request.config.getoption`. | the card's "regenerate with `--snapshot-update`" | done |
| V3-05-2 | V3-06 | `deploy/docker-compose.dev.yml` | V3-05 added the card's **commented** `mcp` service stub after `supervisor` (the build, the `--http` command, `127.0.0.1:8090`, `LKAP_API_URL=http://api:8080`), headed "V3-06 replaces this commented stub with the real service". Replace the whole block, and its comment header, with the real service; do not keep both. | the card's stub; V3-06 owns the real service | done (V3-06): replaced by the real `mcp` service |
| V3-05-3 | V3-06 | `mcp/README.md` | V3-05 created the file with a `## Remote (HTTP) mode` heading holding an HTML-comment placeholder and a three-line pointer to `docs/RUNBOOK.md` "Remote MCP". Replace the placeholder and the pointer with the remote section, including the Codex forms of R-V3-25. V3-08 already filled `## Claude Code skill and plugin` (V3-08-2). | shared-file split | done (V3-06): the `## Remote (HTTP) mode` section only |
| V3-05-4 | V3-08 (info) | `.github/workflows/contracts.yml` (G3) | V3-05 added `mcp/claude-plugin/skills/lkap/recipes` to the `git diff --exit-code` line, next to `mcp/src/lkap_mcp/generated`. It also added `scripts/export_contracts.sh`, the generated dir, both recipe dirs and the script to the trigger `paths`. This makes V3-08's "recipe copies are byte-identical (contracts gate)" a CI check. Verified locally: `scripts/export_contracts.sh` followed by `git diff --exit-code` on the four paths is clean. The plugin dir is still untracked, so this holds for CI once it is committed. | V3-08's acceptance relies on the contracts gate | done — ratified R-V3-36 (the contracts gate's fourth path) |
| V3-05-5 | V3-07 (note) | — | In the offline scenario, the fake Twirp server listens on a loopback `http://` url, which by itself reads as `self_hosted`. The api then refuses the `insurance_claim` pack's LiveKit Inference defaults ("self-hosted connections cannot use LiveKit Inference"). The scenario therefore passes `deployment_type="cloud"` explicitly. The live run uses a real `*.livekit.cloud` url, so it is not affected; the note is only for anyone who builds on the scenario against a self-hosted url. | a finding from the scenario test | ruled — R-V3-36 (accepted; the api's rule is right, `deployment_type` is the override) |
| V3-05-6 | V3-02 (observation) | `mcp/tests/test_chat.py` | The full `mcp` suite prints one `UserWarning` ("Remove `format_exc_info` from your processor chain…"). It comes from structlog, at `test_chat.py::test_chat_empty_text_is_refused`, and only appears in the full-suite order. It does not appear with `test_chat.py` alone, with any pair that includes it, or when `test_catalog_snapshot.py` and `test_scenario_build_agent.py` are deselected. It is the api's structlog configuration (installed by an earlier in-process `create_app`) meeting a manager-level `exc_info` log. It is not a failure, and no V3-05 file logs it. | noise in the gate output | ruled — R-V3-36 (noise; Phase 2 backlog: reset structlog in the mcp conftest or make the api's setup idempotent) |
| V3-05-7 | coordinator | — | Acceptance "CI green on a PR touching `mcp/`" cannot run: the repo has no remote (V3-00-6). Substitute: the exact job steps (`uv sync --frozen`, `ruff check`, `ruff format --check`, `mypy --strict src`, `pytest -q -m "not live"`) run locally in `mcp/`, and both workflow files parse as YAML. | no remote | ruled — R-V3-36 (the local substitute stands; open with V3-00-6 until the remote exists) |

## Open — left by V3-06 (remote HTTP mode, core — R-V3-15)

| # | Owner | File | Edit | Why | Status |
|---|---|---|---|---|---|
| V3-06-1 | V3-01 | `mcp/src/lkap_mcp/server.py` | Add a registry hook to `build_server`, for example `registry_cls: type[Registry] = Registry` (or a `registry_factory`), so `http.build_session_server` can call it instead of mirroring its ~10-line composition (`load_tool_modules(TOOL_MODULES)` → `register` → `LkapServer` → resources → prompts) with `SessionRegistry` (the per-call timeout, the process chat cap and the "401 during the call" override). | `http.py` duplicates `build_server`'s body; a later change there would not reach HTTP sessions | ruled — R-V3-30: V3-06F adds `registry_factory` to `build_server` (G9) and deletes the mirror |
| V3-06-2 | architect | — (R-V3-24 wording) | R-V3-24 says a foreign `chat_id` is `not_found`. The shipped `ChatManager` (V3-02) answers `unknown_chat` for a chat that is ended, idle-closed, never started **or another session's**, which does not reveal that the id exists elsewhere. `test_http_mode.py` asserts `unknown_chat` for session B of the same key, a session of another key, and `chat_end` from B. Please ratify `unknown_chat` as the ruling's wording, or ask V3-02 to change the code. | acceptance wording vs code | ruled — R-V3-29: `unknown_chat` ratified; R-V3-24's "not_found" was wording |
| V3-06-3 | V3-02 (info) | `mcp/src/lkap_mcp/chat/manager.py` | HTTP mode builds one `LkapServer` per MCP session (per-session key, per-session tool list), so there is one `ChatManager` per session: `max_per_owner=3` is per session, as §9.3 says, but the manager's `max_total=20` no longer spans the process. `http.py` enforces the process cap itself before `chat_start` (`CallLimits`, `LKAP_MCP_MAX_CHATS_TOTAL=20`: open chats across every session plus starts in progress); tested. If `ChatManager` ever takes a shared counter, `http.py` can drop its own. Session end awaits `close_owner(session:<id>)`, then `server.aclose()` runs `close_all()`, which also cancels the manager's reaper (pinned by a test, so there is no per-session task leak). | no edit needed; recorded so the two caps are not "fixed" twice | ruled — R-V3-35 (ratified; two caps, never fixed twice) |
| V3-06-4 | coordinator (web/Dockerfile owner) | `web/Dockerfile` | Add `ARG NEXT_PUBLIC_LKAP_MCP_PUBLIC_URL=` and `ENV NEXT_PUBLIC_LKAP_MCP_PUBLIC_URL=${NEXT_PUBLIC_LKAP_MCP_PUBLIC_URL}` next to the `NEXT_PUBLIC_API_BASE_URL` pair. Both compose files already pass the build arg (dev: `http://127.0.0.1:8090/mcp`; prod: `https://$LKAP_PUBLIC_DOMAIN/mcp` or `LKAP_MCP_PUBLIC_URL`). | `connect-agent-dialog.tsx` reads `process.env.NEXT_PUBLIC_LKAP_MCP_PUBLIC_URL` at build time; without the `ARG`, container builds never offer "Remote (HTTP)" (§9.2). Dev servers (`pnpm dev`) read it from the environment already. | ruled — R-V3-31: V3-06F (G10), plus the `docker.yml` web build arg |
| V3-06-5 | V3-01 | `mcp/src/lkap_mcp/settings.py` | Fold `http.HttpSettings` into `McpSettings`: `LKAP_ENV`, `LKAP_MCP_MAX_IN_FLIGHT_PER_SESSION` (10), `LKAP_MCP_CALL_TIMEOUT_S` (60), `LKAP_MCP_SESSION_IDLE_S` (1800), `LKAP_MCP_MAX_BODY_BYTES` (1 MB), `LKAP_MCP_MAX_CHATS_TOTAL` (20), `LKAP_MCP_SWEEP_INTERVAL_S` (30). | one settings class; `settings.py` is V3-01's | ruled — R-V3-30: accepted as shipped; folding is Phase 2 backlog |
| V3-06-6 | architect | — (design question) | A mid-session HTTP `429` (the 121st call a minute, the 11th request in flight) makes the Python SDK client raise out of its task group (`raise_for_status` in a child task), so the whole client session dies rather than one call failing. Claude Code and Codex are unverified. The card says `429` and V3-06 implements it as written (JSON-RPC error body, `error.data.retry_after_s`, `Retry-After`). Proposal: for requests on an open session, answer per-call limits as a tool result `ok=false, code="rate_limited", status=429, details.retry_after_s` with HTTP 200, and keep the HTTP `429` for the 6th session. V3-07 step 6 can observe what Claude Code does. | client robustness vs the card's letter | ruled — R-V3-28: per-call limits become in-band `rate_limited` tool results (HTTP 200); HTTP 429 only for the 6th `initialize`; owner V3-06F; V3-07 step 6d observes Claude Code |
| V3-06-7 | V3-05 (packaging) | `mcp/pyproject.toml` | Declare `starlette` and `uvicorn` directly. `http.py` imports both; today they arrive only as dependencies of `mcp>=1.30` (same versions in `uv.lock`: starlette 1.7.0, uvicorn 0.53.0). | dependency hygiene | ruled — R-V3-32: V3-06F (G11) |
| V3-06-8 | coordinator / user | `deploy/Caddyfile` | Run `LKAP_PUBLIC_DOMAIN=lkap.example.com caddy validate --config deploy/Caddyfile --adapter caddyfile`. No `caddy` binary exists on this host (and Docker is not running), so the new `/mcp` route was reviewed by hand, as V2-22 did (V2-22-5): the `@mcp path /mcp /mcp/*` handle before the api and web handles, `request_body { max_size 1MB }`, `reverse_proxy mcp:8090 { flush_interval -1; transport http { read_timeout 5m } }`, and `encode` limited to `@compressible` (not `/mcp`). The directives that were not machine-checked are `request_body`'s `max_size` block, the `transport http` `read_timeout` and the block-form `not path` matcher. | no validator available | ruled — R-V3-33: a `caddy-validate` CI job (V3-06F, `docker.yml`, same `caddy:` tag as prod compose, both env vars set) plus a one-time user run before the first prod deploy; the user pastes the last output line here. Not a V3 blocker |
| V3-06-9 | V3-07 (info) | — | Notes for live step 6: (a) start the service from a shell **without** `LKAP_API_KEY`, `LKAP_SERVICE_TOKEN`, `LKAP_ADMIN_TOKEN` or `LKAP_MASTER_KEY`, because it refuses to start with any of them (`env:` refs resolve in its environment); (b) `LKAP_MCP_PUBLIC_URL`'s host must be the host clients connect to (loopback is accepted in dev without it); (c) `me` shows `"transport": "http"`; (d) the identity check runs at `initialize`, before the client's `clientInfo.name` is known, so that one `/v1/api-keys/self` call is attributed `lkap-mcp/<v>` without `client=`; every tool call carries the client name as in stdio, and `last_client` still reads `lkap-mcp` (R-V3-19); (e) any api `401` seen by a session's client ends that session after the call. Today only key authentication answers `401` on `/v1`, so this equals "revoked or expired"; an `api_request` to a route that ever answered `401` for another reason would also end the session; (f) observe what Claude Code does on a mid-session `429` (V3-06-6). | live run prep | ruled — R-V3-35 (folded into the V3-07 card, step 6 prerequisites) |
| V3-06-11 | coordinator | `docs/RUNBOOK.md` (intro), RUNBOOK §5, `deploy/README.md` §4 | The intro says "Sections 1–11 are v2 … 12–19 are the v1 runbook"; add "Section 20 is v3 (Remote MCP, V3-06)". §5 and `deploy/README.md` §4 list every image build command; add `docker build -f mcp/Dockerfile -t lkap-mcp .` (from the platform root). | V3-06 owns only the new §20 | ruled — R-V3-34: V3-06F (G12) |
| V3-06-12 | V3-06 (announced) | `mcp/Dockerfile` | Deviation from the card's "vendored `lkap-contracts` wheel like the agent image": the image uses the api/supervisor pattern instead, with the platform root as build context (`COPY contracts/ mcp/`, `uv sync --frozen --no-dev --no-editable` plus `--no-install-package` for the three dev path deps). This installs `lkap-contracts` and `lkap-mcp` as built wheels, not editable links. The agent image vendors only because `lk agent deploy` forces its context to `agent/`; compose already builds `mcp` with `context: ..`. The builder stage asserts that the rtc wheel, `lkap_contracts`, `docs/guide.md`, the recipes and `generated/providers.json` load. The stage was simulated locally (the same `uv sync` flags on a copy of `contracts/` + `mcp/`, then `lkap-mcp --http` run from that venv); there was no `docker build` (no daemon), so CI's `docker.yml` `mcp` row is the check. | same outcome, simpler build | done — ratified R-V3-35 (card amended) |
| V3-06-10 | V3-06 (announced) | `deploy/docker-compose.{dev,prod}.yml` | Besides the `mcp` service, both files gained one `web.build.args` line, `NEXT_PUBLIC_LKAP_MCP_PUBLIC_URL`, which is inert until V3-06-4. Prod's `mcp` takes an optional `./mcp.env` (ignored by `deploy/.gitignore`'s `*.env`) for operator-provisioned `env:` values, and no `api.env`. The dev stub V3-05 left (V3-05-2) is replaced, not kept. Caddy does not `depends_on: mcp`, so a refused `mcp` (for example the `:80` smoke mode, which has no https url) never holds up the rest of the site. | the card's compose scope | done — ratified R-V3-35 |

## Open — left by V3-06F (remote-mode follow-up fixes, R-V3-28/30/31/32/33/34)

| # | Owner | File | Edit | Why | Status |
|---|---|---|---|---|---|
| V3-06F-1 | V3-06F (announced) | `mcp/src/lkap_mcp/http.py` | Shape of the R-V3-28 change: a per-session `SessionMeter` (the `calls` window, `in_flight`, the service clock) is built in `HttpSessions._start` and handed to both the `SessionRegistry` and the `HttpSession`. `build_session_server` gained an optional 4th parameter `meter` (`None` = unmetered, which the fail-closed `no_session` test uses). `HttpSession.in_flight` is now a read-only property over the meter, so it counts running **tool calls**, not requests (the idle sweep still skips a session with a call running; any request refreshes `last_seen` as before). `HttpSessions.serve` lost its `requests=` keyword and no longer checks limits. The in-flight check runs before the window check, and neither refusal is counted. A refusal logs `mcp_call_rate_limited` at INFO with the tool name and `scope` only. | the ruling fixes behaviour, not the internals | done |
| V3-06F-2 | coordinator | `.github/workflows/docker.yml` | The trigger `paths` gained exactly the two files the card names (`deploy/Caddyfile`, `deploy/docker-compose.prod.yml`), not `deploy/**` (the coordinator's brief said `deploy/**`): a `deploy/README.md` or env-example edit should not rebuild every image. The `caddy-validate` job hard-codes `caddy:2-alpine`, read from `deploy/docker-compose.prod.yml` line 47; its comment says to keep the two in step. Widen to `deploy/**` if you prefer. The job has never run (no remote). | card vs brief | done — coordinator may widen |
| V3-06F-3 | V3-06F (announced) | `docs/RUNBOOK.md` §20 | Besides §20.4 (three rows, the transport-refusal sentence, the deleted "can end some clients' sessions" sentence, plus a paragraph on the in-band shape), three other §20 lines were stale after this change and were fixed: §20.1 (the web image now declares the build arg, so the "must declare the `ARG` first (V3-06-4)" clause is gone), §20.2 (the "Not validated locally" bullet now names the CI job and the `docker run … caddy validate` command, R-V3-33 b), and §20.9 row 5 (the renamed and new test names). | same change, same file | done |
| V3-06F-4 | architect | `docs/v3/AGENT-ACCESS.md` §9.2 | §9.3 and §9.5 item 5 were re-checked against the code and match (6th `initialize` → HTTP 429 + `Retry-After`; per-call limits in band on `tools/call` only; `tools/list`/`resources/read`/`prompts/get` unmetered). One stale line elsewhere in §9: §9.2 still says the image uses a "vendored `lkap-contracts` wheel like the agent image", which R-V3-35 (V3-06-12) replaced with a root-context build like the api/supervisor images. | architect's file; not in V3-06F's grant | open |
| V3-06F-5 | coordinator | `docs/RUNBOOK.md` §5 | G12 covered only the build line. §5's compose bullets ("api (+ web on :3000), supervisor, …") still do not name the `mcp` service that both compose files now run (V3-06-10). A one-word addition to each bullet would do. | outside G12 | open |

## Open — left by V3-08 (Claude Code skill, plugin, Codex guidance)

| # | Owner | File | Edit | Why | Status |
|---|---|---|---|---|---|
| V3-08-1 | V3-08 (announced) | `mcp/tests/test_docs_lint.py` | Populated `EXTRA_LINT_FILES` (was `[]`, left empty by V3-03 specifically "until V3-08 lands") with `skills/lkap/SKILL.md`, its 10 recipe copies, `AGENTS.md` and `llms.txt`; added one new test, `test_skill_recipes_match_source_recipes`, asserting the recipe copies stay byte-identical to `mcp/src/lkap_mcp/docs/recipes/` (mirrors `test_generated_dir_matches_contracts_generated`'s pattern). Not in V3-08's exclusive-files list, but this is the exact, pre-built extension point V3-03's own comment names for this purpose ("Empty until V3-08 lands … see the V3-08 card"); no other line in the file touched. `cd mcp && uv run pytest tests/test_docs_lint.py -q` → 398 passed. | R-V3-16 "doc-lint covers the skill and AGENTS.md"; D-V3-10 | done |
| V3-08-2 | V3-08 (announced) | `mcp/README.md` | Filled in the "## Claude Code skill and plugin" section only — V3-05 (running in the same wave) had already created the file and left that exact heading as an HTML-comment placeholder ("V3-08 writes this section"). No other line of the file touched. | the card's shared-file split with V3-05 | done |
| V3-08-3 | V3-08 (verified, no edit) | `web/src/components/console/settings/snippets.ts` / `web/tests/console-settings-snippets.test.ts` | G8 checked: `skillInstallLine()` already returns `<checkout>/scripts/install_claude_skill.sh`, byte-identical to the command `mcp/README.md`'s new section documents; `CODEX_AGENTS_NOTE` ("Copy AGENTS.md from the repo into your project, or point Codex at the checkout.") already matches `AGENTS.md`'s own instruction and `mcp/README.md`'s Codex section; `ASK_AGENT_LINE` already reads "Call lkap_guide and tell me what this workspace has." (R-V3-26's exact wording). No string in any of the three needed to change, so `snippets.ts` and its test pins were left untouched. Web gate re-run regardless: `pnpm lint` (0 errors), `pnpm typecheck` (clean), `pnpm test` (80 files / 1220 tests green, incl. `console-settings-snippets.test.ts` and `console-settings-ai-agents.test.tsx`). | R-V3-26 / G8 | done — no diff |
| V3-08-4 | V3-08 (info) | `mcp/claude-plugin/.claude-plugin/marketplace.json` (new) | `AGENT-ACCESS.md` §10 offers "add the directory as a local marketplace" as the persistent alternative to a one-off `claude --plugin-dir`; a bare `plugin.json` with no `marketplace.json` is **not** enough — `claude plugin marketplace add <dir>` fails with "Marketplace file not found" (tested against installed Claude Code 2.1.226 under a temporary `HOME`). Added a minimal self-referencing marketplace (`"source": "./"`, the same shape the `ecc` marketplace already installed on this machine uses for its own single-plugin repo) so the documented command actually works. Verified end to end under a temp `HOME`: `claude plugin marketplace add <checkout>/mcp/claude-plugin` → `claude plugin install lkap@lkap` → `claude plugin list` shows `lkap@lkap` enabled, `scope: user`. Within `mcp/claude-plugin/**` (exclusive). | R-V3-16 §10 "add the directory as a local marketplace" | done — ratified R-V3-37 (§10 amended; V3-07 step 6h runs the `--plugin-dir` form) |

## Open — left by V3-07 (live acceptance)

| # | Owner | File | Edit | Why | Status |
|---|---|---|---|---|---|
| V3-07-1 | V3-06 / coordinator (low) | `docs/RUNBOOK.md` §20 (the remote-MCP launch line), `mcp/README.md` "Run it locally" | `uv run --project <checkout>/mcp lkap-mcp --http` does not forward SIGINT to its python child when the parent was started in its own session. **Repro:** `Popen([... "uv","run",...,"--http"], start_new_session=True)`, then `kill -INT <uv pid>`: after 45 s the uv parent was SIGKILLed and the child (uvicorn) kept `:8090` until it got its own SIGINT, after which it exited within 15 s. **Suggested fix:** document that a service manager must signal the process group (or `exec` the venv's `lkap-mcp` binary directly, which is what V3-07's launcher does now). Compose is unaffected: the image runs the binary as PID 1. | an orphaned service holds the port and answers with stale settings (V3-07's first 6d attempt hit it) | done (V3-07, docs; RUNBOOK §20 edited under the coordinator's resume grant, since it is outside V3-07's exclusive files): `mcp/README.md` "Run it locally" and RUNBOOK §20.1 now say to run `mcp/.venv/bin/lkap-mcp --http` directly under a process manager, or signal the process group; the `mcp/` gate was re-run and is green |
| V3-07-2 | **user** | — | Headless `claude -p` cannot use the desktop app's login: `claude auth status` → `loggedIn: false` (the same with a clean env), and the first run answered "OAuth session expired and could not be refreshed". Pick one: (a) run `claude login` (or `claude auth login`) once in your own terminal, so `claude -p` uses your default config dir, as you decided; or (b) provide an `ANTHROPIC_API_KEY` that V3-07 exports only in the `claude` process env. | blocks V3-07 steps 2a, 2b-sim, 4, 5 (Claude part), 6a–6c, 6d (Claude part) and 6h | done: the user ran `claude login` (`loggedIn: true`, claude.ai); every Claude step ran on 2026-09-24 18:45–19:05 UTC |
| V3-07-3 | V3-07 (announced, docs) | `mcp/README.md` "Claude Code skill and plugin" | With `--strict-mcp-config`, Claude Code 2.1.226 loads a `--plugin-dir` plugin and its skill (`lkap:lkap`) but **drops the plugin's MCP server** (`mcp_servers=[]`, live step 6h). README now adds the working form `--plugin-dir … --strict-mcp-config --mcp-config <checkout>/mcp/claude-plugin/.mcp.json`; the `${VAR}` expansion works there too (verified live: `me` answered). | the documented plugin line silently loses the server for anyone pinning MCP config | done (correction in V3-07's grant) |
| V3-07-4 | architect / user (info) | — (R-V3-3) | Inline secrets reach more client-side places than the Claude transcript. In the 2b-sim session, the user's own `PreToolUse`/`PostToolUse` hooks (from installed plugins) echoed the `connection_create` input, including the pasted synthetic secret, into their hook `stdout`. Claude Code stores that in the persisted session file (8 copies, plus the prompt, the `tool_use` and 2 queue records). A path-only search of `~/.claude` found it nowhere else. The MCP server, api log and audit stayed clean. **Suggestion:** R-V3-3's one-time console warning could say "…and any hooks or plugins your client runs"; `SKILL.md` already prefers `file:` refs. | the acknowledged exposure is wider than "your transcript" when hooks log tool input | open |
| V3-07-5 | V3-08 / architect | `mcp/claude-plugin/skills/lkap/SKILL.md` (description) | A plain build prompt ("Build a simple insurance first-notice-of-loss intake agent … on the cloud-v3 connection from the insurance_claim starter …") did **not** make Claude Code (sonnet) invoke the `lkap` skill; it went straight to `lkap_guide`, `lkap_describe(recipe)` and the tools, and still followed the workflow. `/lkap <prompt>` loads it (verified in the persisted session file). The description lists "LKAP" and "the LiveKit agent platform", but the prompt said neither. **Suggestion:** add "voice agent", "first-notice-of-loss", "knowledge base", "test it in chat", "publish" and the MCP server name as trigger phrases, or accept that the MCP guide already covers the model. | skill auto-invocation is the card's evidence | open |
| V3-07-6 | **user** | — | `~/.codex/config.toml` changed at **09:22:33 UTC** 2026-09-24: size 11095 → 11202, sha256 `72c855e0…` → `999240d4…`. V3-07 ran its last Codex command at 09:04 UTC (`codex exec --ignore-user-config --ephemeral`), and its 09:10 UTC snapshot still showed the file unchanged; part 2 ran no Codex command. The contents were not read. Please check whether another session or Codex itself wrote it at that time. | the isolation check requires the file unchanged | open — for the user |

## For the architect (design questions; answered as R-V3-n rulings)

| # | Asked by | Question | Recommendation | Ruling |
|---|---|---|---|---|
| A-V3-1 | V3-00 | KB url import: the request-time check (`net_guard.validate_url`) uses an **empty** policy, so `LKAP_ENV=dev`'s loopback default and `LKAP_NET_ALLOW_PRIVATE_HOSTS` never admit a private destination (the acceptance needs `http://127.0.0.1/…` → 422, and tests run in dev). The fetch still goes through `deps.get_http_client` as the card says, whose connect-time guard uses the *process* policy. So in dev, or with an operator allowlist, a public *name* that resolves to loopback or an allowlisted private range would pass. Metadata addresses are always refused. Keep this, or give the import route its own strict client dependency? | Accept for v3: prod's default policy is empty, and the offline check already refuses every literal and private name. If internal wiki imports are ever wanted, add an explicit `LKAP_KB_IMPORT_ALLOW_HOSTS` rather than reusing the LiveKit allowlist. | R-V3-18 — accepted as recommended |
| A-V3-2 | V3-00 | `X-LKAP-Client` is parsed for every principal (cookie, key, admin token), not only API keys, and `last_client` is written only inside the existing 60 s `last_used_at` window. A header-less request by the same key just before an MCP call therefore delays `last_client` until the next window. | Keep both. The header is attribution, not authority. The 60 s rule is the card's; V3-04 can show `last_client` as "last seen via". | R-V3-19 — keep both; V3-07 records `last_client` after step 4 |
| A-V3-3 | V3-02 | §4.7 gates the chat tools on `agents:write` and says `POST /v1/agents/{id}/text-sessions` treats an API key as privileged "through `member_role_in`". It does not: `routers/connect.py::is_privileged` also requires the **`sessions:write`** scope for API keys, so an `agents:write`-only key gets 403 on a draft agent (and, unprivileged, the origin check refuses a published one). The worker preflight also reads `GET /v1/connections/{id}/fleet` (`connections:read`). V3-02 therefore gates the four chat tools on `agents:write` + `sessions:write` + `connections:read` (tested: hidden for the Read-only preset and for `agents:write` without `sessions:write`). | Amend §4.7's scope column and note to "`agents:write`, `sessions:write`, `connections:read`". The Builder preset (D-V3-6) already holds all three, so no console change. | R-V3-20 — §4.7 amended (table + note); V3-05 snapshot pins the scopes; V3-08 docs say "Builder or higher" |

## Coordinator decisions

Binding. One line each, dated.

- 2026-09-24 — The user ruled "if MCP itself can do it, no need for a CLI": no CLI package, commands or JSON output mode (R-V3-1).
- 2026-09-24 — The user ruled no side drawers or sheets anywhere in the console; dialogs only (R-V3-2). Existing conversions belong to another agent; v3 packages do not touch those files.
- 2026-09-24 — The user ruled "Allow pasting in chat": inline secrets are first-class, never echoed, redacted, straight to the vault; one-time console warning (R-V3-3, amended).
- 2026-09-24 — The user ruled "Build remote now too": V3-06 is core, own service, never mounted in the api (R-V3-15).
- 2026-09-24 — The user ruled "Yes, include a skill": new V3-08, `mcp/claude-plugin/`, `AGENTS.md` for Codex (R-V3-16).
- 2026-09-24 — Approved: migration `v3_001` (coordinator applies with a backup); a second worker under a fresh agent name for V3-07; local `uv run --project` install line until the git remote exists (R-V3-17).
- 2026-09-24 — Architect rulings R-V3-18 … R-V3-27 close every open wave-1 ask above (A-V3-1/2/3, V3-01-6/7/8, V3-02-1, V3-04-1/2/3); grants G7 (V3-06) and G8 (V3-08) recorded. Still open for the coordinator: V3-00-4 (the `main.py` OpenAPI auth docstring) and V3-00-6 (CI Postgres, until the remote exists).
- 2026-09-24 — Architect rulings R-V3-28 … R-V3-38 close every open wave-2 ask above (V3-05-4/5/6/7, V3-06-1…12, V3-08-4); grants G9–G12 recorded for the new package **V3-06F** (Opus, before V3-07); V3-07 is amended to run headlessly (R-V3-38) with a step-0 auth preflight that may need the user once. User tasks recorded: the one-time local `caddy validate` (R-V3-33, before the first prod deploy) and, if the preflight fails, Claude Code auth for the scratch config dir. Still open for the coordinator: V3-00-6 and V3-05-7 (CI, until the remote exists), V3-06-8's user half.

## Closed

| # | Owner | Closed by | Note |
|---|---|---|---|
| | | | |
