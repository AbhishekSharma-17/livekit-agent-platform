## A/B summary — price list programmatic? / actual usage & cost after the fact

All pages accessed 2026-09-25.

| Vendor | (A) Price list programmatic? | (B) Actual usage / cost after the fact via API | Units | Notes |
|---|---|---|---|---|
| **Deepgram** | **Page only** — https://deepgram.com/pricing. No pricing API found on pricing page or Management API docs. | **Yes, per request with USD cost.** `GET /v1/projects/{project_id}/requests` (query `start`, `end`, `page`, `limit` 1–1000, `status`=succeeded/failed) and `GET /v1/projects/{project_id}/requests/{request_id}` — each request has `response.details.usd`, plus `duration`, `total_audio`, `channels`, `streams`, `models`, `method`, `tags`, `features`, `config`, `tier`, `api_key_id`, `code`, `deployment`. `GET /v1/projects/{id}/usage` (query `start`/`end` YYYY-MM-DD + many feature filters) → `start`, `end`, `resolution.units/amount` (the `results[]` list is not shown in the fetched page). `GET /v1/projects/{id}/usage/breakdown` (grouping: accessor, endpoint, feature_set, models, method, tags, deployment) → `hours`, `total_hours`, `agent_hours`, `tokens_in`, `tokens_out`, `tts_characters`, `requests` — **no USD in aggregates**. `GET /v1/projects/{id}/balances` → `balances[].balance_id`, `amount`, `units` ("USD" in example), `purchase_order_id`. Auth: `Authorization: Token <API_KEY>` (no scope stated). Delay: not stated; logs guide describes a "Pending" request state meaning response data not yet recorded. | STT per minute; TTS per 1k chars; per-request `usd` | Streaming WebSocket sends a `Metadata` message with `request_id` (UUID), `sha256`, `created`, `duration`, `channels` — confirmed on https://developers.deepgram.com/reference/speech-to-text/listen-streaming. Logs guide (https://developers.deepgram.com/docs/using-logs-usage) says per-request cost is looked up by Project ID + Request ID → `response.details.usd`, but no page explicitly states the streaming `Metadata.request_id` is that same ID (search snippets say so; snippet-only). `dg-request-id` HTTP header: search-snippet only. |
| **ElevenLabs** | **Page only** — https://elevenlabs.io/pricing/api (plan selector; per-1k rates shown). No pricing API found. | **Yes, in credits and fiat.** `GET /v1/user/subscription` → `tier`, `character_count`, `character_limit`, `next_character_count_reset_unix`, `status`, `currency`, `billing_period`, `next_invoice`, `open_invoices`, `can_extend_character_limit`. `GET /v1/usage/character-stats` (**deprecated**; `start_unix`/`end_unix` ms; `breakdown_type` ∈ none, voice, voice_multiplier, user, groups, api_keys, all_api_keys, product_type, model, resource, request_queue, region, subresource_id, reporting_workspace_id, has_api_key, request_source; `aggregation_interval` hour/day/week/month/cumulative; `metric` ∈ credits, tts_characters, minutes_used, request_count, ttfb_avg, ttfb_p95, **fiat_units_spent**, concurrency…) → `time[]`, `usage{}`. Replacement: `POST /v1/workspace/analytics/query/usage-by-product-over-time` (`start_time`, `end_time` ms, `interval_seconds` default 60, `group_by` incl. product_type, model, voice_id, user_id, **hashed_xi_api_key**, billing_group_id, fiat_currency, fiat_charge_type…, `filters`) → tabular `columns`, `column_types`, `column_units` (credits, **usd**, eur, inr, pln, gbp…), `rows`. Per-request after the fact: `GET /v1/history` items carry `request_id`, `model_id`, `date_unix`, `source`, `character_count_change_from`, `character_count_change_to`. Auth: API key header; admin/workspace permission for analytics endpoint not confirmed. Delay: not stated. | credits / characters; STT hours; fiat units | `character-cost`, `request-id`, `x-trace-id` response headers are documented on the API Introduction page (https://elevenlabs.io/docs/api-reference/introduction) for raw HTTP responses; **not** documented on the TTS convert page, and the WebSocket schema only sends `AudioOutput` (audio, alignment, normalizedAlignment) and `FinalOutput` (isFinal) — no per-message cost. Dashboard has a "Request Log" tab. |
| **Cartesia** | **Page only** — https://www.cartesia.ai/pricing (plans) + https://docs.cartesia.ai/pricing (credit rates). No pricing API. | **Yes, credits (and cents for agents), daily granularity, admin key.** `GET /usage/credits` (auth `Bearer sk_car_admin_...`; `start_ts`/`end_ts` RFC 3339 rounded to UTC days; `interval` day/week/month; `group_by` capability/model/voice/**api_key**; `api_key_id` filter; ≤1 year span) → buckets `{start_ts, end_ts, credits}` or grouped `{id, label, buckets[]}` — **credits only, no dollars**. `GET /usage/agents` → per bucket `cents` ("Total cost in US cents"), `minutes`, `calls`. Delay: not stated. | ~1 credit/char TTS; Ink-Whisper 1 credit/sec WS (1 credit/2 sec batch); Ink-2 3 credits/sec | Every response body includes `request_id` (UUID for HTTP/SSE; WS may include extra context) per https://docs.cartesia.ai/use-the-api/api-conventions; an `X-Request-ID` **header** is not documented there. Credits consumed are not returned in TTS/STT responses (not documented). Overage $/credit not published on the page (FAQ answers not rendered). |
| **AssemblyAI** | **Page only** — https://www.assemblyai.com/pricing. No pricing API. | **No usage/cost API today.** Account-management docs list only dashboard Cost/Usage pages (account/product/model/project/API-key breakdowns; dashboard "updates every 2 minutes") and Billing page. Roadmap (https://roadmap.assemblyai.com/) lists **"Usage and Spend API" — Upcoming (Q4 2026)**. `GET /v2/account`: not found on any fetched page. Per-session self-metering: v3 WebSocket `Termination` message → `audio_duration_seconds`, `session_duration_seconds` (https://www.assemblyai.com/docs/api-reference/streaming); async: `audio_duration` on transcript + `customer_id` query param on webhook URL (billing docs). | per hour (streaming billed on WebSocket open-to-close time, not audio) | `enable_extra_session_information` / `SessionInformation` belong to the legacy v2 endpoint `wss://api.assemblyai.com/v2/realtime/ws` — search-snippet only; not on the fetched v3 reference. v3 endpoint `wss://streaming.assemblyai.com/v3/ws`, auth via `Authorization` header (no Bearer) or temp `token` query param. |
| **Rime** | Page only — https://www.rime.ai/pricing | Not documented; docs intro says usage/billing reviewed in the Dashboard (https://docs.rime.ai/docs/introduction). | $/1k chars | 3,000 free minutes on Starter. |
| **Inworld** | Page only — https://inworld.ai/pricing | Portal only; Usage page per-model, monthly UTC periods; "Coming soon: filtering by workspace, API key, and time range" (https://docs.inworld.ai/portal/usage). | $/1M chars; STT $/hr; dollar-denominated credits | Credit balance shown on Billing page only. |
| **Hume** | Page only — https://www.hume.ai/pricing | Not documented (https://dev.hume.ai/docs/resources/billing has no usage API). | included chars + $/1k overage; EVI $/min | — |
| **Speechmatics** | Page only — https://www.speechmatics.com/pricing (docs plans page explicitly says prices are not listed there) | **Yes, usage (not cost).** `GET https://eu1.asr.api.speechmatics.com/v2/usage` (`Authorization: Bearer <API key>`; `since`, `until` YYYY-MM-DD) → `summary[]`, `details[]`, `count`, `duration_hrs`; **per UTC calendar day; current UTC day excluded** (explicit delay). https://docs.speechmatics.com/speech-to-text/batch/usage | $/hr; volume discount 20% above 500 hrs/month | — |

## List prices found

| Vendor | Model | Unit | Price | Source | Accessed |
|---|---|---|---|---|---|
| Deepgram | Nova-3 monolingual, streaming | per min | $0.0048 ("Current price" — possibly promotional) | https://deepgram.com/pricing | 2026-09-25 |
| Deepgram | Nova-3 multilingual, streaming | per min | $0.0058 ("Current price") | same | 2026-09-25 |
| Deepgram | Nova-3 monolingual / multilingual, pre-recorded | per min | $0.0043 / $0.0052 | same | 2026-09-25 |
| Deepgram | Flux English / Flux multilingual (streaming STT) | per min | $0.0065 / $0.0078 | same | 2026-09-25 |
| Deepgram | Aura-2 TTS | per 1k chars | $0.030 | same | 2026-09-25 |
| Deepgram | Aura-1 TTS / Flux TTS | per 1k chars | $0.0150 / $0.0450 | same | 2026-09-25 |
| Deepgram | Whisper Large (pre-recorded) | per min | $0.0048 | same | 2026-09-25 |
| ElevenLabs | Eleven v3 / v2 Multilingual (API) | per 1k chars | $0.10 (as displayed; plan selector state not captured) | https://elevenlabs.io/pricing/api | 2026-09-25 |
| ElevenLabs | v3 Conversational; Flash / Turbo | per 1k chars | $0.05 (Turbo v2.5 marked deprecated on models page; applies to Flash v2.5) | same; https://elevenlabs.io/docs/models | 2026-09-25 |
| ElevenLabs | Scribe v2 / Scribe v2 Realtime (STT) | per hour | $0.22 / $0.39 | https://elevenlabs.io/pricing/api | 2026-09-25 |
| ElevenLabs | Plans | per month | Starter $6/60k chars; Creator $22/220k; Pro $99/990k; Scale $299/2.99M; Business $990/9.9M (≈$0.10/1k, derived) | same | 2026-09-25 |
| Cartesia | Sonic TTS (all `/tts/*` endpoints; no separate Sonic 3 rate) | credits | ~1 credit per character | https://docs.cartesia.ai/pricing | 2026-09-25 |
| Cartesia | Ink-Whisper STT | credits | 1 credit/sec (WebSocket); 1 credit per 2 sec (batch) | same | 2026-09-25 |
| Cartesia | Ink-2 STT | credits | 3 credits/sec (WebSocket) | same | 2026-09-25 |
| Cartesia | Plans | per month | Free $0/20k credits; Pro $5/100k; Startup $49/1.25M; Scale $299/8M → **derived** ≈ $0.05, $0.039, $0.037 per 1k chars | https://www.cartesia.ai/pricing | 2026-09-25 |
| Cartesia | Voice Agent / Telephony | per min | $0.06 / $0.014 | same | 2026-09-25 |
| AssemblyAI | Universal-Streaming English / Multilingual | per hour | $0.15 (billed on session duration) | https://www.assemblyai.com/pricing | 2026-09-25 |
| AssemblyAI | Universal-3.5 Pro Realtime | per hour | $0.45 | same | 2026-09-25 |
| AssemblyAI | Universal-3.5 Pro (async) / Universal-2 (async) | per hour | $0.21 / $0.15 | same | 2026-09-25 |
| AssemblyAI | Voice Agent API | per hour | $4.50 ($0.075/min) | same | 2026-09-25 |
| AssemblyAI | Streaming diarization add-on | per hour | +$0.12 | same | 2026-09-25 |
| Rime | Mist v3 / Coda | per 1k chars | $0.03 / $0.05 (Starter) | https://www.rime.ai/pricing | 2026-09-25 |
| Inworld | TTS-2 | per 1M chars | $25 on-demand; $12.50 Growth; "as low as $5" Enterprise | https://inworld.ai/pricing | 2026-09-25 |
| Inworld | TTS-2 Flash | per 1M chars | $15 on-demand; $7 Growth | same | 2026-09-25 |
| Inworld | STT 1 | per hour | $0.15 | same | 2026-09-25 |
| Hume | Octave TTS overage | per 1k chars | $0.15 (Free–Creator), $0.12 (Pro), $0.10 (Scale), $0.05 (Business); plans $0–$500/mo | https://www.hume.ai/pricing | 2026-09-25 |
| Hume | EVI 3 overage | per min | $0.06 / $0.05 / $0.04 (Pro / Scale / Business) | same | 2026-09-25 |
| Speechmatics | Pro | unit not rendered | "$0.129" shown; $100 free credit; 20% discount above 500 hrs/month | https://www.speechmatics.com/pricing | 2026-09-25 |

## Could not confirm

- **Deepgram**: that the streaming `Metadata.request_id` is the same ID accepted by `GET /requests/{request_id}` (implied by the logs guide, not stated); the `dg-request-id` header and 30-day retention (search snippet only); the `results[]` fields of `GET /usage` (page only showed `start/end/resolution`); any required key scope; any stated usage delay; console CSV export.
- **ElevenLabs**: which plan the `/pricing/api` per-1k figures were displayed for; whether `character-cost` is returned on streaming HTTP responses (only the generic Introduction page mentions it) — the WebSocket schema has no cost field; auth level for `POST /v1/workspace/analytics/query/usage-by-product-over-time`; separate published enterprise pay-as-you-go per-character rates (none found); an explicit `eleven_turbo_v2_5` price (model deprecated).
- **Cartesia**: overage $/credit (FAQ answers not rendered; search says not public); an `X-Request-ID` response header; credits-consumed in TTS/STT responses; dashboard export; any $/min for Ink Whisper from Cartesia itself (only LiveKit's page quotes $0.0023/min — third-party, dropped).
- **AssemblyAI**: any `GET /v2/account` or usage endpoint (none documented; roadmap says Q4 2026); `enable_extra_session_information`/`SessionInformation` on the v3 endpoint (v2-only per snippet).
- **Speechmatics**: unit of "$0.129" (third-party sites say per hour and quote $0.0050/min batch, $0.0067/min real-time, $0.0537/min Flow — not verifiable on the official page).
- **Rime / Inworld / Hume**: no usage/billing API found; Rime and Hume usage APIs not checked beyond one docs page each.