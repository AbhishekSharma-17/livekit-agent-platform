"""Agent tool sources: built-in, declarative (HTTP/MCP), and background execution.

See docs/CONTRACTS.md §9 and docs/ARCHITECTURE.md §7 for the tool sources and
the background ("non-blocking") tool pattern (`background.py`, W1-AGENT-UI).
Built-in tools (`builtin/`) and declarative tools (`declarative.py`) are owned
by W1-AGENT-TOOLS.
"""
