## Findings (all pages fetched 2026-09-25)

### Table 1 — Price list & after-the-fact usage/cost

| Provider | Price list programmatic? | Actual usage / cost after the fact | Units | Notes |
|---|---|---|---|---|
| **OpenRouter** | **Yes.** `GET https://openrouter.ai/api/v1/models` — no auth needed (curl succeeded unauthenticated; OpenAPI spec has no per-op security override). `pricing` object per model (from OpenAPI `PublicPricing` schema at https://openrouter.ai/openapi.json): `prompt` = USD per input token, `completion` = USD per output token, `request` = USD per request, `image` = USD per input image, `image_output`, `image_token`, `audio` = USD per audio input token, `audio_output`, `input_cache_read`, `input_cache_write` (5-min default), `input_cache_write_1h`, `input_audio_cache`, `internal_reasoning` = USD per reasoning token, `web_search` = USD per web search, `discount`, `overrides[]` (conditional long-context / time-window pricing). Values are decimal strings. Default listing hides non-text output models; pass `?output_modalities=all` for STT/TTS. Per-provider endpoint prices: `GET /api/v1/models/{author}/{slug}/endpoints`. | (1) **In-response**: `usage.cost` (credits), `usage.cost_details.upstream_inference_cost`, `prompt_tokens_details.cached_tokens`, `completion_tokens_details.reasoning_tokens` — now always included; `usage:{include:true}` is marked deprecated; in streaming it arrives in the last SSE chunk (https://openrouter.ai/docs/use-cases/usage-accounting). (2) **Per request**: `GET /api/v1/generation?id=` (regular key) → `total_cost` (USD), `usage` (USD), `upstream_inference_cost`, `cache_discount`, `native_tokens_prompt/completion/cached/reasoning`, `tokens_prompt/completion`, `latency` ms, `generation_time` ms, `moderation_latency`, `num_input_audio_prompt`, `num_search_results`, `is_byok`, `provider_name`, `data_region`. Availability delay after the request: **not documented**. (3) **Per key**: `GET /api/v1/key` (regular key) → `usage`, `usage_daily/weekly/monthly`, `byok_usage*`, `limit`, `limit_remaining`, `limit_reset`, `is_free_tier`, `free_model_daily_requests{limit,used,remaining}`. (4) **Account**: `GET /api/v1/credits` (management key only, 403 otherwise) → `total_credits`, `total_usage`. (5) **Export/analytics** (management key): `GET /api/v1/activity` — last 30 completed UTC days, rows per date × model × endpoint: `requests`, `prompt_tokens`, `completion_tokens`, `reasoning_tokens`, `usage` (USD), `byok_usage_inference`; filters `date`, `api_key_hash`, `user_id`, `workspace_id`, `group_by=workspace`. Also `GET /api/v1/analytics/meta` + `POST /api/v1/analytics/query` (metrics/dimensions/granularity e.g. `day`). `GET /api/v1/keys` lists all keys with the same usage fields. | Per token / per request / per image / per search, USD. `usage.cost` is "credits"; `/generation.total_cost` and `/activity.usage` are "USD". | Management key vs regular key: https://openrouter.ai/docs/guides/overview/auth/management-api-keys (management keys can't call completions; required for keys/credits/activity/analytics). STT/TTS `prompt` unit is **not documented** — inferred: nova-3 `0.0000716667 × 60 = $0.0043/min` (= Deepgram pre-recorded rate) → per second of audio; aura-2 `0.00003` = $0.030/1k chars → per character. |
| **OpenAI** | **Page only.** https://developers.openai.com/api/docs/pricing (platform.openai.com and openai.com/api/pricing returned 403 to fetch). No pricing API found. | **Admin API only** (Admin key; https://developers.openai.com/cookbook/examples/completions_usage_api says set up an Admin Key). `GET /v1/organization/usage/completions` (https://developers.openai.com/api/reference/ruby/resources/admin/subresources/organization/subresources/usage/methods/completions): `start_time`/`end_time` (Unix s), `bucket_width` `1m|1h|1d`, `group_by` `project_id|user_id|api_key_id|model|batch|service_tier`, filters `models[]`, `api_key_ids[]`, `project_ids[]`, `user_ids[]`; `limit` max 31 (1d) / 168 (1h) / 1440 (1m). Result fields: `input_tokens`, `input_cached_tokens`, `input_uncached_tokens`, `output_tokens`, `input_text_tokens`, `output_text_tokens`, `input_audio_tokens`, `input_cached_audio_tokens`, `output_audio_tokens`, `input_image_tokens`, `output_image_tokens`, `num_model_requests`. Siblings: `audio_speeches`, `audio_transcriptions`, `embeddings`, `images`, `moderations`, `vector_stores`, `code_interpreter_sessions`, `file_search_calls`, `web_search_calls`, `costs`. `GET /v1/organization/costs` (https://developers.openai.com/api/reference/ruby/resources/admin/subresources/organization/subresources/usage/methods/costs): `bucket_width` **`1d` only**, `group_by` `project_id|line_item|api_key_id`, `line_items[]` filter, `limit` 1–180; results `amount.value`, `amount.currency`, `line_item`, `project_id`, `api_key_id`. **Delay: not documented** on any fetched page (announcement thread also silent). Per-request: Chat Completions `usage` (https://developers.openai.com/api/reference/ruby/resources/chat): `prompt_tokens`, `completion_tokens`, `total_tokens`, `prompt_tokens_details{cached_tokens,audio_tokens}`, `completion_tokens_details{reasoning_tokens,audio_tokens,accepted_prediction_tokens,rejected_prediction_tokens}`; `stream_options.include_usage` for streams. Realtime `response.done` → `response.usage` (https://developers.openai.com/api/reference/ruby/resources/realtime): `input_tokens`, `output_tokens`, `total_tokens`, `input_token_details{text_tokens,audio_tokens,image_tokens,cached_tokens,cached_tokens_details{text_tokens,audio_tokens,image_tokens}}`, `output_token_details{text_tokens,audio_tokens}`; doc says these "will correspond to billing". | Tokens (per 1M), minutes for STT estimates; costs in USD | Usage endpoints give tokens, not dollars; `costs` gives dollars but only daily. |
| **Anthropic** | **Page only.** https://platform.claude.com/docs/en/about-claude/pricing (docs.claude.com redirects there). No pricing API. | https://platform.claude.com/docs/en/manage-claude/usage-cost-api. Auth: Admin API key (`sk-ant-admin01-…`), **or** OAuth token with `org:admin`, **or** a personal/service-account key not scoped to a workspace; workspace keys fail. `GET /v1/organizations/usage_report/messages` (https://platform.claude.com/docs/en/api/admin-api/usage-cost/get-messages-usage-report): `bucket_width` `1m|1h|1d` (max 1440/168/31 buckets), `group_by` `api_key_id|workspace_id|model|service_tier|context_window|inference_geo|account_id|service_account_id|speed`; results `uncached_input_tokens`, `cache_creation{ephemeral_5m_input_tokens,ephemeral_1h_input_tokens}`, `cache_read_input_tokens`, `output_tokens`, `server_tool_use{web_search_requests}`. `GET /v1/organizations/cost_report` (https://platform.claude.com/docs/en/api/admin-api/usage-cost/get-cost-report): `bucket_width` **`1d` only**, `group_by` `workspace_id|description`; results `amount` (decimal string, lowest currency unit = cents), `currency` "USD", `cost_type` `tokens|web_search|code_execution|session_usage`, `token_type`, `model`, `service_tier`, `context_window`. **Delay: "typically appears within 5 minutes"**; poll ≤1/min. Per-response `usage` (https://platform.claude.com/docs/en/build-with-claude/prompt-caching): `input_tokens` (post-breakpoint only), `cache_creation_input_tokens`, `cache_read_input_tokens`, `output_tokens`, `cache_creation{ephemeral_5m_input_tokens,ephemeral_1h_input_tokens}`; pricing page examples also show `server_tool_use{web_search_requests,web_fetch_requests,code_execution_requests}`. | Per MTok USD; cost report in cents | Admin API unavailable for individual accounts; not available on Claude Platform on AWS. |
| **Google Gemini API** | **Page only** for the Gemini Developer API: https://ai.google.dev/gemini-api/docs/pricing (last updated 2026-09-24; tables are in JS tabs — Standard/Batch/Flex/Priority). No Gemini-API pricing endpoint. Google Cloud does have the Cloud Billing Catalog API (`GET https://cloudbilling.googleapis.com/v1/services/{id}/skus`, OAuth `cloud-billing.readonly`; https://docs.cloud.google.com/billing/docs/reference/rest/v1/services.skus/list) returning per-SKU `unitPrice`/`usageUnit` — Gemini API SKU coverage **not confirmed** from any fetched page. | **No usage/cost API from the Gemini API itself.** Per-response `usageMetadata` (https://ai.google.dev/api/generate-content): `promptTokenCount`, `cachedContentTokenCount`, `candidatesTokenCount`, `toolUsePromptTokenCount`, `thoughtsTokenCount`, `totalTokenCount`, plus modality breakdowns `promptTokensDetails[]`, `cacheTokensDetails[]`, `candidatesTokensDetails[]`, `toolUsePromptTokensDetails[]` (each `{modality: TEXT|IMAGE|VIDEO|AUDIO|DOCUMENT, tokenCount}`). Live API server messages carry `usageMetadata` with `promptTokenCount`, `responseTokenCount`, `totalTokenCount`, `promptTokensDetails[]`, `responseTokensDetails[]` (https://ai.google.dev/api/live). After the fact (https://ai.google.dev/gemini-api/docs/billing): AI Studio usage/billing/spend pages and Cloud Billing console (group by SKU); billing graphs "can take up to 24 hours to update". Cloud Billing BigQuery export (https://docs.cloud.google.com/billing/docs/how-to/export-data-bigquery) gives standard/detailed usage-cost tables plus a pricing table. | Per 1M tokens USD; audio = 25 tokens/s; DOCUMENT tokens billed at image rate | Free tier: "Free of charge" input/output on listed models, content **used** to improve products; paid tier not. Tier via Cloud Billing account (prepay min $5 or postpay). |
| **Groq** | **Page only** — and `https://groq.com/pricing` now 301-redirects to the groq.com homepage (curl and browser). Working sources: https://console.groq.com/docs/models (and `.md`), per-model pages e.g. https://console.groq.com/docs/model/whisper-large-v3-turbo. `GET /openai/v1/models` returns `id, object, created, owned_by, active, context_window, public_apps` — **no pricing**. | **No usage/billing API documented** (https://console.groq.com/docs/api-reference lists chat/audio/batches/files/models only). Per-response `usage`: `prompt_tokens`, `completion_tokens`, `total_tokens`, `queue_time`, `prompt_time`, `completion_time`, `total_time` (seconds); `x_groq` documents only `id`. Transcription responses: `text` + `x_groq.id`, no duration/usage. Rate-limit headers only; usage viewed in console. | Per 1M tokens; STT per hour of audio; TTS per 1M characters | Community request for a billing endpoint (community.groq.com/t/…/378) now redirects to the homepage. |

### Table 2 — List prices found (accessed 2026-09-25)

| Provider | Model | Unit | Price | Source |
|---|---|---|---|---|
| OpenRouter | `openai/gpt-4.1-mini` | per token (USD) | prompt 0.0000004 ($0.40/1M), completion 0.0000016 ($1.60/1M), cache read 0.0000001, web_search 0.01/search | https://openrouter.ai/api/v1/models |
| OpenRouter | `google/gemini-3.5-flash` | per token | prompt 0.0000015 ($1.50/1M), completion 0.000009 ($9/1M), audio 0.000003 ($3/1M), image 0.0000015, cache read 0.00000015, cache write 0.0000000833, internal_reasoning 0.000009, web_search 0.014 | same |
| OpenRouter | `anthropic/claude-sonnet-4.6` | per token | prompt 0.000003 ($3/1M), completion 0.000015 ($15/1M), cache read 0.0000003, cache write 0.00000375, cache write 1h 0.000006, web_search 0.01 | same |
| OpenRouter | `deepgram/nova-3` (STT, `?output_modalities=all`) | per unit, **undocumented** (inferred: per second) | prompt 0.0000716667 (= $0.0043/min), completion 0 | https://openrouter.ai/api/v1/models?output_modalities=all ; https://openrouter.ai/api/v1/models/deepgram/nova-3/endpoints |
| OpenRouter | `deepgram/aura-2` (TTS) | inferred: per character | prompt 0.00003 (= $0.030/1k chars), completion 0 | same |
| Deepgram (cross-check) | Nova-3 / Aura-2 | per min / per 1k chars | Nova-3 pre-recorded $0.0043/min, streaming $0.0048/min; Aura-2 $0.030/1k chars | https://deepgram.com/pricing |
| OpenAI | gpt-4.1 | per 1M tokens | in $2.00, cached $0.50, out $8.00 (Standard) | https://developers.openai.com/api/docs/pricing |
| OpenAI | gpt-4.1-mini | per 1M | in $0.40, cached $0.10, out $1.60 | same |
| OpenAI | gpt-4o-mini | per 1M | in $0.15, cached $0.075, out $0.60 | same |
| OpenAI | gpt-realtime (in `hiddenModels`, not default view) | per 1M | Text in $4.00 / cached $0.40 / out $16.00; Audio in $32.00 / cached $0.40 / out $64.00 | same |
| OpenAI | gpt-realtime-2.1 (visible default) | per 1M | Text $4.00 / $0.40 / **$24.00**; Audio $32 / $0.40 / $64; Image in $5.00 | same |
| OpenAI | gpt-realtime-mini / -2.1-mini | per 1M | Text $0.60 / $0.06 / $2.40; Audio $10 / $0.30 / $20 | same |
| OpenAI | gpt-4o-mini-tts (hidden row) | per 1M | Text in $0.60; Audio out $12.00 (no per-minute figure in the row data) | same |
| OpenAI | gpt-4o-transcribe | per 1M / per min | $2.50 in, $10.00 out, est. $0.006/min | same |
| OpenAI | gpt-4o-mini-transcribe | per 1M / per min | $1.25 in, $5.00 out, est. $0.003/min | same |
| OpenAI | gpt-transcribe / gpt-realtime-whisper / gpt-live-transcribe | per min | $0.0045 / $0.017 / $0.017 | same |
| OpenAI | whisper-1 | — | **not on the current pricing page** | same |
| Anthropic | Claude Sonnet 4.6 | per MTok | in $3, 5m cache write $3.75, 1h $6, cache read $0.30, out $15; batch $1.50/$7.50 | https://platform.claude.com/docs/en/about-claude/pricing |
| Anthropic | Claude Sonnet 5 (newer) | per MTok | in $2, out $10, cache read $0.20 | same |
| Anthropic | Claude Haiku 4.5 | per MTok | in $1, cache write $1.25/$2, read $0.10, out $5; batch $0.50/$2.50 | same |
| Google | gemini-2.5-flash (Standard) | per 1M | in $0.30 text/image/video, $1.00 audio; out $2.50; cache $0.03/$0.10 + $1/1M-tok/hr storage; batch half | https://ai.google.dev/gemini-api/docs/pricing |
| Google | gemini-3.5-flash (Standard) | per 1M | in $1.50, out $9.00, cache $0.15 (+$1/1M/hr); Batch/Flex $0.75/$4.50; Priority $2.70/$16.20; free tier "Free of charge" | same |
| Google | gemini-3.6/3.7/3.8-flash | per 1M | $0.75 in / $3.75 out through 2026-12-31, then $1.50 / $7.50 | same |
| Google | gemini-2.5-flash-native-audio-preview-12-2025 (Live) | per 1M | text in $0.50, audio/video in $3.00, text out $2.00, audio out $12.00 | same |
| Google | gemini-3.8-live, gemini-3.8-live-extended-thinking, gemini-3.1-flash-live-preview | per 1M | text in $0.75, audio in $3.00 (≈$0.005/min), image/video $1.00, text out $4.50, audio out $12.00 (≈$0.018/min) | same |
| Google | gemini-2.5-flash-preview-tts / gemini-2.5-pro-preview-tts | per 1M | $0.50 text in, $10 audio out / $1.00 in, $20 out | same |
| Google | gemini-3.8-flash-tts / -flash-lite-tts | per 1M | $0.50 in, $9.00 / $6.00 audio out through 2026-12-31 (double from 2027); 25 audio tokens/s | same |
| Groq | openai/gpt-oss-120b / -20b | per 1M | $0.15/$0.60 ; $0.075/$0.30 | https://console.groq.com/docs/models |
| Groq | whisper-large-v3 / -turbo | per hour of audio | $0.111 / $0.04 | same ; https://console.groq.com/docs/model/whisper-large-v3-turbo |
| Groq | Orpheus V1 English / Arabic Saudi (TTS; PlayAI deprecated Dec 2025) | per 1M characters | $22.00 / $40.00 | https://console.groq.com/docs/models |
| Groq | llama-3.3-70b, llama-3.1-8b, MiniMax M2.7 | — | "Enterprise — contact sales" | same |

### Could not confirm
- OpenRouter: how soon `GET /generation` data is available after a request (undocumented); the pricing unit for STT/TTS models (per-second / per-character inferred by arithmetic only); whether `usage.cost` "credits" equals USD (other schemas say USD).
- OpenAI: any documented delay for `/organization/usage/*` and `/organization/costs`; canonical (non-`ruby`) reference URLs 404'd, so field lists come from the `…/api/reference/ruby/…` pages; `whisper-1` price (absent from the pricing page); gpt-4o-mini-tts per-minute estimate.
- Anthropic: the full `usage` object list in the Messages reference (page fetch truncated before the response schema; fields above come from the prompt-caching and pricing pages).
- Google: whether Gemini API (`generativelanguage.googleapis.com`) SKUs are exposed by the Cloud Billing Catalog API; audio-input price for gemini-3.5-flash (Google's table shows none; OpenRouter lists $3/1M); free-tier RPM/RPD numbers (rate-limits page defers to AI Studio).
- Groq: any usage/billing API (none documented); whether a streaming final chunk carries `x_groq.usage` (only `x_groq.id` is documented); prices for Llama models (now "contact sales").