# Copy to deploy/web.env (human-created; read by deploy/docker-compose.yml
# `web.env_file`) and fill in real values. Never commit the copy.
#
# NEXT_PUBLIC_API_BASE_URL is NOT read from here — it's baked in at image
# BUILD time (`docker compose build --build-arg` / the `web.build.args` in
# docker-compose.yml). This file only holds the web server's runtime env.

LKAP_ADMIN_TOKEN=
