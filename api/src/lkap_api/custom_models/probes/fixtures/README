ok_1s_16k.wav — the "Test model" STT probe clip (docs/v4/CUSTOM-MODELS.md D-V4-26): 1.0 s, 16 kHz mono 16-bit PCM WAVE, one word ("okay"). Origin: synthesized on 2026-09-25 with the macOS `say` command (`say --data-format=LEI16@16000 --file-format=WAVE "okay"`), then zero-padded to exactly 16 000 frames with Python's `wave` module; no human voice was recorded. Replace it with a CC0 recording if the synthesized voice's terms ever matter for redistribution.
