"""Provider constructors that live outside the `livekit-agents`/plugin ecosystem.

`factory.py` (W1-AGENT-CORE) maps `lkap_contracts.providers.ProviderSpec`
entries to constructible classes; `image_gen.py` (W1-AGENT-TOOLS) is the
`python_class` target for the `google-image-gen`/`openai-image-gen` registry
entries (docs/CONTRACTS.md §4).
"""
